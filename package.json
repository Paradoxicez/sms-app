{
  "name": "sms-platform",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/api",
    "apps/web"
  ],
  "scripts": {
    "dev:api": "npm -w apps/api run start:dev",
    "dev:web": "npm -w apps/web run dev",
    "test": "vitest run",
    "lint": "echo 'lint not configured yet'"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
