import { describe, it } from 'vitest';

describe('ScheduleProcessor - Recording Schedules (D-03)', () => {
  it.todo('starts recording at scheduled start time');
  it.todo('stops recording at scheduled end time');
  it.todo('skips disabled schedules');
  it.todo('handles daily schedule type with start/end times');
  it.todo('handles weekly schedule type with day-of-week filter');
});
