import { describe, it } from 'vitest';

describe('StreamProcessor — Phase 19 (D-14 rename + existing guard)', () => {
  // All entries are it.todo stubs — Wave 1-2 tasks convert them to real tests.
  it.todo('StreamJobData uses inputUrl field, not rtspUrl');
  it.todo('existing defensive guard (lines 47-56) rejects job with empty cameraId or empty inputUrl');
  it.todo('refuses to enqueue ffmpeg start when inputUrl is empty string');
});
