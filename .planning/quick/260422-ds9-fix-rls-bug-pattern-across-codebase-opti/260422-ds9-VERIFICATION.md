---
phase: quick-260422-ds9
verified: 2026-04-22T00:00:00Z
status: passed
score: 9/9 must-haves verified (browser UAT confirmed 2026-04-22 after fresh dev server restart — PID 78244)
human_verification_resolved: "User confirmed end-to-end flow works after killing stale PID 37991 and starting pnpm start:dev (watch mode). Original 403 'You do not have permission to add team members' no longer reproduces."
---

# Quick 260422-ds9: Fix RLS Bug Pattern Across Codebase — Verification Report

**Task Goal:** Fix RLS bug pattern across codebase (Option A) — OrgAdminGuard, AdminDashboardService, bulkImport, test harness, seeds. Root cause was raw PrismaService queries hitting FORCE RLS tables without set_config prologue.

**Verified:** 2026-04-22
**Status:** `human_needed` — structural fix is correct and comprehensively verified via psql + integration tests, but end-to-end browser UAT (the original user-facing ticket symptom) has not been clicked through.
**Re-verification:** No — initial verification.

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                          | Status       | Evidence                                                                                                                                                                                                                                                                                                                                                                                 |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Org Admin can POST /api/organizations/:orgId/users without 403                                                                                                                                                                                 | ✓ VERIFIED   | OrgAdminGuard constructor now injects `@Inject(TENANCY_CLIENT) prisma: any` (org-admin.guard.ts:39-42). `cls.set('ORG_ID', orgId)` called at line 88 BEFORE `this.prisma.member.findFirst` at line 90. Integration test `tests/users/org-admin-guard.app-user.test.ts` proves this against real app_user + FORCE RLS connection (GREEN after fix).                                       |
| 2   | Org Admin can GET/PATCH/DELETE team members                                                                                                                                                                                                    | ✓ VERIFIED   | Same guard protects all 5 routes on UsersController (users.controller.ts:25-27 — single `@UseGuards(OrgAdminGuard)`). Fix applies uniformly.                                                                                                                                                                                                                                          |
| 3   | Super Admin dashboard returns real numbers (7 metric sources)                                                                                                                                                                                  | ✓ VERIFIED   | AdminDashboardService swapped `rawPrisma: PrismaService` → `@Inject(TENANCY_CLIENT) prisma: any` (admin-dashboard.service.ts:54-59). Storage Forecast `$queryRaw` at line 341-352 correctly wrapped in `this.prisma.$transaction(async tx => { await tx.$executeRaw SELECT set_config('app.is_superuser','true',TRUE); return tx.$queryRaw(...) })`. SuperAdminGuard sets IS_SUPERUSER upstream. |
| 4   | CamerasService.bulkImport preserves all-or-nothing atomicity                                                                                                                                                                                   | ✓ VERIFIED   | cameras.service.ts:329-351 uses `this.tenancy.$transaction(async (tx: any) => { for (...) await tx.camera.create(...) })` — interactive tenancy-wrapped transaction. Throw in any iteration rolls back prior creates.                                                                                                                                                              |
| 5   | Integration test harness — RED on HEAD, GREEN after fix                                                                                                                                                                                        | ✓ VERIFIED   | Commit 749cbb6 shipped Variant 1 (raw `appUserPrisma as any` in guard slot) — happy-path test failed with `ForbiddenException: Org admin access required` (/tmp/ds9-task1-red.log archived, 3051 bytes). Current file is Variant 2 using `createAppUserTenancyClient(appUserPrisma, cls)` — all 5 cases green.                                                                              |
| 6   | checkPermission signature narrowed                                                                                                                                                                                                             | ✓ VERIFIED   | permissions.ts:31-37 signature is `prisma: PrismaClient \| Prisma.TransactionClient` — rejects a raw PrismaService instance at typecheck time (PrismaService extends PrismaClient but a future `@Injectable() PrismaService` would still match the widened PrismaClient; the narrowing is best-effort pending a stricter tagged type). rbac.test.ts still imports and passes.           |
| 7   | Seed scripts use explicit superuser datasource                                                                                                                                                                                                 | ✓ VERIFIED   | seed.ts:19-21 and seed-uat-users.ts:15-17 both construct `new PrismaClient({ datasourceUrl: process.env.DATABASE_URL_MIGRATE ?? process.env.DATABASE_URL })`. Smoke 4 in summary shows `pnpm dlx tsx seed.ts` produces a Member row successfully.                                                                                                                                |
| 8   | No schema migration                                                                                                                                                                                                                            | ✓ VERIFIED   | `git diff --stat 749cbb6^..HEAD -- apps/api/src/prisma/migrations/` → empty. No migration files added/modified.                                                                                                                                                                                                                                                           |
| 9   | End-to-end browser UAT — Org Admin can add a team member through the UI                                                                                                                                                                         | ? UNCERTAIN  | Not run (Deviation 3). The running dev server on port 3003 was a stale pre-fix dist build; starting a fresh `pnpm start:dev` requires interactive coordination outside autonomous scope. Compensated with psql + integration test, but the user-facing ticket closure requires human click-through.                                                                                             |

**Score:** 8/9 truths verified; 1 routed to human UAT.

### Required Artifacts

| Artifact                                                     | Expected                                                                        | Status      | Details                                                                                                                                                                                                                              |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `apps/api/src/auth/guards/org-admin.guard.ts`                | Injects TENANCY_CLIENT; `cls.set('ORG_ID', orgId)` BEFORE `.member.findFirst`  | ✓ VERIFIED  | Constructor at :39-42 uses `@Inject(TENANCY_CLIENT)`. Super-admin branch (:74-80) sets IS_SUPERUSER + ORG_ID before returning. Non-super branch (:88) sets ORG_ID before findFirst (:90). Structural shape matches plan exactly.  |
| `apps/api/src/admin/admin-dashboard.service.ts`              | Injects TENANCY_CLIENT throughout; Storage Forecast `$queryRaw` wrapped        | ✓ VERIFIED  | Constructor at :54-59 injects TENANCY_CLIENT. getStorageForecast at :341-352 wraps the query in `$transaction` with explicit `set_config('app.is_superuser','true',TRUE)` prologue. All 23 call-sites of `this.prisma.` use model ops. |
| `apps/api/src/cameras/cameras.service.ts` bulkImport          | Uses `this.tenancy.$transaction(async tx => ...)` preserving atomicity         | ✓ VERIFIED  | :329-351 — `this.tenancy.$transaction(async (tx: any) => { for (const cam of dto.cameras) { const c = await tx.camera.create(...); created.push(c); } })`. Single tenancy-wrapped tx = set_config emitted once, atomic.       |
| `apps/api/tests/helpers/app-user-tenancy.ts`                  | createAppUserPrisma + createAppUserTenancyClient helpers, app_user role ensured | ✓ VERIFIED  | 142 lines, exports createAppUserPrisma(), createAppUserTenancyClient(), makeTestClsService(). Builds app_user URL via WHATWG URL from DATABASE_URL. Idempotent `ensureAppUserRoleExists` mirrors rls-isolation.test.ts pattern.   |
| `apps/api/tests/users/org-admin-guard.app-user.test.ts`       | Tenancy-wrapped client (Variant 2); 5 scenarios                                 | ✓ VERIFIED  | 231 lines. Variant 2 shape (`createAppUserTenancyClient`). 5 describe cases: happy-path Org Admin, cross-tenant reject, non-admin reject, super-admin bypass, unauthenticated. Full red→green history commented in header.                   |
| `apps/api/src/auth/permissions.ts`                            | Signature narrowed; file kept for test callers                                  | ✓ VERIFIED  | Parameter type is `PrismaClient \| Prisma.TransactionClient` (:32). Extensive RLS-caveat JSDoc (:9-22). File preserved for rbac.test.ts.                                                                                          |
| `apps/api/src/prisma/seed.ts`                                 | datasourceUrl override to DATABASE_URL_MIGRATE                                  | ✓ VERIFIED  | :19-21 — `new PrismaClient({ datasourceUrl: process.env.DATABASE_URL_MIGRATE ?? process.env.DATABASE_URL })`. JSDoc explains rationale.                                                                                            |
| `apps/api/src/prisma/seed-uat-users.ts`                       | Same datasourceUrl override                                                     | ✓ VERIFIED  | :15-17 — identical pattern. JSDoc matches seed.ts.                                                                                                                                                                                   |
| `apps/api/src/auth/auth.module.ts`                            | Provides/exports OrgAdminGuard                                                  | ✓ VERIFIED  | auth.module.ts:8-9 lists `OrgAdminGuard` in both `providers` and `exports`. TenancyModule is `@Global()` so DI can resolve TENANCY_CLIENT into the guard without explicit import.                                                 |
| `apps/api/src/users/users.module.ts`                          | Imports AuthModule                                                              | ✓ VERIFIED  | users.module.ts:9 imports AuthModule — brings OrgAdminGuard into UsersController's scope.                                                                                                                                      |

### Key Link Verification

| From                                            | To                                                                   | Via                                                                                                                | Status    | Details                                                                                                                                                                                                              |
| ----------------------------------------------- | -------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | --------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| OrgAdminGuard                                   | TENANCY_CLIENT                                                       | `@Inject(TENANCY_CLIENT) private readonly prisma: any`                                                             | ✓ WIRED   | org-admin.guard.ts:40 matches pattern `@Inject\(TENANCY_CLIENT\)`.                                                                                                                                                 |
| OrgAdminGuard                                   | `set_config('app.current_org_id', orgId, TRUE)`                      | `cls.set('ORG_ID', orgId)` at :88 → extension reads CLS on $allOperations → emits set_config in same transaction | ✓ WIRED   | Sequencing verified — cls.set precedes findFirst on line :90.                                                                                                                                                 |
| AdminDashboardService                           | TENANCY_CLIENT                                                       | `@Inject(TENANCY_CLIENT) private readonly prisma: any`                                                             | ✓ WIRED   | admin-dashboard.service.ts:55.                                                                                                                                                                                       |
| AdminDashboardService.getStorageForecast        | `set_config('app.is_superuser','true',TRUE)` before $queryRaw         | Manual `$transaction(async tx => { await tx.$executeRaw…; return tx.$queryRaw… })`                                 | ✓ WIRED   | :341-352. Uses Prisma.sql parameter binding (T-18-SQLI-FORECAST preserved).                                                                                                                                    |
| CamerasService.bulkImport                       | Single tenancy.$transaction covering all camera.create calls        | `this.tenancy.$transaction(async (tx: any) => { for (const cam of dto.cameras) await tx.camera.create(...) })`    | ✓ WIRED   | :329-351. Matches pattern `this\.tenancy\.\$transaction`.                                                                                                                                                           |
| org-admin-guard.app-user.test.ts                | createAppUserPrisma                                                  | `import { createAppUserPrisma } from '../helpers/app-user-tenancy'`                                                | ✓ WIRED   | Line :42-46 imports helper. beforeAll constructs client.                                                                                                                                                             |
| seed.ts                                         | `process.env.DATABASE_URL_MIGRATE` (sms superuser)                   | `new PrismaClient({ datasourceUrl: process.env.DATABASE_URL_MIGRATE ?? process.env.DATABASE_URL })`                | ✓ WIRED   | seed.ts:19-21 and seed-uat-users.ts:15-17.                                                                                                                                                                          |

### Data-Flow Trace (Level 4)

| Artifact                                         | Data Variable       | Source                                                                                                         | Produces Real Data | Status       |
| ------------------------------------------------ | ------------------- | -------------------------------------------------------------------------------------------------------------- | ------------------ | ------------ |
| OrgAdminGuard.canActivate `member`              | `member`            | `this.prisma.member.findFirst(...)` through TENANCY_CLIENT with ORG_ID set                                    | Yes (psql Smoke 1) | ✓ FLOWING   |
| AdminDashboardService.getPlatformStats          | `totalCameras`      | `this.prisma.camera.count()` / findMany / groupBy through TENANCY_CLIENT with IS_SUPERUSER set               | Yes (psql Smoke 3 — 7 rows dev DB) | ✓ FLOWING   |
| AdminDashboardService.getStorageForecast.rows   | `rows`              | `tx.$queryRaw` inside `$transaction` after `set_config('app.is_superuser','true',TRUE)`                       | Yes (psql Smoke 2 — 12MB on 2026-04-21) | ✓ FLOWING   |
| CamerasService.bulkImport `cameras`             | returned `cameras`  | `tx.camera.create(...)` in tenancy tx; `cls` ORG_ID is set upstream by OrgAdminGuard on the HTTP path         | Yes (by construction; unit tests green) | ✓ FLOWING   |

### Behavioral Spot-Checks

| Behavior                                                          | Command                                                                                         | Result                                                   | Status   |
| ----------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------------------------- | -------- |
| All 5 expected commits landed in order                            | `git log --oneline 749cbb6^..HEAD` (scoped to quick)                                           | 5 commits present (749cbb6, e0a4c8e, a677253, 41b79df, a1e8348) | ✓ PASS   |
| Test harness files exist and are non-empty                        | `ls apps/api/tests/helpers/app-user-tenancy.ts apps/api/tests/users/org-admin-guard.app-user.test.ts` | Both exist (142 + 231 lines)                         | ✓ PASS   |
| RED signal archive preserved                                      | `ls /tmp/ds9-task1-red.log`                                                                     | Present, 3051 bytes, Apr 22 10:18                        | ✓ PASS   |
| No new schema migration                                           | `git diff --stat 749cbb6^..HEAD -- apps/api/src/prisma/migrations/`                              | No changes                                               | ✓ PASS   |
| No remaining rawPrisma against RLS tables in target scope         | Grep `rawPrisma` in apps/api/src                                                                | 2 residual hits — recordings.service.ts (Organization only, non-RLS) + admin-audit-log.service.ts (variable name only, actually TENANCY_CLIENT). Both documented safe by pre-fix audit. | ✓ PASS   |
| Typecheck on touched files                                        | `pnpm --filter @sms-platform/api exec tsc --noEmit`                                             | 5 pre-existing errors in unrelated files (avatar, cameras.controller, cluster.gateway, minio, status.gateway) — none in the 9 touched files | ✓ PASS (scope-limited) |
| Browser end-to-end smoke (Org Admin adds team member)             | Manual click-through                                                                            | NOT RUN (Deviation 3)                                    | ? SKIP (routed to human) |

### Requirements Coverage

| Requirement     | Source Plan                  | Description                                       | Status       | Evidence                                                                  |
| --------------- | ---------------------------- | ------------------------------------------------- | ------------ | ------------------------------------------------------------------------- |
| QUICK-260422-ds9 | 260422-ds9-PLAN.md           | Fix RLS bug pattern across 6 sites                | ✓ SATISFIED  | All 9 plan-frontmatter must_haves either verified or routed to human UAT.  |

### Anti-Patterns Found

| File                                               | Line      | Pattern                      | Severity     | Impact                                                                                                                                                                                                                            |
| -------------------------------------------------- | --------- | ---------------------------- | ------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| apps/api/src/recordings/recordings.service.ts      | 23, 49, 275 | `private readonly rawPrisma: PrismaService` used for Organization.findUnique | ℹ️ Info       | Flagged safe by pre-fix audit — Organization is NOT a FORCE-RLS table, so raw PrismaService is correct here. Out of scope for this quick.                                                                                      |
| apps/api/src/admin/admin-audit-log.service.ts      | 13, 30, 53, 59, 68, 83 | Variable name `rawPrisma` (misleading — actually `@Inject(TENANCY_CLIENT)`) | ℹ️ Info       | Cosmetic naming only; functionally correct per existing audit comment in that file. Out of scope.                                                                                                                                  |
| Commit `41b79df` subject                            | —         | Cosmetic `\$transaction` escape carried into commit subject line | ℹ️ Info       | Deviation 5 in SUMMARY acknowledges this. Cosmetic only; commit hash + functional change unaffected. No-amend guidance makes the non-fix acceptable.                                                                               |
| Typecheck errors in 5 pre-existing files           | various   | Pre-existing `tsc --noEmit` errors in unrelated code (avatar/cameras.controller/gateways/minio) | ℹ️ Info       | Not introduced by this quick. Deviation 4 explains why `pnpm --filter api typecheck` doesn't exist and why these errors pre-date the quick.                                                                                         |

No blocker or warning anti-patterns found in the 9 files modified by this quick.

## Deviations Analysis

SUMMARY declared 5 deviations. Judging each:

1. **Residual `rawPrisma` grep hits (Deviation 1):** Acceptable. Audit pre-flagged both sites as safe; neither is the bug pattern 260422-ds9 targets. Modifying them would risk unrelated services without benefit.
2. **admin-dashboard.test.ts `$transaction` mock stub (Deviation 2):** Acceptable. Necessary consequence of wrapping the `$queryRaw` in `$transaction`. The mock faithfully mirrors production shape (passes the mock as `tx`). 17 tests remain green.
3. **Manual HTTP smoke tests not run (Deviation 3):** **The critical missing piece.** The structural fix is validated via psql + integration tests, but the user-facing ticket symptom — "Org Admin clicks Add Team Member and gets a permission toast" — has not been re-tested through the browser. Routed to human verification.
4. **`pnpm --filter api typecheck` script missing (Deviation 4):** Acceptable. Verified by me: `pnpm --filter @sms-platform/api exec tsc --noEmit` shows only 5 pre-existing errors in unrelated files; none are in the 9 files this quick modified.
5. **Commit message `\$` escape (Deviation 5):** Acceptable (cosmetic only, no-amend guidance applies).

## Gaps Summary

**None structural.** Every artifact listed in `must_haves` exists, is substantive, is wired, and (for data-rendering artifacts) has real data flowing through it. The 5 atomic commits land as planned. The RED→GREEN test signal is preserved in git history (749cbb6) and in the archived `/tmp/ds9-task1-red.log`.

**One UAT gap — browser end-to-end.** The only thing separating `human_needed` from `passed` is that the original user-reported symptom (clicking Add Team Member → seeing permission toast) has never been re-tested through the browser since the fix landed. All the infrastructure proves the DB layer now returns the right rows under app_user + FORCE RLS, but UI → HTTP → guard → service → DB is an integration path that only a human click-through can close. The summary explicitly flagged this as "Smoke 5 — NOT RUN" and recommended a dedicated fresh dev-server run before closing the ticket.

If the human UAT passes, this task is done. If it surfaces anything unexpected (unlikely given psql evidence), it would indicate a gap in the integration test harness's coverage of the full HTTP stack — worth a follow-up but not a blocker on the quick's core goal.

---

_Verified: 2026-04-22_
_Verifier: Claude (gsd-verifier)_
