---
phase: quick-260426-07r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/streams/streams.service.ts
  - apps/api/src/streams/stream-profile.service.ts
  - apps/api/tests/streams/streams-service-default-profile-fallback.test.ts
  - apps/api/tests/streams/stream-profile-delete-default-block.test.ts
autonomous: true
requirements:
  - quick-260426-07r
must_haves:
  truths:
    - "When a camera has streamProfileId=null AND its org has an isDefault=true profile, startStream's enqueued FFmpeg job carries that org-default profile's codec/preset/resolution/fps/bitrates (NOT the hardcoded {codec:'auto', audioCodec:'aac'})."
    - "When a camera has streamProfileId=null AND its org has NO isDefault profile, startStream falls back to the existing hardcoded {codec:'auto' as const, audioCodec:'aac' as const} (last-resort safety net for legacy null cameras BKR01–BKR04)."
    - "Deleting a profile with isDefault=true while OTHER profiles exist in the same org throws ConflictException with message starting 'Set another profile as default before deleting this one.' — distinct from the existing Phase 21 D-10 'Reassign before deleting' message so the frontend can disambiguate."
    - "Deleting a profile with isDefault=true when it is the ONLY profile in the org succeeds (org returns to a 0-profile state, runtime hardcoded fallback handles cameras)."
    - "Existing Phase 21 D-10 ConflictException ('camera using profile' / usedBy: [...]) is preserved unchanged — the new isDefault check is ADDED before the existing camera-usage check."
  artifacts:
    - path: "apps/api/src/streams/streams.service.ts"
      provides: "Org-default profile lookup in startStream null branch (lines 80-93 region)"
      contains: "streamProfile.findFirst"
    - path: "apps/api/src/streams/stream-profile.service.ts"
      provides: "isDefault precondition check in delete() with ConflictException (Set another profile as default ...)"
      contains: "Set another profile as default before deleting"
    - path: "apps/api/tests/streams/streams-service-default-profile-fallback.test.ts"
      provides: "Unit tests for cases (a) + (b) — null streamProfileId fallback to org isDefault and to hardcoded"
    - path: "apps/api/tests/streams/stream-profile-delete-default-block.test.ts"
      provides: "Unit tests for cases (c) + (d) — delete-default-block 409 and only-profile-allowed"
  key_links:
    - from: "apps/api/src/streams/streams.service.ts startStream null branch"
      to: "this.systemPrisma.streamProfile.findFirst({ where: { orgId, isDefault: true } })"
      via: "SystemPrismaService injection (already present, optional, line 26)"
      pattern: "streamProfile\\.findFirst"
    - from: "apps/api/src/streams/stream-profile.service.ts delete()"
      to: "ConflictException with 'Set another profile as default' message"
      via: "isDefault precondition check BEFORE existing usedBy check"
      pattern: "Set another profile as default before deleting"
---

<objective>
Backend semantic alignment for default-profile UX:
1. When `camera.streamProfileId` is null, `StreamsService.startStream` must look up the org's `isDefault=true` profile and use that profile's settings — NOT the hardcoded `{codec:'auto', audioCodec:'aac'}` fallback.
2. Block deletion of an `isDefault=true` profile when other profiles exist in the same org with HTTP 409 + a distinct error message ("Set another profile as default before deleting this one.") — preserve the org's invariant that a populated org always has exactly one default.

Purpose: Edge Cases A1 (locked choice 1A) and A3 (locked choice 3B) from the parent decision pack. Closes the runtime gap where 4 legacy null cameras (BKR01–BKR04 per choice 2C — no migration) get FFmpeg jobs with `codec:'auto'` instead of their org's default profile, and prevents accidental loss of the org's default profile while alternatives exist.

Output: Updated `streams.service.ts` + `stream-profile.service.ts` + 2 new test files (4 unit tests total). No DB migration. No frontend changes. No touching `enqueueProfileRestart` line 241 nor `job-data.helper.ts:27` — those serve different paths (hot-reload restart already has profile context; helper is consumed by recovery paths where the same fallback IS desired for now).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@apps/api/src/streams/streams.service.ts
@apps/api/src/streams/stream-profile.service.ts
@apps/api/src/streams/stream-profile.controller.ts
@apps/api/tests/streams/streams-service-push.test.ts
@apps/api/tests/streams/profile-restart-dedup.test.ts
@apps/api/tests/streams/stream-profile-restart.test.ts
@apps/api/src/prisma/prisma.module.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From apps/api/src/streams/streams.service.ts (constructor + relevant block):
```typescript
constructor(
  @Inject(TENANCY_CLIENT) private readonly prisma: any,
  @InjectQueue('stream-ffmpeg') private readonly streamQueue: Queue,
  private readonly ffmpegService: FfmpegService,
  private readonly statusService: StatusService,
  @Optional() private readonly systemPrisma?: SystemPrismaService,   // ← already injected, USE THIS
  @Optional() private readonly auditService?: AuditService,
  @Optional() @Inject(REDIS_CLIENT) private readonly redis?: Redis,
)
```

Current null branch in startStream (lines 80-93 — this is the ONLY site to modify per scoping):
```typescript
const profile = camera.streamProfile
  ? { codec: camera.streamProfile.codec, preset: ..., resolution: ..., fps: ...,
      videoBitrate: ..., audioCodec: ..., audioBitrate: ... }
  : { codec: 'auto' as const, audioCodec: 'aac' as const };  // ← REPLACE this branch
```

Note: `enqueueProfileRestart` line 241 has a similar `{codec:'auto', audioCodec:'aac'}` fallback but it is the explicit `'none-sentinel'` path (intentional non-null→null reassignment). DO NOT touch.
Note: `apps/api/src/resilience/job-data.helper.ts:27` has the same fallback; recovery paths are NOT in scope for this quick task.

From apps/api/src/streams/stream-profile.service.ts delete() at lines 109-134:
```typescript
async delete(id: string) {
  // existing Phase 21 D-10 check — DO NOT modify, only ADD a new check BEFORE it
  const usedBy = await this.prisma.camera.findMany({ where: { streamProfileId: id }, select: { id: true, name: true } });
  if (usedBy.length > 0) {
    throw new ConflictException({
      message: 'Stream profile is in use by one or more cameras. Reassign before deleting.',
      usedBy: usedBy.map((c: { id: string; name: string }) => ({ cameraId: c.id, name: c.name })),
    });
  }
  return this.prisma.streamProfile.delete({ where: { id } });
}
```

The new isDefault check must run BEFORE the existing usedBy check. Strategy: read the target row first (single findUnique), then sequence: (1) isDefault block check → (2) usedBy block check → (3) actual delete.

From apps/api/src/streams/stream-profile.controller.ts: `@Delete('stream-profiles/:id')` simply calls `this.profileService.delete(id)`. NestJS auto-maps `ConflictException` → HTTP 409. No controller change required.

From apps/api/src/prisma/prisma.module.ts: `@Global()` — `SystemPrismaService` is injectable everywhere with no module imports needed.

Test mock pattern (from streams-service-push.test.ts lines 39-69 and profile-restart-dedup.test.ts):
- Constructor positional args order: `(prisma, queue, ffmpeg, status, systemPrisma?, audit?, redis?)`.
- `mockPrisma.camera.findUnique` returns the camera row; tests can add `mockSystemPrisma.streamProfile.findFirst` separately.
- `vi.mock('fluent-ffmpeg', ...)` boilerplate at top of file (copy from streams-service-push.test.ts:14-30).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace null branch in startStream with org-default lookup + add isDefault delete-block in stream-profile.service</name>
  <files>apps/api/src/streams/streams.service.ts, apps/api/src/streams/stream-profile.service.ts</files>
  <behavior>
    streams.service.ts startStream null branch:
    - When `camera.streamProfile` is truthy → behavior unchanged (use embedded profile fields).
    - When `camera.streamProfile` is falsy AND `this.systemPrisma` is defined:
        - Query `this.systemPrisma.streamProfile.findFirst({ where: { orgId: camera.orgId, isDefault: true } })`.
        - If a row is returned → build the profile object with the SAME 7 fields used in the truthy branch (codec, preset, resolution, fps, videoBitrate, audioCodec, audioBitrate) and emit `this.logger.log` with text exactly: `camera ${camera.id} streamProfileId is null — using org default profile ${defaultRow.id} (${defaultRow.name})`.
        - If null is returned → emit `this.logger.warn` with text exactly: `camera ${camera.id} streamProfileId is null AND no isDefault profile in org ${camera.orgId} — using hardcoded {codec:'auto'} fallback`, then fall through to existing `{ codec: 'auto' as const, audioCodec: 'aac' as const }`.
    - When `camera.streamProfile` is falsy AND `this.systemPrisma` is undefined (defensive — should not happen in prod) → fall through to existing hardcoded fallback (no log noise).
    - Downstream `jobData` shape and `streamQueue.add` call unchanged.

    stream-profile.service.ts delete():
    - Read target row first: `const target = await this.prisma.streamProfile.findUnique({ where: { id }, select: { id: true, orgId: true, isDefault: true } })`. If null → throw NotFoundException-equivalent error (or let downstream `prisma.streamProfile.delete` fail naturally — check existing pattern; current code does not pre-fetch, so add a defensive `if (!target) throw new NotFoundException('Stream profile not found')` to keep the controller's NotFound behavior consistent — import NotFoundException from @nestjs/common alongside existing ConflictException import).
    - If `target.isDefault === true`:
        - `const otherCount = await this.prisma.streamProfile.count({ where: { orgId: target.orgId, id: { not: target.id } } })`.
        - If `otherCount > 0` → `throw new ConflictException('Set another profile as default before deleting this one. Run PATCH /api/stream-profiles/:id with isDefault=true on the profile you want to promote, then retry delete.')` (PLAIN STRING message, NOT object form — keeps the message string distinct and easily matched, and Phase 21 D-10's object-form usedBy stays differentiable by shape).
        - If `otherCount === 0` → fall through (allow delete; org returns to 0-profile state).
    - Existing usedBy check + final delete unchanged.
  </behavior>
  <action>
    1. Edit `apps/api/src/streams/streams.service.ts`:
       - Locate the null branch at lines 80-93 in `startStream`.
       - Replace the single ternary with the resolution sequence described in <behavior>. Use `let profile` so the value can be assigned across branches; or wrap in an IIFE / helper inline — pick the cleanest local form. Preserve the resulting object shape exactly (7 fields when isDefault found, 2 fields when hardcoded).
       - DO NOT modify `enqueueProfileRestart` (lines 158-329) — its `'none-sentinel'` path (line 226) is intentionally distinct.
       - DO NOT touch `apps/api/src/resilience/job-data.helper.ts` — out of scope per quick-task constraints.

    2. Edit `apps/api/src/streams/stream-profile.service.ts`:
       - Add `NotFoundException` to the existing `@nestjs/common` import line (currently imports `ConflictException, Inject, Injectable, Logger, Optional`).
       - Modify `delete(id: string)` per <behavior>: pre-fetch target, isDefault precondition (with otherCount), then existing usedBy check, then existing prisma.delete.
       - Order matters — isDefault check FIRST, usedBy SECOND. Rationale: an admin trying to delete THE default profile needs to know "promote another default first" before being told "and also reassign cameras"; the two errors are independent failure modes and 409 returns the first one hit.
       - Keep the existing usedBy ConflictException unchanged (object form with `usedBy: [...]` array). The new ConflictException uses a plain string message — frontend disambiguates by shape (string vs object) AND message prefix.

    3. Run TypeScript check: `pnpm --filter @sms-platform/api exec tsc --noEmit` to confirm no type regression. (No Prisma schema change → no `db:push` / regenerate / restart cycle needed per CLAUDE.md "Prisma schema change workflow" guidance — that workflow only applies to schema.prisma edits.)
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && pnpm --filter @sms-platform/api exec tsc --noEmit 2>&amp;1 | head -40</automated>
  </verify>
  <done>
    - `streams.service.ts` startStream null branch queries `systemPrisma.streamProfile.findFirst` for org default, logs which path was taken, falls back to hardcoded only when no isDefault exists.
    - `stream-profile.service.ts` delete() throws ConflictException with "Set another profile as default" when isDefault profile + other profiles exist; allows delete when isDefault is the only profile; existing usedBy check preserved unchanged.
    - `tsc --noEmit` reports no errors in the modified files (other pre-existing errors elsewhere acceptable — judge by diff).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 4 unit tests covering null-fallback (a, b) and delete-default-block (c, d)</name>
  <files>apps/api/tests/streams/streams-service-default-profile-fallback.test.ts, apps/api/tests/streams/stream-profile-delete-default-block.test.ts</files>
  <behavior>
    Test (a) in streams-service-default-profile-fallback.test.ts:
    - Camera with `streamProfileId: null`, `streamProfile: null`, `ingestMode: 'pull'`, `needsTranscode: true`, `streamUrl: 'rtsp://cam/1'`, `orgId: 'orgA'`.
    - mockSystemPrisma.streamProfile.findFirst returns `{ id: 'pDef', orgId: 'orgA', isDefault: true, codec: 'libx264', preset: 'fast', resolution: '1280x720', fps: 25, videoBitrate: '1500k', audioCodec: 'aac', audioBitrate: '96k' }`.
    - Call `svc.startStream('cam-null')`.
    - Assert `queue.add` called once with name `'start'`, `jobId: 'camera:cam-null:ffmpeg'`, and `jobData.profile` equals exactly `{ codec: 'libx264', preset: 'fast', resolution: '1280x720', fps: 25, videoBitrate: '1500k', audioCodec: 'aac', audioBitrate: '96k' }`.
    - Assert mockSystemPrisma.streamProfile.findFirst called with `{ where: { orgId: 'orgA', isDefault: true } }`.

    Test (b) in same file:
    - Same camera shape as (a).
    - mockSystemPrisma.streamProfile.findFirst returns `null`.
    - Call `svc.startStream('cam-null')`.
    - Assert `queue.add` called once with `jobData.profile` equals exactly `{ codec: 'auto', audioCodec: 'aac' }` (the `as const` literals serialize to plain strings).

    Test (c) in stream-profile-delete-default-block.test.ts:
    - mockPrisma.streamProfile.findUnique returns `{ id: 'pDef', orgId: 'orgA', isDefault: true }`.
    - mockPrisma.streamProfile.count (called with `where: { orgId: 'orgA', id: { not: 'pDef' } }`) returns `2`.
    - Call `svc.delete('pDef')`.
    - Assert promise rejects with `ConflictException` whose `.message` (or `.getResponse()`) contains substring `'Set another profile as default'`.
    - Assert `mockPrisma.streamProfile.delete` was NOT called.
    - Assert `mockPrisma.camera.findMany` was NOT called (we short-circuited before the usedBy check — proves ordering).

    Test (d) in same file:
    - mockPrisma.streamProfile.findUnique returns `{ id: 'pDef', orgId: 'orgA', isDefault: true }`.
    - mockPrisma.streamProfile.count returns `0` (only profile in org).
    - mockPrisma.camera.findMany returns `[]` (no cameras using it either).
    - mockPrisma.streamProfile.delete returns `{ id: 'pDef' }`.
    - Call `svc.delete('pDef')` → resolves successfully.
    - Assert `mockPrisma.streamProfile.delete` called with `{ where: { id: 'pDef' } }`.
  </behavior>
  <action>
    1. Create `apps/api/tests/streams/streams-service-default-profile-fallback.test.ts`:
       - Copy the `vi.mock('fluent-ffmpeg', ...)` boilerplate from streams-service-push.test.ts:14-30 verbatim (required so FfmpegService import doesn't try to spawn).
       - Build harness with `mockPrisma` (camera.findUnique returns the null-profile camera), `mockSystemPrisma` (streamProfile.findFirst per test), mocked `queue`, `ffmpegService`, `statusService`.
       - Constructor: `new StreamsService(mockPrisma, mockQueue, mockFfmpegService, mockStatusService, mockSystemPrisma)` (5th positional arg is systemPrisma).
       - Two `it()` blocks for cases (a) and (b).
       - For case (b), keep mockSystemPrisma defined but its findFirst returns null — proves the "exists, no row" path triggers hardcoded fallback (NOT the "no systemPrisma" path).

    2. Create `apps/api/tests/streams/stream-profile-delete-default-block.test.ts`:
       - Standard vitest imports.
       - Build harness with mocked prisma object exposing `streamProfile.findUnique`, `streamProfile.count`, `streamProfile.delete`, `camera.findMany`.
       - Constructor: `new StreamProfileService(mockPrisma)` — second arg streamsService is optional and not exercised by `delete()`, so omit.
       - Two `it()` blocks for cases (c) and (d).
       - Use `await expect(svc.delete('pDef')).rejects.toBeInstanceOf(ConflictException)` and `await expect(svc.delete('pDef')).rejects.toThrow(/Set another profile as default/)` (run as separate awaits or use `.rejects.toMatchObject(...)` — pick the form that survives ConflictException's wrapping; refer to profile-restart-dedup.test.ts for working `rejects` patterns if any are present).
       - Import `ConflictException` from `@nestjs/common`.

    3. Run the new tests:
       `pnpm --filter @sms-platform/api exec vitest run tests/streams/streams-service-default-profile-fallback.test.ts tests/streams/stream-profile-delete-default-block.test.ts`
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app &amp;&amp; pnpm --filter @sms-platform/api exec vitest run tests/streams/streams-service-default-profile-fallback.test.ts tests/streams/stream-profile-delete-default-block.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <done>
    - Both new test files exist with 2 tests each (4 tests total).
    - All 4 tests pass.
    - No regression in existing streams test suite — verify with `pnpm --filter @sms-platform/api exec vitest run tests/streams/ 2>&amp;1 | tail -20` and confirm `Test Files` count and `Tests` count both show all-passing.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request → StreamProfileController.delete | Authenticated org admin request crosses into service layer. AuthGuard already enforced at controller. |
| StreamsService → SystemPrismaService | RLS bypass — but scoped query (where: { orgId: camera.orgId, isDefault: true }) prevents cross-org leak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07r-01 | Information Disclosure | streams.service.ts org-default lookup | mitigate | Query is `{ orgId: camera.orgId, isDefault: true }` — scoped by the camera's own orgId (already validated by upstream tenancy lookup or SRS callback streamKey resolution). No cross-org read possible. SystemPrismaService is appropriate here because we resolve by explicit orgId rather than relying on RLS context (the SRS on_publish callback path has no CLS context — same reason existing line 56-61 uses systemPrisma). |
| T-07r-02 | Denial of Service | stream-profile delete isDefault branch | accept | Adding 1 extra Prisma `count()` query per delete is O(1) per org, runs only on DELETE (low-volume admin op). No unbounded fan-out. |
| T-07r-03 | Tampering | stream-profile delete order-of-checks | mitigate | isDefault check runs BEFORE usedBy check — both throw ConflictException with distinct shapes (string message vs `{message, usedBy[]}` object). Frontend cannot be tricked into showing the wrong error since the shapes are inspected. Test (c) explicitly asserts ordering by checking that camera.findMany was NOT called when isDefault check trips. |
| T-07r-04 | Repudiation | (audit logging) | accept | This quick task does not introduce new state mutations needing audit beyond what `delete` already implicitly creates (Prisma row removal). No new audit row required. The new ConflictException paths are validation-only and do not mutate. |
</threat_model>

<verification>
1. Run all streams tests: `pnpm --filter @sms-platform/api exec vitest run tests/streams/` — no regressions.
2. Manual smoke (optional, post-restart): Pick a BKR0X camera with `streamProfileId IS NULL`. Ensure the org has an `isDefault=true` profile. POST `/api/cameras/{id}/start` and tail API logs — should see `camera {id} streamProfileId is null — using org default profile {pId} ({pName})`. Then check the BullMQ stream-ffmpeg queue payload (or FFmpeg ps args) for the org-default codec/bitrate, NOT `-c:v copy` (auto).
3. Manual smoke for delete: Create a 2nd profile in an org, mark it isDefault. Try DELETE the original default — expect 409 with body containing "Set another profile as default". Promote the new one (PATCH isDefault=true on it — which un-defaults the original). Retry DELETE on the original — should succeed (now non-default).
</verification>

<success_criteria>
- All 4 new unit tests pass.
- No regression in existing `tests/streams/` suite.
- `tsc --noEmit` clean for the two modified files.
- Logs at the runtime fallback boundary are present and discoverable (`grep "streamProfileId is null"`).
- Delete-default 409 message is verifiably distinct from Phase 21 D-10 usedBy 409 (different message string AND different response body shape).
</success_criteria>

<output>
After completion, create `.planning/quick/260426-07r-backend-default-profile-semantic-alignme/260426-07r-SUMMARY.md` documenting:
- The two file edits with line-range references
- The 4 test cases and their pass status
- Confirmation that `enqueueProfileRestart` line 241 and `job-data.helper.ts:27` were intentionally NOT modified (out of scope for this quick task)
- Note that 4 legacy null cameras BKR01–BKR04 stay null in DB per choice 2C (no migration); runtime fallback now correctly resolves them to the org default profile when one exists.
</output>
