---
phase: quick-260426-2vj
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/quick-start-guide.tsx
autonomous: true
requirements:
  - QUICK-260426-2vj-01
must_haves:
  truths:
    - "Step 2 curl example shows literal CAMERA_ID placeholder, not a real camera UUID"
    - "Step 2 curl example shows literal sk_live_YOUR_API_KEY placeholder, not a real key prefix/suffix"
    - "The two green hint paragraphs (apiKeyHint, cameraHint) under Step 2's description are removed"
    - "Step 1 and Step 3 are unchanged"
    - "The component renders immediately without a loading skeleton (no remaining async dependency)"
  artifacts:
    - path: "apps/web/src/components/quick-start-guide.tsx"
      provides: "QuickStartGuide component with fully static Step 2 template"
      contains: "CAMERA_ID"
  key_links:
    - from: "apps/web/src/components/quick-start-guide.tsx"
      to: "step2Curl rendered via CodeBlock"
      via: "literal string template"
      pattern: "CAMERA_ID/sessions"
---

<objective>
Make Developer Portal Overview Step 2 ("Create a Playback Session") a static template
matching Step 1's pattern: replace dynamic camera ID and API key prefix with literal
placeholders (CAMERA_ID, sk_live_YOUR_API_KEY), and drop the two green hint lines that
referenced the user's actual key/camera. With the dynamic data no longer rendered,
remove the dead fetch + state + interfaces + skeleton-loading branch.

Purpose: Step 2 should read as generic example documentation, not personalized output.
Output: A leaner QuickStartGuide that fetches no data and renders synchronously.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/web/src/components/quick-start-guide.tsx

<interfaces>
<!-- Current shape (from apps/web/src/components/quick-start-guide.tsx) -->
<!-- After this plan, BOTH ApiKeyInfo and CameraInfo interfaces are deleted. -->

```typescript
// To be removed:
interface ApiKeyInfo {
  id: string;
  name: string;
  prefix: string;
  lastFour: string;
  revokedAt: string | null;
}

interface CameraInfo {
  id: string;
  name: string;
}

// To be removed: useState<ApiKeyInfo[]>, useState<CameraInfo[]>, useState(loading)
// To be removed: useEffect fetching /api/api-keys and /api/cameras via apiFetch
// To be removed: activeKey/apiKeyDisplay/apiKeyHint and firstCamera/cameraIdDisplay/cameraHint derivations
// To be removed: if (loading) return <Skeleton .../> branch
// To be removed: imports for useState, useEffect, Skeleton, apiFetch (if unused after edit)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip dynamic data from QuickStartGuide and make Step 2 static</name>
  <files>apps/web/src/components/quick-start-guide.tsx</files>
  <action>
Edit `apps/web/src/components/quick-start-guide.tsx` to make Step 2 a fully static
template and remove all now-dead code paths. Specifically:

1. Replace the `step2Curl` template literal so the URL uses the literal token
   `CAMERA_ID` and the X-API-Key header uses the literal `sk_live_YOUR_API_KEY`:

   ```ts
   const step2Curl = `curl -X POST ${baseUrl}/api/cameras/CAMERA_ID/sessions \\
     -H "X-API-Key: sk_live_YOUR_API_KEY"`;
   ```

   (Two-space indent on the continuation line, matching the existing Step 1 style.)

2. Delete both green-hint `<p>` blocks rendered between the description paragraph
   and the `<CodeBlock code={step2Curl} ... />` in Step 2's `<CardContent>`:
   - `{activeKey ? (<p className="text-xs text-primary font-medium">{apiKeyHint}</p>) : (<p className="text-xs text-amber-600 font-medium">{apiKeyHint}</p>)}`
   - `{firstCamera ? (<p className="text-xs text-primary font-medium">{cameraHint}</p>) : (<p className="text-xs text-amber-600 font-medium">{cameraHint}</p>)}`

   After removal, Step 2's `<CardContent>` should contain only:
   description `<p>` -> `<CodeBlock code={step2Curl} language="bash" />`.

3. Step 1 (`step1Curl`) and Step 3 (`step3Iframe`, `step3Hls`) are unchanged.

4. Because nothing else in the component reads `apiKeys`, `cameras`, `loading`,
   `activeKey`, `apiKeyDisplay`, `apiKeyHint`, `firstCamera`, `cameraIdDisplay`,
   or `cameraHint`, remove all of them per the "no dead code" constraint:

   - Delete `interface ApiKeyInfo` and `interface CameraInfo`.
   - Delete the three `useState` calls (`apiKeys`, `cameras`, `loading`).
   - Delete the entire `useEffect` block that calls `apiFetch("/api/api-keys")`
     and `apiFetch("/api/cameras")`.
   - Delete the `activeKey` / `apiKeyDisplay` / `apiKeyHint` derivations.
   - Delete the `firstCamera` / `cameraIdDisplay` / `cameraHint` derivations.
   - Delete the `if (loading) { return <Skeleton .../> }` branch.

5. Clean up imports that are now unused. After step 4 the only remaining hook usage
   is the `baseUrl` derivation which uses `typeof window`, no React hooks. Drop:
   - `useState`, `useEffect` from `react` (the `react` import line itself can be
     deleted entirely if nothing else from `react` is imported).
   - `Skeleton` from `@/components/ui/skeleton`.
   - `apiFetch` from `@/lib/api`.

   Keep: `Card`/`CardContent`/`CardHeader`/`CardTitle`, `CodeBlock`,
   `Tabs`/`TabsContent`/`TabsList`/`TabsTrigger`.

6. Keep the `"use client"` directive (Tabs uses client-side state internally, and
   `typeof window` access requires it).

Do not add comments unless a non-obvious WHY needs documenting; the change is
self-explanatory. Do not modify any other component or import sites of
`QuickStartGuide`.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>
- `apps/web/src/components/quick-start-guide.tsx` no longer imports `useState`,
  `useEffect`, `Skeleton`, or `apiFetch`.
- `grep -n "apiKeyHint\|cameraHint\|ApiKeyInfo\|CameraInfo\|/api/api-keys\|/api/cameras" apps/web/src/components/quick-start-guide.tsx` returns no matches.
- `grep -n "CAMERA_ID/sessions" apps/web/src/components/quick-start-guide.tsx` returns exactly one match inside `step2Curl`.
- `grep -n "sk_live_YOUR_API_KEY" apps/web/src/components/quick-start-guide.tsx` returns exactly one match inside `step2Curl`.
- Step 2's `<CardContent>` renders description paragraph + `<CodeBlock>` only (no `<p className="text-xs ...">` siblings).
- Step 1 and Step 3 markup is byte-identical to the previous version.
- `pnpm --filter @sms-platform/web exec tsc --noEmit` passes.
  </done>
</task>

</tasks>

<verification>
- TypeScript: `pnpm --filter @sms-platform/web exec tsc --noEmit` clean.
- Lint (if configured for the web package): `pnpm --filter @sms-platform/web lint`
  reports no new warnings/errors against `quick-start-guide.tsx`.
- Manual smoke (the developer can do this in the running dev server, not required
  to gate the task):
  1. Navigate to Developer Portal Overview while signed in.
  2. Step 2 curl block shows
     `curl -X POST <origin>/api/cameras/CAMERA_ID/sessions` and
     `-H "X-API-Key: sk_live_YOUR_API_KEY"`, regardless of how many keys/cameras
     the org has.
  3. No green or amber hint text is rendered between the Step 2 description and
     the curl block.
  4. The page no longer briefly shows three skeleton blocks before mounting --
     content paints synchronously.
</verification>

<success_criteria>
- Step 2 is a generic template identical in spirit to Step 1 (Step 1 uses
  YOUR_PROJECT_ID / YOUR_SESSION; Step 2 now uses CAMERA_ID / sk_live_YOUR_API_KEY).
- No code remains in `quick-start-guide.tsx` that reads from `/api/api-keys` or
  `/api/cameras`.
- TypeScript compiles cleanly with no unused-import or unused-variable diagnostics
  attributable to this file.
</success_criteria>

<output>
After completion, create `.planning/quick/260426-2vj-developer-portal-overview-step-2-remove-/260426-2vj-SUMMARY.md`.
</output>
