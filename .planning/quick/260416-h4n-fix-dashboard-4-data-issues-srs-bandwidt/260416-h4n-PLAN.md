---
phase: quick
plan: 260416-h4n
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/dashboard/dashboard.service.ts
  - apps/api/src/dashboard/dashboard.module.ts
  - apps/web/src/hooks/use-dashboard-stats.ts
  - apps/web/src/components/dashboard/camera-status-table.tsx
  - apps/web/src/components/pages/tenant-dashboard-page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Bandwidth stat card shows live SRS stream bandwidth (kbps from SRS API), not API response bytes"
    - "Camera status table shows per-camera bandwidth without NaN — displays '0 B/s' for cameras with no data"
    - "Camera status table shows correct viewer count (field name matches between backend and frontend)"
    - "Usage charts include today's data from Redis, not just PostgreSQL aggregated data"
  artifacts:
    - path: "apps/api/src/dashboard/dashboard.service.ts"
      provides: "SRS stream bandwidth in getStats(), fixed field names in getCameraStatusList(), Redis today-data in getUsageTimeSeries()"
    - path: "apps/web/src/components/dashboard/camera-status-table.tsx"
      provides: "NaN-safe formatBandwidth()"
    - path: "apps/web/src/components/pages/tenant-dashboard-page.tsx"
      provides: "Stat card wired to streamBandwidth field"
  key_links:
    - from: "dashboard.service.ts getStats()"
      to: "SrsApiService.getStreams()"
      via: "sum kbps.send_30s for org's streams"
      pattern: "srsApiService\\.getStreams"
    - from: "dashboard.service.ts getUsageTimeSeries()"
      to: "Redis apikey:usage keys"
      via: "REDIS_CLIENT scan for today's date"
      pattern: "redis\\.keys.*apikey:usage"
    - from: "tenant-dashboard-page.tsx"
      to: "stats.streamBandwidth"
      via: "formatBandwidth for stat card"
      pattern: "stats\\.streamBandwidth"
---

<objective>
Fix 4 confirmed dashboard data issues: (1) bandwidth stat card shows 0 because it reads API bytes not SRS stream bandwidth, (2) usage charts empty because today's data is only in Redis not PostgreSQL, (3) camera table shows "NaN GB/s" because backend field names don't match frontend interface, (4) bandwidth type mismatch between backend string and frontend number.

Purpose: Make tenant dashboard show accurate, live data instead of zeros and NaN.
Output: Working dashboard with live SRS bandwidth, correct camera table, and same-day usage charts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@apps/api/src/dashboard/dashboard.service.ts
@apps/api/src/dashboard/dashboard.module.ts
@apps/api/src/srs/srs-api.service.ts
@apps/api/src/api-keys/api-keys.module.ts
@apps/api/src/api-keys/api-keys.service.ts
@apps/web/src/hooks/use-dashboard-stats.ts
@apps/web/src/components/dashboard/camera-status-table.tsx
@apps/web/src/components/pages/tenant-dashboard-page.tsx
@.planning/debug/dashboard-static-data.md

<interfaces>
<!-- Backend services already available -->

From apps/api/src/srs/srs-api.service.ts:
```typescript
@Injectable()
export class SrsApiService {
  async getStreams(nodeApiUrl?: string): Promise<any>; // Returns { streams: [{ app, name, kbps: { recv_30s, send_30s }, ... }] }
  async getSummaries(nodeApiUrl?: string): Promise<any>;
}
```

From apps/api/src/api-keys/api-keys.service.ts:
```typescript
export const REDIS_CLIENT = Symbol('REDIS_CLIENT');
// Redis key pattern: apikey:usage:{keyId}:{YYYY-MM-DD}:requests
// Redis key pattern: apikey:usage:{keyId}:{YYYY-MM-DD}:bandwidth
```

SRS stream URL pattern: live/{orgId}/{cameraId}
- getStreams() returns streams with `app` = "live" and `name` = "{orgId}/{cameraId}" (or `app` = "live/{orgId}", `name` = "{cameraId}")

From apps/web/src/hooks/use-dashboard-stats.ts:
```typescript
export interface DashboardStats {
  camerasOnline: number; camerasOffline: number; totalCameras: number;
  totalViewers: number; bandwidth: number; // Currently: API bytes as string from backend
}
export interface DashboardCamera {
  id: string; name: string; status: ...; lastOnlineAt: string | null;
  viewerCount: number; // Backend returns "viewers" -- MISMATCH
  bandwidth: number;   // Backend doesn't return this -- MISSING
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend — wire SRS bandwidth, fix field names, add Redis today-data</name>
  <files>apps/api/src/dashboard/dashboard.service.ts, apps/api/src/dashboard/dashboard.module.ts</files>
  <action>
**dashboard.module.ts:**
- Import `ApiKeysModule` from `../api-keys/api-keys.module` and add to `imports` array (this exports `ApiKeysService` and makes `REDIS_CLIENT` available if needed, but we need direct Redis access).
- Add a `REDIS_CLIENT` provider to DashboardModule providers (same factory as ApiKeysModule: `new Redis({ host: process.env.REDIS_HOST || 'localhost', port: parseInt(process.env.REDIS_PORT || '6379', 10) })`). Import `REDIS_CLIENT` symbol from `../api-keys/api-keys.service`. Import `Redis` from `ioredis`.

**dashboard.service.ts:**
- Add `@Inject(REDIS_CLIENT) private readonly redis: Redis` to constructor. Import `Redis` from `ioredis`, `REDIS_CLIENT` from `../api-keys/api-keys.service`.

**Fix getStats() — add streamBandwidth from SRS:**
- After existing bandwidth calculation, query SRS: `const srsStreams = await this.srsApiService.getStreams();`
- Extract org's camera IDs from the cameras query already done above.
- Filter SRS streams matching this org: parse each stream's full path from `stream.app + '/' + stream.name`, check if path starts with `live/${orgId}/` or if `stream.app === 'live'` and `stream.name` starts with `${orgId}/`. Extract cameraId from the path.
- Sum `stream.kbps?.send_30s || 0` across matching streams to get `streamBandwidth` (in kbps).
- Build a Map of cameraId -> bandwidth (kbps) for use in getCameraStatusList (store on a class-level cache or pass through — simplest: just return it from getStats and let controller handle, OR compute in getCameraStatusList separately).
- Return object: add `streamBandwidth` (number, kbps). Keep existing `bandwidth` (string, API bytes) for charts.
- Wrap SRS call in try/catch, default `streamBandwidth: 0` on failure.

**Fix getCameraStatusList() — rename field + add per-camera bandwidth:**
- Query SRS streams: `const srsStreams = await this.srsApiService.getStreams();`
- Build a map: for each SRS stream matching pattern `live/{orgId}/{cameraId}`, map cameraId -> `{ bandwidth: stream.kbps?.send_30s || 0, viewers: stream.clients || 0 }`.
- In the camera map function, change `viewers: this.statusService.getViewerCount(camera.id)` to:
  - `viewerCount: this.statusService.getViewerCount(camera.id)` (renamed to match frontend)
  - `bandwidth: (srsStreamMap.get(camera.id)?.bandwidth || 0) * 1000 / 8` — convert kbps to bytes/sec for formatBandwidth() on frontend. Actually SRS kbps is kilobits per second. Convert: `kbps * 1000 / 8` = bytes/sec. OR simpler: just return kbps and let frontend handle. Decision: return raw kbps and document it. Frontend formatBandwidth expects bytes, so convert here: `Math.round((kbps * 1000) / 8)` to get bytes/sec.
- Wrap SRS call in try/catch, default empty map on failure.

**Fix getUsageTimeSeries() — supplement with Redis today-data:**
- After the existing PostgreSQL query, check if today's date (`new Date().toISOString().slice(0, 10)`) falls within the range.
- If yes, query Redis for today's usage: scan keys matching `apikey:usage:*:${today}:requests` using `this.redis.keys(...)`.
- For each found key, also read the bandwidth key. Sum all requests and bandwidth values.
- If today already exists in the grouped map (from PostgreSQL), add Redis values on top. If not, insert a new entry for today.
- Wrap Redis call in try/catch so chart still works if Redis is down.
- Need to filter Redis keys to only this org's API keys. Query org's API key IDs: `const orgKeys = await this.prisma.apiKey.findMany({ where: { orgId }, select: { id: true } });` then `const orgKeyIds = new Set(orgKeys.map(k => k.id));`. Only sum Redis entries where keyId is in orgKeyIds.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && npx nx run api:build 2>&1 | tail -5</automated>
  </verify>
  <done>
- getStats() returns `streamBandwidth` (number, kbps from SRS) alongside existing `bandwidth` (string, API bytes)
- getCameraStatusList() returns `viewerCount` (not `viewers`) and `bandwidth` (bytes/sec from SRS) per camera
- getUsageTimeSeries() includes today's Redis data merged with PostgreSQL data
- Backend compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Frontend — fix types, NaN guard, wire streamBandwidth</name>
  <files>apps/web/src/hooks/use-dashboard-stats.ts, apps/web/src/components/dashboard/camera-status-table.tsx, apps/web/src/components/pages/tenant-dashboard-page.tsx</files>
  <action>
**use-dashboard-stats.ts:**
- Add `streamBandwidth: number` to `DashboardStats` interface.
- `bandwidth` stays as `number` in the interface (backend sends string but JSON parse handles "0" fine; for safety, in `fetchStats` callback after `apiFetch`, coerce: `data.bandwidth = Number(data.bandwidth); data.streamBandwidth = Number(data.streamBandwidth ?? 0);`). Actually since apiFetch returns typed, do the coercion after: `setStats({ ...data, bandwidth: Number(data.bandwidth), streamBandwidth: Number(data.streamBandwidth ?? 0) });`.

**camera-status-table.tsx:**
- Fix `formatBandwidth()` to guard against NaN/undefined/null: At the top of the function, add `if (bytes == null || isNaN(bytes) || typeof bytes !== 'number') return '0 B/s';`

**tenant-dashboard-page.tsx:**
- Change the Bandwidth stat card from `formatBandwidth(stats.bandwidth)` to use `stats.streamBandwidth`.
- Since streamBandwidth is in kbps (kilobits/sec) and formatBandwidth expects bytes: either (a) convert here `formatBandwidth(stats.streamBandwidth * 125)` (kbps * 1000/8 = bytes/s... 1 kbps = 125 bytes/s), OR (b) if backend Task 1 already returns bytes/sec for streamBandwidth, use directly. Decision: Backend should return kbps (raw SRS value) for clarity. Frontend converts: `stats.streamBandwidth * 125` to bytes/sec for formatBandwidth.
- Actually, re-reading Task 1: getStats returns streamBandwidth in kbps. So: `formatBandwidth(stats.streamBandwidth * 125)`.
- Also update stat card label from "Bandwidth" to "Stream Bandwidth" to distinguish from API bandwidth in charts.
- Add NaN guard on the formatBandwidth call in this file too: same pattern as camera-status-table.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && npx nx run web:build 2>&1 | tail -10</automated>
  </verify>
  <done>
- DashboardStats interface includes streamBandwidth field
- Bandwidth stat card shows live SRS stream bandwidth (converted from kbps), labeled "Stream Bandwidth"
- Camera table formatBandwidth never produces NaN — returns "0 B/s" for undefined/null/NaN
- Both bandwidth values properly coerced to numbers from backend strings
- Frontend compiles without errors
  </done>
</task>

</tasks>

<verification>
1. Backend builds: `npx nx run api:build`
2. Frontend builds: `npx nx run web:build`
3. Manual spot-check: With a camera streaming, dashboard Bandwidth card should show non-zero kbps value. Camera table should show per-camera bandwidth without NaN. Usage charts should show today's data points.
</verification>

<success_criteria>
- Bandwidth stat card displays live SRS stream bandwidth (not API response bytes)
- Camera status table shows numeric bandwidth per camera, never "NaN GB/s"
- Camera status table viewerCount field matches between backend and frontend
- Usage charts include same-day data from Redis
- No TypeScript compilation errors in either app
</success_criteria>

<output>
After completion, create `.planning/quick/260416-h4n-fix-dashboard-4-data-issues-srs-bandwidt/260416-h4n-SUMMARY.md`
</output>
