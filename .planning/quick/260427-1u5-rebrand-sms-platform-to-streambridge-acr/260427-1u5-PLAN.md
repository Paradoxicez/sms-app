---
phase: 260427-1u5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/src/components/brand/streambridge-logo.tsx
  - apps/web/src/app/layout.tsx
  - apps/web/src/app/(auth)/sign-in/page.tsx
  - apps/web/src/components/nav/app-sidebar.tsx
  - apps/web/src/app/admin/developer/docs/api-workflow/page.tsx
  - apps/web/src/app/admin/developer/docs/streaming-basics/page.tsx
  - apps/web/src/components/pages/tenant-developer-docs-page.tsx
  - apps/api/src/main.ts
autonomous: true
requirements:
  - REBRAND-01
  - REBRAND-02
  - REBRAND-03

must_haves:
  truths:
    - "Browser tab on every page reads 'StreamBridge' (not 'SMS Platform')"
    - "Sign-in page left panel shows the StreamBridge lockup, the existing 'Surveillance Management System' headline, the new sub-tagline, four protocol pills (RTSP / RTMP / SRT / WebRTC) with a caption, and the 2026 StreamBridge footer — the three stat boxes (99.9% / <1s / 24/7) are gone"
    - "Sidebar header shows the StreamBridge brand (lockup or icon+wordmark) instead of the bare 'SMS' text"
    - "All four developer-docs strings call the product 'StreamBridge', not 'SMS Platform'"
    - "API Swagger title and boot console message say 'StreamBridge API'"
    - "Internal package names (@sms-platform/*), docker-compose service names, env vars, DB schema, SRS templates, and test fixtures are unchanged"
    - "pnpm --filter @sms-platform/web build exits 0"
    - "grep for 'SMS Platform' under apps/web/src and apps/api/src (excluding tests/node_modules) returns zero matches"
  artifacts:
    - path: "apps/web/src/components/brand/streambridge-logo.tsx"
      provides: "Reusable logo component with icon + lockup variants and light/dark themes"
      exports: ["StreamBridgeLogo"]
    - path: "apps/web/src/app/layout.tsx"
      provides: "Updated browser tab title metadata"
      contains: "title: 'StreamBridge'"
    - path: "apps/web/src/app/(auth)/sign-in/page.tsx"
      provides: "Redesigned left brand panel: lockup + tagline + protocol pills + 2026 footer"
      contains: "StreamBridgeLogo"
    - path: "apps/web/src/components/nav/app-sidebar.tsx"
      provides: "Sidebar header showing StreamBridge brand"
      contains: "StreamBridgeLogo"
    - path: "apps/api/src/main.ts"
      provides: "Updated Swagger title + boot log message"
      contains: ".setTitle('StreamBridge API')"
  key_links:
    - from: "apps/web/src/app/(auth)/sign-in/page.tsx"
      to: "apps/web/src/components/brand/streambridge-logo.tsx"
      via: "named import { StreamBridgeLogo }"
      pattern: "import.*StreamBridgeLogo.*from.*brand/streambridge-logo"
    - from: "apps/web/src/components/nav/app-sidebar.tsx"
      to: "apps/web/src/components/brand/streambridge-logo.tsx"
      via: "named import { StreamBridgeLogo }"
      pattern: "import.*StreamBridgeLogo.*from.*brand/streambridge-logo"
---

<objective>
Rebrand all user-facing references from "SMS Platform" / "SMS" to "StreamBridge" across the web UI and API boot strings, and redesign the sign-in left panel (drop the three stat boxes, add four protocol pills, update tagline). Internal identifiers (package names, docker service names, env vars, DB schema, SRS config templates, test fixtures, hook IDs) MUST remain unchanged.

Purpose: Single source-of-truth product name that users see, with a polished login first impression that highlights the supported ingest protocols rather than vanity uptime numbers.

Output: One new logo component + six modified files (layout, sign-in, sidebar, three docs files, api main).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md

<interfaces>
<!-- Key snippets the executor needs. Extracted from the codebase already read by the planner. -->
<!-- The executor should use these directly without re-exploring. -->

From apps/web/src/app/layout.tsx (current):
```tsx
export const metadata: Metadata = {
  title: 'SMS Platform',
  description: 'Surveillance Management System - Live CCTV Stream Platform',
};
```

From apps/web/src/components/nav/app-sidebar.tsx (current header — line 65–84):
```tsx
return (
  <SidebarHeader className="h-14 flex-row items-center justify-between px-4">
    <div className="flex items-center gap-2 overflow-hidden">
      {collapsed ? (
        <span className="text-base font-bold text-primary">SM</span>
      ) : (
        <>
          <span className="text-xl font-bold text-primary">SMS</span>
          <span
            className="truncate rounded bg-primary/10 px-2 py-0.5 text-xs font-medium text-primary"
            title={portalBadgeTitle}
          >
            {portalBadge}
          </span>
        </>
      )}
    </div>
    {!collapsed && <NotificationBell />}
  </SidebarHeader>
);
```
The collapsed branch shows "SM" inside a 56px-wide collapsed rail — keep it small. The expanded branch shows "SMS" + a portal badge pill — has room for a lockup but the portalBadge pill must remain visible alongside.

From apps/web/src/app/(auth)/sign-in/page.tsx (current left panel — lines 84–147):
- Top wordmark: line 91–99 — circular icon div + `<span>SMS Platform</span>`. REPLACE with `<StreamBridgeLogo variant="lockup" theme="dark" />`.
- Hero block: lines 102–131 — keep `<h1>` "Surveillance / Management / System". REPLACE the existing `<p>` tagline (lines 110–114) with the new one-line tagline. REMOVE the entire `<div className="mt-10 flex gap-8">…</div>` stat block (lines 117–130) and replace with the protocol-pill row + caption.
- Footer: line 135 — `© 2026 SMS Platform.` → `© 2026 StreamBridge.` Keep the "Developed by Sura Boonsung" line.

From apps/api/src/main.ts (current — lines 31–51):
```ts
const swaggerConfig = new DocumentBuilder()
  .setTitle('SMS Platform API')
  .setDescription(
    'Surveillance Management System — Developer API for managing cameras, streams, playback sessions, and webhooks.',
  )
  ...
console.log(`SMS Platform API running on http://localhost:${port}`);
```
Update `.setTitle(...)` and the `console.log(...)` only. Leave `.setDescription(...)` untouched (it references "Surveillance Management System" which is the product subtitle/headline, not the brand name — and the sign-in page also keeps that headline).

Color tokens (provided by user, use as hex literals — NOT Tailwind classes for the gradient stops):
- Gradient stops: `#22C55E` (green) → `#06B6D4` (cyan)
- Accent dots: `#22C55E` on light theme, `#06B6D4` on dark theme
- Lockup text: `#0F172A` on light, `#FFFFFF` on dark
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StreamBridgeLogo component</name>
  <files>apps/web/src/components/brand/streambridge-logo.tsx</files>
  <action>
Create a NEW file at `apps/web/src/components/brand/streambridge-logo.tsx` exporting a named React function component `StreamBridgeLogo`. The component must NOT be marked `"use client"` — it has no hooks-that-need-the-client. Wait — it uses `useId()`, which is a client-only hook? Actually `useId` works in both server and client components in Next.js 15 (RSC-safe), so leave the file as a server-compatible component (no `"use client"` directive). If the build complains, add `"use client";` at the top.

Implementation:

```tsx
import { useId } from 'react';

type Variant = 'icon' | 'lockup';
type Theme = 'light' | 'dark';

export interface StreamBridgeLogoProps {
  variant?: Variant;
  theme?: Theme;
  className?: string;
}

const GRADIENT_FROM = '#22C55E';
const GRADIENT_TO = '#06B6D4';

export function StreamBridgeLogo({
  variant = 'lockup',
  theme = 'light',
  className,
}: StreamBridgeLogoProps) {
  const gradientId = useId();
  const dotFill = theme === 'dark' ? '#06B6D4' : '#22C55E';
  const textFill = theme === 'dark' ? '#FFFFFF' : '#0F172A';

  if (variant === 'icon') {
    return (
      <svg
        viewBox="0 0 48 48"
        width="48"
        height="48"
        xmlns="http://www.w3.org/2000/svg"
        className={className}
        role="img"
        aria-label="StreamBridge"
      >
        <defs>
          <linearGradient id={gradientId} x1="0" y1="0" x2="1" y2="1">
            <stop offset="0%" stopColor={GRADIENT_FROM} />
            <stop offset="100%" stopColor={GRADIENT_TO} />
          </linearGradient>
        </defs>
        <polygon points="8,8 40,24 8,40" fill={`url(#${gradientId})`} />
        <circle cx="4" cy="18" r="2" fill={dotFill} />
        <circle cx="4" cy="24" r="2" fill={dotFill} />
        <circle cx="4" cy="30" r="2" fill={dotFill} />
      </svg>
    );
  }

  return (
    <svg
      viewBox="0 0 220 48"
      width="220"
      height="48"
      xmlns="http://www.w3.org/2000/svg"
      className={className}
      role="img"
      aria-label="StreamBridge"
    >
      <defs>
        <linearGradient id={gradientId} x1="0" y1="0" x2="1" y2="1">
          <stop offset="0%" stopColor={GRADIENT_FROM} />
          <stop offset="100%" stopColor={GRADIENT_TO} />
        </linearGradient>
      </defs>
      <polygon points="8,8 40,24 8,40" fill={`url(#${gradientId})`} />
      <circle cx="4" cy="18" r="2" fill={dotFill} />
      <circle cx="4" cy="24" r="2" fill={dotFill} />
      <circle cx="4" cy="30" r="2" fill={dotFill} />
      <text
        x="56"
        y="30"
        fontFamily="Inter, sans-serif"
        fontSize="20"
        fontWeight="600"
        fill={textFill}
      >
        StreamBridge
      </text>
    </svg>
  );
}
```

Notes for the executor:
- `useId()` is required so multiple logos on the same page do not collide on the linearGradient `id`. Do NOT replace it with a hardcoded id.
- TypeScript strict; do NOT use `any`. Do NOT use `React.FC`.
- Pass through `className` so callers can override sizing (e.g. `className="h-6 w-6"` for the icon variant in the collapsed sidebar).
- If the Next.js build complains that `useId()` requires a client component, add `"use client";` as the first line. Otherwise leave it as a server component (preferred — smaller bundle).
- Do NOT import lucide-react or any other icon lib — this is hand-rolled SVG.
- Per user request, the gradient hex values `#22C55E` and `#06B6D4` MUST be hardcoded — do NOT replace with Tailwind classes or theme tokens.
  </action>
  <verify>
    <automated>cat apps/web/src/components/brand/streambridge-logo.tsx | grep -E "export function StreamBridgeLogo|useId\\(\\)|#22C55E|#06B6D4|polygon|StreamBridge</text>" | wc -l | awk '$1 >= 5 {print "ok"; exit 0} {print "missing required content"; exit 1}'</automated>
  </verify>
  <done>File exists at the exact path, exports `StreamBridgeLogo` as a named function, uses `useId()` for the gradient id, contains both hex literals (`#22C55E` and `#06B6D4`), renders different markup for icon vs lockup variants, and accepts the documented props with the documented defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Rebrand UI strings + redesign sign-in left panel + update sidebar</name>
  <files>apps/web/src/app/layout.tsx, apps/web/src/app/(auth)/sign-in/page.tsx, apps/web/src/components/nav/app-sidebar.tsx, apps/web/src/app/admin/developer/docs/api-workflow/page.tsx, apps/web/src/app/admin/developer/docs/streaming-basics/page.tsx, apps/web/src/components/pages/tenant-developer-docs-page.tsx</files>
  <action>
Six file edits, all in the web app. Each is a precise string replacement except the sign-in page which gets a structural redesign.

**A. apps/web/src/app/layout.tsx** — line 6 inside `metadata`:
- `title: 'SMS Platform'` → `title: 'StreamBridge'`
- Leave `description: 'Surveillance Management System - Live CCTV Stream Platform'` UNCHANGED. The product subtitle "Surveillance Management System" is intentionally preserved (it appears in the sign-in headline and the Swagger description).

**B. apps/web/src/app/(auth)/sign-in/page.tsx** — full left-panel redesign.

1. Add the import (alongside existing imports):
```tsx
import { StreamBridgeLogo } from "@/components/brand/streambridge-logo";
```

2. Replace the wordmark block (current lines 91–99):
```tsx
{/* Logo */}
<div className="flex items-center gap-2.5 relative z-10">
  <div className="flex h-8 w-8 items-center justify-center rounded-full bg-white/15">
    <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
      <circle cx="12" cy="12" r="10" />
      <circle cx="12" cy="12" r="3" />
    </svg>
  </div>
  <span className="text-sm font-semibold">SMS Platform</span>
</div>
```
With:
```tsx
{/* Logo */}
<div className="relative z-10">
  <StreamBridgeLogo variant="lockup" theme="dark" />
</div>
```

3. Replace the existing sub-tagline `<p>` (current lines 110–114):
```tsx
<p className="mt-4 text-sm text-white/70 leading-relaxed">
  Centralized CCTV streaming, recording, and monitoring
  <br />
  platform for enterprise deployments.
</p>
```
With:
```tsx
<p className="mt-4 text-sm text-white/70 leading-relaxed">
  Centralized CCTV streaming, Ingest, transcode, record, monitoring and deliver — all from a single API.
</p>
```
(KEEP the `<h1>` headline "Surveillance / Management / System" exactly as-is — only the `<p>` below it changes.)

4. Replace the entire stat-box block (current lines 116–130 — the `{/* Stats */}` comment and the `<div className="mt-10 flex gap-8">…</div>` containing 99.9%, <1s, 24/7):
With the protocol-pill row + caption:
```tsx
{/* Protocols */}
<div className="mt-10 flex flex-wrap gap-2">
  {(["RTSP", "RTMP", "SRT", "WebRTC"] as const).map((proto) => (
    <span
      key={proto}
      className="inline-flex items-center px-3.5 py-2 rounded-md bg-white/10 border border-white/20 text-white font-mono text-[13px] tracking-wide"
    >
      {proto}
    </span>
  ))}
</div>
<p className="mt-3 text-sm text-white/60">Ingest protocols supported</p>
```

5. Footer (current line 135): `&copy; 2026 SMS Platform. All rights reserved.` → `&copy; 2026 StreamBridge. All rights reserved.`. Leave the "Developed by Sura Boonsung" line unchanged.

Do NOT alter the gradient `<div>` background, the right-edge gradient overlay, the headline `<h1>`, or anything in the right-side form panel.

**C. apps/web/src/components/nav/app-sidebar.tsx** — `SidebarHeaderContent` function (lines 55–85).

Add the import alongside existing imports:
```tsx
import { StreamBridgeLogo } from "@/components/brand/streambridge-logo";
```

Replace the inner `<div className="flex items-center gap-2 overflow-hidden">…</div>` block (lines 67–81) with:
```tsx
<div className="flex items-center gap-2 overflow-hidden">
  {collapsed ? (
    <StreamBridgeLogo variant="icon" theme="light" className="h-6 w-6" />
  ) : (
    <>
      <StreamBridgeLogo variant="icon" theme="light" className="h-6 w-6 shrink-0" />
      <span className="text-base font-bold text-foreground">StreamBridge</span>
      <span
        className="truncate rounded bg-primary/10 px-2 py-0.5 text-xs font-medium text-primary"
        title={portalBadgeTitle}
      >
        {portalBadge}
      </span>
    </>
  )}
</div>
```

Rationale: The expanded sidebar already has the portalBadge pill ("ADMIN" / "TENANT") rendered alongside the brand. Using `variant="lockup"` would consume the entire header width and push the badge off-screen. Icon-only + a separate `<span>StreamBridge</span>` keeps the badge visible. The collapsed rail uses just the icon (sized 24x24 via `h-6 w-6`). Switch from `text-primary` to `text-foreground` for the wordmark so it adopts theme color rather than the brand-green primary (the icon already carries the brand gradient).

**D. apps/web/src/app/admin/developer/docs/api-workflow/page.tsx** — line 14:
`The SMS Platform API lets you manage cameras` → `The StreamBridge API lets you manage cameras`.

**E. apps/web/src/app/admin/developer/docs/streaming-basics/page.tsx** — two replacements:
- Line 12: `The SMS Platform takes video feeds` → `StreamBridge takes video feeds`
- Line 28: `This is the core problem the SMS Platform solves.` → `This is the core problem StreamBridge solves.`

**F. apps/web/src/components/pages/tenant-developer-docs-page.tsx** — line 19:
`Learn how to use the SMS Platform API` → `Learn how to use the StreamBridge API`.

Do NOT touch any other strings. Do NOT rename any `@sms-platform/*` package, docker-compose service, env var, database column, hook ID, fixture string, or SRS config template.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && grep -rn "SMS Platform" apps/web/src apps/api/src --include="*.tsx" --include="*.ts" 2>/dev/null | grep -vE "node_modules|\\.test\\.|\\.spec\\." | wc -l | awk '$1 == 0 {print "ok: zero SMS Platform matches"; exit 0} {print "FAIL: still has matches"; exit 1}'</automated>
  </verify>
  <done>(1) Browser tab title is `StreamBridge`. (2) Sign-in page imports `StreamBridgeLogo`, renders the lockup at top-left, the new one-line tagline, the four protocol pills (RTSP / RTMP / SRT / WebRTC) with `Ingest protocols supported` caption, and `© 2026 StreamBridge` footer; the three stat boxes are gone. (3) Sidebar header imports `StreamBridgeLogo` and renders the icon (collapsed) or icon+wordmark+badge (expanded). (4) The four docs strings all say "StreamBridge". (5) `grep -rn "SMS Platform" apps/web/src apps/api/src --include="*.tsx" --include="*.ts"` (excluding tests/node_modules) returns zero matches. (6) `grep -rnE ">SMS<|\"SMS\"" apps/web/src --include="*.tsx"` returns zero matches.</done>
</task>

<task type="auto">
  <name>Task 3: API boot strings + verification</name>
  <files>apps/api/src/main.ts</files>
  <action>
Two precise edits in `apps/api/src/main.ts`:

1. Line 33: `.setTitle('SMS Platform API')` → `.setTitle('StreamBridge API')`
2. Line 51: ``console.log(`SMS Platform API running on http://localhost:${port}`)`` → ``console.log(`StreamBridge API running on http://localhost:${port}`)``

Leave `.setDescription('Surveillance Management System — Developer API …')` UNCHANGED. The "Surveillance Management System" subtitle is intentionally preserved as a product descriptor (it also appears in the sign-in headline and the layout description metadata).

After saving, run the full verification suite (in this exact order — fail fast):

```bash
# 1. grep — zero "SMS Platform" matches in user-facing source
grep -rn "SMS Platform" apps/web/src apps/api/src --include="*.tsx" --include="*.ts" 2>/dev/null | grep -vE "node_modules|\\.test\\.|\\.spec\\." | wc -l
# Expected: 0

# 2. grep — zero bare "SMS" literals in JSX in apps/web
grep -rnE ">SMS<|\"SMS\"" apps/web/src --include="*.tsx" 2>/dev/null | wc -l
# Expected: 0

# 3. Confirm internal identifiers are untouched
grep -c "@sms-platform" package.json apps/web/package.json apps/api/package.json
# Expected: each file still references @sms-platform/* (>= 1 line per file)

grep -E "container_name|service" docker-compose.yml | head -5
# Expected: no diff vs main; sms-* service/container names intact

# 4. Web build (workspace package name @sms-platform/web is preserved on purpose)
pnpm --filter @sms-platform/web build
# Expected: exit 0

# 5. HTTP smoke test against the dev server already running on port 3000
for path in /sign-in /app/dashboard /app/developer/docs/api-workflow; do
  code=$(curl -sk -o /dev/null -w "%{http_code}" "http://localhost:3000$path")
  echo "$path -> $code"
done
# Expected: each path returns 200 (or 302/307 redirect to /sign-in for auth-protected routes — acceptable; the build itself + grep already prove the rebrand)
```

If `pnpm --filter @sms-platform/web build` fails because `useId()` cannot be used in a server component, add `"use client";` as the first line of `apps/web/src/components/brand/streambridge-logo.tsx` and re-run.

If any grep returns >0 matches, locate the offending file and either (a) update the string to "StreamBridge" if it is a user-facing label, or (b) confirm it is an internal identifier (package name, env var, schema, fixture) and document the exception. Internal identifiers are preserved — the rebrand is UI-only.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && grep -E "setTitle\\('StreamBridge API'\\)|StreamBridge API running on" apps/api/src/main.ts | wc -l | awk '$1 >= 2 {print "ok"; exit 0} {print "FAIL: api/main.ts not fully updated"; exit 1}' && pnpm --filter @sms-platform/web build 2>&1 | tail -5</automated>
  </verify>
  <done>`apps/api/src/main.ts` Swagger title is `StreamBridge API` and the boot console log says `StreamBridge API running on http://localhost:${port}`. `pnpm --filter @sms-platform/web build` exits 0. `grep -rn "SMS Platform" apps/web/src apps/api/src --include="*.tsx" --include="*.ts"` (excluding tests/node_modules) returns 0 matches. `grep -rnE ">SMS<|\"SMS\"" apps/web/src --include="*.tsx"` returns 0 matches. The three smoke-test routes (`/sign-in`, `/app/dashboard`, `/app/developer/docs/api-workflow`) return 200 (or auth-redirect 302/307). `@sms-platform/*` package names, docker-compose service names, env vars, and DB schema are unchanged from `main`.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run a final sweep to confirm scope discipline:

```bash
# A. User-facing rebrand is complete
grep -rn "SMS Platform" apps/web/src apps/api/src --include="*.tsx" --include="*.ts" 2>/dev/null | grep -vE "node_modules|\\.test\\.|\\.spec\\." | wc -l
# Expected: 0

grep -rnE ">SMS<|\"SMS\"" apps/web/src --include="*.tsx" 2>/dev/null | wc -l
# Expected: 0

# B. Internal identifiers are untouched (sanity check — these MUST still match)
grep -c "@sms-platform/" package.json
# Expected: >= 1

git diff --stat -- docker-compose.yml apps/api/src/prisma/schema.prisma apps/api/src/cluster/templates/
# Expected: empty (no diff in any of these)

# C. Web build is green
pnpm --filter @sms-platform/web build
# Expected: exit 0

# D. Diff scope
git diff --stat | grep -cE "^ apps/(web|api)/" 
# Expected: exactly 7 changed files (1 new logo + 6 modified)

# E. Smoke routes return 200 (dev server already running on :3000)
for path in /sign-in /app/dashboard /app/developer/docs/api-workflow; do
  code=$(curl -sk -o /dev/null -w "%{http_code}" "http://localhost:3000$path")
  echo "$path -> $code"
done
```

Manual visual check (optional — not blocking):
- Visit `http://localhost:3000/sign-in` and confirm the left panel shows: green gradient background, StreamBridge lockup at top-left, "Surveillance / Management / System" headline, the new one-line tagline, four protocol pills (RTSP / RTMP / SRT / WebRTC), "Ingest protocols supported" caption, and "© 2026 StreamBridge. All rights reserved. Developed by Sura Boonsung" footer.
- Sign in and visit any tenant page; sidebar header shows the StreamBridge icon + wordmark + portal badge.
</verification>

<success_criteria>
- [ ] `apps/web/src/components/brand/streambridge-logo.tsx` exists, exports `StreamBridgeLogo`, uses `useId()` for the gradient id, supports `variant` (icon | lockup) and `theme` (light | dark) with documented defaults, hardcodes hex literals `#22C55E` and `#06B6D4`.
- [ ] `apps/web/src/app/layout.tsx` `metadata.title` is `'StreamBridge'`.
- [ ] `apps/web/src/app/(auth)/sign-in/page.tsx`: imports `StreamBridgeLogo`, renders the dark-theme lockup at top-left, KEEPS the "Surveillance / Management / System" headline, replaces the sub-tagline with the new one-liner, REMOVES the three stat boxes (99.9% / <1s / 24/7), ADDS the four protocol pills + "Ingest protocols supported" caption, footer reads "© 2026 StreamBridge. All rights reserved." with the existing "Developed by Sura Boonsung" line preserved.
- [ ] `apps/web/src/components/nav/app-sidebar.tsx`: imports `StreamBridgeLogo`, expanded header shows icon + "StreamBridge" wordmark + portal badge, collapsed rail shows just the icon (`h-6 w-6`).
- [ ] All four docs strings (1× api-workflow, 2× streaming-basics, 1× tenant-developer-docs-page) say "StreamBridge".
- [ ] `apps/api/src/main.ts`: Swagger title is `StreamBridge API`, boot console log says `StreamBridge API running on http://localhost:${port}`.
- [ ] `pnpm --filter @sms-platform/web build` exits 0.
- [ ] `grep -rn "SMS Platform" apps/web/src apps/api/src --include="*.tsx" --include="*.ts"` (excluding tests/node_modules) returns 0 matches.
- [ ] `grep -rnE ">SMS<|\"SMS\"" apps/web/src --include="*.tsx"` returns 0 matches.
- [ ] `@sms-platform/*` workspace package names, docker-compose service/container/network names, env var names, DB schema, and `apps/api/src/cluster/templates/` are unchanged.
- [ ] Diff scope: exactly 7 files (1 new component + 6 modified).
- [ ] Smoke-test routes (`/sign-in`, `/app/dashboard`, `/app/developer/docs/api-workflow`) return 200 (or auth-redirect 302/307 for protected routes).
</success_criteria>

<output>
After completion, create `.planning/quick/260427-1u5-rebrand-sms-platform-to-streambridge-acr/260427-1u5-SUMMARY.md` summarizing:
- Files created (1) and modified (6)
- Scope guardrails honored (internal identifiers untouched)
- Build + grep verification results
- Any deviations (e.g. if `"use client";` had to be added to the logo component)
</output>
