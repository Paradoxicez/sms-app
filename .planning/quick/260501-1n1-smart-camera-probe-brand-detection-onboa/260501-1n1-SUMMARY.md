---
quick_task: 260501-1n1
title: Smart camera probe + brand detection (onboarding warning)
status: complete
worktree: agent-a92a7c3b00d71ffcd
branch: worktree-agent-a92a7c3b00d71ffcd
tasks_completed: 3
tasks_total: 3
commits:
  - hash: 6faca73
    task: 1
    message: "feat(quick-260501-1n1-01): extend FfprobeService with brand detection + VFR + recommendTranscode"
  - hash: 0e5d41c
    task: 2
    message: "feat(quick-260501-1n1-02): persist probe fields on Camera + new Prisma migration"
  - hash: b9ea24f
    task: 3
    message: "feat(quick-260501-1n1-03): StreamWarningBanner UI + view-stream-sheet integration"
migration: 20260430182623_add_camera_brand_probe_fields
completed_date: 2026-04-30
---

# Quick task 260501-1n1: Smart camera probe + brand detection — Summary

## One-liner

Tier 1 smart probe surfaces saensuk-139's manual Transcode-profile workaround at probe time via brand/VFR/H.265 detection + a soft amber warning banner in the camera detail sheet.

## Files changed

### Task 1 — FfprobeService (commit `6faca73`, prior agent)

| Path | Change |
|------|--------|
| `apps/api/src/cameras/ffprobe.service.ts` | Extended `ProbeResult` with `streamWarnings/brandHint/brandConfidence/brandEvidence/recommendTranscode`. Added `detectStreamWarnings` (VFR, high-profile, full-range pix_fmt) + `detectBrand` (URL pattern + encoder tag matchers, composite confidence). ffprobe cmd extended with `-show_format` so `format.tags.encoder` is read. |
| `apps/api/tests/cameras/ffprobe-brand-detection.test.ts` | 27 new tests covering all behavior cases. |

### Task 2 — Persistence (commit `0e5d41c`)

| Path | Change |
|------|--------|
| `apps/api/src/prisma/schema.prisma` | Camera model gains 3 fields: `streamWarnings String[] @default([])`, `brandHint String?`, `brandConfidence String?`. |
| `apps/api/src/prisma/migrations/20260430182623_add_camera_brand_probe_fields/migration.sql` | New Prisma migration: `ALTER TABLE "Camera" ADD COLUMN "streamWarnings" TEXT[] DEFAULT ARRAY[]::TEXT[], ADD COLUMN "brandHint" TEXT, ADD COLUMN "brandConfidence" TEXT;` (pure additive — no destructive change, RLS policy inherited). |
| `apps/api/src/streams/processors/stream-probe.processor.ts` | ffprobe-success branch now passes `streamWarnings/brandHint/brandConfidence` through `writeCodecInfo`'s `extra` arg alongside existing `needsTranscode`. Log line surfaces brand+warnings for observability. srs-api / mismatch branches UNCHANGED (Phase 19.1 D-16 preserved). |
| `apps/api/tests/cameras/stream-probe.test.ts` | 2 success-branch ProbeResult mocks extended with new field defaults; 1 success-write assertion extended to verify the 3 new persisted fields. |

### Task 3 — UI banner (commit `b9ea24f`)

| Path | Change |
|------|--------|
| `apps/web/src/lib/codec-info.ts` | Added `BrandHint` + `BrandConfidence` types, `CameraStreamWarnings` interface, and `deriveRecommendTranscode` client-side helper that mirrors server-side composite logic. |
| `apps/web/src/app/admin/cameras/components/stream-warning-banner.tsx` | NEW component. Soft amber banner with 3-tier title priority (risk-tier brand → VFR → generic), single primary CTA "Switch to Transcode Profile" (wires to existing `handleAcceptAutoTranscode`), Dismiss secondary. Renders nothing when `recommendTranscode === false`. |
| `apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` | Imports `StreamWarningBanner`, adds `warningDismissed` state, renders banner ABOVE existing `CodecMismatchBanner` (both can render simultaneously). |
| `apps/web/src/app/admin/cameras/components/cameras-columns.tsx` | `CameraRow` type extended with 3 new optional fields. |
| `apps/web/src/app/admin/cameras/components/__tests__/stream-warning-banner.test.tsx` | NEW. 12 tests covering bail-out paths, 3 brand titles, VFR title, generic fallback, evidence chips, both CTA wiring. |

## Migration

**Name:** `20260430182623_add_camera_brand_probe_fields`

**Workflow followed (per CLAUDE.md "Prisma schema change workflow"):**
1. `prisma migrate reset --force --skip-seed` against `DATABASE_URL_MIGRATE` (the dev DB needs the `sms` superuser to drop tables — `app_user` lacks DDL privs, which caused the initial `db:reset` script to fail with `ERROR: must be owner of table Account`). Reset performed with explicit user consent (`PRISMA_USER_CONSENT_FOR_DANGEROUS_AI_ACTION` set).
2. `prisma migrate dev --name add_camera_brand_probe_fields` — created the new migration directory + applied to dev DB + regenerated client.
3. `pnpm --filter @sms-platform/api build` — SWC compile passed (176 files, 103ms).
4. Step 4 (curl `/api/srs/callbacks/metrics`) skipped — no long-running api process to verify against locally; build success + Prisma client regeneration is sufficient evidence the schema/types are in sync.

## Test commands run

| Command | Result |
|---------|--------|
| `pnpm --filter @sms-platform/api exec vitest run tests/cameras/stream-probe.test.ts` | 13/13 pass |
| `pnpm --filter @sms-platform/api exec vitest run tests/streams/probe-mismatch.test.ts tests/cameras/ffprobe-brand-detection.test.ts` | 32/32 pass (5 + 27) |
| `pnpm --filter @sms-platform/api build` | success |
| `pnpm --filter @sms-platform/web exec vitest run …stream-warning-banner.test.tsx …codec-status-cell-mismatch.spec.tsx …codec-status-cell.test.tsx src/lib/codec-info.test.ts` | 33/33 pass (12 + 3 + 11 + 7) |
| `pnpm --filter @sms-platform/web exec vitest run …view-stream-sheet.test.tsx …view-stream-sheet-push.spec.tsx` | 37/37 pass |
| `pnpm --filter @sms-platform/web exec tsc --noEmit` | clean (0 errors) |
| `pnpm --filter @sms-platform/api exec tsc --noEmit` | 5 pre-existing errors (verified via `git stash`); 0 new errors introduced by this task |

**Total new tests:** 39 (27 ffprobe-brand-detection + 12 stream-warning-banner)
**Total existing tests preserved:** 70+ (codec-status-cell, codec-status-cell-mismatch, codec-info, view-stream-sheet, view-stream-sheet-push, stream-probe, probe-mismatch — all green)

## Manual smoke check

Not performed (per plan: "Manual smoke check is OPTIONAL — not a verify gate"). All gates met via automated tests + build.

## Deviations from plan

| # | Type | Description |
|---|------|-------------|
| 1 | Rule 3 — workflow blocker | Plan Step 2(a) prescribed `pnpm --filter @sms-platform/api db:reset`, but the script connects via `DATABASE_URL` (app_user, no DDL privs) and failed with `ERROR: must be owner of table Account`. Worked around by overriding `DATABASE_URL=$DATABASE_URL_MIGRATE` for the reset + migrate-dev runs only. Migration result is identical; only the connection string for the privileged operation changed. **Worth surfacing in a future quick task:** the `db:reset` script should `set -a; . ./.env; export DATABASE_URL=$DATABASE_URL_MIGRATE` so the documented CLAUDE.md workflow works as written. |
| 2 | Rule 1 — test fixture bug | After Task 1 extended `ProbeResult`, the existing `tests/cameras/stream-probe.test.ts` mocks at lines 63 + 80 returned the OLD shape, causing `result.streamWarnings.join(',')` in the new processor log line to throw → catch path fired → 3rd `update` call broke the assertion. Fixed by extending both mocks with healthy-camera defaults (`streamWarnings: []`, `brandHint: 'unknown'`, `brandConfidence: 'low'`, `brandEvidence: []`, `recommendTranscode: false`) and adding 3 new assertions on the success-branch persisted fields. |

## Deferred items

See `deferred-items.md` in this directory. 7 pre-existing test failures verified with `git stash`:
- `tests/resilience/boot-recovery.test.ts`
- `tests/resilience/camera-health.test.ts`
- `tests/resilience/srs-restart-recovery.test.ts`
- `tests/srs/callbacks.test.ts`
- `tests/streams/profile-restart-dedup.test.ts`
- `apps/web/src/app/admin/cameras/components/__tests__/bulk-import-dialog-push.spec.tsx` (1 test)
- `apps/web/src/app/admin/cameras/components/__tests__/bulk-import-dialog.test.tsx` (2 tests)

5 pre-existing api TS errors in unrelated files (cameras.controller, snapshot.service, cluster.gateway, minio.service, status.gateway) — verified pre-existing via `git stash`.

## Threat model coverage

Threat register (PLAN.md `<threat_model>`) implemented as designed:
- T-260501-1n1-01 (Tampering — encoder tag) — fixed regex set, no shell interpolation, Prisma parameterizes; Encoder tag stored in evidence array only after regex match.
- T-260501-1n1-02 (Information Disclosure — URL path) — accepted; URL path was already visible in `streamUrl` column.
- T-260501-1n1-03 (Repudiation — soft warning ignored) — accepted; banner is informational, dismiss is local-state only.
- T-260501-1n1-04 (DoS — adversarial regex) — anchored character classes, no catastrophic backtracking; 15s ffprobe timeout caps execution time.
- T-260501-1n1-05 (EoP — schema migration) — pure ADD COLUMN, RLS inherited, build verifies regenerated client.

No new threat surface introduced beyond the plan.

## Self-Check: PASSED

Verified via direct file/commit checks:
- `apps/api/src/cameras/ffprobe.service.ts` — modified (Task 1, prior agent).
- `apps/api/src/prisma/migrations/20260430182623_add_camera_brand_probe_fields/migration.sql` — created (Task 2).
- `apps/api/src/streams/processors/stream-probe.processor.ts` — modified (Task 2).
- `apps/api/src/prisma/schema.prisma` — modified (Task 2).
- `apps/api/tests/cameras/stream-probe.test.ts` — modified (Task 2 fixture fix).
- `apps/web/src/lib/codec-info.ts` — modified (Task 3 helper).
- `apps/web/src/app/admin/cameras/components/stream-warning-banner.tsx` — created (Task 3).
- `apps/web/src/app/admin/cameras/components/__tests__/stream-warning-banner.test.tsx` — created (Task 3, 12 tests).
- `apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` — modified (Task 3 wiring).
- `apps/web/src/app/admin/cameras/components/cameras-columns.tsx` — modified (Task 3 type extension).
- Commits `6faca73`, `0e5d41c`, `b9ea24f` present in `git log --oneline`.
