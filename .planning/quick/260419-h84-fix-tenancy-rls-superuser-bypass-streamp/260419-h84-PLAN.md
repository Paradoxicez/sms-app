---
phase: quick-260419-h84
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/prisma/rls.policies.sql
  - apps/api/src/prisma/migrations/rls_superuser_bypass_positive_signal/migration.sql
  - apps/api/src/tenancy/prisma-tenancy.extension.ts
  - apps/api/src/tenancy/tenancy.module.ts
  - apps/api/src/auth/guards/auth.guard.ts
  - apps/api/tests/tenancy/rls-isolation.test.ts
  - apps/api/tests/auth/auth-guard-active-org.test.ts
  - apps/api/src/status/status.service.ts
  - apps/api/tests/status/state-machine.test.ts
autonomous: true
requirements:
  - GAP-15.1-RLS-BYPASS
  - GAP-15.1-STREAM-TRANSITION

must_haves:
  truths:
    - "User authenticated with session.activeOrganizationId=null cannot read Camera rows from other orgs via GET /api/cameras"
    - "User authenticated with session.activeOrganizationId=null cannot mutate Camera rows via POST /api/cameras/:id/maintenance or PATCH /api/cameras/:id (returns 403)"
    - "Super admin (session.user.role='admin') still sees and can mutate rows across all orgs"
    - "StreamProcessor can start FFmpeg when camera.status='reconnecting' without throwing 'Invalid transition: reconnecting -> connecting'"
  artifacts:
    - path: "apps/api/src/prisma/migrations/rls_superuser_bypass_positive_signal/migration.sql"
      provides: "Atomic migration that drops old superuser_bypass_* policies and recreates them using positive signal current_setting('app.is_superuser', true) = 'true'"
      contains: "DROP POLICY superuser_bypass_camera"
    - path: "apps/api/src/prisma/rls.policies.sql"
      provides: "Source-of-truth SQL updated so fresh installs apply new superuser bypass shape"
      contains: "app.is_superuser"
    - path: "apps/api/src/auth/guards/auth.guard.ts"
      provides: "Active-org enforcement + superuser flag propagation into CLS"
      contains: "IS_SUPERUSER"
    - path: "apps/api/src/tenancy/prisma-tenancy.extension.ts"
      provides: "Tenancy extension sets app.is_superuser from CLS alongside app.current_org_id"
      contains: "app.is_superuser"
    - path: "apps/api/src/status/status.service.ts"
      provides: "validTransitions allows reconnecting -> connecting"
      contains: "reconnecting: ['online', 'offline', 'connecting']"
  key_links:
    - from: "AuthGuard"
      to: "nestjs-cls (ORG_ID, IS_SUPERUSER)"
      via: "cls.set()"
      pattern: "cls\\.set\\('(ORG_ID|IS_SUPERUSER)'"
    - from: "prisma-tenancy.extension.ts"
      to: "Postgres session"
      via: "set_config('app.current_org_id', ...) + set_config('app.is_superuser', 'true', TRUE)"
      pattern: "set_config\\('app\\.is_superuser'"
    - from: "StreamProcessor.process"
      to: "StatusService.transition"
      via: "reconnecting -> connecting allowed in validTransitions"
      pattern: "reconnecting.*connecting"
---

<objective>
ปิด 2 gap ที่ค้นพบจาก Phase 15 live UAT:

1. **Tenancy RLS superuser bypass** (severity: high) — ผู้ใช้ที่ authenticated แต่ไม่มี activeOrganizationId (เช่นเพิ่ง sign-up) สามารถ read/write ข้อมูลข้ามองค์กรได้ทั้งหมดเพราะ policy `superuser_bypass_*` ใช้สัญญาณลบ (`current_setting('app.current_org_id', true) IS NULL OR = ''`)
2. **StreamProcessor invalid transition** (severity: medium) — เมื่อกล้องอยู่สถานะ `reconnecting` แล้ว enqueue stream job ใหม่ StreamProcessor call `transition(..., 'connecting')` ซึ่งไม่ถูก allow → throw → BullMQ retry loop → ไม่มี FFmpeg spawn

Purpose: ล็อก multi-tenancy boundary ให้ปลอดภัย + แก้ retry loop ที่บล็อก recovery path
Output: migration SQL + AuthGuard enforcement + 1-line validTransitions fix + unit tests ทั้งหมดเขียนแบบ RED→GREEN
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/15-ffmpeg-resilience-camera-maintenance/15-HUMAN-UAT.md

<!-- Source files being modified -->
@apps/api/src/prisma/rls.policies.sql
@apps/api/src/prisma/migrations/rls_apply_all/migration.sql
@apps/api/src/auth/guards/auth.guard.ts
@apps/api/src/auth/guards/super-admin.guard.ts
@apps/api/src/tenancy/prisma-tenancy.extension.ts
@apps/api/src/streams/processors/stream.processor.ts
@apps/api/src/status/status.service.ts

<!-- Existing tests being extended -->
@apps/api/tests/status/state-machine.test.ts
@apps/api/tests/tenancy/rls-isolation.test.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From apps/api/src/auth/guards/auth.guard.ts (current shape):
```typescript
const session = await auth.api.getSession({ headers });
if (!session) throw new UnauthorizedException('Not authenticated');
const orgId = session.session?.activeOrganizationId;
if (orgId) this.cls.set('ORG_ID', orgId);
// (no IS_SUPERUSER flag today)
```

From apps/api/src/auth/guards/super-admin.guard.ts — canonical super-admin identifier:
```typescript
if (session.user.role !== 'admin') {
  throw new UnauthorizedException('Super admin access required');
}
```
→ Super admin is `session.user.role === 'admin'`. Use the SAME rule in AuthGuard to derive IS_SUPERUSER.

From apps/api/src/tenancy/prisma-tenancy.extension.ts (current):
```typescript
const orgId = cls.get('ORG_ID');
if (!orgId) return query(args);           // no set_config happens
const [, result] = await prisma.$transaction([
  prisma.$executeRaw`SELECT set_config('app.current_org_id', ${orgId}, TRUE)`,
  query(args),
]);
```

From apps/api/src/status/status.service.ts validTransitions:
```typescript
private readonly validTransitions: Record<string, string[]> = {
  offline: ['connecting'],
  connecting: ['online', 'offline'],
  online: ['reconnecting', 'degraded', 'offline'],
  reconnecting: ['online', 'offline'],    // ← missing 'connecting'
  degraded: ['online', 'offline'],
};
```

From apps/api/src/prisma/rls.policies.sql + migrations/rls_apply_all/migration.sql — every tenant table has BOTH policies:
```sql
CREATE POLICY tenant_isolation_<table> ON "<Table>"
  USING ("orgId" = current_setting('app.current_org_id', true)::text)
  WITH CHECK ("orgId" = current_setting('app.current_org_id', true)::text);
CREATE POLICY superuser_bypass_<table> ON "<Table>"
  USING (current_setting('app.current_org_id', true) IS NULL
      OR current_setting('app.current_org_id', true) = '');
```
Tables with this pattern (COMPLETE LIST — new migration must touch ALL of them):
- Camera, Project, Site, StreamProfile, PlaybackSession, Policy
- ApiKey, WebhookSubscription, OrgSettings
- Recording, RecordingSegment, RecordingSchedule
- Member, Invitation, UserPermissionOverride
- AuditLog, Notification, NotificationPreference

Super admin flow (must continue working after fix):
- Session has user.role='admin' and activeOrganizationId='system-org-id' (or any org they switch to)
- When super admin operates WITHOUT an active org set, AuthGuard must set CLS.IS_SUPERUSER='true' and extension sets app.is_superuser='true' instead of (or alongside) app.current_org_id
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gap 1 — Close tenancy RLS superuser bypass with positive-signal policy + AuthGuard active-org enforcement</name>
  <files>
    apps/api/tests/tenancy/rls-isolation.test.ts,
    apps/api/tests/auth/auth-guard-active-org.test.ts,
    apps/api/src/prisma/migrations/rls_superuser_bypass_positive_signal/migration.sql,
    apps/api/src/prisma/rls.policies.sql,
    apps/api/src/tenancy/prisma-tenancy.extension.ts,
    apps/api/src/tenancy/tenancy.module.ts,
    apps/api/src/auth/guards/auth.guard.ts
  </files>
  <behavior>
    RED tests (MUST fail against current code before fix):

    **A) Extend `apps/api/tests/tenancy/rls-isolation.test.ts`** — add a new `describe('RLS superuser bypass uses positive signal app.is_superuser')` block:
    - Test 1: `SET ROLE app_user` + NO set_config for either `app.current_org_id` OR `app.is_superuser` → `tx.camera.findMany()` / `tx.member.findMany()` returns **0 rows** (previously bypass returned all). Seed 1 camera + 1 member in two distinct orgs as superuser beforehand.
    - Test 2: `SET ROLE app_user` + `set_config('app.is_superuser', 'true', TRUE)` + NO current_org_id → returns rows from ALL orgs (super admin still sees everything).
    - Test 3: `SET ROLE app_user` + `set_config('app.current_org_id', '<orgA>', TRUE)` + NO is_superuser → returns only orgA rows (tenant isolation still works).
    - The existing test at line 143 ("without org context (superuser bypass), returns all members") MUST be rewritten to match new contract: without org AND without is_superuser='true' → 0 rows.

    **B) New `apps/api/tests/auth/auth-guard-active-org.test.ts`** — unit-test AuthGuard in isolation using mocked `getAuth()` + mocked `ClsService`:
    - Test 1: session returns `{ user: { role: 'viewer' }, session: { activeOrganizationId: null } }` → `canActivate` throws `ForbiddenException` with message matching /no active organization|active org required/i. Mock the Better Auth `getAuth().api.getSession` via `vi.mock('../auth.config')` similar to existing auth tests.
    - Test 2: session returns `{ user: { role: 'admin' }, session: { activeOrganizationId: null } }` → `canActivate` returns true, cls.set called with ('IS_SUPERUSER', 'true'), cls.set NOT called with ORG_ID.
    - Test 3: session returns `{ user: { role: 'viewer' }, session: { activeOrganizationId: 'org-1' } }` → returns true, cls.set('ORG_ID', 'org-1') called, cls.set('IS_SUPERUSER', ...) NOT called.
    - Test 4: session returns `{ user: { role: 'admin' }, session: { activeOrganizationId: 'org-1' } }` → returns true, cls.set('ORG_ID', 'org-1') called AND cls.set('IS_SUPERUSER', 'true') called (super admin operating inside an org — both flags set so RLS allows either match).

    All 4 new AuthGuard tests + 3 new RLS tests MUST be run (and fail) BEFORE implementation.
  </behavior>
  <action>
    **RED phase:**
    1. Write the RED tests above. Run `cd apps/api && pnpm vitest run tests/tenancy/rls-isolation.test.ts tests/auth/auth-guard-active-org.test.ts` — confirm the new cases fail (RLS bypass still returns rows; AuthGuard does not throw on null activeOrgId; AuthGuard does not set IS_SUPERUSER). Do NOT edit production code yet.
    2. Commit: `test(quick-260419-h84): add failing tests for positive-signal RLS bypass + active-org enforcement`

    **GREEN phase:**

    3. **Create migration** `apps/api/src/prisma/migrations/rls_superuser_bypass_positive_signal/migration.sql`:
       - For each of the 18 tables listed in `<interfaces>`: `DROP POLICY IF EXISTS superuser_bypass_<table> ON "<Table>";` then `CREATE POLICY superuser_bypass_<table> ON "<Table>" USING (current_setting('app.is_superuser', true) = 'true');` (note: NO `WITH CHECK` on bypass — it's a USING-only permissive policy; matches existing pattern). Tenant-isolation policies are left untouched.
       - Wrap each table pair in `DO $$ BEGIN ... END $$;` for idempotency (check `pg_policies` for new policy name before re-creating, like existing rls_apply_all).
       - Use EXACT policy names and table casing from `rls_apply_all/migration.sql` and `rls.policies.sql` — names: `superuser_bypass_camera`, `superuser_bypass_project`, `superuser_bypass_site`, `superuser_bypass_streamprofile`, `superuser_bypass_playbacksession`, `superuser_bypass_policy`, `superuser_bypass_apikey`, `superuser_bypass_webhook`, `superuser_bypass_orgsettings`, `superuser_bypass_recording`, `superuser_bypass_recordingsegment`, `superuser_bypass_recordingschedule`, `superuser_bypass_member`, `superuser_bypass_invitation`, `superuser_bypass_permission_override`, `superuser_bypass_audit_log`, `superuser_bypass_notification`, `superuser_bypass_notification_pref`.

    4. **Update source-of-truth SQL** `apps/api/src/prisma/rls.policies.sql`:
       - Replace every `USING (current_setting('app.current_org_id', true) IS NULL OR current_setting('app.current_org_id', true) = '')` with `USING (current_setting('app.is_superuser', true) = 'true')`. This keeps `db:push` / fresh seeds aligned with the migration.
       - Also update `apps/api/src/prisma/migrations/rls_apply_all/migration.sql` SAME-LINE so re-running a clean DB from scratch picks up the new shape. (Idempotency guards already check `pg_policies` by name — new shape will apply on fresh DBs, no-op on DBs where the new migration already ran.)
       - Do NOT touch `rls-phase5.sql` or `rls_phase02/migration.sql` history files — only the two live apply paths (`rls.policies.sql` + `rls_apply_all/migration.sql`) need to match the new migration.

    5. **Update `apps/api/src/tenancy/prisma-tenancy.extension.ts`**:
       ```typescript
       const orgId = cls.get('ORG_ID');
       const isSuperuser = cls.get('IS_SUPERUSER');
       if (!orgId && !isSuperuser) return query(args);

       const stmts: any[] = [];
       if (orgId) stmts.push(prisma.$executeRaw`SELECT set_config('app.current_org_id', ${orgId}, TRUE)`);
       if (isSuperuser) stmts.push(prisma.$executeRaw`SELECT set_config('app.is_superuser', 'true', TRUE)`);
       stmts.push(query(args));
       const results = await prisma.$transaction(stmts);
       return results[results.length - 1];
       ```
       This is the minimal change — no new modules, no new DI.

    6. **Update `apps/api/src/auth/guards/auth.guard.ts`**:
       - Add import for `ForbiddenException` from `@nestjs/common`.
       - After `session` resolution: compute `const isSuperAdmin = session.user.role === 'admin';` and `const orgId = session.session?.activeOrganizationId;`.
       - **Enforce:** `if (!orgId && !isSuperAdmin) throw new ForbiddenException('No active organization. User must be a member of at least one organization.');`
       - `if (orgId) this.cls.set('ORG_ID', orgId);`
       - `if (isSuperAdmin) this.cls.set('IS_SUPERUSER', 'true');`
       - Do NOT change the unauthenticated path, do NOT change request.user/session attachment.

    7. Apply migration locally: `cd apps/api && pnpm prisma migrate dev --name rls_superuser_bypass_positive_signal` (this also syncs Prisma's `_prisma_migrations` table). If `db:push` is the project's chosen flow (per recent commit 829361f), ALSO verify `pnpm db:push` re-applies `rls.policies.sql` cleanly on a fresh DB — just ensure the SQL is updated; the migration stands on its own.

    8. Run `pnpm vitest run tests/tenancy/rls-isolation.test.ts tests/auth/auth-guard-active-org.test.ts` — all 7 cases MUST pass. Also run the full auth + tenancy suites to catch regressions: `pnpm vitest run tests/auth tests/tenancy`.

    9. Commit: `fix(auth,rls): close tenancy bypass with positive-signal superuser flag + active-org enforcement`

    **Explicitly NOT in scope (per quick-task constraints):**
    - Do NOT rewrite tenant_isolation_* policies
    - Do NOT change AuditInterceptor / audit log behavior
    - Do NOT touch permissions.ts / roles.ts
    - Do NOT introduce a new System-org membership lookup — super admin is identified by `user.role === 'admin'` (matches existing SuperAdminGuard)
    - Do NOT modify other guards (OrgAdminGuard etc.) — only AuthGuard
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run tests/tenancy/rls-isolation.test.ts tests/auth/auth-guard-active-org.test.ts tests/auth tests/tenancy</automated>
  </verify>
  <done>
    - All 3 new RLS isolation cases pass (no-org returns 0 rows; is_superuser='true' returns all; org-scoped returns that org only)
    - All 4 new AuthGuard cases pass (null-org viewer→403; null-org admin→ok + IS_SUPERUSER; orged viewer→ok + ORG_ID; orged admin→ok + both)
    - Migration SQL applied cleanly against dev DB; `SELECT polname FROM pg_policies WHERE polname LIKE 'superuser_bypass_%'` shows all 18 policies with `qual` containing `app.is_superuser`
    - Existing auth/tenancy test suites still green
    - Two commits landed: test-first RED commit, implementation GREEN commit
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gap 2 — Allow reconnecting → connecting transition in StatusService</name>
  <files>
    apps/api/tests/status/state-machine.test.ts,
    apps/api/src/status/status.service.ts
  </files>
  <behavior>
    RED test (MUST fail against current code):

    Extend `apps/api/tests/status/state-machine.test.ts` — add inside the existing `describe('Camera Status State Machine')` block:
    - `it('should allow transition reconnecting -> connecting (retry re-enqueue)', async () => {...})`:
      - `mockPrisma.camera.findUnique.mockResolvedValue({ id: 'cam-1', status: 'reconnecting' })`
      - `await service.transition('cam-1', 'org-1', 'connecting')`
      - `expect(mockPrisma.camera.update).toHaveBeenCalledWith(expect.objectContaining({ where: { id: 'cam-1' }, data: expect.objectContaining({ status: 'connecting' }) }))`
      - `expect(mockGateway.broadcastStatus).toHaveBeenCalledWith('org-1', 'cam-1', 'connecting')`
    - Also assert an invalid-transition case that SHOULD remain invalid (sanity check that we didn't open the floodgates): `it('should still reject transition offline -> reconnecting', ...)` — call `transition` on a camera with status=offline targeting 'reconnecting' → `rejects.toThrow('Invalid transition: offline -> reconnecting')`.

    Do NOT modify the existing passing cases.
  </behavior>
  <action>
    **RED phase:**
    1. Add the two test cases above. Run `cd apps/api && pnpm vitest run tests/status/state-machine.test.ts` — the `reconnecting -> connecting` case MUST fail with `Error: Invalid transition: reconnecting -> connecting`; the `offline -> reconnecting` rejection case will already pass (covered by current validTransitions).
    2. Commit: `test(quick-260419-h84): add failing test for reconnecting -> connecting transition`

    **GREEN phase:**

    3. Edit `apps/api/src/status/status.service.ts` line 18 — change:
       ```typescript
       reconnecting: ['online', 'offline'],
       ```
       to:
       ```typescript
       reconnecting: ['online', 'offline', 'connecting'],
       ```
       This reflects the real retry lifecycle: a camera in `reconnecting` can enter a new `connecting` attempt when CameraHealthService re-enqueues or the user clicks Start Stream.

    4. Run `pnpm vitest run tests/status/state-machine.test.ts` — new case passes, all existing cases still pass. Also run `pnpm vitest run tests/status tests/streams tests/resilience` to confirm no regression in debounce/maintenance-suppression/resilience paths.

    5. Commit: `fix(status): allow reconnecting -> connecting transition for retry re-enqueue`

    **Explicitly NOT in scope:**
    - Do NOT change StreamProcessor.process logic (no caller-side reset to 'offline' — contradicts decision matrix option B)
    - Do NOT add idempotency/same-target skip (already handled at line 39-42 via `if (newStatus === currentStatus) return`)
    - Do NOT touch other transition entries (offline, connecting, online, degraded)
    - Do NOT touch the maintenance-gate or notify debounce logic
  </action>
  <verify>
    <automated>cd apps/api && pnpm vitest run tests/status/state-machine.test.ts tests/status tests/streams tests/resilience</automated>
  </verify>
  <done>
    - New `reconnecting -> connecting` case passes
    - New `offline -> reconnecting` rejection sanity case passes
    - All pre-existing state-machine, debounce, maintenance-suppression, stream, and resilience tests remain green
    - Two commits landed: test-first RED commit, implementation GREEN commit
    - Manual reasoning: when StreamProcessor runs against a reconnecting camera (from CameraHealthService re-enqueue), `statusService.transition(cameraId, orgId, 'connecting')` now succeeds and FFmpeg startStream is reached
  </done>
</task>

</tasks>

<verification>
Run full vitest suite to confirm no collateral damage:

```bash
cd apps/api && pnpm vitest run
```

Post-fix live-stack spot-check (can be done outside this plan, but listed for the human operator):
1. Sign up a fresh user via `POST /api/auth/sign-up/email` (no org membership) → `GET /api/cameras` now returns HTTP 403 with message about no active organization (was: HTTP 200 with cross-tenant rows).
2. Super admin (`sura.bs007@gmail.com` or seeded `admin@superadmin.local`) with NO active org set → can still list all cameras across orgs (regression check).
3. Start a camera → let it retry-fail until `status=reconnecting` in DB → click Start Stream OR wait for CameraHealthService tick → log shows `Processing stream job ... (attempt 1)` followed by `FfmpegService: FFmpeg started` (was: `Error: Invalid transition: reconnecting -> connecting` in BullMQ retry loop).
</verification>

<success_criteria>
- [ ] Gap 1 — new RLS isolation tests (3) pass; new AuthGuard tests (4) pass; `app.is_superuser='true'` is now the ONLY path to bypass tenant_isolation_* policies; authenticated user without activeOrgId + without admin role receives HTTP 403 at the guard boundary (never reaches Prisma).
- [ ] Gap 1 — migration `rls_superuser_bypass_positive_signal` drops+recreates ALL 18 `superuser_bypass_*` policies atomically; `rls.policies.sql` + `rls_apply_all/migration.sql` updated so fresh DBs match.
- [ ] Gap 2 — new state-machine test for `reconnecting -> connecting` passes; existing 13+ transition tests remain green; `validTransitions.reconnecting` now `['online', 'offline', 'connecting']`.
- [ ] 4 commits total across the 2 tasks (2 RED + 2 GREEN), each commit isolated and reversible.
- [ ] Zero changes outside the scoped files in `files_modified`.
</success_criteria>

<output>
After completion, create `.planning/quick/260419-h84-fix-tenancy-rls-superuser-bypass-streamp/260419-h84-SUMMARY.md` summarising:
- Migration filename + tables touched
- AuthGuard diff (IS_SUPERUSER flag + 403 branch)
- Extension diff (dual set_config)
- validTransitions diff (1-line change)
- Commits made (4 SHAs)
- Live-stack verification results (if run)
- Any follow-ups (e.g., Gap 3 hls_use_fmp4 is still open — unrelated)
</output>
