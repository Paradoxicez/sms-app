---
phase: 30
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/SMOKE-TEST-LOG.md
autonomous: true
requirements:
  - DEPLOY-25
must_haves:
  truths:
    - "Operator has a structured Markdown template with one row per Success Criterion (SC#1-#4) and one row per deferred UAT item (4 from Phase 27 + 5 from Phase 29 = 9 rows)."
    - "Template carries a redaction notice at the top warning the operator NOT to commit ADMIN_PASSWORD, RTSP credentials, or VM_IP to git."
    - "Template has explicit columns for Result (PASS/FAIL/DRIFT), Evidence (path/excerpt/timestamp), and Duration (seconds)."
    - "Template has a Drift section with a 3-column table format (docs ref / actual / action) so operator can paste drift entries without re-deciding structure."
    - "Template has a chronological timing log section with placeholder lines for bootstrap ELAPSED, each verify-* exit code, and total wall-clock seconds."
  artifacts:
    - path: "deploy/SMOKE-TEST-LOG.md"
      provides: "Per-SC + per-UAT smoke test result table template; Drift backlog ledger; chronological timing log; evidence-folder cross-reference"
      contains: "## Redaction notice, ## Success Criteria results, ## Deferred UAT cross-reference, ## Drift, ## Timing log"
  key_links:
    - from: "deploy/SMOKE-TEST-LOG.md"
      to: "deploy/smoke-evidence/"
      via: "inline screenshot path reference"
      pattern: "deploy/smoke-evidence/"
---

<objective>
Author the structured `deploy/SMOKE-TEST-LOG.md` Markdown template that the operator fills during a Phase 30 smoke run. This is a TEMPLATE not a populated artifact — Phase 30 ships the empty form; the operator paints over it during the actual VM run.

Purpose: Single canonical artifact captures every SC outcome, every deferred UAT outcome, every drift entry, and the timing log in one consistent shape — the file `verify-*.sh` scripts append to (`tee -a`), and the file `30-VERIFICATION.md` consumes when the phase verifier runs.

Output: `deploy/SMOKE-TEST-LOG.md` checked into the repo, mode 0644 (regular Markdown, not executable).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/30-smoke-test-on-clean-vm-ga-gate/30-CONTEXT.md
@.planning/phases/27-caddy-reverse-proxy-auto-tls/27-HUMAN-UAT.md
@.planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-HUMAN-UAT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/SMOKE-TEST-LOG.md template skeleton</name>
  <files>deploy/SMOKE-TEST-LOG.md</files>
  <read_first>
    - .planning/phases/30-smoke-test-on-clean-vm-ga-gate/30-CONTEXT.md (read §decisions D-11 for table shape, D-13 for SC↔UAT mapping table, D-14 for manual UI checklist that operator follows in tandem)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-HUMAN-UAT.md (4 deferred items — copy verbatim into the UAT cross-reference table rows)
    - .planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-HUMAN-UAT.md (5 deferred items — copy verbatim)
    - .planning/ROADMAP.md §Phase 30 lines 196-205 (SC#1-#4 wording — copy verbatim into SC results table)
  </read_first>
  <action>
Create `deploy/SMOKE-TEST-LOG.md` with the EXACT structure below. Do not improvise — D-11 locks the layout. The file is checked into the repo as a template; subsequent smoke runs overwrite/append. No frontmatter (it is operator-facing markdown, not a GSD planning doc).

Template structure (use these literal H1/H2 headings):

```markdown
# SMS Platform — Phase 30 Smoke Test Log

**Status:** TEMPLATE — overwrite during smoke run.
**Phase:** 30 (Smoke Test on Clean VM — v1.3 GA gate)
**SC mapping:** ROADMAP.md §Phase 30 lines 196-205

> **Redaction notice (D-11):** Before committing this file or sharing it, redact:
> - `${ADMIN_PASSWORD}` — never paste plaintext credentials
> - `${RTSP_TEST_URL}` user:pass — strip the userinfo segment
> - `${VM_IP}` — replace with `<vm-public-ip>` if the VM remains in service
> - Any LE cert serial / ACME order ID — operator-private, no value to repo readers
>
> Evidence files (screenshots, raw logs) live alongside this log under `deploy/smoke-evidence/<UTC-ts>/`. That folder is `.gitignore`-d; only this Markdown ships with the repo as the public smoke-run record.

## Run metadata

| Field | Value |
|-------|-------|
| Run UTC start | _<YYYY-MM-DDTHH:MMZ>_ |
| VM hostname | _<hostname>_ |
| VM specs | _<vCPUs / RAM / disk>_ |
| OS | Ubuntu 22.04 LTS |
| Docker version | _<output of `docker --version`>_ |
| Compose plugin | _<output of `docker compose version`>_ |
| Domain | _<smoke.example.com>_ |
| ACME run #1 CA | staging (acme-staging-v02.api.letsencrypt.org/directory) |
| ACME run #2 CA | production (acme-v02.api.letsencrypt.org/directory) |
| IMAGE_TAG | _<vX.Y.Z or `latest`>_ |
| Operator | _<name / handle>_ |

## Success Criteria results (hard gate per D-12 except SC#4 = soft)

| SC | Description | Verifier | Result | Evidence | Duration |
|----|-------------|----------|--------|----------|----------|
| #1 | Cold deploy <10 min wall-clock from `bootstrap.sh` to logged-in super-admin session | verify-deploy.sh | _PASS/FAIL_ | _bootstrap.sh ELAPSED line + verifier exit code_ | _<N>s_ |
| #2 | E2E playback + record + WebSocket on deployed VM (RTSP→HLS→.ts archive→WS frame) | verify-playback.sh + manual UI (D-14) | _PASS/FAIL_ | _MinIO `mc ls` output + DevTools screenshot path_ | _<N>s_ |
| #3 | nmap port lockdown — only 22/80/443/1935/8080/8000-udp/10080-udp open externally | verify-nmap.sh | _PASS/FAIL_ | _nmap stdout pasted below_ | _<N>s_ |
| #4 | Drift log captured (soft gate — does NOT block GA) | smoke-test.sh wrapper + this section | _PASS/DRIFT_ | _Drift section row count_ | _N/A_ |

## Deferred UAT cross-reference (D-13 mapping)

Each row inherits the hard/soft status of its SC owner.

| UAT origin | Description | Folded into | SC owner | Result | Evidence |
|-----------|-------------|-------------|----------|--------|----------|
| Phase 27 SC#1 | Live LE cert + 308 redirect on real DNS | verify-deploy.sh | SC#1 | _PASS/FAIL_ | _curl -i excerpt_ |
| Phase 27 SC#2 | Live wss:// upgrade through caddy to NotificationsGateway + StatusGateway | verify-playback.sh | SC#2 | _PASS/FAIL_ | _HTTP/1.1 101 line_ |
| Phase 27 SC#3 | Cert persistence across `docker compose down/up` | verify-deploy.sh | SC#1 | _PASS/FAIL_ | _grep -c 'certificate obtained' = 0 on second boot_ |
| Phase 27 SC#4 | Re-run `bash deploy/scripts/verify-phase-27.sh` exits 0 on healthy host | verify-deploy.sh | SC#1 | _PASS/FAIL_ | _verifier exit code + tail_ |
| Phase 29 SC#1 | Cold deploy <10-min wall-clock claim | verify-deploy.sh | SC#1 | _PASS/FAIL_ | _bootstrap ELAPSED line_ |
| Phase 29 SC#2 | bin/sms create-admin runtime + idempotent --force rotation | verify-deploy.sh | SC#1 | _PASS/FAIL_ | _exec exit code + login screenshot_ |
| Phase 29 SC#3 | update.sh atomic recycle without dropping in-flight requests | verify-deploy.sh | SC#1 | _PASS/FAIL_ | _curl-probe 200 count during recycle_ |
| Phase 29 SC#4 | backup.sh + restore.sh byte-equivalent round-trip | verify-backup.sh | SC#4 (informational; backup is SC#4 evidence not GA gate) | _PASS/FAIL_ | _SELECT count diff = 0 + mc diff empty_ |
| Phase 29 SC#5 | README quickstart end-to-end (operator follows verbatim, captures drift) | smoke-test.sh wrapper + manual | SC#1 | _PASS/FAIL/DRIFT_ | _operator self-report_ |

## Manual UI checklist (D-14, executed alongside automated verifiers)

Operator ticks each step + pastes evidence reference.

- [ ] 1. Login as super-admin (ADMIN_EMAIL/ADMIN_PASSWORD from `.env`) — confirm session cookie + redirect to dashboard. _Evidence: <screenshot path>_
- [ ] 2. Register test camera (RTSP URL from D-07) — confirm UI shows status `connecting → live` within 30s. _Evidence: <screenshot path>_
- [ ] 3. Click camera card → play HLS player → confirm video playback (10s observation). _Evidence: <screenshot path>_
- [ ] 4. Toggle Record → wait 60s → toggle off (D-08). _Evidence: <MinIO mc ls excerpt>_
- [ ] 5. DevTools WebSocket frame inspection during status change (D-08). _Evidence: <screenshot path>_
- [ ] 6. Stop external RTSP feed → confirm UI status pill changes `live → offline` within <30s (Phase 15 resilience demo). _Evidence: <screenshot path>_
- [ ] 7. README.md follow-along — operator reads `deploy/README.md` Quickstart 1-5 verbatim and notes any docs-vs-reality mismatch in the Drift section. _Evidence: <drift entries below>_

## Drift (D-16 — queue, do not fix inline during smoke)

Add one bullet per drift discovery. Format: `- <docs ref> says X, actual Y → action: <queued v1.3.1 / fixed inline / no-op>`.

- _<no drift detected — delete this placeholder if empty>_

If any rows exist below, also create `.planning/todos/v1.3.1-drift-from-phase-30.md` with one entry per row.

## Timing log

Chronological event log; copy from terminal output as smoke progresses.

```
<UTC-ts>  bootstrap.sh start
<UTC-ts>  bootstrap.sh exit 0  ELAPSED=<N>s
<UTC-ts>  verify-deploy.sh start
<UTC-ts>  verify-deploy.sh exit <0|1|2>  duration=<N>s
<UTC-ts>  manual UI checklist steps 1-6 complete
<UTC-ts>  verify-playback.sh start
<UTC-ts>  verify-playback.sh exit <0|1|2>  duration=<N>s
<UTC-ts>  verify-backup.sh start
<UTC-ts>  verify-backup.sh exit <0|1|2>  duration=<N>s
<UTC-ts>  verify-nmap.sh start (operator's local machine)
<UTC-ts>  verify-nmap.sh exit <0|1|2>  duration=<N>s
<UTC-ts>  smoke-test.sh wrapper exit <0|1|2>  total=<N>s
```

## GA verdict

> Operator fills after the wrapper completes. Hard gate (SC#1 + SC#2 + SC#3 + Phase 29 SC#4) all PASS → GA approved. Any HARD FAIL → re-smoke required after fix lands. SC#4 DRIFT → ship + queue v1.3.1.

**Verdict:** _GA APPROVED / RE-SMOKE REQUIRED / GA APPROVED WITH DRIFT_

**Drift backlog file:** `.planning/todos/v1.3.1-drift-from-phase-30.md` _(create only if drift entries exist)_

---

*Template authored Phase 30 Plan 01.*
```

Specific points the executor must respect:

- Use the exact section ordering above. Subsequent verify-*.sh scripts grep section anchors (e.g., `## Drift`, `## Timing log`); reordering breaks `tee -a`.
- Italic `_<placeholder>_` markers indicate operator-fill spots — keep them as-is.
- Do NOT populate any field with real values (this is a template; D-11 locks "operator fills at execution").
- File mode is 0644 (regular markdown, NOT 0755 — only the verify-* shell scripts in deploy/scripts/ are executable per D-20).
  </action>
  <verify>
    <automated>test -f deploy/SMOKE-TEST-LOG.md && grep -qE '^# SMS Platform — Phase 30 Smoke Test Log$' deploy/SMOKE-TEST-LOG.md && grep -qE '^## Success Criteria results' deploy/SMOKE-TEST-LOG.md && grep -qE '^## Deferred UAT cross-reference' deploy/SMOKE-TEST-LOG.md && grep -qE '^## Drift' deploy/SMOKE-TEST-LOG.md && grep -qE '^## Timing log' deploy/SMOKE-TEST-LOG.md && grep -qE '^## Manual UI checklist' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 27 SC#1' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 27 SC#2' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 27 SC#3' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 27 SC#4' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 29 SC#1' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 29 SC#2' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 29 SC#3' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 29 SC#4' deploy/SMOKE-TEST-LOG.md && grep -qE 'Phase 29 SC#5' deploy/SMOKE-TEST-LOG.md && grep -qE 'Redaction notice' deploy/SMOKE-TEST-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File `deploy/SMOKE-TEST-LOG.md` exists at the deploy/ folder root (NOT under deploy/scripts/)
    - File mode is 0644 (run `stat -f '%Lp' deploy/SMOKE-TEST-LOG.md` on macOS or `stat -c '%a' …` on Linux — NOT 0755)
    - Top-level H1 heading is exactly `# SMS Platform — Phase 30 Smoke Test Log`
    - Contains 6 H2 sections in this order: `## Run metadata`, `## Success Criteria results`, `## Deferred UAT cross-reference`, `## Manual UI checklist`, `## Drift`, `## Timing log` plus `## GA verdict` (the H1 + redaction blockquote come before any H2)
    - Success Criteria table has exactly 4 rows (SC#1, SC#2, SC#3, SC#4)
    - Deferred UAT table has exactly 9 rows (4 Phase 27 + 5 Phase 29) — verify with `grep -c '^| Phase 2[79] SC#' deploy/SMOKE-TEST-LOG.md` returning 9
    - Manual UI checklist has 7 numbered steps with `- [ ]` checkbox markers — verify with `grep -c '^- \[ \] [1-7]\.' deploy/SMOKE-TEST-LOG.md` returning 7
    - Redaction notice contains `ADMIN_PASSWORD`, `RTSP_TEST_URL`, and `VM_IP` placeholders verbatim
    - File does NOT contain real credentials, real domains (other than `smoke.example.com` placeholder), or real LE cert serials
  </acceptance_criteria>
  <done>
    - deploy/SMOKE-TEST-LOG.md exists with the 7 H2 sections (4 SC rows + 9 UAT rows + 7 manual checklist items + drift section + timing log + GA verdict)
    - Redaction notice in place; no real credentials/IPs leaked
    - File mode 0644
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| smoke run → committed log | Operator fills SMOKE-TEST-LOG with potentially-sensitive run details (VM IP, ADMIN_PASSWORD echoes from terminal scrollback, RTSP creds); committing without redaction leaks them to anyone reading the repo. |
| evidence folder → log file | Inline screenshot paths reference `deploy/smoke-evidence/`; if that folder is committed (not gitignored), screenshots leak. Phase 30 must keep the evidence folder out of git. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | I (Information disclosure) | SMOKE-TEST-LOG.md template | mitigate | Template has explicit redaction notice at top with the four sensitive field names (`ADMIN_PASSWORD`, `RTSP_TEST_URL` userinfo, `VM_IP`, LE cert serials). Operator reads the notice before pasting evidence. Plan 01 task creates the notice; downstream plans inherit it. |
| T-30-05 | I (Information disclosure) | committed evidence folder | mitigate | Template documents `deploy/smoke-evidence/` as `.gitignore`-d so screenshots stay local. Plan 06 wrapper creates the evidence folder at run time and the wrapper task adds an entry to deploy/.gitignore (or the existing root .gitignore) to enforce. |
</threat_model>

<verification>
- File exists at the expected path with the documented section anchors.
- All 9 deferred UAT rows match the source HUMAN-UAT.md descriptions verbatim (no paraphrasing or scope drift).
- Redaction notice covers the four sensitive field categories listed in the threat model.
- Template is operator-fill-ready — no real credentials/IPs/serials baked in.
</verification>

<success_criteria>
- `test -f deploy/SMOKE-TEST-LOG.md` returns true
- `grep -c '^| Phase 2[79] SC#' deploy/SMOKE-TEST-LOG.md` returns 9
- `grep -c '^- \[ \] [1-7]\.' deploy/SMOKE-TEST-LOG.md` returns 7
- File contains all 6 H2 anchors used by downstream verify-*.sh `tee -a` operations
- File mode is 0644
</success_criteria>

<output>
After completion, create `.planning/phases/30-smoke-test-on-clean-vm-ga-gate/30-01-SUMMARY.md` documenting the template structure and the section anchors that downstream verifier scripts append against.
</output>
