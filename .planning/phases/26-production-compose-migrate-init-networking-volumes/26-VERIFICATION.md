---
phase: 26-production-compose-migrate-init-networking-volumes
verified: 2026-04-28T00:00:00Z
status: passed
score: 6/6 must-haves verified (static scope; live-boot SCs deferred to Phase 30 per phase contract)
deferred:
  - truth: "docker compose up -d brings full stack up under 2 minutes; all healthchecks pass on a clean Linux VM"
    addressed_in: "Phase 30"
    evidence: "Phase 30 SC #1: 'total wall-clock from ssh first-login to operator can log in at HTTPS URL is under 10 minutes' — encompasses Phase 26 SC #1 live-boot. Phase scope guidance from operator: 'Phase 26 explicitly stops at the YAML is provably correct'."
  - truth: "Named volumes survive docker compose down && up -d with data intact; FFmpeg child processes reaped (no zombies) at runtime"
    addressed_in: "Phase 30"
    evidence: "Phase 30 is the GA gate that exercises the live stack on a clean VM. SC #4's runtime persistence claims cannot be validated without booting the stack — explicitly deferred per phase contract."
  - truth: "First-run init creates avatars and recordings MinIO buckets at runtime; default Stream Profile seeded; subsequent boots are idempotent (no duplicate buckets/profiles)"
    addressed_in: "Phase 30"
    evidence: "Phase 30 SC #2 boots the full stack including sms-migrate. Phase 26 verified the init scripts EXIST, COMPILE, and have the correct contracts (idempotency guards on bucketExists() and per-org streamProfile.count); the actual MinIO + Postgres state mutation is Phase 30 territory."
  - truth: "External nmap shows postgres/redis/minio/srs-admin 1985 closed from outside the host"
    addressed_in: "Phase 30"
    evidence: "Phase 30 SC #3: 'nmap -p 22,80,443,1935,8080,8000,10080,5432,6379,9000,9001,1985 <vm-public-ip> from an external machine shows ONLY [public ports] open; postgres 5432, redis 6379, minio 9000+9001, srs admin 1985 are all closed externally'. Phase 26 verifies the YAML topology (no host ports on stateful services + 127.0.0.1 binding for SRS 1985) — runtime nmap is the canonical authority and lives in Phase 30."
---

# Phase 26: production-compose-migrate-init-networking-volumes Verification Report

**Phase Goal:** A single `deploy/docker-compose.yml` brings up the entire production stack on any Linux host with Docker Compose v2 — Postgres + Redis + MinIO + SRS + sms-migrate (init) + api + web — using GHCR image references only (no `build:` context). The two-network topology hides stateful services from the host, the migrate service runs once before api boots, and first-run init creates required MinIO buckets + seeds a default Stream Profile.

**Verified:** 2026-04-28T00:00:00Z
**Status:** passed (within Phase 26 static-artifact scope per operator-supplied verification contract)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

The 6 ROADMAP success criteria, scoped per the operator-supplied phase contract (SC #1 live-boot, SC #4 runtime persistence, and SC #5 runtime buckets/profile state are explicitly deferred to Phase 30 — Phase 26 verifies the static-artifact precondition).

| #  | Truth (ROADMAP SC)                                                                                                                                  | Status                  | Evidence                                                                                                                                                                                                                                                                                                                                                              |
| -- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1  | SC #1 — Single `deploy/docker-compose.yml` boot one-liner valid for full stack                                                                      | VERIFIED (static scope) | `deploy/docker-compose.yml` exists (10K, 7 services, 2 networks, 5 volumes); `docker compose -f deploy/docker-compose.yml --env-file <stub>.env config --quiet` exits 0. Live boot deferred to Phase 30.                                                                                                                                                              |
| 2  | SC #2 — sms-migrate runs `prisma migrate deploy` once with `restart: "no"`; api gated by `service_completed_successfully`                           | VERIFIED                | compose.yml lines 146-176: sms-migrate has `restart: "no"`, command chains `prisma migrate deploy && init-buckets.js && seed-stream-profile.js`. Lines 211-213: api `depends_on.sms-migrate.condition: service_completed_successfully`.                                                                                                                              |
| 3  | SC #3 — nmap shows 5432/6379/9000/9001/1985 closed externally; SRS 1985 binds 127.0.0.1                                                             | VERIFIED (static scope) | postgres/redis/minio/sms-migrate/api/web have NO `ports:` blocks (only srs has one); SRS 1985 line: `"127.0.0.1:1985:1985"` (line 119). Rendered (post-interpolation) compose confirms `host_ip: 127.0.0.1` + `target: 1985` long-form. External nmap from a foreign machine deferred to Phase 30 SC #3.                                                              |
| 4  | SC #4 — 5 named volumes survive recycle; `init: true` reaps FFmpeg zombies                                                                          | VERIFIED (static scope) | Top-level `volumes:` block declares postgres_data, redis_data, minio_data, caddy_data, hls_data (lines 268-273). `init: true` present on 6 long-running services (postgres, redis, minio, srs, api, web). Live persistence + zombie-reap behavior deferred to Phase 30.                                                                                              |
| 5  | SC #5 — First-run init creates avatars + recordings MinIO buckets, seeds default Stream Profile, idempotent on subsequent boots                     | VERIFIED (static scope) | `apps/api/src/scripts/init-buckets.ts` (80 LOC): `Client.bucketExists()` guard, makeBucket on avatars (publicRead=true) + recordings (publicRead=false). `apps/api/src/scripts/seed-stream-profile.ts` (77 LOC): per-org `prisma.streamProfile.count()` guard before `create()`. Both compile to `dist/scripts/*.js` (verified). Runtime mutation deferred to Phase 30. |
| 6  | SC #6 — `.env.production.example` documents every required variable; `init-secrets.sh` generates 32-char random values and chmods 600               | VERIFIED                | `deploy/.env.production.example` contains all 7 SC#6 vars (DOMAIN, DB_PASSWORD, NEXTAUTH_SECRET, BETTER_AUTH_SECRET, MINIO_ROOT_USER, MINIO_ROOT_PASSWORD, IMAGE_TAG) + GHCR_ORG + JWT_PLAYBACK_SECRET + ADMIN_EMAIL/ADMIN_PASSWORD + POSTGRES_USER/POSTGRES_DB. `deploy/scripts/init-secrets.sh` uses `openssl rand -base64 32` and `chmod 600`. End-to-end smoke confirmed below. |

**Score:** 6/6 truths verified (static scope per phase contract; runtime behavior deferred to Phase 30).

### Deferred Items

Items explicitly scoped to Phase 30 by the operator's verification contract for this phase. Not actionable gaps — addressed by a later phase in the v1.3 milestone.

| # | Item                                                                                                       | Addressed In | Evidence                                                                                                                                                                              |
| - | ---------------------------------------------------------------------------------------------------------- | ------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | "<2 min boot on clean Linux VM; all healthchecks pass" (SC #1 runtime claim)                               | Phase 30     | Phase 30 SC #1: bootstrap end-to-end on fresh DigitalOcean/Hetzner droplet, total wall-clock <10 min; encompasses Phase 26 SC #1 live boot                                            |
| 2 | "Volumes survive `down && up -d` with data intact; no FFmpeg zombies" (SC #4 runtime claim)                | Phase 30     | Phase 30 is the GA gate; live persistence + PID lifecycle observable only after boot                                                                                                  |
| 3 | "First-run init creates avatars + recordings MinIO buckets, seeds default profile" (SC #5 runtime mutation) | Phase 30     | Phase 30 SC #2 boots the full stack including sms-migrate; verifies init chain ran successfully against real MinIO and Postgres                                                       |
| 4 | "External nmap from off-host shows stateful service ports closed" (SC #3 runtime check)                    | Phase 30     | Phase 30 SC #3 explicitly runs `nmap -p ... <vm-public-ip>` from an external machine                                                                                                  |

### Required Artifacts

| Artifact                                              | Expected                                                       | Status     | Details                                                                                                                                                                                              |
| ----------------------------------------------------- | -------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `deploy/docker-compose.yml`                           | 7 services, 2 networks, 5 volumes, GHCR-only images            | VERIFIED   | 273 LOC, 10K. 7 services match (postgres, redis, minio, sms-migrate, srs, api, web). 2 networks (edge bridge + internal:true bridge). 5 named volumes. Zero `build:`. Zero `host.docker.internal`.   |
| `deploy/.env.production.example`                      | 4 D-25 sections, all 7 SC#6 vars + GHCR_ORG + JWT_PLAYBACK_SECRET, change-me-* placeholders | VERIFIED   | 13/13 required vars present. 4/4 section markers. 7 `change-me-*` placeholders. `IMAGE_TAG=latest` default. POSTGRES_USER=sms / POSTGRES_DB=sms_platform defaults present. Computed vars commented out. |
| `deploy/scripts/init-secrets.sh`                      | bash, executable, idempotent, openssl rand -base64 32, chmod 600 | VERIFIED   | 86 LOC, mode 0755 (rwxr-xr-x). `bash -n` exit 0. Contains `set -euo pipefail`, `openssl rand -base64 32`, `chmod 600`, `change-me-*` detection, BSD/GNU sed shim. SECRET_VARS array excludes DOMAIN/GHCR_ORG/ADMIN_EMAIL. |
| `apps/api/src/scripts/init-buckets.ts`                | Idempotent MinIO bootstrap; avatars public-read; recordings private | VERIFIED   | 80 LOC. `Client.bucketExists()` guard before `makeBucket()`. `setBucketPolicy` only on avatars (publicRead=true). `process.exit(1)` in `.catch`. Env var fallback: MINIO_ACCESS_KEY ?? MINIO_ROOT_USER. Compiles to `dist/scripts/init-buckets.js`. |
| `apps/api/src/scripts/seed-stream-profile.ts`         | Per-org idempotent default StreamProfile seed; fresh-VM no-orgs friendly path | VERIFIED   | 77 LOC. Per-org `prisma.streamProfile.count()` guard. `isDefault: true`, codec `h264`, resolution `1920x1080`, fps 25, videoBitrate `2500k`, audioCodec `aac`, audioBitrate `128k`. PrismaClient with `datasourceUrl: DATABASE_URL_MIGRATE ?? DATABASE_URL`. Empty-orgs path logs and returns. Compiles to `dist/scripts/seed-stream-profile.js`. |

### Key Link Verification

| From                                                      | To                                                                            | Via                                                                                                                                | Status     | Details                                                                                                                                                                       |
| --------------------------------------------------------- | ----------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `deploy/docker-compose.yml sms-migrate.command`           | `dist/scripts/{init-buckets,seed-stream-profile}.js`                          | sh -c heredoc chain after `prisma migrate deploy --schema=src/prisma/schema.prisma`                                                | WIRED      | Lines 150-159: chain references `dist/scripts/init-buckets.js` AND `dist/scripts/seed-stream-profile.js` AND `node node_modules/prisma/build/index.js migrate deploy`           |
| `api.depends_on.sms-migrate`                              | Compose v2 condition gate                                                     | `condition: service_completed_successfully`                                                                                        | WIRED      | Lines 211-213 of compose.yml — exact literal match for the gate condition                                                                                                     |
| `deploy/docker-compose.yml --env-file consumer`           | `deploy/.env` (Plan 02 operator-populated)                                    | `${VAR}` interpolation                                                                                                              | WIRED      | DOMAIN, GHCR_ORG, IMAGE_TAG, DB_PASSWORD, MINIO_ROOT_PASSWORD, BETTER_AUTH_SECRET, NEXTAUTH_SECRET, JWT_PLAYBACK_SECRET all interpolated; rendered config has empty values where stub env left blank, but exits 0 with stub values present. |
| `api environment SRS_HLS_PATH`                            | `hls_data` volume mounted at `/srs-hls:ro`                                    | shared named volume between srs (RW) and api (RO)                                                                                  | WIRED      | Line 202: `SRS_HLS_PATH: /srs-hls`. Line 210: `- hls_data:/srs-hls:ro`. Line 125: srs writes RW at `/usr/local/srs/objs/nginx/html`                                            |
| `init-buckets.ts`                                         | minio (S3 SDK Client)                                                         | `new Client(...) → bucketExists/makeBucket/setBucketPolicy`                                                                        | WIRED      | Lines 19, 39-48 of init-buckets.ts: imports `Client` from `'minio'`, calls `bucketExists`, `makeBucket`, `setBucketPolicy`                                                     |
| `seed-stream-profile.ts`                                  | Postgres (Prisma)                                                             | `prisma.streamProfile.create` after orgsWithoutDefault loop                                                                        | WIRED      | Lines 21-25 import + construct PrismaClient with datasourceUrl fallback. Lines 42-65: per-org loop with count guard then create()                                              |
| `init-secrets.sh`                                         | `deploy/.env` (mutates)                                                       | in-place sed substitution + chmod 600                                                                                              | WIRED      | Lines 49-55 sed_inplace shim; line 67 sed substitution; line 76 `chmod 600 "${ENV_FILE}"`                                                                                      |
| `apps/api/Dockerfile (Phase 25 builder stage)`            | `apps/api/src/scripts/*.ts → dist/scripts/*.js`                               | SWC compile via `pnpm build`                                                                                                       | WIRED      | `apps/api/dist/scripts/init-buckets.js` and `apps/api/dist/scripts/seed-stream-profile.js` exist on disk (post-build artifacts present, ~2.6-2.8K each + sourcemaps)            |

### Behavioral Spot-Checks

Static-artifact behavioral checks within Phase 26 scope (no live boot):

| Behavior                                                                  | Command                                                                                                            | Result                                                                       | Status |
| ------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- | ------ |
| `docker compose config --quiet` validates compose file structurally       | `docker compose -f deploy/docker-compose.yml --env-file /tmp/<stub>.env config --quiet`                            | exit 0; stderr empty                                                         | PASS   |
| `bash -n` syntax check on init-secrets.sh                                 | `bash -n deploy/scripts/init-secrets.sh`                                                                            | exit 0                                                                       | PASS   |
| init-secrets.sh first-run generates secrets + chmod 600                   | end-to-end smoke against tmp-copied template                                                                       | 6 generated / 0 already-set / perms 600                                      | PASS   |
| init-secrets.sh second-run is idempotent (real values preserved)          | re-run on now-populated env                                                                                        | 0 generated / 6 already-set / 0 placeholders-hit                             | PASS   |
| Rendered compose has zero `build:` directives                              | `docker compose ... config \| grep -E "(build:|host.docker.internal)" \| wc -l`                                      | 0                                                                            | PASS   |
| Rendered compose preserves SRS 1985 loopback binding (long-form)          | inspect `srs.ports` block in rendered output                                                                       | `host_ip: 127.0.0.1` + `target: 1985` adjacent                               | PASS   |
| TypeScript scripts compile to `dist/scripts/*.js`                          | `ls apps/api/dist/scripts/`                                                                                        | `init-buckets.js` (2.8K), `seed-stream-profile.js` (2.6K), both with sourcemaps | PASS   |
| Service count, init:true count, restart:unless-stopped count              | regex match against compose.yml                                                                                    | services=7, init:true=6, restart:unless-stopped=6 (all expected)             | PASS   |

### Requirements Coverage

All 8 requirement IDs declared by phase plans (DEPLOY-10..16, DEPLOY-22) are accounted for. ROADMAP §Coverage Map confirms Phase 26 = 8 requirements with no orphans.

| Requirement | Source Plan(s)            | Description                                                                                                                                                                                                              | Status                  | Evidence                                                                                                                                                                                                            |
| ----------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| DEPLOY-10   | 26-03                     | `deploy/docker-compose.yml` references GHCR images only (no `build:` context); fresh server runs `docker compose pull && docker compose up -d` against the file alone                                                     | SATISFIED               | All 4 image-using services reference `ghcr.io/${GHCR_ORG}/sms-{api,web}:${IMAGE_TAG:-latest}` or external public images (postgres, redis, minio, srs). Zero `build:` directives in source AND rendered compose.       |
| DEPLOY-11   | 26-03                     | Two-network topology (edge + internal: true); postgres/redis/minio have no host ports; SRS admin port 1985 binds 127.0.0.1 only                                                                                            | SATISFIED (static)      | edge + internal networks declared; `internal: true` on internal. Only srs has `ports:` block. SRS 1985 → `127.0.0.1:1985:1985` short-form, normalized to `host_ip: 127.0.0.1` long-form by compose. External nmap deferred to Phase 30. |
| DEPLOY-12   | 26-03                     | Named volumes for postgres_data, redis_data, minio_data, caddy_data, hls_data (shared SRS↔api); volumes survive container recycle                                                                                          | SATISFIED (static)      | All 5 volumes declared at top-level. hls_data RW on srs (`/usr/local/srs/objs/nginx/html`), RO on api (`/srs-hls:ro`). Live "survive recycle" behavior deferred to Phase 30 SC #4.                                  |
| DEPLOY-13   | 26-03                     | Every long-running service has `init: true`, healthcheck with `start_period`, `restart: unless-stopped`, JSON-file logging with rotation (10m × 5)                                                                         | SATISFIED               | 6 long-running services (postgres, redis, minio, srs, api, web): all have `init: true`, all reference `*default-logging` anchor (json-file, max-size 10m, max-file 5), all `restart: unless-stopped`. Healthchecks present on postgres/redis/minio/srs (api/web rely on Phase 25 image-side HEALTHCHECK per D-20). |
| DEPLOY-14   | 26-03                     | Dedicated `sms-migrate` init service runs `prisma migrate deploy` once with `restart: "no"`; api service depends on it via `condition: service_completed_successfully`                                                    | SATISFIED               | sms-migrate `restart: "no"` (line 148). Init chain executes prisma migrate deploy first. api `depends_on.sms-migrate.condition: service_completed_successfully` (lines 211-213).                                    |
| DEPLOY-15   | 26-01, 26-03              | First-run init creates required MinIO buckets (avatars, recordings) automatically; idempotent on subsequent boots                                                                                                          | SATISFIED (static)      | `init-buckets.ts` exists, compiles to `dist/scripts/init-buckets.js`, uses `bucketExists()` guard. Compose chain calls `node dist/scripts/init-buckets.js` after migrate. Runtime bucket creation verified at Phase 30 boot. |
| DEPLOY-16   | 26-01, 26-03              | First-run seed inserts default Stream Profile if no profiles exist; idempotent                                                                                                                                             | SATISFIED (static)      | `seed-stream-profile.ts` exists, compiles to `dist/scripts/seed-stream-profile.js`, uses per-org `streamProfile.count()` guard. Compose chain calls `node dist/scripts/seed-stream-profile.js` after init-buckets. Runtime seed verified at Phase 30 boot. |
| DEPLOY-22   | 26-02                     | `deploy/.env.production.example` documents every required variable (DOMAIN, DB_PASSWORD, NEXTAUTH_SECRET, BETTER_AUTH_SECRET, MINIO_ROOT_USER, MINIO_ROOT_PASSWORD, IMAGE_TAG); `deploy/scripts/init-secrets.sh` generates 32-char random values for empty fields and chmods to 600 | SATISFIED               | All 7 SC#6 vars present in template + GHCR_ORG + JWT_PLAYBACK_SECRET + admin vars. init-secrets.sh: `openssl rand -base64 32` (256-bit entropy → 43 base64 chars), `chmod 600` runs unconditionally before summary echo, idempotency guard on empty/`change-me-*`/`<generated>`. End-to-end smoke confirmed first-run + idempotent re-run. |

**Coverage:** 8/8 requirement IDs satisfied. No orphans. No gaps.

### Anti-Patterns Found

Scanned all 5 phase artifacts (`deploy/docker-compose.yml`, `deploy/.env.production.example`, `deploy/scripts/init-secrets.sh`, `apps/api/src/scripts/init-buckets.ts`, `apps/api/src/scripts/seed-stream-profile.ts`).

| File                                                          | Line | Pattern                                                              | Severity   | Impact                                                                                                                                                          |
| ------------------------------------------------------------- | ---- | -------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `deploy/.env.production.example`                              | 13, 35, 41 | Empty values for DOMAIN, ADMIN_EMAIL, GHCR_ORG (`DOMAIN=`, etc.)     | Info       | Intentional — these are operator-supplied identifiers, NOT secrets. The 4-section structure documents this. init-secrets.sh deliberately does NOT include them in SECRET_VARS. |
| `deploy/.env.production.example`                              | 17,20,24,28,31,36 | `change-me-*` placeholder values for secrets                          | Info       | Intentional placeholder convention. init-secrets.sh detection regex requires this prefix. Phase 30 smoke detects placeholders shipping to prod via auth-failure observability. |
| `deploy/docker-compose.yml`                                   | -    | No `build:`, no `host.docker.internal`, no `version: '3'`             | -          | None — these are GA-blocker anti-patterns confirmed ABSENT.                                                                                                     |
| `apps/api/src/scripts/init-buckets.ts`                        | -    | No TODO/FIXME/PLACEHOLDER markers                                    | -          | None.                                                                                                                                                           |
| `apps/api/src/scripts/seed-stream-profile.ts`                 | -    | No TODO/FIXME/PLACEHOLDER markers; no use of stale CONTEXT D-13 schema field names (`videoCodec`, `width`, `height`, `framerate`, `gopSize`) | -          | None — the plan's documented schema-divergence correction was honored. Field names match actual `StreamProfile` model.                                          |
| `deploy/scripts/init-secrets.sh`                              | -    | No `--force` flag (would overwrite real secrets); no `sudo`; no Node/npm; no mktemp+mv (in-place sed only) | -          | None — anti-pattern surface explicitly avoided per plan.                                                                                                         |

**Net:** Zero blockers, zero warnings. Three Info markers — all intentional design choices documented by the phase plans.

### Human Verification Required

None within Phase 26 scope. The runtime acceptance gates (live boot, external nmap, persistent volumes, real bucket creation) are explicitly carried by Phase 30 (DEPLOY-25, the v1.3 GA gate phase).

### Gaps Summary

No gaps within Phase 26's stated scope. The phase contract (provided by the operator at verification time) explicitly bounds Phase 26 at "the YAML is provably correct" and the static artifact set. All 8 requirement IDs are SATISFIED, all 6 ROADMAP success criteria are VERIFIED at the static-artifact level, and all 4 runtime claims (SC #1 boot time, SC #3 external nmap, SC #4 volume persistence, SC #5 runtime bucket/profile creation) are deferred to Phase 30 where they belong.

The phase output is ready for the Phase 27 hand-off:
- `deploy/docker-compose.yml` is the canonical baseline that Phase 27 will EXTEND (add `caddy` service to `edge` network, attach pre-declared `caddy_data` volume) without modifying any of the 7 services already present
- `deploy/.env.production.example` + `deploy/scripts/init-secrets.sh` provide the stable variable surface that Phase 27 (Caddy reads DOMAIN) and Phase 29 (`bootstrap.sh` wraps `init-secrets.sh`) consume
- `apps/api/src/scripts/{init-buckets,seed-stream-profile}.ts` are wired into the sms-migrate command chain and ship inside the api image (Phase 25 SWC pipeline)

---

_Verified: 2026-04-28T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
