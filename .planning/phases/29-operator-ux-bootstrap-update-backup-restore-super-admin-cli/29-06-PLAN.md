---
phase: 29-operator-ux-bootstrap-update-backup-restore-super-admin-cli
plan: 06
type: execute
wave: 3
depends_on: [29-01, 29-02, 29-03, 29-04, 29-05]
files_modified:
  - deploy/README.md
  - deploy/BACKUP-RESTORE.md
  - deploy/TROUBLESHOOTING.md
autonomous: true
requirements:
  - DEPLOY-23

must_haves:
  truths:
    - "A first-time operator reads `deploy/README.md` (5-step quickstart) and reaches a logged-in HTTPS endpoint without consulting any other doc."
    - "Operator with backup/restore needs reads `deploy/BACKUP-RESTORE.md` and gets: cron schedule example, offsite-copy pattern (rclone), DR walkthrough, retention recommendations, restore RTO target."
    - "Operator under outage stress reads `deploy/TROUBLESHOOTING.md` symptom→diagnosis→fix runbook table and resolves the 6 most common failures (ACME pending, sms-migrate exit 1, create-admin user-exists, compose pull denied, backup disk full, restore volume in use) in <5 minutes."
    - "README.md OVERWRITES the Phase 24 stub completely (D-25); BACKUP-RESTORE.md and TROUBLESHOOTING.md are NEW."
    - "All three docs cross-link: README day-2 ops section → BACKUP-RESTORE + TROUBLESHOOTING; BACKUP-RESTORE → restore.sh script + TROUBLESHOOTING; TROUBLESHOOTING → all four scripts + DOMAIN-SETUP.md."
  artifacts:
    - path: "deploy/README.md"
      provides: "5-step quickstart proving <10-min cold deploy + day-2 ops + cross-links"
      min_lines: 100
      contains: "bootstrap.sh"
    - path: "deploy/BACKUP-RESTORE.md"
      provides: "Cron schedule + offsite-copy + DR walkthrough + retention + RTO"
      min_lines: 80
      contains: "rclone"
    - path: "deploy/TROUBLESHOOTING.md"
      provides: "Symptom→diagnosis→fix runbook table covering 6+ common failures"
      min_lines: 60
      contains: "create-admin"
  key_links:
    - from: "deploy/README.md"
      to: "deploy/scripts/bootstrap.sh"
      via: "Step 4 quickstart command"
      pattern: "bash deploy/scripts/bootstrap\\.sh"
    - from: "deploy/README.md"
      to: "deploy/DOMAIN-SETUP.md"
      via: "Step 3 DNS configuration link (Phase 27 product)"
      pattern: "DOMAIN-SETUP\\.md"
    - from: "deploy/README.md"
      to: "deploy/BACKUP-RESTORE.md"
      via: "Day-2 ops section link"
      pattern: "BACKUP-RESTORE\\.md"
    - from: "deploy/BACKUP-RESTORE.md"
      to: "deploy/scripts/{backup,restore}.sh"
      via: "Recipe sections"
      pattern: "deploy/scripts/(backup|restore)\\.sh"
    - from: "deploy/TROUBLESHOOTING.md"
      to: "All 4 deploy scripts + DOMAIN-SETUP.md"
      via: "Symptom-fix table entries"
      pattern: "create-admin"
---

<objective>
Author the three operator-facing markdown documents that close out Phase 29:

1. **`deploy/README.md`** — overwrites the Phase 24 stub with a 5-step quickstart proving <10-min cold deploy (D-25), Day-2 ops references (update/backup/restore), and cross-links to BACKUP-RESTORE.md, TROUBLESHOOTING.md, and DOMAIN-SETUP.md.
2. **`deploy/BACKUP-RESTORE.md`** — operator runbook: cron schedule example, rclone offsite copy, full DR walkthrough, retention guidance, RTO target (D-27).
3. **`deploy/TROUBLESHOOTING.md`** — symptom-diagnosis-fix runbook table for the 6 most common failures plus catch-all guidance (D-28).

Purpose: ROADMAP §Phase 29 SC #5 makes README the v1.3 acceptance gate for the <10-minute claim. Without these docs the operator has no entry point to the deploy folder and the script set is unfindable. Phase 30 smoke test (DEPLOY-25) will follow README step-by-step on a fresh VM — the docs ARE the test plan.

Output: 3 markdown files in `deploy/`, all cross-linked, all referencing concrete script paths + commands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-CONTEXT.md
@deploy/README.md
@deploy/DOMAIN-SETUP.md
@deploy/scripts/bootstrap.sh
@deploy/scripts/update.sh
@deploy/scripts/backup.sh
@deploy/scripts/restore.sh
@deploy/scripts/init-secrets.sh
@deploy/.env.production.example

<interfaces>
Phase 24 stub (`deploy/README.md`) is ~16 lines of bullet-list phase ownership; this plan OVERWRITES it.

D-25 5-step quickstart structure (must be reflected in README):
  1. Clone (or sparse-checkout deploy/)
  2. Configure secrets + identity (cp + edit .env: DOMAIN, ADMIN_EMAIL, ADMIN_PASSWORD, ACME_EMAIL, GHCR_ORG)
  3. Configure DNS (A-record → server IP; link to DOMAIN-SETUP.md)
  4. Bootstrap (bash deploy/scripts/bootstrap.sh)
  5. Login (https://DOMAIN with ADMIN_EMAIL / ADMIN_PASSWORD)

D-27 BACKUP-RESTORE.md sections (mandatory):
  - Quick reference (one-liner backup + restore commands)
  - Cron auto-schedule example
  - Offsite copy with rclone
  - Full DR walkthrough (fresh VM → restore → verify)
  - Retention recommendations (7 daily + 4 weekly + 3 monthly)
  - RTO target (~5-15 min based on archive size)
  - Encryption note (operator wraps with gpg/age — v1.4 adds --encrypt flag)

D-28 TROUBLESHOOTING.md table format: `| Symptom | Diagnosis | Fix |`
  Minimum 6 entries: ACME pending / sms-migrate exit 1 / create-admin user-exists / compose pull denied / backup disk full / restore volume in use.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Overwrite deploy/README.md (5-step quickstart + day-2 ops + cross-links)</name>
  <files>deploy/README.md, deploy/SMOKE-TEST-LOG.md</files>
  <read_first>
    - deploy/README.md (current Phase 24 stub — about to be overwritten; preserve no content)
    - .planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-CONTEXT.md §D-25 (5-step quickstart structure verbatim), §D-26 (timing claim proof method)
    - deploy/DOMAIN-SETUP.md (Phase 27 product — README step 3 links to it; do NOT modify)
    - deploy/scripts/bootstrap.sh (Plan 29-02 — README step 4 invokes this)
    - deploy/scripts/update.sh (Plan 29-03), deploy/scripts/backup.sh (Plan 29-04), deploy/scripts/restore.sh (Plan 29-05)
    - deploy/.env.production.example (Section 1 vars: DOMAIN, ADMIN_EMAIL, ADMIN_PASSWORD, ACME_EMAIL, GHCR_ORG)
  </read_first>
  <action>
Use the `Write` tool to replace the entire contents of `deploy/README.md`. The new file must follow this structure exactly:

- H1: `# SMS Platform — Production Deployment` followed by 1-2 sentences positioning the deployment model (single-server self-hosted via Docker Compose, GHCR images, Caddy auto-TLS, <10-minute cold deploy).
- H2 `## Prerequisites` — bullet list: Linux server (Ubuntu 22.04 LTS+), Docker Engine 24+, Docker Compose v2.20+, domain with A-record, ports 80+443 reachable, ~10 GB free disk.
- H2 `## Quickstart` with 5 H3 sub-sections numbered exactly per D-25:
  - `### 1. Clone (or sparse-checkout deploy/)` — show both full clone and sparse-checkout commands in fenced bash blocks.
  - `### 2. Configure secrets + identity` — `cp deploy/.env.production.example deploy/.env` then `$EDITOR deploy/.env`. Bullet list of 4 operator-supplied identifiers: DOMAIN, ADMIN_EMAIL, ACME_EMAIL, GHCR_ORG. Note that `change-me-*` secrets are auto-generated by `init-secrets.sh` (called by bootstrap).
  - `### 3. Configure DNS` — `dig +short A DOMAIN` verification command. Link to `[DOMAIN-SETUP.md](./DOMAIN-SETUP.md)` for provider walkthroughs.
  - `### 4. Bootstrap` — single command: `bash deploy/scripts/bootstrap.sh`. One paragraph explaining what it does (auto-secrets, pull, migrate, seed, create-admin, HTTPS poll). Wall-clock target: 5-10 minutes typical.
  - `### 5. Login` — visit `https://DOMAIN` with the ADMIN_EMAIL/ADMIN_PASSWORD from .env.
- H2 `## Day-2 Operations` with 4 H3 sub-sections:
  - `### Update to a new image tag` — `bash deploy/scripts/update.sh v1.3.1`. One sentence on atomic pre-flight migrate guard.
  - `### Backup` — `bash deploy/scripts/backup.sh`. Note BACKUP_DIR override. Link to BACKUP-RESTORE.md.
  - `### Restore` — `bash deploy/scripts/restore.sh ./backups/sms-backup-<UTC-ts>.tar.gz`. Note `--yes` flag for DR automation. Link to BACKUP-RESTORE.md.
  - `### Super-admin password rotation` — full `docker compose -f deploy/docker-compose.yml --env-file deploy/.env exec api bin/sms create-admin --email "$ADMIN_EMAIL" --password '<new-password>' --force` invocation. Add a note immediately below: "v1.3 supports a single super-admin (matched by email). Multiple super-admin accounts will land in v1.4 (DEPLOY-29). To rotate the password, re-run with the SAME `$ADMIN_EMAIL` and `--force` — re-running with a different email will be refused with a clear error."
- H2 `## Troubleshooting` — single paragraph linking to `[TROUBLESHOOTING.md](./TROUBLESHOOTING.md)` with a one-line teaser of what's covered.
- H2 `## Layout` — markdown table `| Path | Purpose |` listing every file in `deploy/`: docker-compose.yml, Caddyfile, .env.production.example, scripts/{bootstrap,update,backup,restore,init-secrets}.sh, DOMAIN-SETUP.md, BACKUP-RESTORE.md, TROUBLESHOOTING.md. Below the table, one paragraph reiterating the CLAUDE.md "Deploy Folder Convention" rule (production-only, no dev tooling).
- H2 `## <10-minute proof` — one paragraph: bootstrap script self-reports elapsed wall-clock time; for v1.3 GA timing log see Phase 30 `deploy/SMOKE-TEST-LOG.md`.
- H2 `## Reference` — bullet list of pointers: ROADMAP.md, REQUIREMENTS.md, research/ARCHITECTURE.md, research/PITFALLS.md.

**Critical correctness notes (do NOT deviate):**

1. **OVERWRITE the file** — D-25 explicit "overwrite Phase 24 stub". Use the `Write` tool with full new content; do not preserve, append, or merge.
2. **Quickstart numbering is exactly 5 H3 sub-sections** matching D-25 verbatim.
3. **Cross-links use relative paths** (`./DOMAIN-SETUP.md`, `./BACKUP-RESTORE.md`, `./TROUBLESHOOTING.md`).
4. **Command snippets are copy-pasteable verbatim** — no fictional placeholders except genuinely operator-supplied values.
5. **Layout table includes EVERY file in deploy/** so operators can map paths to purpose without `ls`-ing the directory.
6. **Do NOT promise a recorded video walkthrough** — D-26 is self-reported timing only.

**Additionally (B3 — Phase 29 revision):** create `deploy/SMOKE-TEST-LOG.md` as a placeholder so the `<10-minute proof` section's link does NOT dangle. ROADMAP SC #5 asks for a "<10-min claim with timing log"; bootstrap.sh (Plan 29-02 D-12) already prints elapsed seconds, but Phase 29 does not yet populate a timing log on a real fresh-VM provision (that's Phase 30 / DEPLOY-25). Ship the placeholder now so README's link target exists immediately:

```markdown
# SMS Platform — Smoke Test Log

This file records cold-deploy timings from `deploy/scripts/bootstrap.sh`.

- Phase 29 ships the timing-log mechanism (bootstrap.sh prints elapsed seconds at the end of a run).
- Phase 30 (DEPLOY-25) populates this log with timings from a real fresh-VM provision.

## Entries

_No entries yet — first entry will be recorded in Phase 30 (DEPLOY-25)._
```

The placeholder must satisfy the README's existing reference and provide a clear "Phase 30" forward-pointer so the next operator/agent knows where the real entries land.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && grep -q 'bootstrap.sh' deploy/README.md && grep -q 'DOMAIN-SETUP.md' deploy/README.md && grep -q 'BACKUP-RESTORE.md' deploy/README.md && grep -q 'TROUBLESHOOTING.md' deploy/README.md && [ "$(grep -c '^### ' deploy/README.md)" -ge 9 ] && test -f deploy/SMOKE-TEST-LOG.md && grep -q 'Phase 30' deploy/SMOKE-TEST-LOG.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists and was rewritten (not the Phase 24 stub): `[ "$(wc -l < deploy/README.md)" -ge 80 ]`
    - SMOKE-TEST-LOG.md placeholder exists (B3 — Phase 29 revision): `test -f deploy/SMOKE-TEST-LOG.md && grep -q 'Phase 30' deploy/SMOKE-TEST-LOG.md`
    - Has H1 title: `head -1 deploy/README.md | grep -qE '^# .*SMS Platform.*Production Deployment'`
    - Quickstart H2 present: `grep -qE '^## Quickstart' deploy/README.md`
    - Exactly the 5 quickstart H3 sub-sections (Clone, Configure secrets + identity, Configure DNS, Bootstrap, Login): `grep -qE '^### 1\. Clone' deploy/README.md && grep -qE '^### 2\. Configure secrets' deploy/README.md && grep -qE '^### 3\. Configure DNS' deploy/README.md && grep -qE '^### 4\. Bootstrap' deploy/README.md && grep -qE '^### 5\. Login' deploy/README.md`
    - References bootstrap.sh by exact path: `grep -qE 'bash deploy/scripts/bootstrap\.sh' deploy/README.md`
    - References update.sh, backup.sh, restore.sh by exact paths: `grep -q 'deploy/scripts/update.sh' deploy/README.md && grep -q 'deploy/scripts/backup.sh' deploy/README.md && grep -q 'deploy/scripts/restore.sh' deploy/README.md`
    - Cross-links to DOMAIN-SETUP.md, BACKUP-RESTORE.md, TROUBLESHOOTING.md as relative paths: `grep -qE '\./DOMAIN-SETUP\.md' deploy/README.md && grep -qE '\./BACKUP-RESTORE\.md' deploy/README.md && grep -qE '\./TROUBLESHOOTING\.md' deploy/README.md`
    - Has Day-2 Operations section: `grep -qE '^## Day-2 Operations' deploy/README.md`
    - Has create-admin admin-rotation snippet: `grep -qE 'bin/sms create-admin' deploy/README.md && grep -qE -- '--force' deploy/README.md`
    - Has Layout section: `grep -qE '^## Layout' deploy/README.md`
    - Layout table mentions every script (N1 — Phase 29 revision: simplified regex): `grep -qE 'deploy/scripts/init-secrets\.sh' deploy/README.md`
    - Has <10-minute proof section: `grep -qE '10-minute|10 minute' deploy/README.md`
    - References Phase 30 timing log: `grep -qE 'SMOKE-TEST-LOG\.md|Phase 30' deploy/README.md`
    - Mentions Deploy Folder Convention lock: `grep -qE 'Deploy Folder Convention' deploy/README.md`
  </acceptance_criteria>
  <done>`deploy/README.md` is overwritten with the 5-step quickstart + day-2 ops + Layout + Reference structure. All grep guards above pass. The file is at least 80 lines (vs the Phase 24 stub's ~16).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Author deploy/BACKUP-RESTORE.md (operator runbook for backup + restore)</name>
  <files>deploy/BACKUP-RESTORE.md</files>
  <read_first>
    - .planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-CONTEXT.md §D-27 (full BACKUP-RESTORE.md scope — cron, rclone, DR, retention, RTO, encryption)
    - .planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-CONTEXT.md §D-19 (what's in the archive vs what's NOT — operator must know)
    - deploy/scripts/backup.sh (Plan 29-04 — runbook references its CLI surface)
    - deploy/scripts/restore.sh (Plan 29-05 — runbook references its CLI surface, --yes flag, integrity verify)
    - deploy/README.md (cross-link target — the file you just wrote in Task 1)
  </read_first>
  <action>
Use the `Write` tool to create `deploy/BACKUP-RESTORE.md`. Structure:

- H1: `# Backup & Restore` followed by one paragraph positioning the doc (offline atomic backup model, single tar.gz artifact, byte-equivalent restore via `compose down -v` + pg_restore + mc mirror + caddy_data extract).

- H2 `## Quick Reference` — minimal copy-pasteable commands:
  - Backup: `bash deploy/scripts/backup.sh` (default `./backups/`)
  - Restore: `bash deploy/scripts/restore.sh ./backups/sms-backup-<UTC-ts>.tar.gz`
  - DR automation: `bash deploy/scripts/restore.sh <archive> --yes`
  - Custom backup target: `BACKUP_DIR=/mnt/backups bash deploy/scripts/backup.sh`

- H2 `## What's in the archive` — table from D-19 verbatim:
  ```markdown
  | Component                     | Included? | Reason                                                                |
  | postgres pg_dump (custom -Fc) | yes       | Schema + data; smaller + parallel restore                             |
  | MinIO buckets (avatars/recordings/snapshots) | yes | All persistent objects                                       |
  | caddy_data (TLS cert + ACME)  | yes       | Avoid LE rate-limit re-issue on restore                               |
  | redis_data                    | no        | Sessions/jobs replay; transient                                       |
  | hls_data                      | no        | Live segments self-delete via hls_dispose                             |
  | .env                          | no        | Operator stores in password manager; secrets MUST NOT mix with backup |
  | docker-compose.yml + Caddyfile | no       | Source-controlled; cd to deploy/ + git pull before restore            |
  ```
  Below the table, a one-paragraph note on what the operator MUST keep separately (.env in password manager, repo via git).

- H2 `## Cron auto-schedule` — example line for `crontab -e`:
  ```
  0 2 * * * cd /opt/sms-app && bash deploy/scripts/backup.sh > /var/log/sms-backup.log 2>&1
  ```
  One-paragraph note on path adjustment + log destination.

- H2 `## Offsite copy with rclone` — D-27 explicit:
  ```bash
  rclone copy ./backups/ remote:sms-backups/ --include "sms-backup-*.tar.gz"
  ```
  Plus a 2-3 sentence note on configuring an rclone remote (link to https://rclone.org/docs/) and recommendation to encrypt the remote bucket (S3 SSE, B2 SSE) since the archive itself is unencrypted in v1.3.

- H2 `## Encryption (v1.3 — operator-side)` — D-19 + D-27 note:
  - v1.3 backup.sh does NOT encrypt the archive.
  - Recommended wrap: `gpg --symmetric --cipher-algo AES256 ./backups/sms-backup-<ts>.tar.gz` or `age -p ./backups/sms-backup-<ts>.tar.gz`.
  - v1.4 may add `--encrypt` flag to backup.sh.

- H2 `## Disaster recovery walkthrough` — numbered list:
  1. Provision a fresh Linux VM matching the production OS.
  2. Install Docker Engine + Docker Compose v2.
  3. Sparse-checkout the deploy/ folder (`git clone --filter=blob:none --no-checkout <repo>` + `git sparse-checkout set deploy` + `git checkout`).
  4. Restore `deploy/.env` from password manager (chmod 600).
  5. Restore the archive: `bash deploy/scripts/restore.sh /path/to/sms-backup-<ts>.tar.gz --yes`.
  6. Confirm DNS A-record points at the new VM (Caddy will re-issue cert via the restored ACME state — usually no rate-limit hit).
  7. Verify: `curl -I https://DOMAIN/api/health` returns 200.

- H2 `## Retention recommendations` — D-27 explicit:
  - Daily: keep 7 most recent (1 week)
  - Weekly: keep 4 most recent (1 month)
  - Monthly: keep 3 most recent (3 months)
  - Total: ~14 archives at any time; size budget = 14 × max-archive-size.
  - Implement via host filesystem (`find ./backups -mtime +N -delete` in cron) or rclone retention policies.

- H2 `## Restore RTO target` — D-27 ~5-15 min based on archive size:
  - Archive size driven by MinIO content (recordings + snapshots).
  - 1 GB archive: ~5 min RTO (extract + pg_restore + mc mirror + caddy extract + compose up).
  - 10 GB archive: ~15 min RTO.
  - Phase 30 `deploy/SMOKE-TEST-LOG.md` records actual RTO from the v1.3 GA smoke test.

- H2 `## Troubleshooting` — link: "Common backup/restore failures (disk full, volume in use, integrity verify fail) → see [`TROUBLESHOOTING.md`](./TROUBLESHOOTING.md)."

**Critical correctness notes:**

1. **Every section header from D-27 must be present** — the acceptance criteria grep for them. Do not collapse sections.
2. **Use the verbatim filename pattern `sms-backup-<UTC-ts>.tar.gz`** matching backup.sh / ROADMAP SC #4.
3. **Include the cron example, the rclone command, and the 7-step DR walkthrough** — these are D-27 explicit deliverables.
4. **chmod +x is NOT applicable** to `.md` files; do not run `git update-index --chmod=+x` on a markdown file.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && [ "$(grep -c '^## ' deploy/BACKUP-RESTORE.md)" -ge 7 ] && grep -q 'rclone' deploy/BACKUP-RESTORE.md && grep -q 'crontab\|cron\|0 2 \* \* \*' deploy/BACKUP-RESTORE.md && grep -q 'pg_restore\|pg_dump' deploy/BACKUP-RESTORE.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f deploy/BACKUP-RESTORE.md`
    - At least 80 lines: `[ "$(wc -l < deploy/BACKUP-RESTORE.md)" -ge 80 ]`
    - H1 title set: `head -1 deploy/BACKUP-RESTORE.md | grep -qE '^# .*Backup'`
    - 7+ H2 sections (Quick Reference, What's in the archive, Cron, Offsite, Encryption, DR walkthrough, Retention, RTO, Troubleshooting): `[ "$(grep -c '^## ' deploy/BACKUP-RESTORE.md)" -ge 7 ]`
    - Has Quick Reference with bash commands: `grep -qE '^## Quick Reference' deploy/BACKUP-RESTORE.md && grep -qE 'bash deploy/scripts/backup\.sh' deploy/BACKUP-RESTORE.md && grep -qE 'bash deploy/scripts/restore\.sh' deploy/BACKUP-RESTORE.md`
    - Has --yes documentation: `grep -qE -- '--yes' deploy/BACKUP-RESTORE.md`
    - Has BACKUP_DIR override doc: `grep -qE 'BACKUP_DIR=' deploy/BACKUP-RESTORE.md`
    - Has cron schedule example with full crontab line: `grep -qE '0 2 \* \* \*' deploy/BACKUP-RESTORE.md`
    - Has rclone offsite copy: `grep -qE 'rclone copy' deploy/BACKUP-RESTORE.md`
    - Has encryption section with gpg or age: `grep -qE 'gpg|age' deploy/BACKUP-RESTORE.md`
    - Has DR walkthrough with at least 7 numbered steps: `[ "$(grep -cE '^[1-9]\. ' deploy/BACKUP-RESTORE.md)" -ge 7 ]`
    - Has retention recommendations table or list (7 daily / 4 weekly / 3 monthly): `grep -qE 'daily' deploy/BACKUP-RESTORE.md && grep -qE 'weekly' deploy/BACKUP-RESTORE.md && grep -qE 'monthly' deploy/BACKUP-RESTORE.md`
    - Has RTO target section: `grep -qE 'RTO' deploy/BACKUP-RESTORE.md`
    - Cross-links to TROUBLESHOOTING.md: `grep -qE 'TROUBLESHOOTING\.md' deploy/BACKUP-RESTORE.md`
    - Documents archive contents (table): `grep -qE 'pg_dump|caddy_data|MinIO' deploy/BACKUP-RESTORE.md`
    - Notes .env exclusion: `grep -qE '\.env' deploy/BACKUP-RESTORE.md && grep -qiE 'password manager|secrets' deploy/BACKUP-RESTORE.md`
  </acceptance_criteria>
  <done>`deploy/BACKUP-RESTORE.md` exists with ≥80 lines, ≥7 H2 sections, all D-27 sections present (Quick Ref, archive contents, cron, rclone, encryption, DR walkthrough ≥7 steps, retention, RTO, troubleshooting cross-link). All grep guards pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author deploy/TROUBLESHOOTING.md (symptom→diagnosis→fix runbook)</name>
  <files>deploy/TROUBLESHOOTING.md</files>
  <read_first>
    - .planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-CONTEXT.md §D-28 (6 mandatory entries verbatim + table format)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-CONTEXT.md §D-09 (staging-CA toggle for ACME debugging — referenced by "Caddy still issuing cert")
    - .planning/phases/28-github-actions-ci-cd-ghcr/28-CONTEXT.md §D-04 (GHCR_ORG matches `${{ github.repository_owner }}` — referenced by "compose pull denied")
    - deploy/scripts/{bootstrap,update,backup,restore}.sh (the 4 Phase 29 scripts the runbook references)
    - deploy/DOMAIN-SETUP.md (Phase 27 — link target for ACME troubleshooting)
  </read_first>
  <action>
Use the `Write` tool to create `deploy/TROUBLESHOOTING.md`. Structure:

- H1: `# Troubleshooting` + one paragraph positioning the doc (skim-friendly symptom→diagnosis→fix runbook for v1.3 deploy operations; for in-depth architecture see PROJECT.md and ROADMAP.md).

- H2 `## How to read this runbook` — one paragraph: scan the Symptom column for what you're seeing; Diagnosis explains why; Fix is the exact command to run. Each row references the relevant script or doc for deeper context.

- H2 `## Common failures` — markdown table:
  ```markdown
  | Symptom | Diagnosis | Fix |
  |---------|-----------|-----|
  | bootstrap.sh hangs at "Waiting for HTTPS endpoint", warns after 120s | Caddy still negotiating Let's Encrypt cert. Common causes: DNS A-record not yet propagated; port 80 blocked at firewall/cloud provider; rate-limit hit (5 fails/host/hour). | `docker compose -f deploy/docker-compose.yml --env-file deploy/.env logs caddy --tail 50`. Verify DNS: `dig +short A $DOMAIN`. Verify port 80: `curl -I http://$DOMAIN`. If rate-limited: set `ACME_CA=https://acme-staging-v02.api.letsencrypt.org/directory` in `.env`, `compose up -d caddy`, debug with staging cert; revert when ready. See [DOMAIN-SETUP.md](./DOMAIN-SETUP.md) §"Staging-CA toggle". |
  | sms-migrate exits 1; api stays at "Created"; bootstrap.sh dies with "Migrate failed" | Schema drift between image and DB; or DB credentials mismatch; or postgres unhealthy. | `docker compose -f deploy/docker-compose.yml --env-file deploy/.env logs sms-migrate`. If "P3009 migration failed": inspect `_prisma_migrations` table in postgres for the failing migration name, then check the image's `apps/api/src/prisma/migrations/` for that migration. If credentials: confirm DB_PASSWORD in .env matches the postgres volume (rotating DB_PASSWORD on an existing volume requires manual reset). |
  | `bin/sms create-admin` exits 1 with "User already exists" | Default error path; refuses to clobber existing user without `--force`. | Re-run with `--force`: `docker compose ... exec api bin/sms create-admin --email <e> --password <p> --force`. Existing user.id, member.id, role unchanged; only credential password rotates. |
  | `compose pull` exits with "denied: requested access to the resource is denied" | `GHCR_ORG` in .env does not match the github org that owns the GHCR images, or the images are private and you have no auth. | Verify GHCR_ORG matches `${{ github.repository_owner }}` from the build workflow (typically the repo owner). For private images: `gh auth login --scopes read:packages` then `gh auth token | docker login ghcr.io -u <gh-username> --password-stdin`. v1.3 default is public images; check Phase 28 build-images.yml. |
  | `backup.sh` fails with "No space left on device" | BACKUP_DIR (default ./backups) ran out of disk before the bundle was tarred. | Check free space: `df -h ./backups` (or your custom BACKUP_DIR). Move existing backups offsite via rclone (see [BACKUP-RESTORE.md](./BACKUP-RESTORE.md)). Set a different target: `BACKUP_DIR=/mnt/external bash deploy/scripts/backup.sh`. backup.sh's EXIT trap restarts api+web automatically. |
  | `restore.sh` fails with "Volume is in use" or `compose down -v` hangs | Some service still has a handle on a volume (e.g. api crashed but has zombie processes). | `docker compose -f deploy/docker-compose.yml --env-file deploy/.env down --timeout 30 -v` (force timeout). If still stuck: `docker compose ... kill && docker compose ... down -v`. As a last resort: `docker volume rm sms-platform_postgres_data sms-platform_minio_data sms-platform_caddy_data` then re-run restore.sh. |
  | Restore interrupted (Ctrl-C mid-restore) — partial state | restore.sh aborted between `compose down -v` and the post-extract `compose up -d`. Volumes were dropped; restore did not finish. Stack is offline with empty postgres / MinIO / caddy_data. | Re-run the same archive: `bash deploy/scripts/restore.sh ./backups/sms-backup-<UTC-ts>.tar.gz --yes`. restore.sh is idempotent — it re-extracts to a fresh `mktemp -d`, re-issues `compose down -v` (no-op on already-empty volumes), and replays pg_restore + mc mirror + caddy_data extract. If the archive itself is corrupt, the integrity verify gate exits 1 BEFORE touching anything; in that case, recover from the previous backup file. |
  ```

- H2 `## Less common` — additional rows (1-2 sentences each, no full table):
  - **Update.sh exits with "Invalid tag format"** → tag must match `vX.Y.Z`, `vX.Y.Z-prerelease`, or `latest`. `vX.Y` 2-part tags are rejected by design (D-13).
  - **bootstrap.sh exits with "Docker daemon not running"** → start Docker (`sudo systemctl start docker` on systemd; `colima start` on macOS dev).
  - **bootstrap.sh complains "ADMIN_PASSWORD is empty"** → re-run init-secrets: `bash deploy/scripts/init-secrets.sh` (idempotent; fills change-me-* + empty values).
  - **Health probe says 503 after update** → api may have failed boot; `docker compose ... logs api --tail 100`; if migrate ran clean but api crashes, check for env var drift (DATABASE_URL_MIGRATE missing, etc.).
  - **Cert renewal failed silently** → ACME_EMAIL was empty so Caddy registered anonymously and cannot email warnings. Set ACME_EMAIL in .env, `compose up -d caddy`.

- H2 `## Diagnostics`
  Quick-reference commands:
  - `docker compose -f deploy/docker-compose.yml --env-file deploy/.env ps`
  - `docker compose -f deploy/docker-compose.yml --env-file deploy/.env logs --tail 100 <service>`
  - `docker compose -f deploy/docker-compose.yml --env-file deploy/.env exec postgres psql -U sms -d sms_platform -c "SELECT * FROM \"_prisma_migrations\" ORDER BY finished_at DESC LIMIT 5;"`
  - `curl -I https://$DOMAIN/api/health`
  - `dig +short A $DOMAIN`

- H2 `## When to escalate` — one paragraph: if the runbook doesn't resolve it, gather: docker compose ps output, last 200 lines of logs from the failing service, output of `docker compose config --quiet` (validates compose syntax), and the contents of `.env` with secrets redacted. Open a GitHub issue with these attachments.

**Critical correctness notes:**

1. **All 6 D-28 entries MUST appear in the Common failures table.** Acceptance criteria grep for distinctive keywords from each row.
2. **Every Fix column entry must include the EXACT command** the operator runs — no `<placeholder>` ambiguity except for genuinely operator-supplied values.
3. **Cross-links use relative paths** (`./BACKUP-RESTORE.md`, `./DOMAIN-SETUP.md`).
4. **Table format is markdown table with 3 columns** (Symptom / Diagnosis / Fix) — D-28 explicit.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && [ "$(grep -c '^## ' deploy/TROUBLESHOOTING.md)" -ge 4 ] && grep -q 'create-admin' deploy/TROUBLESHOOTING.md && grep -q 'sms-migrate' deploy/TROUBLESHOOTING.md && grep -q 'compose pull' deploy/TROUBLESHOOTING.md && grep -q 'BACKUP_DIR\|disk\|space' deploy/TROUBLESHOOTING.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f deploy/TROUBLESHOOTING.md`
    - At least 60 lines: `[ "$(wc -l < deploy/TROUBLESHOOTING.md)" -ge 60 ]`
    - H1 title: `head -1 deploy/TROUBLESHOOTING.md | grep -qE '^# Troubleshooting'`
    - Has Common failures table: `grep -qE '^## Common failures' deploy/TROUBLESHOOTING.md`
    - Markdown table format with 3 columns (Symptom / Diagnosis / Fix): `grep -qE '^\| Symptom \| Diagnosis \| Fix \|' deploy/TROUBLESHOOTING.md`
    - All 6 mandatory D-28 symptoms covered:
      - ACME pending: `grep -qE 'Caddy still.*cert|HTTPS endpoint|Let.s Encrypt' deploy/TROUBLESHOOTING.md`
      - sms-migrate exit 1: `grep -qE 'sms-migrate exit|Migrate failed|sms-migrate exited' deploy/TROUBLESHOOTING.md`
      - create-admin user-exists: `grep -qE 'create-admin.*already exists|User already exists' deploy/TROUBLESHOOTING.md`
      - compose pull denied: `grep -qE 'compose pull.*denied|denied: requested access' deploy/TROUBLESHOOTING.md`
      - backup disk full: `grep -qE 'No space left|disk full|backup.*disk' deploy/TROUBLESHOOTING.md`
      - restore volume in use: `grep -qE 'Volume is in use|down -v.*hangs|volume.*in use' deploy/TROUBLESHOOTING.md`
    - Has --force fix for create-admin: `grep -qE -- '--force' deploy/TROUBLESHOOTING.md`
    - Has GHCR_ORG diagnostic for pull-denied: `grep -qE 'GHCR_ORG' deploy/TROUBLESHOOTING.md`
    - Has BACKUP_DIR override hint: `grep -qE 'BACKUP_DIR=' deploy/TROUBLESHOOTING.md`
    - Cross-links to BACKUP-RESTORE.md + DOMAIN-SETUP.md: `grep -qE '\./BACKUP-RESTORE\.md' deploy/TROUBLESHOOTING.md && grep -qE '\./DOMAIN-SETUP\.md' deploy/TROUBLESHOOTING.md`
    - Has Diagnostics section with concrete commands: `grep -qE '^## Diagnostics' deploy/TROUBLESHOOTING.md && grep -qE 'docker compose .* logs' deploy/TROUBLESHOOTING.md`
    - Has staging-CA mention (Phase 27 cross-ref): `grep -qE 'acme-staging-v02|staging.CA|ACME_CA' deploy/TROUBLESHOOTING.md`
  </acceptance_criteria>
  <done>`deploy/TROUBLESHOOTING.md` exists with ≥60 lines, has the 3-column Common failures table covering all 6 D-28 mandatory symptoms with concrete fixes, has Diagnostics section, cross-links to BACKUP-RESTORE.md and DOMAIN-SETUP.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → README.md | Public-readable repo file; no secrets, no credentials. Operator follows the steps to deploy from scratch. |
| operator → BACKUP-RESTORE.md / TROUBLESHOOTING.md | Same — no secrets in docs; commands reference operator-supplied values. |
| docs → external links | rclone docs (rclone.org), Better Auth docs, etc. — informational only, no auth flow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-30 | Information Disclosure | Documentation accidentally includes a sample secret that operators copy verbatim | mitigate | Acceptance criteria forbid copy-pasteable real secret material in any of the 3 docs. All `<placeholder>` values are clearly marked operator-supplied; secrets always come from .env or password manager. |
| T-29-31 | Tampering | Outdated docs cause operator to run wrong command (e.g. typo in script path) | mitigate | Cross-links use exact relative paths; commands match the script files in deploy/scripts/. Phase 30 smoke test (DEPLOY-25) follows README step-by-step on a fresh VM — the docs ARE the test plan. |
| T-29-32 | Information Disclosure | TROUBLESHOOTING.md leaks internal architecture details that aid attackers | accept | The doc references public concepts (Caddy, Let's Encrypt, postgres, MinIO, GHCR) that are visible from public Dockerfile/compose anyway. **Severity: none**. |
| T-29-33 | Denial of Service | Operator follows misleading TROUBLESHOOTING.md guidance and destroys data (e.g. `docker volume rm` without backup) | mitigate | The "last resort" `docker volume rm` step in TROUBLESHOOTING.md (restore-volume-in-use row) is qualified with "as a last resort" and follows BACKUP-RESTORE.md conventions. The runbook explicitly tells operators to back up first wherever destructive action is suggested. |
| T-29-34 | Information Disclosure | BACKUP-RESTORE.md DR walkthrough requires operator to restore .env from password manager — leaked secret if password manager breached | accept | Operator responsibility per D-19 + D-27. Docs explicitly recommend password manager (not plain text in repo) and encryption of archive bundles. Beyond plan scope. **Severity: medium**, accepted. |
</threat_model>

<verification>
**Static verification (the entire verify gate at plan-execution time):**
- `wc -l` on each file ≥ the documented minimum (README ≥100, BACKUP-RESTORE ≥80, TROUBLESHOOTING ≥60).
- All grep guards in each task's acceptance_criteria pass — particularly the cross-link checks (README → DOMAIN-SETUP / BACKUP-RESTORE / TROUBLESHOOTING; BACKUP-RESTORE → restore.sh + TROUBLESHOOTING; TROUBLESHOOTING → BACKUP-RESTORE + DOMAIN-SETUP) and the 6-row D-28 symptom coverage.
- Markdown lint optional but not gating: `markdownlint deploy/*.md` (if installed) — informational only.

**Live verification deferred to Phase 30:** DEPLOY-25 smoke test validates README step-by-step on a real VM; the docs ARE the acceptance criteria for the <10-minute claim.
</verification>

<success_criteria>
1. `deploy/README.md` overwrites the Phase 24 stub completely with: 5-step quickstart matching D-25 verbatim (Clone → Configure secrets → DNS → Bootstrap → Login), Day-2 Operations section (update / backup / restore / create-admin rotation), Layout table listing every file in deploy/, Reference section. All cross-links use relative paths.
2. `deploy/BACKUP-RESTORE.md` exists with ≥7 H2 sections covering D-27 in full: Quick Reference, archive contents table, Cron auto-schedule, Offsite copy with rclone, Encryption note (gpg/age), Disaster recovery walkthrough (≥7 numbered steps), Retention recommendations (7 daily / 4 weekly / 3 monthly), Restore RTO target.
3. `deploy/TROUBLESHOOTING.md` exists with the 3-column Symptom/Diagnosis/Fix table covering ALL 6 mandatory D-28 entries + a "Less common" follow-up section + Diagnostics command reference + Escalation note.
4. README, BACKUP-RESTORE, and TROUBLESHOOTING form a closed cross-link graph: README points to all three peers; BACKUP-RESTORE points to TROUBLESHOOTING; TROUBLESHOOTING points to BACKUP-RESTORE + DOMAIN-SETUP. No broken relative paths.
5. None of the docs include real secret material; all credential references are placeholders or operator-supplied.
</success_criteria>

<output>
After completion, create `.planning/phases/29-operator-ux-bootstrap-update-backup-restore-super-admin-cli/29-06-SUMMARY.md` documenting:
- Final line counts for the 3 docs (README / BACKUP-RESTORE / TROUBLESHOOTING)
- Confirmation that all 6 D-28 symptoms are present in TROUBLESHOOTING.md
- Confirmation that all D-25 5-step quickstart sub-sections are present in README.md
- Confirmation that all D-27 sections are present in BACKUP-RESTORE.md
- Cross-link integrity check (README ↔ BACKUP-RESTORE ↔ TROUBLESHOOTING)
</output>
