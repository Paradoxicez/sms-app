---
phase: 25-multi-stage-dockerfiles-image-hardening
plan: 03
subsystem: web-build-config
tags: [next-config, monorepo, standalone-output, file-tracing, deploy-prerequisite]
requirements:
  - DEPLOY-02
dependencies:
  requires: []
  provides:
    - "Monorepo-aware Next.js standalone output (apps/web/.next/standalone/apps/web/server.js)"
    - "Plan 05 web Dockerfile precondition: COPY .next/standalone now produces complete tree"
  affects:
    - "apps/web build artifact layout (server.js path shifts from flat → nested apps/web/)"
tech-stack:
  added: []
  patterns:
    - "ESM __dirname polyfill via fileURLToPath(import.meta.url)"
    - "outputFileTracingRoot pinned at monorepo root for pnpm workspace resolution"
key-files:
  created: []
  modified:
    - apps/web/next.config.ts
decisions:
  - "Surgical addition only — preserved output:'standalone', skipTrailingSlashRedirect:true, and full rewrites() chain byte-identical (Phase 25 D-18)"
  - "Used path.join(__dirname, '../../') (two levels up from apps/web/) to point at repo root vs hardcoded path"
  - "No package.json or build script changes — runtime semantics unchanged for dev workflow"
metrics:
  duration_minutes: 2
  tasks_completed: 2
  files_changed: 1
  commits: 1
  completed_at: "2026-04-27T16:08:47Z"
---

# Phase 25 Plan 03: Web next.config.ts outputFileTracingRoot Summary

**Phase 25 Plan 03 — adds `outputFileTracingRoot` to `apps/web/next.config.ts` so Next.js 15 standalone build correctly traces pnpm-workspace symlinks into `.next/standalone/`, unblocking Plan 05's web Dockerfile from shipping a complete runtime tree.**

## What Shipped

- **`apps/web/next.config.ts`** (+12 lines, -0 lines): added `path` + `fileURLToPath` ESM imports, ESM `__dirname` polyfill, and `outputFileTracingRoot: path.join(__dirname, '../../')` pointing at the monorepo root. All existing keys (`output: 'standalone'`, `skipTrailingSlashRedirect: true`, the full Socket.IO + API `rewrites()` chain) preserved byte-identical.
- **No new files created.** No package.json or build-script modifications. Pure config-key addition.
- **Build artifact contract change**: `apps/web/.next/standalone/server.js` (flat) → `apps/web/.next/standalone/apps/web/server.js` (monorepo-aware). Plan 05's Dockerfile `CMD` will reference the new path.

## Decisions Made

| Decision | Rationale | Outcome |
|----------|-----------|---------|
| Surgical addition only | Phase 25 D-18 contract: do NOT touch existing keys, comment blocks, or rewrite chain | ✓ Diff is +12/-0 — every existing line byte-identical |
| `path.join(__dirname, '../../')` over hardcoded path | Two levels up from `apps/web/next.config.ts` resolves to repo root regardless of clone location | ✓ Works on any developer machine + CI runner without env-specific config |
| ESM `__dirname` polyfill via `fileURLToPath(import.meta.url)` | next.config.ts is loaded as ES module (verified: file uses `import type` + `export default`); `__dirname` is CommonJS-only | ✓ Standard ESM pattern, no transpilation magic |
| Skip wiring `--filter @sms-platform/web build` into automated CI gate | Phase 28 owns CI workflow; this plan is config-only | ✓ Manual local build verification sufficient per plan acceptance criteria |

## Verification Performed

| Check | Command | Result |
|-------|---------|--------|
| Config additions present | `grep -q "outputFileTracingRoot: path.join(__dirname, '../../'" apps/web/next.config.ts` | PASS |
| ESM imports present | `grep -q "import path from 'node:path';" apps/web/next.config.ts` + `grep -q "import { fileURLToPath } from 'node:url';" apps/web/next.config.ts` | PASS |
| Existing keys preserved | grep for `output: 'standalone'`, `skipTrailingSlashRedirect: true`, `'/socket.io/'`, `'/socket.io/:path+'`, `'/api/:path*'` | ALL PASS |
| Production build succeeds | `pnpm --filter @sms-platform/web build` (log: `/tmp/web-build-25-03.log`) | EXIT 0 |
| Standalone dir exists | `test -d apps/web/.next/standalone` | PASS |
| Monorepo-aware standalone layout | `test -f apps/web/.next/standalone/apps/web/server.js` | PASS — confirms outputFileTracingRoot captured workspace |
| Dev workflow regression check | `bash scripts/dev-smoke.sh` (log: `/tmp/dev-smoke-25-03.log`) | EXIT 0 (web :3000 → 200, api :3003 health → 404 acceptable; HealthController arrives via Plan 01) |

## Key Files

**Modified (1):**
- `apps/web/next.config.ts` — +12 lines (2 imports, 4-line `__dirname` polyfill block including comment, 5-line `outputFileTracingRoot` block including comment, 1 blank line). 0 deletions.

**Generated by build (build artifact, not committed):**
- `apps/web/.next/standalone/apps/web/server.js` — proves tracing root captured monorepo correctly.

## Deviations from Plan

None — plan executed exactly as written.

The plan's `<action>` block specified the exact final file contents; the Write produced that exact content. Both tasks' `<verify>` blocks passed on first run. No Rule 1/2/3 auto-fixes triggered. No checkpoints (this is an autonomous plan).

## Threat Surface Notes

Plan's threat register entries:

- **T-25-06 (Tampering / DoS — Standalone build missing files):** mitigated. The standalone output now contains `apps/web/server.js` at the workspace-aware path. Plan 06 D-19 step 8 (`docker run sms-web:phase25-test ... curl /api/health`) becomes achievable.
- **T-25-07 (Information Disclosure — Tracing root pulls in extra files):** accepted, no widening. Verified the build context is filtered by Phase 24 root `.dockerignore` (which excludes `.env*`, `.git`, `.planning/`, etc.). Per-app `apps/web/.dockerignore` will land in Plan 05 to add additional belt-and-braces filtering.

**Observed but out of scope:** `apps/web/.next/standalone/apps/web/.env` (611B) is bundled into the standalone output by Next.js's tracer. This is benign for production:
1. Phase 24 root `.dockerignore` excludes `.env*` from the Docker build context entirely → the file never reaches Plan 05's builder stage.
2. Plan 05 will add `apps/web/.dockerignore` as a second layer of defense.
3. The local `.next/standalone/` directory is a build artifact, not committed.

No new threat surface introduced by this plan beyond what's already in the threat register.

## Known Stubs

None. The change is purely additive config; no UI components or data flows touched.

## Self-Check: PASSED

Verified before writing this section:

- File `apps/web/next.config.ts` exists and contains all required strings — FOUND
- Commit `212bc22` (`feat(25-03): add outputFileTracingRoot to next.config.ts`) exists in git log — FOUND
- Build artifact `apps/web/.next/standalone/apps/web/server.js` produced by `pnpm build` — FOUND

```
$ git log --oneline -1
212bc22 feat(25-03): add outputFileTracingRoot to next.config.ts

$ test -f apps/web/next.config.ts && echo FOUND
FOUND
```

## Hand-off to Plan 05 (Web Dockerfile)

When Plan 05 lands `apps/web/Dockerfile`:

1. **Builder stage** must run `pnpm --filter @sms-platform/web build` — produces `.next/standalone/` correctly because `outputFileTracingRoot` is now set.
2. **Runtime stage** must `COPY --from=builder /app/apps/web/.next/standalone ./` — the `apps/web/server.js` path inside is monorepo-aware (Plan 03 D-18 outcome).
3. **CMD** should be `["node", "apps/web/server.js"]` — NOT `["node", "server.js"]` flat. The path reflects `outputFileTracingRoot=repo-root` resolution.
4. **No package-level changes needed in Plan 05** — Plan 03 already locked the build behavior contract.
