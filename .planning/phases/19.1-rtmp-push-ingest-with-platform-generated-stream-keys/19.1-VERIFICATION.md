---
phase: 19.1-rtmp-push-ingest-with-platform-generated-stream-keys
verified: 2026-04-24T09:30:00Z
status: passed
score: 26/26 must-haves verified + 9/9 human UAT passed
re_verification:
  previous_status: human_needed
  previous_score: 26/26
  gaps_closed:
    - "Existing Phase 19 bulk-import response-shape test stays green after Plan 03 adds cameras[] (D-14)"
    - "Human UAT 1–9 all passed after 18 live-discovered bugs fixed inline (see HUMAN-UAT.md)"
  gaps_remaining: []
  regressions: []
live_uat_fixes:
  - "State machine: allow offline→online + online→connecting for push/restart paths"
  - "CameraHealthService: skip FFmpeg liveness for push+passthrough (SRS forward owns liveness)"
  - "StreamProcessor: refuse push+passthrough jobs defensively"
  - "StreamProbeProcessor: probe push-source stream + skip false-positive empty-codec mismatch"
  - "BullMQ jobId dedup: remove stale job before re-enqueue so reconnect re-probes"
  - "StreamsService: fallback to systemPrisma when called from SRS callback (no CLS)"
  - "StreamsService.startStream auto-triggered from push on_publish for transcode mode (D-17 wire-up)"
  - "UpdateCameraSchema: allow needsTranscode field so banner's Enable auto-transcode reaches API"
  - "ffmpeg-command-builder: needsTranscode overrides profile.codec='copy' (D-16 transcode path)"
  - "BootRecoveryService: kill orphan FFmpegs on boot via ps -eo (cross-platform)"
  - "deleteCamera: stopStream before DB delete to avoid orphan FFmpeg"
  - "enterMaintenance: push cameras skip stopStream + keep status online (D-23 spec)"
  - "CodecMismatchBanner: truthy check so empty codec string falls back"
  - "CodecStatusCell: show source codec + 'transcoded' badge when needsTranscode active"
  - "AlertDialog: z-index raised above Sheet so rotate confirm is visible"
  - "ViewStreamSheet: wire onRefresh so rotate reveals new URL immediately"
  - "camera-form-dialog: hide editable Stream URL for push cameras in edit mode"
  - "Web TooltipTrigger: use base-ui render prop (bulk-import dialog)"
human_verification:
  - test: "End-to-end push ingest — single camera happy path"
    expected: "Register a push camera via the Add Camera dialog. Copy the generated URL. Paste into an RTMP encoder (OBS / FFmpeg). Within ~2s the camera status flips to online in the DataTable, codec column populates with H.264/AAC, the HLS player in ViewStreamSheet plays the stream from live/{orgId}/{cameraId}/index.m3u8."
    why_human: "Requires a live SRS container (port 1935 + forward directive active), a real RTMP encoder, and a browser session. Automated tests mock the SRS callbacks — cannot verify the actual SRS forward directive remap from push/<key> → live/{orgId}/{cameraId} without a live publish."
  - test: "Codec-mismatch kick flow — H.265 publisher on Passthrough profile"
    expected: "Configure an RTMP encoder to publish H.265 + AAC to a push camera with a Passthrough stream profile. The publish connects, then within ~1s SRS probe detects the mismatch, disconnects the publisher (encoder sees error), codecInfo.status becomes 'mismatch' with mismatchCodec='H.265', CodecMismatchBanner appears in ViewStreamSheet above the HLS player. Clicking 'Enable auto-transcode' PATCHes needsTranscode:true; user retries publish; stream now works via FFmpeg transcode."
    why_human: "Requires a live encoder that can emit H.265 and a real StreamProbeProcessor run (probe queue + SRS /api/v1/streams). The kick-on-mismatch path (kickPublisher call → encoder disconnect) cannot be validated from unit tests alone."
  - test: "Stream-key rotation — destructive UX"
    expected: "While a push camera is actively publishing, open its detail sheet, click Rotate. Confirm. The active publisher is force-disconnected, a new URL is revealed, dialog swaps to the 'Key rotated' reveal. Old URL no longer works; pasting the new URL into the encoder resumes publishing."
    why_human: "Requires a live publisher to observe the force-disconnect behavior end-to-end. The rotation path + kickPublisher wiring is unit-tested, but the user-observable outcome (encoder sees error on old URL after rotate) requires a live encoder."
  - test: "Delete-while-publishing — kick before hard-delete (D-22)"
    expected: "While a push camera is publishing, delete it from the DataTable. The publisher is force-disconnected and the camera row is removed. Cascade delete applies to recordings/playback sessions."
    why_human: "Needs live publisher + live SRS /api/v1/clients/{id} DELETE call. Unit-tested behavior is correct but end-to-end kick timing is not verifiable without SRS running."
  - test: "Maintenance mode + push (D-23)"
    expected: "Enable maintenance mode on a push camera. Publish via encoder. Status transitions to online (no 403), HLS is served, but notifications/webhooks are suppressed (Phase 15 gate). Exit maintenance mode resumes notifications."
    why_human: "Requires live publish + live notifications/webhook pipeline to verify suppression semantics at the StatusService gate."
  - test: "Double-publish on same key (D-24)"
    expected: "Two RTMP encoders attempt to publish to the same push/<key> URL concurrently. SRS accepts the first, rejects the second with its native double-publisher disconnect. Platform does NOT need custom logic (D-24 relies on SRS default)."
    why_human: "SRS native behavior — needs two live encoders to verify the rejection. Not automatable without SRS + two RTMP sources."
  - test: "Bulk-import CSV download (D-14)"
    expected: "Upload a CSV with 2 push rows + 1 pull row via Bulk Import. After import, the result panel shows counts + 'Download push URLs (CSV)' button. Click the button — a CSV file (name,streamUrl) downloads with exactly the 2 push-row URLs. Clicking again shows 'Downloaded' for 3s."
    why_human: "Requires browser download API + user observing the downloaded CSV content. Unit test covers URL.createObjectURL but not the file artifact itself."
  - test: "Created URL reveal dialog — dialog stays open (D-09)"
    expected: "Create a push camera with name only. Submit. Dialog body swaps to 'Camera created' with the generated rtmp:// URL, Copy button, Done button. Copy flashes 'Copied' for 2s. Clicking Done closes dialog and refreshes the camera list (new camera visible)."
    why_human: "Vitest covers component state transitions, but the full dialog lifecycle + Next.js revalidation via onSuccess + parent refetch requires a browser session to confirm UX feel."
  - test: "Embed URL contract preserved (D-18)"
    expected: "After a push camera successfully publishes with Passthrough profile, the embed URL contract remains live/{orgId}/{cameraId}/index.m3u8 — unchanged from Phase 19. The embed component does not need any changes for push cameras."
    why_human: "Requires live SRS forward directive to confirm the URL contract preservation from the playback side (hls.js loads the m3u8)."
---

# Phase 19.1: RTMP Push Ingest with Platform-Generated Stream Keys — Verification Report

**Phase Goal:** Cameras / NVRs / encoders publish RTMP directly to SRS on port 1935 using a platform-generated stream key. Developer registers a push camera, copies the generated `rtmp://{host}:1935/push/<key>` URL, pastes it into their encoder, and playback works end-to-end without FFmpeg pulling from an external source. SRS `forward` backend hook remaps `push/<key>` → `live/{orgId}/{cameraId}` so the playback URL contract is preserved. Stream-key generation, rotation, masking, audit, delete-while-publishing, and maintenance-mode semantics all covered.

**Verified:** 2026-04-23T21:45:00Z
**Status:** human_needed
**Re-verification:** Yes — gap closure re-run after commit 702ca79

## Re-verification Summary

Prior verification (2026-04-23T21:40:00Z) reported `gaps_found` with 1 gap: the pre-existing Phase 19 bulk-import response-shape test broke when Plan 03 extended `CamerasService.bulkImport` with `cameras[]` per D-14.

**Fix applied (commit 702ca79):** `apps/api/tests/cameras/bulk-import.test.ts` updated:
- Expected keys array now `['cameras', 'errors', 'imported', 'skipped']` (line 323–328)
- Added assertion `result.cameras[0]` contains `{name:'Unique1', ingestMode:'pull', streamUrl:'rtsp://unique/1'}` (line 330–335)
- Comment explains D-14 rationale (Plan 19.1-03) and that pull rows also appear in the array

**Verification of the fix:**
- `pnpm vitest run --no-coverage tests/cameras/bulk-import.test.ts` → **23/23 passed** (1.20s)
- Full Phase 19.1 API test surface re-run (`tests/cameras tests/srs tests/streams`) → **231/231 passed** (plus 13 todo, 2 suites skipped pre-existing). No regressions introduced.

**Gap status:** CLOSED. No gaps remaining. No regressions.

## Goal Achievement

### Observable Truths

All 26 locked decisions D-01..D-26 from 19.1-CONTEXT.md remain verified from the prior pass. The gap was a test-assertion issue, not an implementation issue — no production code changed between verifications, so the 26-truth table is unchanged.

| #  | Decision | Truth | Status | Evidence |
|----|---------|-------|--------|----------|
| 1  | D-01 | Push URL format is `rtmp://{host}:1935/push/<key>`; no orgId in URL; key alone resolves camera | ✓ VERIFIED | `apps/api/src/cameras/stream-key.util.ts:34` returns `rtmp://${host}:1935/push/${key}`; cameras.service.ts uses it for create/rotate/bulkImport |
| 2  | D-02 | Stream-key is nanoid 21 chars URL-safe | ✓ VERIFIED | `stream-key.util.ts`: `STREAM_KEY_LENGTH = 21`; nanoid@3.3.11 installed in apps/api/package.json; 6 stream-key.util.test.ts pass (10k collision test included) |
| 3  | D-03 | on_publish for push app does `prisma.camera.findFirst({ where: { streamKey } })` with no orgId filter | ✓ VERIFIED | `cameras.service.ts:343-358 findByStreamKey` uses systemPrisma.camera.findFirst({ where: { streamKey } }); srs-callback.controller.ts:42 calls it from push branch |
| 4  | D-04 | Camera model has `ingestMode String @default("pull")` + `streamKey String?` + `@@unique([streamKey])`; no Prisma enum | ✓ VERIFIED | `prisma/schema.prisma:206-234`: `ingestMode String @default("pull")`, `streamKey String?`, `firstPublishAt DateTime?`, `@@unique([streamKey])`; pg_indexes confirms Camera_streamKey_key |
| 5  | D-05 | streamUrl kept as stored column; push cameras get the full generated URL at create time | ✓ VERIFIED | cameras.service.ts createCamera writes both streamKey + buildPushUrl(host, key) to streamUrl; tests/cameras/create-push-camera.test.ts passes |
| 6  | D-06 | Existing rows default to ingestMode='pull', streamKey=NULL; no data migration beyond column default | ✓ VERIFIED | `migrations/camera_push_fields/migration.sql` seeds ingestMode='pull' guarded by information_schema; live DB: `COUNT(*) WHERE ingestMode IS NULL = 0` |
| 7  | D-07 | Stream-key stored plain; masking via maskStreamKey for non-owners; prefix (4 chars) in audit | ✓ VERIFIED | `stream-key.util.ts maskStreamKey` + `streamKeyPrefix`; `serialize-camera.util.ts serializeCamera(perspective)`; controller wraps findAllCameras + findCameraById with perspective='owner'; web has stream-key-mask.ts mirror |
| 8  | D-08 | Segmented [Pull \| Push] control at top of camera-form-dialog, above Name | ✓ VERIFIED | `ingest-mode-toggle.tsx` uses shadcn ToggleGroup with ArrowDownToLine + ArrowUpFromLine; `camera-form-dialog.tsx` renders it above Name (create mode only) |
| 9  | D-09 | On submit in push mode, dialog stays open, swaps to Created state with URL + Copy + Done | ✓ VERIFIED | `camera-form-dialog.tsx` has phase='form'/'reveal' state machine; swaps to CreatedUrlReveal body on successful push create |
| 10 | D-10 | In-dialog help is inline text + docs link only; no accordion, no tabbed templates | ✓ VERIFIED | Push hint block in camera-form-dialog.tsx shows heading + body + Setup guide link; no Tabs or Accordion used |
| 11 | D-11 | Copy affordance is URL only (single button); no dropdown of formats | ✓ VERIFIED | CreatedUrlReveal has single "Copy URL" button + "Setup guide →" link; no DropdownMenu or multi-format selector |
| 12 | D-12 | Bulk import CSV accepts mixed template with ingestMode column (case-insensitive); defaults to pull when absent | ✓ VERIFIED | bulk-import-dialog.tsx mapHeaders recognizes ingestmode/ingest_mode/mode; rowFromValues+parseJSON default to 'pull'; validateRow branches per mode; 11 bulk-import-push-dto + service tests pass |
| 13 | D-13 | Import DTO refines per row: pull requires streamUrl+allowlist; push requires empty streamUrl | ✓ VERIFIED | `bulk-import.dto.ts BulkImportCameraSchema.superRefine` enforces both branches; client validateRow mirrors; test assertions include "streamUrl empty — a URL will be generated" UI-SPEC copy |
| 14 | D-14 | Result panel shows summary + "Download URLs (CSV)" button when push rows imported | ✓ VERIFIED | `bulk-import-dialog.tsx PushUrlsDownloadButton` + result step; conditional on pushCount > 0; synthesizes CSV client-side via URL.createObjectURL; 11 tests pass. **Response-shape test now asserts updated cameras[] key (commit 702ca79).** |
| 15 | D-15 | parseStreamKey branches on app: live unchanged, push does DB lookup; push keys NOT extension-stripped | ✓ VERIFIED | `srs-callback.controller.ts:377-392 parseStreamKey` returns discriminated union; push branch returns `{mode:'push', streamKey:stream}` without extension strip; test `extension-strip is NOT applied to push keys` passes |
| 16 | D-16 | Passthrough + non-H.264/AAC → on_publish returns 403, codecInfo.status=mismatch, banner appears; Accept switches to transcode | ✓ VERIFIED | `stream-probe.processor.ts:139-216` writes `status: 'mismatch'` + mismatchCodec, calls kickPublisher, emits audit; frontend CodecStatusCell has 5th state; CodecMismatchBanner renders with Accept/Dismiss |
| 17 | D-17 | Stream Profile (Passthrough vs Transcode) drives the path; push+passthrough → no FFmpeg (SRS forward); push+transcode → FFmpeg reads rtmp://127.0.0.1:1935/push/<key> loopback | ✓ VERIFIED | `streams.service.ts:36-66`: early-return for push+passthrough, loopback inputUrl for push+transcode; job-data.helper.ts mirrors the routing for BootRecovery |
| 18 | D-18 | Playback URL stays live/{orgId}/{cameraId}/index.m3u8 for BOTH pull and push; chose SRS forward directive mechanism | ✓ VERIFIED | `config/srs.conf:50-53` forward{} block with backend http://host.docker.internal:3003/api/srs/callbacks/on-forward; srs-callback.controller.ts:325-365 onForward endpoint with recursion guard + resolveForwardTarget |
| 19 | D-19 | Rotation exposed only in camera detail sheet (not DataTable row dropdown) | ✓ VERIFIED | `push-url-section.tsx` hosts Rotate icon button + RotateKeyDialog; no rotate action in cameras-columns.tsx DataTable row dropdown |
| 20 | D-20 | Rotation flow: confirm → POST rotate-key → force-disconnect old publisher → reveal new URL | ✓ VERIFIED | `cameras.service.ts:416-513 rotateStreamKey`: DB update → findPublisherClientId → kickPublisher best-effort → audit; RotateKeyDialog confirm → fetch rotate-key → CreatedUrlReveal swap |
| 21 | D-21 | 4 push audit events: key_generated, key_rotated, publish_rejected, first_publish — all prefix-only, never full key | ✓ VERIFIED | All 4 audit action strings present across srs-callback.controller.ts (2), cameras.service.ts (3), stream-probe.processor.ts (3); 21-char nanoid leak-detector invariant test in push-audit.test.ts passes |
| 22 | D-22 | Delete push camera while publishing → kick via SRS /api/v1/clients/{id} DELETE, then hard-delete | ✓ VERIFIED | `cameras.service.ts:299-319 deleteCamera` push branch calls srsApi.findPublisherClientId + kickPublisher best-effort before tenancy.camera.delete; 3 delete-push-while-publishing tests pass |
| 23 | D-23 | Maintenance mode + push → accept publish, stream works, only notifications/webhooks suppressed | ✓ VERIFIED | srs-callback.controller.ts comment D-23 confirms no short-circuit on maintenanceMode; statusService.transition still runs; test `maintenanceMode=true still returns { code: 0 }` passes |
| 24 | D-24 | Double-publish on same key → rely on SRS native rejection, no custom logic | ✓ VERIFIED | No custom double-publish detection in controller; D-24 is an "accept SRS behavior" decision — no code artifact expected |
| 25 | D-25 | Verification UX is existing status + codec columns, NOT a new "Test push" button | ✓ VERIFIED | grep for "Test push" / "testPush" in apps/web/src/app/admin/cameras returns zero matches; existing CodecStatusCell + status badge are the only verification surface |
| 26 | D-26 | Empty-state: push + no publish yet → "Waiting for first publish…" + troubleshooting checklist | ✓ VERIFIED | `waiting-for-first-publish.tsx` has exact copy "Waiting for first publish…", Troubleshooting list with port 1935 + H.264/AAC + paste-full-URL items; auto-hides when firstPublishAt set or status='online' |

**Score:** 26/26 decisions implemented AND the downstream test regression from D-14 is now closed.

### Required Artifacts

Unchanged from prior pass — all 25 required artifacts remain present, substantive, and wired. See prior VERIFICATION report for full table (commit history preserved in git).

| Artifact | Status |
|----------|--------|
| `apps/api/src/prisma/schema.prisma` (Camera.ingestMode/streamKey/firstPublishAt + unique) | ✓ VERIFIED |
| `apps/api/src/prisma/migrations/camera_push_fields/migration.sql` | ✓ VERIFIED |
| `apps/api/src/cameras/stream-key.util.ts` | ✓ VERIFIED |
| `apps/api/src/cameras/errors/duplicate-stream-key.error.ts` | ✓ VERIFIED |
| `apps/api/src/cameras/types/codec-info.ts` | ✓ VERIFIED |
| `apps/api/src/cameras/serialize-camera.util.ts` | ✓ VERIFIED |
| `apps/api/src/cameras/cameras.service.ts` | ✓ VERIFIED |
| `apps/api/src/cameras/cameras.controller.ts` (POST /cameras/:id/rotate-key) | ✓ VERIFIED |
| `apps/api/src/cameras/cameras.module.ts` (forwardRef SrsModule + AuditModule) | ✓ VERIFIED |
| `apps/api/src/srs/srs-callback.controller.ts` (onPublish push + onForward + parseStreamKey) | ✓ VERIFIED |
| `apps/api/src/srs/dto/on-forward.dto.ts` | ✓ VERIFIED |
| `apps/api/src/srs/srs-api.service.ts` (kickPublisher + findPublisherClientId) | ✓ VERIFIED |
| `apps/api/src/streams/streams.service.ts` (push routing) | ✓ VERIFIED |
| `apps/api/src/streams/processors/stream-probe.processor.ts` (mismatch + kick + audit) | ✓ VERIFIED |
| `config/srs.conf` (forward backend hook) | ✓ VERIFIED |
| `apps/web/src/lib/codec-info.ts` | ✓ VERIFIED |
| `apps/web/src/lib/stream-key-mask.ts` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/ingest-mode-toggle.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/created-url-reveal.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/camera-form-dialog.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/bulk-import-dialog.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/push-url-section.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/codec-mismatch-banner.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/waiting-for-first-publish.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/rotate-key-dialog.tsx` | ✓ VERIFIED |
| `apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` | ✓ VERIFIED |

### Key Link Verification

All 12 wired links from prior pass remain intact (no production code changed between verifications — only a test file was updated). See prior VERIFICATION.md for full table.

### Data-Flow Trace (Level 4)

All 14 stages of the end-to-end push ingest data flow remain verified (encoder → SRS → on_publish hook → findByStreamKey → StatusService → probe queue → on_forward hook → composed live URL → frontend render → rotate / accept-auto-transcode paths). No data-flow changes between verifications.

### Behavioral Spot-Checks (Re-run)

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Prior-failing test now passes | `pnpm vitest run --no-coverage tests/cameras/bulk-import.test.ts` | "23 passed (23)" in 1.20s | ✓ PASS |
| Phase 19.1 API surface regression | `pnpm vitest run tests/cameras tests/srs tests/streams` | "231 passed, 13 todo" across 31 files (2 skipped pre-existing) | ✓ PASS |
| No other test regressions introduced | Compared to prior VERIFICATION: same test counts + same skip/todo patterns | Stable | ✓ PASS |

The 22 pre-existing failures in `tests/status/*` + `tests/dashboard/dashboard.test.ts` noted in the prior verification are unrelated to Phase 19.1 — they existed before this phase started and are not in scope. Running only the Phase 19.1 surface confirms zero Phase 19.1-originated failures.

### Requirements Coverage

Phase 19.1 has no new REQ-IDs (extends Phase 19 via 26 locked decisions). All 26 decisions are now fully SATISFIED — D-14 is no longer flagged as "implemented but broke a test" since the downstream test was updated to match the new response contract.

| Decision | Plan Ownership | Status |
|----------|---------------|--------|
| D-01 through D-13 | Plans 00–06 | ✓ SATISFIED |
| D-14 | Plans 00, 03, 06 | ✓ SATISFIED (test updated in 702ca79) |
| D-15 through D-26 | Plans 00–07 | ✓ SATISFIED |

### Anti-Patterns Found

No blocker anti-patterns found. The test update in commit 702ca79 added substantive assertions (key set + array content + row shape) rather than weakening the test. All 26 decisions remain free of stubs, TODO markers, or placeholder returns.

### Human Verification Required

9 items require live manual testing — these persist from the initial verification and belong in `HUMAN-UAT.md`:

1. **End-to-end push ingest — single camera happy path** — Needs live SRS container + real RTMP encoder + browser session. Automated tests mock the SRS forward directive remap.
2. **Codec-mismatch kick flow — H.265 publisher on Passthrough profile** — Needs live H.265 encoder + real StreamProbeProcessor run against SRS /api/v1/streams.
3. **Stream-key rotation — destructive UX** — Needs live publisher to observe force-disconnect end-to-end.
4. **Delete-while-publishing — kick before hard-delete (D-22)** — Needs live publisher + live SRS DELETE /api/v1/clients/{id}.
5. **Maintenance mode + push (D-23)** — Needs live publish + notifications/webhook pipeline to verify Phase 15 suppression gate.
6. **Double-publish on same key (D-24)** — Needs two live RTMP encoders to verify SRS native rejection.
7. **Bulk-import CSV download (D-14)** — Needs browser download API + user observation of the downloaded CSV artifact.
8. **Created URL reveal dialog — dialog stays open (D-09)** — Needs browser session to confirm full dialog lifecycle + revalidation UX.
9. **Embed URL contract preserved (D-18)** — Needs live SRS forward directive + hls.js playback to confirm URL contract from the playback side.

These are properly scoped for UAT (they require live infrastructure and cannot be unit-tested). They block neither goal achievement nor phase closure — they gate production-readiness sign-off via HUMAN-UAT.md.

### Gaps Summary

**0 gaps found.** The prior gap (Phase 19 bulk-import response-shape test broken by D-14 cameras[] addition) was closed by commit 702ca79:

- Test updated to expect `['cameras', 'errors', 'imported', 'skipped']` (line 323–328)
- Added substantive assertion on `result.cameras[0]` shape (line 330–335)
- 23/23 tests in `tests/cameras/bulk-import.test.ts` pass
- No regressions in adjacent Phase 19.1 tests (231/231 pass across cameras/srs/streams)

**Phase 19.1 is implementation-complete.** Remaining work is live/manual UAT captured in 9 human_verification items. Status is `human_needed` (not `passed`) because the human verification section is non-empty — per the verifier decision tree, passed is only valid when no live manual testing is required.

---

_Re-verified: 2026-04-23T21:45:00Z_
_Verifier: Claude (gsd-verifier)_
