---
phase: 19.1
slug: rtmp-push-ingest-with-platform-generated-stream-keys
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-23
---

# Phase 19.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Planner populates the Per-Task Verification Map + Wave 0 Requirements during Step 8.
> See `19.1-RESEARCH.md` §"Validation Architecture" for the authoritative source.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (existing — `apps/api` + `apps/web`) |
| **Config file** | `apps/api/vitest.config.ts`, `apps/web/vitest.config.ts` |
| **Quick run command** | `pnpm --filter @sms/api test:unit -- --run` |
| **Full suite command** | `pnpm test -- --run` |
| **Estimated runtime** | ~40 seconds (unit only); ~2 minutes (full with integration) |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @sms/api test:unit -- --run` (quick feedback)
- **After every plan wave:** Run `pnpm test -- --run` (full suite)
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 60 seconds for unit; 120 seconds for full

---

## Per-Task Verification Map

*Populated by gsd-planner during Step 8 (planning). Each task in each PLAN.md produces one row.*

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD — planner fills during Step 8 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

*Populated by planner. Research §"Validation Architecture" lists 16 test gaps mapped to decision clusters (Foundation, UX, Security, Zero-transcode, Onboarding, Lifecycle). Expected test files (planner to confirm paths):*

- [ ] `apps/api/src/cameras/__tests__/push-ingest.service.spec.ts` — createCamera(ingestMode=push), rotateStreamKey, deleteCamera kick
- [ ] `apps/api/src/cameras/__tests__/bulk-import-push.spec.ts` — mixed CSV parse, push-row streamUrl empty validation
- [ ] `apps/api/src/srs/__tests__/srs-callback-push.spec.ts` — parseStreamKey push branch, codec-mismatch rejection, maintenance accept
- [ ] `apps/api/src/srs/__tests__/srs-callback-forward.spec.ts` — on-forward hook push→live remap, recursion guard
- [ ] `apps/api/src/srs/__tests__/srs-api-kick.spec.ts` — SrsApiService.kickPublisher, idempotency
- [ ] `apps/api/src/streams/__tests__/stream-processor-push.spec.ts` — FFmpeg inputUrl routing for push+transcode
- [ ] `apps/api/src/prisma/migrations/__tests__/push-fields.spec.ts` — migration applies, existing rows default pull
- [ ] `apps/web/src/app/admin/cameras/components/__tests__/camera-form-dialog-push.spec.tsx` — IngestModeToggle + push body + Created reveal
- [ ] `apps/web/src/app/admin/cameras/components/__tests__/bulk-import-dialog-push.spec.tsx` — ingestMode column parse, push URLs download
- [ ] `apps/web/src/app/admin/cameras/components/__tests__/view-stream-sheet-push.spec.tsx` — PushUrlSection, CodecMismatchBanner, WaitingForFirstPublish, RotateKeyDialog
- [ ] `apps/web/src/app/admin/cameras/components/__tests__/codec-status-cell-mismatch.spec.tsx` — 5th "mismatch" state
- [ ] Shared fixtures: `apps/api/test/fixtures/push-camera.fixture.ts` + `apps/web/test/fixtures/push-camera.fixture.ts`

---

## Manual-Only Verifications

*Populated by planner. Expected manual items based on research:*

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| SRS `forward` directive actually rewrites `push/<key>` → `live/{orgId}/{cameraId}` end-to-end | D-18 | Requires running SRS container + real RTMP publish with FFmpeg/OBS | Start compose stack, create push camera, publish test stream via `ffmpeg -re -i test.mp4 -c copy -f flv rtmp://localhost:1935/push/<key>`, verify HLS available at `http://localhost:8080/live/{orgId}/{cameraId}/index.m3u8` |
| Codec-mismatch kick triggers on actual non-H.264 publish | D-16 | Needs real encoder sending H.265 | Publish H.265 test stream to push URL, verify SRS client gets kicked within ~1s, verify banner appears in camera detail |
| Stream-key rotation force-disconnects active publisher | D-20 | Timing-sensitive SRS API interaction | Start publish, click Rotate, verify ffmpeg `Broken pipe` within ~2s, verify new URL works |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
