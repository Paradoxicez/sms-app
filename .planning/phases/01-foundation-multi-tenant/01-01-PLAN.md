---
phase: 01-foundation-multi-tenant
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/api/src/prisma/schema.prisma
  - apps/api/src/prisma/prisma.module.ts
  - apps/api/src/prisma/prisma.service.ts
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/web/next.config.ts
  - apps/web/src/app/layout.tsx
  - docker-compose.yml
  - .env
  - .env.example
  - package.json
  - vitest.config.ts
  - apps/api/vitest.config.ts
autonomous: true
requirements:
  - TENANT-01
  - TENANT-03
  - TENANT-04
must_haves:
  truths:
    - "NestJS API server starts and responds on port 3001"
    - "Next.js frontend starts and renders on port 3000"
    - "PostgreSQL 16 and Redis 7 are running via Docker Compose"
    - "Prisma schema has Package and Organization models with correct fields"
    - "Vitest runs successfully with zero tests (framework ready)"
  artifacts:
    - path: "apps/api/src/main.ts"
      provides: "NestJS bootstrap with bodyParser disabled"
      contains: "bodyParser: false"
    - path: "apps/api/src/prisma/schema.prisma"
      provides: "Database schema with Package model"
      contains: "model Package"
    - path: "docker-compose.yml"
      provides: "PostgreSQL 16 + Redis 7 containers"
      contains: "postgres:16"
    - path: "vitest.config.ts"
      provides: "Vitest test configuration"
      contains: "vitest"
  key_links:
    - from: "apps/api/src/app.module.ts"
      to: "apps/api/src/prisma/prisma.module.ts"
      via: "NestJS module import"
      pattern: "PrismaModule"
---

<objective>
Scaffold the monorepo with NestJS API, Next.js frontend, Docker Compose (PostgreSQL 16 + Redis 7), Prisma schema with all Phase 1 models, and Vitest test infrastructure.

Purpose: Establish the project foundation that all subsequent plans build on -- database, backend, frontend, and test framework.
Output: Working monorepo with both apps starting, database schema pushed, test framework ready.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-multi-tenant/01-CONTEXT.md
@.planning/phases/01-foundation-multi-tenant/01-RESEARCH.md
@.planning/phases/01-foundation-multi-tenant/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold monorepo with NestJS API + Next.js frontend + Docker Compose</name>
  <files>
    package.json,
    docker-compose.yml,
    .env,
    .env.example,
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/api/src/main.ts,
    apps/api/src/app.module.ts,
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/web/next.config.ts,
    apps/web/src/app/layout.tsx,
    apps/web/src/app/page.tsx
  </files>
  <read_first>
    CLAUDE.md (tech stack reference),
    .planning/phases/01-foundation-multi-tenant/01-RESEARCH.md (standard stack versions)
  </read_first>
  <action>
    Create monorepo structure with npm workspaces:

    1. Root `package.json`:
       - name: "sms-platform"
       - workspaces: ["apps/api", "apps/web"]
       - scripts: { "dev:api": "npm -w apps/api run start:dev", "dev:web": "npm -w apps/web run dev", "test": "vitest run" }

    2. `docker-compose.yml`:
       - postgres service: image `postgres:16`, port 5432:5432, env POSTGRES_USER=sms, POSTGRES_PASSWORD=sms_dev_password, POSTGRES_DB=sms_platform, volume postgres_data:/var/lib/postgresql/data, healthcheck `pg_isready -U sms`
       - redis service: image `redis:7-alpine`, port 6379:6379, healthcheck `redis-cli ping`
       - volumes: postgres_data

    3. `.env` and `.env.example`:
       - DATABASE_URL=postgresql://sms:sms_dev_password@localhost:5432/sms_platform
       - REDIS_URL=redis://localhost:6379
       - BETTER_AUTH_SECRET=dev-secret-change-in-production-min-32-chars
       - BETTER_AUTH_URL=http://localhost:3001
       - NEXT_PUBLIC_API_URL=http://localhost:3001
       - PORT=3001

    4. NestJS API (`apps/api/`):
       - Install: @nestjs/core@11 @nestjs/common@11 @nestjs/platform-express@11 @nestjs/config@4 @nestjs/swagger@11 reflect-metadata rxjs
       - Install dev: typescript@5.7 @types/node @nestjs/cli @nestjs/schematics ts-node
       - `src/main.ts`: Bootstrap NestJS with `{ bodyParser: false }` (per Pitfall 3 in RESEARCH.md), listen on process.env.PORT || 3001, enable CORS for http://localhost:3000
       - `src/app.module.ts`: Import ConfigModule.forRoot({ isGlobal: true }), PrismaModule

    5. Next.js frontend (`apps/web/`):
       - Install: next@15 react@19 react-dom@19
       - Install dev: typescript@5.7 @types/node @types/react @types/react-dom
       - `next.config.ts`: Standard config with output standalone
       - `src/app/layout.tsx`: Root layout with Inter font, html lang="en"
       - `src/app/page.tsx`: Simple "SMS Platform" text (placeholder)
  </action>
  <verify>
    <automated>cd apps/api && npx tsc --noEmit && cd ../../apps/web && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "workspaces" with "apps/api" and "apps/web"
    - docker-compose.yml contains "postgres:16" and "redis:7-alpine"
    - .env contains "DATABASE_URL=postgresql://sms:sms_dev_password@localhost:5432/sms_platform"
    - .env.example contains "DATABASE_URL=" and "REDIS_URL=" and "BETTER_AUTH_SECRET="
    - apps/api/src/main.ts contains "bodyParser: false"
    - apps/api/src/app.module.ts contains "PrismaModule"
    - apps/web/src/app/layout.tsx contains "Inter"
    - TypeScript compiles without errors in both apps
  </acceptance_criteria>
  <done>Both NestJS and Next.js apps compile. Docker Compose config ready. Environment variables set.</done>
</task>

<task type="auto">
  <name>Task 2: Create Prisma schema with all Phase 1 models and push to database</name>
  <files>
    apps/api/src/prisma/schema.prisma,
    apps/api/src/prisma/prisma.module.ts,
    apps/api/src/prisma/prisma.service.ts,
    apps/api/src/prisma/rls.policies.sql
  </files>
  <read_first>
    .planning/phases/01-foundation-multi-tenant/01-RESEARCH.md (Package schema, RLS patterns, Better Auth schema requirements),
    .planning/phases/01-foundation-multi-tenant/01-CONTEXT.md (D-04, D-05 package design)
  </read_first>
  <action>
    1. Install Prisma: `npm install -w apps/api prisma@7 @prisma/client@7`

    2. Create `apps/api/src/prisma/schema.prisma`:
       - datasource db: provider "postgresql", url env("DATABASE_URL")
       - generator client: provider "prisma-client-js", output "./generated/client" (Prisma 7 requires explicit output)

       Better Auth managed tables (generated via `npx auth generate`, then add custom fields):
       - model User: id String @id, name String, email String @unique, emailVerified Boolean @default(false), image String?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, role String? @default("viewer"), banned Boolean? @default(false), banReason String?, banExpires DateTime?, sessions Session[], accounts Account[], members Member[]
       - model Session: id String @id, expiresAt DateTime, token String @unique, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, ipAddress String?, userAgent String?, userId String, user User @relation(fields: [userId], references: [id], onDelete: Cascade), activeOrganizationId String?, impersonatedBy String?
       - model Account: id String @id, accountId String, providerId String, userId String, user User @relation(fields: [userId], references: [id], onDelete: Cascade), accessToken String?, refreshToken String?, idToken String?, accessTokenExpiresAt DateTime?, refreshTokenExpiresAt DateTime?, scope String?, password String?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt
       - model Verification: id String @id, identifier String, value String, expiresAt DateTime, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt

       Better Auth organization tables:
       - model Organization: id String @id, name String, slug String @unique, logo String?, createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, metadata String?, packageId String?, package Package? @relation(fields: [packageId], references: [id]), isActive Boolean @default(true), members Member[], invitations Invitation[]
       - model Member: id String @id, organizationId String, organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade), userId String, user User @relation(fields: [userId], references: [id], onDelete: Cascade), role String @default("viewer"), createdAt DateTime @default(now())
       - model Invitation: id String @id, organizationId String, organization Organization @relation(fields: [organizationId], references: [id], onDelete: Cascade), email String, role String?, status String @default("pending"), expiresAt DateTime, inviterId String, createdAt DateTime @default(now())

       Custom application tables (per D-04, D-05):
       - model Package: id String @id @default(uuid()), name String, description String?, maxCameras Int, maxViewers Int, maxBandwidthMbps Int, maxStorageGb Int, features Json @default("{}"), isActive Boolean @default(true), createdAt DateTime @default(now()), updatedAt DateTime @updatedAt, organizations Organization[]

    3. Create `apps/api/src/prisma/prisma.service.ts`:
       - Injectable class extending PrismaClient
       - Implements OnModuleInit with `this.$connect()` in onModuleInit
       - Implements OnModuleDestroy with `this.$disconnect()` in onModuleDestroy

    4. Create `apps/api/src/prisma/prisma.module.ts`:
       - Global module exporting PrismaService

    5. Create `apps/api/src/prisma/rls.policies.sql`:
       - CREATE ROLE IF NOT EXISTS app_user LOGIN PASSWORD 'sms_app_user_password'
       - GRANT USAGE ON SCHEMA public TO app_user
       - GRANT SELECT, INSERT, UPDATE, DELETE ON ALL TABLES IN SCHEMA public TO app_user
       - ALTER DEFAULT PRIVILEGES IN SCHEMA public GRANT SELECT, INSERT, UPDATE, DELETE ON TABLES TO app_user
       - For each tenant table (Organization, Member, Invitation -- tables that will have org-scoped data in future phases):
         Comment: "RLS policies will be added per-table as tenant-scoped tables are created in future phases. Phase 1 establishes the infrastructure."
       - Note: The Package table is NOT org-scoped (super admin manages globally), so no RLS on Package.

    6. [BLOCKING] Start Docker Compose and push schema:
       - `docker compose up -d`
       - Wait for PostgreSQL healthy
       - `cd apps/api && npx prisma db push`
       - Verify tables exist: `npx prisma db execute --stdin <<< "SELECT tablename FROM pg_tables WHERE schemaname='public';"`
  </action>
  <verify>
    <automated>cd apps/api && npx prisma db push --accept-data-loss && npx prisma validate</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/src/prisma/schema.prisma contains "model Package" with fields "maxCameras Int", "maxViewers Int", "maxBandwidthMbps Int", "maxStorageGb Int", "features Json"
    - apps/api/src/prisma/schema.prisma contains "model Organization" with "packageId String?" and "isActive Boolean"
    - apps/api/src/prisma/schema.prisma contains "model User" with "role String?"
    - apps/api/src/prisma/schema.prisma contains "model Session" with "activeOrganizationId String?" and "impersonatedBy String?"
    - apps/api/src/prisma/prisma.service.ts contains "extends PrismaClient" and "onModuleInit"
    - apps/api/src/prisma/prisma.module.ts contains "@Global()" and "PrismaService"
    - `npx prisma validate` exits 0
    - `npx prisma db push` exits 0 (tables created in PostgreSQL)
  </acceptance_criteria>
  <done>Prisma schema with all Phase 1 models validated and pushed to PostgreSQL. PrismaService and PrismaModule ready for use. RLS policy SQL file prepared.</done>
</task>

<task type="auto">
  <name>Task 3: Set up Vitest test infrastructure (Wave 0)</name>
  <files>
    vitest.config.ts,
    apps/api/vitest.config.ts,
    apps/api/tests/setup.ts,
    apps/api/tests/helpers/auth.ts,
    apps/api/tests/helpers/tenancy.ts
  </files>
  <read_first>
    .planning/phases/01-foundation-multi-tenant/01-VALIDATION.md (Wave 0 requirements, test map),
    .planning/phases/01-foundation-multi-tenant/01-RESEARCH.md (Vitest 4.1.3 config)
  </read_first>
  <action>
    1. Install test dependencies:
       `npm install -D vitest@4 @vitest/coverage-v8`
       `npm install -w apps/api -D vitest@4 @vitest/coverage-v8 @nestjs/testing`

    2. Create root `vitest.config.ts`:
       - import { defineConfig } from 'vitest/config'
       - export default defineConfig({ test: { globals: true, root: '.', projects: ['apps/api/vitest.config.ts'] } })

    3. Create `apps/api/vitest.config.ts`:
       - import { defineConfig } from 'vitest/config'
       - export default defineConfig({ test: { globals: true, root: '.', setupFiles: ['./tests/setup.ts'], include: ['tests/**/*.test.ts'], testTimeout: 30000 } })

    4. Create `apps/api/tests/setup.ts`:
       - Import PrismaClient
       - Create test prisma client connecting to DATABASE_URL (from .env)
       - beforeAll: verify DB connection with prisma.$connect()
       - afterAll: disconnect with prisma.$disconnect()
       - Export testPrisma for use in test files

    5. Create `apps/api/tests/helpers/auth.ts`:
       - Export async function createTestUser(prisma, overrides?): creates a User record with defaults { email: 'test@example.com', name: 'Test User', role: 'viewer' }
       - Export async function createTestSession(prisma, userId): creates a Session record with token (crypto.randomUUID()), expiresAt (7 days from now)
       - Export async function createSuperAdmin(prisma): creates user with role 'admin' in System org

    6. Create `apps/api/tests/helpers/tenancy.ts`:
       - Export async function createTestOrganization(prisma, overrides?): creates Organization with defaults { name: 'Test Org', slug: 'test-org' }
       - Export async function createTestPackage(prisma, overrides?): creates Package with defaults { name: 'Basic', maxCameras: 10, maxViewers: 50, maxBandwidthMbps: 100, maxStorageGb: 50, features: {} }
       - Export async function cleanupTestData(prisma): deletes all test data in correct order (Member, Invitation, Session, Account, User, Organization, Package)

    7. Add test script to apps/api/package.json: "test": "vitest run --reporter=verbose"
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vitest.config.ts exists at root with "projects" containing "apps/api/vitest.config.ts"
    - apps/api/vitest.config.ts contains "setupFiles" with "./tests/setup.ts"
    - apps/api/tests/setup.ts contains "PrismaClient" and "beforeAll" and "afterAll"
    - apps/api/tests/helpers/auth.ts exports "createTestUser" and "createTestSession" and "createSuperAdmin"
    - apps/api/tests/helpers/tenancy.ts exports "createTestOrganization" and "createTestPackage" and "cleanupTestData"
    - `npx vitest run` exits 0 (no tests to run, but framework loads without error)
  </acceptance_criteria>
  <done>Vitest configured for monorepo. Test helpers for auth and tenancy ready. Wave 0 validation requirements met.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker network | PostgreSQL and Redis accessible only from localhost in dev |
| Environment variables | Secrets stored in .env, not committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-00a | Information Disclosure | .env file | mitigate | .gitignore includes .env; .env.example has no real secrets |
| T-01-00b | Tampering | PostgreSQL default credentials | accept | Dev-only credentials; production uses different values |
| T-01-00c | Tampering | NestJS body parser | mitigate | bodyParser: false in main.ts per RESEARCH.md Pitfall 3 |
</threat_model>

<verification>
- `docker compose up -d` starts PostgreSQL and Redis without errors
- `cd apps/api && npx tsc --noEmit` compiles without errors
- `cd apps/web && npx tsc --noEmit` compiles without errors
- `npx prisma validate` passes
- `npx prisma db push` succeeds
- `npx vitest run` exits 0
</verification>

<success_criteria>
- Monorepo structure with apps/api (NestJS) and apps/web (Next.js) both compiling
- Docker Compose running PostgreSQL 16 and Redis 7
- Prisma schema with User, Session, Account, Verification, Organization, Member, Invitation, Package models
- Schema pushed to database with all tables created
- Vitest configured with test helpers for auth and tenancy scenarios
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-multi-tenant/01-01-SUMMARY.md`
</output>
