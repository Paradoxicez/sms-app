---
status: in-progress
phase: 01-foundation-multi-tenant
updated: 2026-04-15
---

## Context

UAT เฟส 1 เสร็จแล้ว (5 pass, 1 issue, 1 skip, 1 blocked)
กำลังแก้ bug ที่เจอระหว่าง UAT

## Bugs to Fix

### 1. Org edit dialog — ไม่ทำงาน
- `apps/web/src/app/admin/organizations/components/org-table.tsx:131` — ปุ่ม "Edit" ใน DropdownMenu ไม่มี onClick
- ต้องสร้าง `edit-org-dialog.tsx` (แก้ name, slug, package assignment)
- ดู pattern จาก `edit-package-dialog.tsx` ที่สร้างไว้แล้ว
- Backend PATCH endpoint มีอยู่แล้วที่ `organizations.controller.ts:42`
- Org table ใช้ DropdownMenu pattern (MoreHorizontal icon) — ให้ตรงกัน

### 2. Package table — UI pattern ไม่ตรงกับหน้าอื่น
- `apps/web/src/app/admin/packages/components/package-table.tsx` — ใช้ icon button (Pencil) แต่หน้าอื่น (org) ใช้ DropdownMenu pattern
- เปลี่ยนเป็น DropdownMenu กับ MoreHorizontal icon ให้ตรงกัน
- เพิ่ม "Deactivate" option ด้วย (backend DELETE endpoint มีอยู่แล้ว)

### 3. timeline-bar.tsx — Runtime error
- `apps/web/src/app/admin/cameras/components/timeline-bar.tsx:115` — `hours.forEach is not a function`
- เกิดเมื่อเปิดแท็บ Recordings ของกล้อง
- `hours` variable ได้ค่าที่ไม่ใช่ array — ต้องเช็คว่า API response ส่งอะไรมาแล้ว guard type

## Already Fixed (committed 0661e18)
- auth-client baseURL cross-origin cookie
- seed with Better Auth scrypt password hash
- tsx runner for seed (ESM compat)
- Redis port 6380→6379 in 3 files
- ConflictException for duplicate org slug
- Package Select UUID→name display
- edit-package-dialog created (basic version)
- ADMIN_EMAIL/ADMIN_PASSWORD env vars
