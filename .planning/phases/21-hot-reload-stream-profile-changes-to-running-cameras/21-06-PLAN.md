---
phase: 21
plan: 06
type: execute
wave: 4
depends_on: [21-01, 21-02, 21-03, 21-04, 21-05]
files_modified:
  - .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md
  - .planning/STATE.md
autonomous: false
requirements: []
# No REQUIREMENTS.md IDs map to Phase 21 — closes audit-found gap (RESEARCH.md §Phase Requirements)
must_haves:
  truths:
    - "Full apps/api vitest suite is green (zero failures, zero todos for Phase 21 test files)"
    - "Full apps/web vitest suite is green for Phase 21 test files"
    - "apps/web build succeeds (no Phase 21 type regressions)"
    - "21-VALIDATION.md per-task map status column is updated to ✅ green for all completed tasks"
    - "Manual UAT items signed off (D-08 recording gap, Activity-tab visibility) — captured in checkpoint"
    - "STATE.md updated to reflect Phase 21 complete"
  artifacts:
    - path: ".planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md"
      provides: "Final per-task map with all rows in ✅ green status"
    - path: ".planning/STATE.md"
      provides: "Phase 21 marked complete; current focus advanced to next phase"
  key_links:
    - from: "21-VALIDATION.md per-task map"
      to: "all SUMMARY.md files for plans 21-01..21-05"
      via: "Status column reflects test outcomes"
      pattern: "✅ green"
---

<objective>
Final verification gate for Phase 21: run the full apps/api + apps/web test suites, verify all Phase 21 test files are green with zero todos remaining, run the apps/web build to confirm no type regressions, perform the two manual UAT items (D-08 recording gap, Activity-tab visibility), and finalize the planning artifacts.

Purpose: Phase 21 changes touch the live FFmpeg lifecycle and the audit + UI layers around it. A green per-file unit suite proves the wiring is right but cannot prove the in-process behavior (does FFmpeg actually restart? does the recording timeline show the gap? does the Activity tab pick up the new audit rows?). This plan closes those by sampling the integrated behavior with a real running stack and rolling everything up into a Phase 21 sign-off.

Output: Updated 21-VALIDATION.md (all rows ✅ green), updated STATE.md, and a Phase 21 summary that is ready for `/gsd-verify-work`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-CONTEXT.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-RESEARCH.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-01-SUMMARY.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-02-SUMMARY.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-03-SUMMARY.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-04-SUMMARY.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 21-06-T1: Run full backend + frontend test suites and apps/web build; mark green rows in 21-VALIDATION.md</name>
  <files>
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md (current per-task map filled by 21-01-T3 — flip ⬜ pending → ✅ green for tasks that ran)
  </read_first>
  <action>
**Step 1 — Run full backend suite:**

```bash
cd apps/api
pnpm exec vitest --run 2>&1 | tee /tmp/phase21-api-test.log
```

Inspect output:
- "Test Files" line: should be `Test Files X passed, 0 failed`
- "Tests" line: ALL Phase 21 test files (the 9 created in Wave 0) should report all assertions passing, 0 todo, 0 failed
- If ANY Phase 21 test file is still pending (it.todo not flipped), trace back to which Plan owned it (02 → fingerprint + restart + audit, 03 → reassign, 04 → graceful + dedup + fallthrough + camera-health-collision, 05 → delete-protection) and create a follow-up issue. Do NOT mark green if pending todos exist.
- If existing pre-Phase-21 test files fail (regression), STOP and surface the failure — do NOT mark green.

**Step 2 — Run full frontend suite:**

```bash
cd apps/web
pnpm exec vitest --run 2>&1 | tee /tmp/phase21-web-test.log
```

Inspect output for `profile-form-dialog-toast.test.tsx` — must show 7 passing, 0 failed, 0 todo.

If existing camera-form-dialog tests show new failures (regression from Plan 03/05's PATCH response shape change), inspect and fix. Likely fix: the existing test mocks for apiFetch return values that don't include `restartTriggered` — update mocks to add `restartTriggered: false` so the test still asserts no toast was called for non-Phase-21 paths.

**Step 3 — Run apps/web build:**

```bash
cd apps/web
pnpm exec next build 2>&1 | tee /tmp/phase21-web-build.log
```

Inspect output: must complete with exit code 0. TypeScript errors here would surface from the new response-shape additions (`affectedCameras`, `restartTriggered`) being read in the dialogs without proper typing.

If TS errors appear, narrow to the offending file and add the missing field to the response type interface.

**Step 4 — Update 21-VALIDATION.md per-task map:**

For every row in the "Per-Task Verification Map" table that has Status `⬜ pending`, update Status to `✅ green` IF the corresponding test command exited 0 and reported all tests passing.

Specifically flip:
- 21-02-T1, 21-02-T2 → ✅ green (assuming Plan 02 finished green)
- 21-03-T1 → ✅ green
- 21-04-T1, 21-04-T2 → ✅ green (T2 covers dedup + failure-fallthrough + stream-processor + camera-health-restart-collision)
- 21-05-T1, 21-05-T2 → ✅ green
- 21-06-T1 → ✅ green (this task itself, after the suites pass)
- 21-06-T2 → leave ⬜ pending until checkpoint completes

If any test fails: leave that row at ⬜ pending or mark `❌ red`, write the failure summary in the Status column, and STOP this plan — fix the failure first.

**Step 5 — Append a "Final Test Run" section at the bottom of 21-VALIDATION.md:**

```markdown
## Final Test Run — 2026-04-25

| Suite | Command | Result |
|-------|---------|--------|
| apps/api unit + integration | `pnpm --filter @sms-platform/api test` | <fill: e.g., "176 tests passed, 0 failed, 0 todo (Phase 21 contributed 54+ new passing)"> |
| apps/web unit | `pnpm --filter @sms-platform/web test` | <fill> |
| apps/web build | `pnpm --filter @sms-platform/web build` | <fill: e.g., "✓ Generated static pages (X/X)"> |

**Phase 21 test files:** 9 backend + 1 frontend, all green.
**Regressions:** none / (list if any with mitigation).
```

Commit nothing in this task — orchestrator-level commit happens after the checkpoint.
  </action>
  <verify>
    <automated>cd apps/api &amp;&amp; pnpm exec vitest --run tests/streams/profile-fingerprint.test.ts tests/streams/stream-profile-restart.test.ts tests/streams/profile-restart-audit.test.ts tests/streams/profile-restart-dedup.test.ts tests/streams/profile-restart-failure-fallthrough.test.ts tests/streams/ffmpeg-graceful-restart.test.ts tests/streams/stream-profile-delete-protection.test.ts tests/cameras/camera-profile-reassign.test.ts tests/resilience/camera-health-restart-collision.test.ts 2>&amp;1 | grep -E "Test Files|Tests:" | tail -5 &amp;&amp; cd ../web &amp;&amp; pnpm exec vitest --run src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx 2>&amp;1 | grep -E "Test Files|Tests:" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - apps/api Phase 21 file run reports `Test Files 9 passed, 0 failed` and total tests >= 54 passed with 0 todo, 0 failed.
    - apps/web Phase 21 file run reports `Test Files 1 passed, 0 failed` and 7 passed with 0 todo, 0 failed.
    - 21-VALIDATION.md "Per-Task Verification Map" has zero ⬜ pending rows for tasks 21-02-T1 through 21-05-T2 (all flipped to ✅ green).
    - 21-VALIDATION.md has a new "Final Test Run" section with the actual command outputs filled in.
    - apps/web build (in the next checkpoint task's preflight) does not surface new TypeScript errors.
  </acceptance_criteria>
  <done>All Phase 21 automated verifications are green. 21-VALIDATION.md is the single source of truth showing every task ✅ green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 21-06-T2: Manual UAT — D-08 recording gap, Activity tab visibility, finalize STATE.md</name>
  <what-built>
The full Phase 21 hot-reload pipeline:
- Profile-side trigger: editing any of the 7 FFmpeg-affecting fields (codec/preset/resolution/fps/videoBitrate/audioCodec/audioBitrate) on a StreamProfile attached to ≥1 online camera enqueues a per-camera restart job with 0-30s jitter.
- Camera-side trigger: changing a camera's streamProfileId to a profile with a different fingerprint enqueues a single restart for that camera.
- Restart execution: SIGTERM → 5s grace → SIGKILL if needed → transition('reconnecting') → spawn new FFmpeg with the new profile values.
- DELETE /api/stream-profiles/:id returns 409 + camera list when cameras still reference the profile.
- Toast feedback in both Edit dialogs (info-level when restart fires, success-level otherwise).
- Audit row `camera.profile_hot_reload` written at enqueue time per affected camera.
- B-1 fix: `CameraHealthService.enqueueStart` collision guard preserves in-flight 'restart' jobs.
  </what-built>
  <how-to-verify>
**Preflight:**
1. Start the stack: `pnpm --filter @sms-platform/api start:dev` (one terminal) + `pnpm --filter @sms-platform/web dev` (second terminal). Confirm SRS is up via `docker ps | grep srs`.
2. Have at least one camera ONLINE and RECORDING. If nothing is online, register an RTSP camera or use the existing seed.

**Test 1 — D-01 profile-side hot-reload (CORE FUNCTIONALITY):**
1. Open `http://localhost:3000/app/stream-profiles`, click Edit on the profile attached to your online camera.
2. Change `videoBitrate` from its current value to a different value (e.g., 2000k → 3000k). Click Save.
3. **Expected within 30 seconds:**
   - Toast appears: "Profile updated · 1 camera(s) restarting with new settings" (info-level)
   - Camera status pill on the Cameras page transitions: online → reconnecting → connecting → online
   - The HLS stream resumes within 2-5 seconds after spawn (the 'connecting' phase)
   - In the View Stream Sheet, the FFmpeg PID changes (or the stream visibly blips)

**Test 2 — D-02 camera-side reassign:**
1. Create a SECOND profile (e.g., 720p Transcode).
2. On the Cameras page, edit your online camera, change Stream Profile to the new one. Save.
3. **Expected within 30 seconds:**
   - Toast: "Stream restarting with new profile"
   - Same status cycle as Test 1.

**Test 3 — D-08 recording gap (manual sample):**
1. Confirm the camera is recording (REC pill visible).
2. Trigger a profile edit (Test 1 again).
3. **Expected:** Recording timeline (View Stream Sheet → Recording tab) shows a 2-5s gap. No errors logged. SRS DVR should auto-rotate the segment.

**Test 4 — D-10 DELETE protection:**
1. On Stream Profiles page, attempt to delete the profile attached to your camera.
2. **Expected:** AlertDialog shows "Reassign before deleting · 1 camera still using this profile:" followed by the camera name. Delete button is hidden; only Cancel.
3. Reassign the camera to a different profile.
4. Attempt the delete again.
5. **Expected:** 200 success, profile row removed.

**Test 5 — D-11 webhook coalescing — DROPPED from manual UAT (W-5 revision iter 1):**

D-11 is verified by automated proof rather than manual UAT:
- RESEARCH §A2 enumerates the proof: Phase 15 D-04 30s notification debounce (status.service.ts:86-106) coalesces consecutive transitions within the 30s window. Phase 21's restart fires `online → reconnecting → connecting → online` within ~2-5 seconds, all inside the debounce window, so the brief offline blip is suppressed.
- Phase 15's existing `apps/api/tests/status/notification-debounce.test.ts` (or equivalent) already asserts the coalescing behavior; that test passes in the regression suite (Step 1 backend run above).
- Phase 21 introduces no new transition that bypasses the debounce, so D-11 is verified by:
  1. Backend regression suite green (Phase 15 debounce test passes).
  2. RESEARCH §A2 written analysis citing the coalescing proof.

If a webhook receiver is later configured for ad-hoc testing, the operator may sanity-check by triggering Test 1 and confirming no `camera.offline` event reaches the receiver. This is OPTIONAL and NOT a Phase 21 acceptance gate.

**Test 6 — D-07 audit visibility (Activity tab — known broken per RESEARCH §3):**
1. Open View Stream Sheet → Activity tab for the affected camera.
2. **Expected if Activity tab works:** Recent rows include `camera.profile_hot_reload` with details = `{profileId, oldFingerprint, newFingerprint, triggeredBy}`.
3. **Expected if Activity tab is broken (per RESEARCH §3):** Empty list. This is the deferred bug — log it as a `/gsd-quick` follow-up. Phase 21 still passes verification because the audit ROWS are written (verify directly via DB or `/api/audit-log` query without the broken `search` filter).

Direct DB sample (fallback if Activity tab is broken):
```sql
SELECT action, "resourceId", details FROM "AuditLog"
WHERE action = 'camera.profile_hot_reload'
ORDER BY "createdAt" DESC LIMIT 5;
```

**Test 7 — Threat verification (T-21-01, T-21-02, T-21-03):**
1. T-21-01: Confirm a non-admin role cannot trigger profile updates. Try with an unauthenticated curl: `curl -X PATCH http://localhost:3003/api/stream-profiles/<id>` → must return 401.
2. T-21-02: Cannot easily test cross-org without two-org seed; verify via test 21-05-T1 case 5 having passed.
3. T-21-03: Save the profile 10 times in rapid succession; confirm the queue still has only 1 in-flight job per camera (visible in BullMQ board if installed; otherwise check `redis-cli LRANGE bull:stream-ffmpeg:wait 0 -1`).

**Finalize STATE.md:**
After all tests pass, update `.planning/STATE.md`:
- Bump `completed_phases` from 8 to 9 (or whatever the current count is)
- Set `progress.percent` accordingly
- Update `Last activity` to today's date
- Update `Current Position` to the next phase or "Phase 21 complete"
- Move any "Pending Todos" related to Phase 21 to a completed state
- Append a "Roadmap Evolution" entry confirming Phase 21 closed the audit gap from 2026-04-25
  </how-to-verify>
  <resume-signal>
Type "approved" if all 6 active tests passed (Test 5 dropped per W-5; D-11 verified by automated proof) and STATE.md is updated.
Type "<test number> failed: <description>" to flag a specific failure for fix-loop.
Type "Activity tab broken (deferred)" if Test 6 only fails because of the pre-existing audit-search bug — Phase 21 still ships in that case.
  </resume-signal>
  <files>.planning/STATE.md, .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/</files>
  <action>Operator runs the manual UAT script described in &lt;how-to-verify&gt; above (6 active tests covering D-01, D-02, D-08, D-10, D-07, and threats T-21-01/02/03; D-11 dropped per W-5 in favor of automated proof from RESEARCH §A2 + Phase 15 debounce regression test). After all tests pass, the operator updates STATE.md per the "Finalize STATE.md" instructions in &lt;how-to-verify&gt; (bump completed_phases, update last_activity, append a Roadmap Evolution entry). Claude does not auto-execute this — it pauses at this checkpoint and resumes on operator signal.</action>
  <verify>
    <automated>test -f .planning/STATE.md &amp;&amp; grep -q "Phase 21" .planning/STATE.md</automated>
  </verify>
  <done>Operator typed "approved" (or accepted "Activity tab broken (deferred)" outcome). STATE.md reflects Phase 21 complete with updated counts and last_activity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (verification only) | This plan runs tests and inspects production behavior; no new boundaries are crossed. |

## STRIDE Threat Register — Phase 21 canonical roll-up

This is the canonical T-21-01 through T-21-07 register that the per-plan threat models reference.
Each plan addresses a subset; this table summarizes for verification.

| Threat ID | Category | Component | Disposition | Owning Plan(s) | One-Line Definition |
|-----------|----------|-----------|-------------|----------------|---------------------|
| T-21-01 | Elevation | PATCH /api/stream-profiles/:id and PATCH /api/cameras/:id | mitigate | 02, 03 | AuthGuard + tenancy client (RLS) prevents cross-org restart fan-out. |
| T-21-02 | Information Disclosure | DELETE /api/stream-profiles/:id 409 usedBy[] payload | mitigate | 05 | TENANCY_CLIENT scopes the cameras.findMany; cross-org camera names cannot leak in 409. |
| T-21-03 | DoS | PATCH rapid-fire profile or camera saves | mitigate | 02, 03 | remove-then-add coalesces to 1 in-flight job per camera; HTTP rate limit caps PATCH frequency. |
| T-21-04 | Information Disclosure | audit log row volume growth | accept | 02 | Each save writes N rows where N = affected cameras; bounded by admin-action frequency + retention policy. |
| T-21-05 | Information Disclosure | fingerprint exposed via API | mitigate | 02 | Fingerprint lives in audit `details` server-side only; PATCH response includes only `affectedCameras` count, never fingerprint. |
| T-21-06 | DoS | StreamProcessor restart retry loop | mitigate | 04 | BullMQ exponential backoff (1s → 5min cap) + 20-attempt limit prevents tight retry loops; final failure transitions to 'degraded'. |
| T-21-07 | Tampering / Race | runningProcesses map after gracefulRestart + camera-health collision (B-1) | mitigate | 04 | (a) gracefulRestart's SIGKILL-after-graceMs ensures runningProcesses is empty before startStream re-spawns. (b) CameraHealthService.enqueueStart guards against in-flight 'restart' jobs to prevent silent demotion to 'start' (B-1 fix in revision iter 1). |

| Threat ID | Verification Method |
|-----------|---------------------|
| T-21-01 | grep `TENANCY_CLIENT`/`tenancy.camera`/`prisma.camera.findMany` in streams.service.ts; AuthGuard at controller; manual test 7.1 |
| T-21-02 | Test 21-05-T1 case 5 passes (no orgId filter; RLS proven). |
| T-21-03 | profile-restart-dedup.test.ts passes (rapid-fire test); manual test 7.3 |
| T-21-04 | Accepted — documented in 21-02-SUMMARY.md row-count growth note |
| T-21-05 | grep `fingerprint` in stream-profile.controller.ts returns 0. |
| T-21-06 | profile-restart-failure-fallthrough.test.ts passes; existing exponential-backoff config asserted in dedup.test.ts |
| T-21-07 | (a) ffmpeg-graceful-restart.test.ts passes. (b) camera-health-restart-collision.test.ts 4 cases pass. |
</threat_model>

<verification>
- All 9 backend Phase 21 test files green with zero todos (8 originals + 1 B-1 collision-guard added in revision iter 1).
- 1 frontend Phase 21 test file green.
- apps/web build succeeds.
- 6 active manual UAT tests pass (Test 5 dropped per W-5; Test 6 has known deferred caveat).
- 21-VALIDATION.md per-task map fully ✅ green.
- STATE.md reflects Phase 21 complete.
</verification>

<success_criteria>
1. Phase 21 closes the original audit gap: editing a profile or reassigning a camera triggers a real FFmpeg restart within 30 seconds, observable end-to-end.
2. DELETE protection is honored: 409 with usedBy[] payload prevents accidental data loss.
3. Audit rows for `camera.profile_hot_reload` exist for every affected camera per profile/camera edit.
4. No regressions in existing camera/stream tests.
5. STATE.md and 21-VALIDATION.md are updated.
6. B-1 collision-guard regression: `CameraHealthService.enqueueStart` never silently demotes an in-flight 'restart' job (verified by camera-health-restart-collision.test.ts).
</success_criteria>

<output>
After completion, create `.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-06-SUMMARY.md` documenting:
- Final test counts (passed, failed, todo) per suite
- Manual UAT outcomes per test
- Activity tab status (working / deferred)
- Threat verification status (T-21-01 through T-21-07)
- D-11 verification path (automated via RESEARCH §A2 + Phase 15 debounce test, not manual)
- Phase 21 close-out checklist (all items ticked)
</output>
</content>
