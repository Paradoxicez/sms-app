---
phase: 02-stream-engine-camera-management
plan: 06
type: execute
wave: 4
depends_on: ["02-04", "02-05"]
files_modified:
  - apps/web/src/app/admin/stream-profiles/page.tsx
  - apps/web/src/app/admin/stream-profiles/components/profile-form-dialog.tsx
  - apps/web/src/app/admin/stream-engine/page.tsx
  - apps/web/src/app/admin/cameras/components/bulk-import-dialog.tsx
  - apps/web/src/app/admin/cameras/page.tsx
  - apps/api/src/cameras/cameras.controller.ts
  - apps/api/src/cameras/cameras.service.ts
  - apps/api/src/cameras/dto/bulk-import.dto.ts
  - apps/api/tests/cameras/bulk-import.test.ts
autonomous: false
requirements:
  - CAM-07
  - STREAM-05
  - STREAM-07

must_haves:
  truths:
    - "Stream Profiles page shows cards in 2-3 column grid with profile details"
    - "Create Profile dialog has RadioGroup for passthrough/transcode with conditional fields"
    - "Stream Engine Settings page has System and Organization Defaults tabs"
    - "System settings form saves and shows success toast"
    - "Bulk import dialog accepts CSV/JSON file upload with editable preview table"
    - "Bulk import validates rows and shows errors inline with red highlighting"
    - "Bulk import creates cameras with status offline and runs ffprobe in background"
  artifacts:
    - path: "apps/web/src/app/admin/stream-profiles/page.tsx"
      provides: "Stream profiles management page"
      contains: "Create Profile"
    - path: "apps/web/src/app/admin/stream-engine/page.tsx"
      provides: "Stream engine settings page"
      contains: "Stream Engine Settings"
    - path: "apps/web/src/app/admin/cameras/components/bulk-import-dialog.tsx"
      provides: "Bulk camera import dialog"
      contains: "Confirm Import"
  key_links:
    - from: "stream-profiles/page.tsx"
      to: "/api/stream-profiles"
      via: "fetch"
      pattern: "apiFetch.*stream-profiles"
    - from: "stream-engine/page.tsx"
      to: "/api/admin/settings/stream-engine"
      via: "fetch"
      pattern: "apiFetch.*stream-engine"
    - from: "bulk-import-dialog.tsx"
      to: "/api/cameras/bulk-import"
      via: "fetch POST"
      pattern: "bulk-import"
---

<objective>
Stream Profiles UI, Stream Engine Settings UI, and Bulk Camera Import with validation preview.

Purpose: Complete the remaining UI screens for camera management and provide the bulk import workflow for efficiently adding multiple cameras.
Output: Stream Profiles page, Stream Engine Settings page, Bulk Import dialog with CSV/JSON support.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-stream-engine-camera-management/02-CONTEXT.md
@.planning/phases/02-stream-engine-camera-management/02-UI-SPEC.md
@.planning/phases/02-stream-engine-camera-management/02-04-SUMMARY.md
@.planning/phases/02-stream-engine-camera-management/02-05-SUMMARY.md

<interfaces>
API endpoints (from Plans 02, 04):
- GET /api/stream-profiles -> StreamProfile[]
- POST /api/stream-profiles -> StreamProfile
- PATCH /api/stream-profiles/:id -> StreamProfile
- DELETE /api/stream-profiles/:id
- POST /api/stream-profiles/validate -> { warnings: string[] }
- GET /api/admin/settings/stream-engine -> SystemSettings
- PATCH /api/admin/settings/stream-engine -> SystemSettings
- GET /api/settings/org -> OrgSettings
- PATCH /api/settings/org -> OrgSettings

StreamProfile fields: id, name, codec, preset, resolution, fps, videoBitrate, audioCodec, audioBitrate, isDefault
SystemSettings fields: hlsFragment, hlsWindow, hlsEncryption, rtmpPort, srtPort, timeoutSeconds
OrgSettings fields: defaultProfileId, maxReconnectAttempts, autoStartOnBoot, defaultRecordingMode

UI-SPEC references:
- Stream Profiles: card grid (2-3 columns), each card shows name/codec/resolution/bitrate/default badge
- Stream Engine Settings: "Stream Engine Settings" title (never "SRS"), System tab + Org Defaults tab
- Bulk Import: medium-sized dialog, editable table, validation inline, "Confirm Import" button
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stream Profiles page + Stream Engine Settings page</name>
  <files>
    apps/web/src/app/admin/stream-profiles/page.tsx,
    apps/web/src/app/admin/stream-profiles/components/profile-form-dialog.tsx,
    apps/web/src/app/admin/stream-engine/page.tsx
  </files>
  <read_first>
    apps/web/src/app/admin/cameras/page.tsx,
    apps/web/src/components/sidebar-nav.tsx,
    apps/web/src/app/globals.css,
    .planning/phases/02-stream-engine-camera-management/02-UI-SPEC.md
  </read_first>
  <action>
1. Create `stream-profiles/page.tsx`:

- Page title "Stream Profiles" (Heading 20px/600)
- "Create Profile" button (top-right, accent, Plus icon)
- Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8`
- Each profile card (Card component):
  - Card header: profile name + default badge (if isDefault)
  - Card body:
    - Mode: "Passthrough" (if codec=copy) or "Transcode" (if codec=libx264) or "Auto" (if codec=auto)
    - Resolution: value or "Original" if null
    - FPS: value or "Original" if null
    - Video Bitrate: value or "Auto" if null
    - Audio: audioCodec + audioBitrate
  - Card footer: Edit + Delete buttons
- Empty state per UI-SPEC: "No stream profiles" heading, description, Create Profile CTA
- Delete Profile AlertDialog: "Cameras using this profile will fall back to the default passthrough profile."

2. Create `stream-profiles/components/profile-form-dialog.tsx`:

- Dialog with form fields per D-11:
  - Name (Input, required)
  - Mode (RadioGroup): "Passthrough" / "Transcode"
  - If Transcode mode selected, show additional fields:
    - Resolution (Select: "Original", "3840x2160", "1920x1080", "1280x720", "854x480", "640x360")
    - FPS (Select: "Original", "60", "30", "25", "15")
    - Bitrate (Slider with numeric display, range 500k-8000k, step 100k, default 2000k)
    - Preset (Select: "ultrafast", "superfast", "veryfast", "faster", "fast", "medium")
    - Audio mode (Select: "Copy" (copy), "Transcode" (aac), "Mute" (mute))
    - Audio bitrate (Select: "64k", "96k", "128k", "192k", "256k") — only if audio mode = Transcode
  - Set as Default (Switch)
- Validation warnings: if POST /api/stream-profiles/validate returns warnings, show as amber inline messages (per D-11)
- "Save Profile" button

3. Create `stream-engine/page.tsx`:

- Page title "Stream Engine Settings" (Heading 20px/600, per D-15 — never "SRS Settings")
- Tabs component with 2 tabs:

**System tab (loads GET /api/admin/settings/stream-engine):**
- HLS fragment size: Input type=number, suffix "seconds", default 2
- HLS window size: Input type=number, suffix "seconds", default 10
- RTMP port: Input type=number, default 1935
- SRT port: Input type=number, default 10080
- Connection timeout: Input type=number, suffix "seconds", default 30
- HLS encryption: Switch toggle
- "Save Settings" button (right-aligned, bottom) — PATCH /api/admin/settings/stream-engine
- On save: loading state on button, then toast "Settings saved"

**Organization Defaults tab (loads GET /api/settings/org):**
- Default stream profile: Select from profiles (loads GET /api/stream-profiles)
- Max reconnect attempts: Input type=number, default 10
- Auto-start on boot: Switch toggle
- Default recording mode: Select ("Off", "Continuous", "Motion")
- "Save Defaults" button — PATCH /api/settings/org
- On save: toast "Organization defaults saved"

Note: System tab should show only for super admin role. Org Defaults for org admin+.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app/apps/web && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - stream-profiles/page.tsx contains `"Create Profile"` button and `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3`
    - stream-profiles/page.tsx contains `"No stream profiles"` empty state text
    - profile-form-dialog.tsx contains RadioGroup with "Passthrough" and "Transcode" options
    - profile-form-dialog.tsx contains Slider for bitrate with range values
    - profile-form-dialog.tsx contains conditional rendering: transcode fields shown only when mode is "Transcode"
    - stream-engine/page.tsx contains `"Stream Engine Settings"` title (NOT "SRS")
    - stream-engine/page.tsx contains Tabs with `"System"` and `"Organization Defaults"` labels
    - stream-engine/page.tsx contains Input for hlsFragment, hlsWindow, rtmpPort, srtPort, timeoutSeconds
    - stream-engine/page.tsx contains Switch for hlsEncryption and autoStartOnBoot
    - stream-engine/page.tsx contains `"Save Settings"` button text
    - `npx next build` exits 0
  </acceptance_criteria>
  <done>Stream Profiles page with card grid and create/edit dialog (per D-11). Stream Engine Settings page with System and Org Defaults tabs (per D-15, D-16). UI labels use "Stream Engine" branding throughout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bulk camera import backend + frontend dialog</name>
  <files>
    apps/api/src/cameras/dto/bulk-import.dto.ts,
    apps/api/src/cameras/cameras.controller.ts,
    apps/api/src/cameras/cameras.service.ts,
    apps/web/src/app/admin/cameras/components/bulk-import-dialog.tsx,
    apps/web/src/app/admin/cameras/page.tsx,
    apps/api/tests/cameras/bulk-import.test.ts
  </files>
  <read_first>
    apps/api/src/cameras/cameras.controller.ts,
    apps/api/src/cameras/cameras.service.ts,
    apps/api/src/cameras/dto/create-camera.dto.ts,
    apps/web/src/app/admin/cameras/page.tsx,
    .planning/phases/02-stream-engine-camera-management/02-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: parse CSV with columns "name,streamUrl,project,site,lat,lng,tags" returns array of camera objects
    - Test: parse JSON array of camera objects returns validated array
    - Test: CSV with missing required field (name or streamUrl) returns error for that row
    - Test: CSV with invalid URL format returns error for that row
    - Test: POST /api/cameras/bulk-import with valid array creates cameras with status "offline"
    - Test: POST /api/cameras/bulk-import checks maxCameras limit for total (existing + new)
    - Test: After import, BullMQ job enqueued to run ffprobe per camera
  </behavior>
  <action>
1. Create `dto/bulk-import.dto.ts`:

```typescript
import { z } from 'zod';

export const BulkImportCameraSchema = z.object({
  name: z.string().min(1).max(100),
  streamUrl: z.string().refine(
    (url) => url.startsWith('rtsp://') || url.startsWith('srt://'),
    { message: 'Stream URL must be rtsp:// or srt://' }
  ),
  projectName: z.string().optional(),
  siteName: z.string().optional(),
  lat: z.number().optional(),
  lng: z.number().optional(),
  tags: z.string().optional(), // comma-separated
  description: z.string().optional(),
});

export const BulkImportSchema = z.object({
  cameras: z.array(BulkImportCameraSchema).min(1).max(500),
  siteId: z.string().uuid(),
});

export type BulkImportCameraDto = z.infer<typeof BulkImportCameraSchema>;
export type BulkImportDto = z.infer<typeof BulkImportSchema>;
```

2. Add bulk import endpoint to `cameras.controller.ts`:

```typescript
@Post('cameras/bulk-import')
@UseGuards(AuthGuard)
async bulkImport(@Body() body: any) {
  const parsed = BulkImportSchema.safeParse(body);
  if (!parsed.success) {
    throw new BadRequestException(parsed.error.flatten());
  }
  return this.camerasService.bulkImport(orgId, parsed.data);
}
```

3. Add `bulkImport(orgId, dto)` method to `cameras.service.ts`:
- Check Package.maxCameras: count existing cameras + dto.cameras.length <= maxCameras
- Validate each row against BulkImportCameraSchema
- Create all valid cameras in a transaction with status "offline"
- After creation, enqueue BullMQ job in 'stream:probe' queue for each camera to run ffprobe in background (per D-05)
- Return { imported: number, errors: ValidationError[] }

4. Create `bulk-import-dialog.tsx` (frontend):

Per D-04, D-05, and UI-SPEC:
- Trigger: "Import Cameras" button on cameras page (secondary variant, Upload icon)
- Dialog opens with file upload zone:
  - Drop zone with dashed border, icon Upload, text "Drop CSV or JSON file here, or click to upload"
  - Accepts `.csv` and `.json` files only
- After file upload, dialog expands to medium size:
  - Parse CSV using browser-side parsing (or send to backend)
  - Show editable Table:
    - Columns: Row #, Name, Stream URL, Project, Site, Location, Tags, Status (validation icon)
    - Rows with errors: `bg-destructive/10` background, red border on invalid cells
    - Status column: check icon (green) for valid, X icon (red) for invalid with Tooltip showing error
    - Cells are editable: click to edit inline (Input overlay)
  - Footer: "{valid_count} valid, {error_count} errors" count + "Confirm Import" button
  - "Confirm Import" disabled when error_count > 0
- On confirm:
  - POST /api/cameras/bulk-import with parsed camera array + siteId
  - Show Progress bar during import
  - On success: toast "Imported {count} cameras successfully", close dialog, refresh camera list
  - Background: BullMQ runs ffprobe per camera and updates status + codec info (handled by backend)

5. Wire "Import Cameras" button in `cameras/page.tsx` to open bulk import dialog.

6. Write backend tests for CSV parsing, validation, and bulk creation.
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app/apps/api && npx vitest run tests/cameras/bulk-import.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - bulk-import.dto.ts contains `BulkImportCameraSchema` with name, streamUrl required fields
    - bulk-import.dto.ts contains URL validation for rtsp:// or srt://
    - cameras.controller.ts contains `@Post('cameras/bulk-import')`
    - cameras.service.ts contains `bulkImport(` method that checks maxCameras limit
    - cameras.service.ts bulk import enqueues BullMQ jobs on 'stream:probe' queue after creation
    - bulk-import-dialog.tsx contains file upload accepting `.csv` and `.json`
    - bulk-import-dialog.tsx contains editable table with Status column (validation icons)
    - bulk-import-dialog.tsx contains `"Confirm Import"` button that is disabled when errors exist
    - bulk-import-dialog.tsx contains `bg-destructive/10` for error row highlighting
    - cameras/page.tsx contains `"Import Cameras"` button that opens bulk import dialog
    - Tests pass
  </acceptance_criteria>
  <done>Bulk camera import with CSV/JSON upload, validation preview with inline editing, and background ffprobe execution (per D-04, D-05). Package camera limits enforced for total count.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Stream Profiles, Settings, and Bulk Import UI</name>
  <action>Human visual verification of Stream Profiles, Settings, and Bulk Import UI</action>
  <what-built>Stream Profiles UI (card grid with create/edit), Stream Engine Settings (System + Org tabs), and Bulk Import dialog (CSV/JSON upload with editable preview table)</what-built>
  <how-to-verify>
    1. Navigate to http://localhost:3000/admin/stream-profiles
    2. Verify empty state shows "No stream profiles"
    3. Click "Create Profile" — verify RadioGroup (Passthrough/Transcode), conditional fields appear for Transcode
    4. Create a profile — verify card appears in grid
    5. Navigate to http://localhost:3000/admin/stream-engine
    6. Verify title is "Stream Engine Settings" (NOT "SRS")
    7. Verify System tab has HLS fragment, window, ports, encryption toggle
    8. Verify Organization Defaults tab has reconnect attempts, auto-start toggle
    9. Navigate to http://localhost:3000/admin/cameras
    10. Click "Import Cameras" — verify file upload zone appears
    11. Upload a test CSV file with columns: name,streamUrl — verify editable table preview
    12. Verify validation: rows with invalid URLs show red highlighting
    13. Verify "Confirm Import" disabled when errors exist
  </how-to-verify>
  <resume-signal>Type "approved" if all UI screens render correctly, or describe issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File upload -> Parser | CSV/JSON file content parsed on client and server |
| Client -> Bulk import API | Array of camera objects sent to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | Tampering | Bulk import CSV injection | mitigate | Server-side Zod validation of every row. URL format validation (rtsp:// or srt:// only). Max 500 cameras per import. |
| T-02-17 | Denial of Service | Large file upload | mitigate | Max 500 cameras per BulkImportSchema. Max file size check on frontend upload handler. |
| T-02-18 | Tampering | Stream profile with malicious values | mitigate | Zod validation constrains all fields: codec enum, preset enum, port ranges, bitrate regex pattern. |
</threat_model>

<verification>
- Stream Profiles: CRUD renders, create dialog works, card grid displays
- Stream Engine Settings: System and Org tabs render, save triggers API call
- Bulk Import: file upload, preview table, validation, import API
- Build: `npx next build` passes
- Backend tests: bulk import tests pass
</verification>

<success_criteria>
Stream Profiles management page with card grid and full CRUD dialog. Stream Engine Settings page with two-tier settings (per D-16) branded as "Stream Engine" (per D-15). Bulk camera import with CSV/JSON upload, editable preview table with validation, and background ffprobe processing (per D-04, D-05). All Phase 2 UI screens are complete.
</success_criteria>

<output>
After completion, create `.planning/phases/02-stream-engine-camera-management/02-06-SUMMARY.md`
</output>
