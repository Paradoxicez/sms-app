---
phase: 07-recordings
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/tests/recordings/archive-segment.test.ts
  - apps/api/tests/recordings/manifest.test.ts
  - apps/api/tests/recordings/recording-lifecycle.test.ts
  - apps/api/tests/recordings/retention.test.ts
  - apps/api/tests/recordings/storage-quota.test.ts
  - apps/api/tests/recordings/schedule.test.ts
autonomous: true
requirements:
  - REC-01
  - REC-02
  - REC-03
  - REC-04
  - REC-05

must_haves:
  truths:
    - "Test stub files exist for all recording behaviors before implementation begins"
    - "Running vitest against tests/recordings/ reports pending (todo) tests, not failures"
  artifacts:
    - path: "apps/api/tests/recordings/archive-segment.test.ts"
      provides: "Test stubs for REC-01 on_hls archival"
    - path: "apps/api/tests/recordings/manifest.test.ts"
      provides: "Test stubs for REC-02 dynamic m3u8"
    - path: "apps/api/tests/recordings/recording-lifecycle.test.ts"
      provides: "Test stubs for REC-03 start/stop"
    - path: "apps/api/tests/recordings/retention.test.ts"
      provides: "Test stubs for REC-04 retention cleanup"
    - path: "apps/api/tests/recordings/storage-quota.test.ts"
      provides: "Test stubs for REC-05 quota enforcement"
    - path: "apps/api/tests/recordings/schedule.test.ts"
      provides: "Test stubs for D-03 BullMQ schedules"
  key_links: []
---

<objective>
Create Nyquist-compliant test stub files for all Phase 7 recording behaviors before implementation begins.

Purpose: Ensure every requirement has a test file ready for the executor to fill in during implementation plans.
Output: 6 test stub files with it.todo() entries covering REC-01 through REC-05 plus D-03 schedules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-recordings/07-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stub directory and all 6 stub files</name>
  <files>
    apps/api/tests/recordings/archive-segment.test.ts
    apps/api/tests/recordings/manifest.test.ts
    apps/api/tests/recordings/recording-lifecycle.test.ts
    apps/api/tests/recordings/retention.test.ts
    apps/api/tests/recordings/storage-quota.test.ts
    apps/api/tests/recordings/schedule.test.ts
  </files>
  <read_first>
    apps/api/tests/cluster/cluster-crud.test.ts
    apps/api/vitest.config.ts
    .planning/phases/07-recordings/07-VALIDATION.md
  </read_first>
  <action>
Create directory `apps/api/tests/recordings/` and 6 test stub files following the existing test pattern from `tests/cluster/`. Each file uses `describe()` with `it.todo()` entries:

**archive-segment.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('RecordingsService - Segment Archival (REC-01)', () => {
  it.todo('archives segment to MinIO when recording is active');
  it.todo('skips archive when recording is not active for camera');
  it.todo('skips archive when orgId/cameraId cannot be parsed from stream key');
  it.todo('detects and archives fMP4 init segment on first callback');
  it.todo('validates file path against allowed mount prefix to prevent path traversal');
  it.todo('updates recording totalSize and totalDuration after segment upload');
});
```

**manifest.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('ManifestService - Dynamic m3u8 Generation (REC-02)', () => {
  it.todo('generates valid fMP4 HLS manifest with EXT-X-MAP for init segment');
  it.todo('includes only segments within the requested time range');
  it.todo('sets EXT-X-VERSION:7 and EXT-X-ENDLIST for VOD playback');
  it.todo('generates correct EXTINF durations for each segment');
  it.todo('returns empty manifest when no segments exist for time range');
});
```

**recording-lifecycle.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('RecordingsService - Recording Lifecycle (REC-03)', () => {
  it.todo('starts recording: creates Recording record with status=recording');
  it.todo('starts recording: sets camera isRecording flag to true');
  it.todo('stops recording: sets Recording status to complete and stoppedAt timestamp');
  it.todo('stops recording: clears camera isRecording flag');
  it.todo('rejects start when camera is already recording');
  it.todo('rejects start when camera is offline');
  it.todo('rejects start when storage quota is exceeded');
});
```

**retention.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('RetentionProcessor - Retention Cleanup (REC-04)', () => {
  it.todo('deletes segments older than camera-level retention period');
  it.todo('falls back to org default retention when camera has no override');
  it.todo('removes MinIO objects for expired segments');
  it.todo('removes DB records for expired segments');
  it.todo('updates recording totalSize after segment deletion');
  it.todo('deletes empty Recording records after all segments removed');
});
```

**storage-quota.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('RecordingsService - Storage Quota (REC-05)', () => {
  it.todo('blocks new recordings when storage usage reaches 100% of maxStorageGb');
  it.todo('sends notification alert at 80% storage threshold');
  it.todo('sends notification alert at 90% storage threshold');
  it.todo('allows recording when storage usage is below quota');
  it.todo('calculates storage usage from DB segment size aggregation');
});
```

**schedule.test.ts:**
```typescript
import { describe, it } from 'vitest';

describe('ScheduleProcessor - Recording Schedules (D-03)', () => {
  it.todo('starts recording at scheduled start time');
  it.todo('stops recording at scheduled end time');
  it.todo('skips disabled schedules');
  it.todo('handles daily schedule type with start/end times');
  it.todo('handles weekly schedule type with day-of-week filter');
});
```
  </action>
  <verify>
    <automated>cd apps/api && npx vitest run tests/recordings/ --reporter=verbose 2>&1 | head -50</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/api/tests/recordings/archive-segment.test.ts` exists and contains `describe('RecordingsService - Segment Archival`
    - File `apps/api/tests/recordings/manifest.test.ts` exists and contains `describe('ManifestService - Dynamic m3u8`
    - File `apps/api/tests/recordings/recording-lifecycle.test.ts` exists and contains `describe('RecordingsService - Recording Lifecycle`
    - File `apps/api/tests/recordings/retention.test.ts` exists and contains `describe('RetentionProcessor - Retention Cleanup`
    - File `apps/api/tests/recordings/storage-quota.test.ts` exists and contains `describe('RecordingsService - Storage Quota`
    - File `apps/api/tests/recordings/schedule.test.ts` exists and contains `describe('ScheduleProcessor - Recording Schedules`
    - `npx vitest run tests/recordings/` exits 0 with all tests showing as todo/skipped
  </acceptance_criteria>
  <done>All 6 test stub files exist with it.todo() entries covering REC-01 through REC-05 plus D-03, vitest runs without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan -- test stubs only, no production code.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| — | — | — | — | No threats: test files only |
</threat_model>

<verification>
- `cd apps/api && npx vitest run tests/recordings/ --reporter=verbose` exits 0
- All 6 files exist under `apps/api/tests/recordings/`
</verification>

<success_criteria>
6 test stub files created, vitest recognizes them, all tests show as todo/skipped (0 failures).
</success_criteria>

<output>
After completion, create `.planning/phases/07-recordings/07-00-SUMMARY.md`
</output>
