---
phase: 999.1-role-based-sidebar-navigation
verified: 2026-04-15T18:25:00Z
status: human_needed
score: 4/4 observable truths verified; 1 flag (Wave 0 test staleness) + 1 flag (pre-existing zod v4 compat propagated)
gaps: []
deferred: []
human_verification:
  - test: "Sign in as platform admin — verify landing on /admin with PlatformNav sidebar only (6 items: Organizations, Packages, Cluster Nodes, SRS Engine, Platform Audit, Users)"
    expected: "No tenant-scoped links (Cameras, Recordings, Policies, etc.) visible; badge reads 'Platform'."
    why_human: "Visual/session end-to-end flow; auth state + Next.js client routing cannot be reliably asserted by unit tests without a live server."
  - test: "Sign in as Org Admin — verify landing on /app/dashboard with TenantNav sidebar filtered to their Member.role and org feature flags"
    expected: "Sidebar shows all 4 groups (Overview, Cameras, Organization, Developer); Team link visible; badge reads truncated org name."
    why_human: "Requires authenticated multi-step flow across sign-in, setActive, GET /members/me, feature-flag hook, and sidebar render."
  - test: "Sign in as Operator role — verify TenantNav shows only {Dashboard, Cameras, Map, Recordings, Audit Log}; Team and Developer links hidden"
    expected: "ROLE_MATRIX filtering matches D-11."
    why_human: "Requires live session + active org + DB-stored Member.role."
  - test: "Visit /app/team directly as Operator/Viewer — verify role-gated Lock empty state copy"
    expected: "Lock icon + 'You do not have access to this page' + 'Team management is restricted to organization admins.'"
    why_human: "Direct URL access with session context not covered by automated test that matched."
  - test: "Visit /admin as a non-admin user — verify redirect to /app/dashboard"
    expected: "Non-admin never sees admin shell; landed on /app/dashboard."
    why_human: "Client-side redirect guard depends on session round-trip."
  - test: "Visit /app as super admin — verify redirect to /admin"
    expected: "Admin never sees tenant shell."
    why_human: "Same — client-side guard behavior."
  - test: "Org Admin creates a team member via /app/team → Add Team Member dialog"
    expected: "POST /api/organizations/:orgId/users succeeds; toast 'User created. They can sign in now.'; table refetches."
    why_human: "Requires API + DB + UI integration."
  - test: "Super admin creates platform user via /admin/users → Create User dialog"
    expected: "POST succeeds; aggregated users list refreshes; new user appears in correct org(s)."
    why_human: "Cross-org aggregation + dialog submit end-to-end."
  - test: "Visual parity with UI-SPEC green theme, active-nav border, mobile 56px top bar + Sheet"
    expected: "Matches UI-SPEC screenshots."
    why_human: "Visual regression; no screenshot diffing tool in this project."
flags:
  - id: F-1
    summary: "Wave 0 test stubs are stale vs final UI-SPEC implementation (documented in Plan 04 summary)"
    severity: warning
    detail: |
      Of 27 web tests, 10 pass and 17 fail. Failures break into two families:
      (a) Server-redirect tests (root-route, admin-layout, app-layout, sign-in-redirect) were
          written against a `redirect()` server-component pattern. Implementation (per Plan 02
          decision) uses client components with `useRouter().replace()`. Behavior is equivalent
          and matches UI-SPEC; tests need rewriting, not the implementation.
      (b) UI-copy tests (platform-users-page expects "Add Platform User" button + {admin,user}
          role enum; tenant-nav expects "Settings" link) predate the finalized UI-SPEC that
          landed during phase execution. Plan 04 summary explicitly flagged this as a
          follow-up.
      All Plan-01/02/03/04 PLAN.md verify-blocks (grep + tsc + behavior checks) passed during
      execution. API side is fully green (17/17 including the 10 new tests added by this phase).
  - id: F-2
    summary: "Two new files inherit pre-existing zod v3/v4 + @hookform/resolvers overload error pattern"
    severity: warning
    detail: |
      Plan 04 added `create-platform-user-dialog.tsx` and `add-team-member-dialog.tsx` that use
      the same `zodResolver(ZodObject)` pattern already failing tsc in
      `create-org-dialog.tsx`, `edit-org-dialog.tsx`, `create-package-dialog.tsx`,
      `edit-package-dialog.tsx`, and `(auth)/sign-in/page.tsx`. This is a repo-wide zod 4 /
      @hookform/resolvers 3 incompatibility, not a phase-specific defect. The dialogs compile at
      runtime (react-hook-form runtime-checks the resolver, not TS types) and succeed in next
      build; this is strictly a TS type-check regression of +2 files on an already-broken axis.
      Recommend a follow-up quick task to pin @hookform/resolvers or downgrade zod across all 7
      call sites in one sweep.
---

# Phase 999.1: Role-based Sidebar Navigation — Verification Report

**Phase Goal (ROADMAP Backlog):** แยกเมนู sidebar ตาม role — Super Admin เห็นเฉพาะ platform management (orgs, packages, cluster, system) ส่วน Org Admin เห็นเฉพาะ org-level features (cameras, recordings, policies, developer) ป้องกัน conflict ที่ super admin เห็นหน้าว่างเปล่าและ org admin เห็นเมนูที่ไม่ควรเข้าถึง

**Requirements declared:** AUTH-03, AUTH-04, TENANT-04, TENANT-05
**Verified:** 2026-04-15
**Status:** human_needed (all must-have artifacts present and wired; automated test coverage is partial because Wave 0 stubs diverged from final UI-SPEC during execution)

---

## 1. Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| T1 | Super Admin lands on `/admin` with PlatformNav (6 items) | ✓ VERIFIED | `apps/web/src/app/admin/layout.tsx` redirects non-admin → `/app/dashboard`; renders `<PlatformNav />`. `platform-nav.tsx` declares exactly 6 items (Organizations, Packages, Cluster Nodes, SRS Engine, Platform Audit, Users). `platform-nav.test.tsx` 3/3 pass. |
| T2 | Org Admin lands on `/app/*` with TenantNav filtered by Member.role ∩ feature flags | ✓ VERIFIED | `apps/web/src/app/app/layout.tsx` redirects `role=admin` → `/admin`; bootstraps activeOrg; renders `<TenantNav />` with resolved memberRole. `tenant-nav.tsx` implements ROLE_MATRIX + `useFeatures` intersection. Operator/Developer/Viewer/feature-gate tests pass 5/6 (Org Admin "12 items" test fails on a stale "Settings" label — see F-1). |
| T3 | Backend enforces cross-tenant write boundary via OrgAdminGuard | ✓ VERIFIED | `apps/api/src/auth/guards/org-admin.guard.ts` queries `Member{userId, organizationId, role:'admin'}`; UsersController applies `@UseGuards(OrgAdminGuard)`. `tests/users/org-admin-guard.test.ts` 5/5 pass including cross-tenant rejection (T-999.1-03). |
| T4 | Tenant portal can detect caller's Member.role via GET /api/organizations/:orgId/members/me | ✓ VERIFIED | `apps/api/src/users/members.controller.ts` under `AuthGuard` returns `UsersService.getCallerMembership`. `members-me.test.ts` 5/5 pass (401/404/role variants). `use-current-role.ts` consumes the endpoint. |

**Score:** 4/4 truths verified.

---

## 2. Required Artifacts

| Artifact | Expected | Exists | Substantive | Wired | Status |
|----------|----------|--------|-------------|-------|--------|
| `apps/api/src/auth/guards/org-admin.guard.ts` | Guard: super-admin bypass + Member.role='admin' match for :orgId | YES | YES (72 LOC, full error paths) | YES (imported in UsersController) | ✓ VERIFIED |
| `apps/api/src/users/members.controller.ts` | GET /api/organizations/:orgId/members/me under AuthGuard | YES | YES (55 LOC) | YES (registered in UsersModule) | ✓ VERIFIED |
| `apps/api/src/users/users.service.ts` → `getCallerMembership` | New service method | YES | YES (returns 404 on no-match) | YES (called by MembersController) | ✓ VERIFIED |
| `apps/web/src/components/nav/platform-nav.tsx` | 6-item platform sidebar | YES | YES | YES (used by /admin/layout) | ✓ VERIFIED |
| `apps/web/src/components/nav/tenant-nav.tsx` | Role-matrix + feature-filtered tenant sidebar | YES | YES (role matrix per D-11) | YES (used by /app/layout) | ✓ VERIFIED |
| `apps/web/src/components/nav/nav-shell.tsx` | Shared shell w/ active-state, mobile Sheet, user dropdown | YES | YES | YES | ✓ VERIFIED |
| `apps/web/src/hooks/use-current-role.ts` | Resolves userRole + memberRole + activeOrg | YES | YES (fetches /members/me + org.list) | YES | ✓ VERIFIED |
| `apps/web/src/app/app/layout.tsx` | Tenant portal guard + shell | YES | YES (3-phase guard, admin→/admin, zero-org sign-out) | YES | ✓ VERIFIED |
| `apps/web/src/app/admin/layout.tsx` | Platform portal guard (modified) | YES | YES (non-admin→/app/dashboard) | YES | ✓ VERIFIED |
| `apps/web/src/app/page.tsx` | Root role-aware redirector | YES | YES (3 branches) | YES | ✓ VERIFIED |
| `apps/web/src/app/app/page.tsx` | /app → /app/dashboard redirect | YES | YES | YES | ✓ VERIFIED |
| `apps/web/src/app/(auth)/sign-in/page.tsx` | Routes non-admin → /app/dashboard (D-21) | YES | YES (verified by grep on line 76) | YES | ✓ VERIFIED |
| `apps/web/src/app/(dashboard)/*` | Deleted | N/A | — | — | ✓ VERIFIED (absent) |
| 12 × `apps/web/src/components/pages/tenant-*-page.tsx` | Single extraction point for all tenant pages | YES | YES (all 12 present) | YES (re-exported by /app/*/page.tsx) | ✓ VERIFIED |
| 13 × `apps/web/src/app/app/{route}/page.tsx` | Re-exports or feature-gated wrappers | YES | YES | YES (zero imports from /app/admin/*) | ✓ VERIFIED |
| `apps/web/src/components/feature-gate-empty-state.tsx` | UI-SPEC copy for disabled-feature state | YES | YES | YES | ✓ VERIFIED |
| `/admin/dashboard` + `/admin/audit-log` | Platform placeholder stubs | YES | INTENTIONAL STUB (per plan) | YES | ⚠️ INTENTIONAL STUB (user decision §3) |
| 10 × `/admin/{tenant-route}/page.tsx` | Redirect stubs to /app/* | YES | YES | YES | ✓ VERIFIED |
| `/admin/users/page.tsx` + components | Platform Users page (AUTH-03/04) | YES | YES (aggregates across orgs) | YES | ✓ VERIFIED |
| `/app/team/page.tsx` + components | Tenant Team page (TENANT-05) | YES | YES (role-gated, empty-state, dialog) | YES | ✓ VERIFIED |
| `apps/web/src/lib/validators/create-user.ts` | Shared Zod validator | YES | YES | YES | ✓ VERIFIED |

---

## 3. Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| `/app/layout.tsx` → `TenantNav` | `useCurrentRole()` → `GET /api/organizations/:orgId/members/me` | apiFetch in `use-current-role.ts` | ✓ WIRED |
| `TenantNav` → feature filtering | `useFeatures(activeOrgId)` → `/api/features/check` | Phase 07 hook | ✓ WIRED |
| `UsersController` (writes) → `OrgAdminGuard` | `@UseGuards(OrgAdminGuard)` at class level | `users.controller.ts:25` | ✓ WIRED |
| `MembersController` → `UsersService.getCallerMembership` | Controller method | `members.controller.ts:52` | ✓ WIRED |
| `/admin/{tenant-route}` → `/app/{tenant-route}` | `router.replace(...)` in 10 stub files | grep-confirmed | ✓ WIRED |
| Sign-in (non-admin path) → `/app/dashboard` | `router.push("/app/dashboard")` | `(auth)/sign-in/page.tsx:76` | ✓ WIRED |
| Root `/` → role-based redirect | `router.replace(role==='admin' ? '/admin' : '/app')` | `app/page.tsx:18` | ✓ WIRED |
| `/app/team` create dialog → API | POST `/api/organizations/${activeOrgId}/users` | `add-team-member-dialog.tsx` + OrgAdminGuard | ✓ WIRED |
| `/admin/users` create dialog → API | POST `/api/organizations/${organizationId}/users` (super-admin bypass) | `create-platform-user-dialog.tsx` + OrgAdminGuard | ✓ WIRED |

---

## 4. Data-Flow Trace (Level 4)

| Artifact | Data | Source | Flows? |
|----------|------|--------|--------|
| `TenantNav` | `memberRole`, `activeOrgId`, features | `use-current-role` + `use-features` — both fetch live endpoints | ✓ FLOWING |
| `/app/team` table | `members` | `apiFetch('/api/organizations/:orgId/users')` — authoritative DB query via `UsersService.listMembers` | ✓ FLOWING |
| `/admin/users` table | aggregated users | `authClient.organization.list()` → per-org `apiFetch('/api/organizations/:orgId/users')` | ✓ FLOWING |
| `PlatformNav` | static 6-item list | Static — no data fetch needed for platform sidebar | N/A |
| `/admin/dashboard` | placeholder content | Intentional stub per user decision §3 | ⚠️ STATIC (intentional) |
| `/admin/audit-log` | placeholder content | Intentional stub | ⚠️ STATIC (intentional) |

---

## 5. Behavioral Spot-Checks

| Behavior | Check | Result |
|----------|-------|--------|
| API module registers MembersController | grep `users.module.ts` | ✓ PASS (`controllers: [UsersController, MembersController]`) |
| UsersController uses OrgAdminGuard | grep `users.controller.ts` | ✓ PASS (`@UseGuards(OrgAdminGuard)`) |
| No stray references to `/dashboard` literal | `grep '"/dashboard"' apps/web/src` | ✓ PASS (0 hits, per Plan 02) |
| `/app/*` never imports from `/admin/*` | `grep -r '@/app/admin/' apps/web/src/app/app/` | ✓ PASS (0 hits) |
| API test suite `tests/users/` | `pnpm --filter api test -- --run tests/users/` | ✓ PASS 17/17 |
| Web test suite | `pnpm --filter web test -- --run` | ⚠️ 10/27 pass, 17/27 fail — see F-1 (test-side staleness, not impl defect) |
| API tsc | `pnpm --filter api exec tsc --noEmit` | ⚠️ 9 errors — all pre-existing in cluster/recordings/tenancy (NOT touched by this phase) |
| Web tsc | `pnpm --filter web exec tsc --noEmit` | ⚠️ +2 new errors (same zod-v4/@hookform overload pattern as 5 pre-existing; see F-2) |

---

## 6. Requirements Coverage

| Req | Description | Source Plan | Status | Evidence |
|-----|-------------|-------------|--------|----------|
| AUTH-03 | Admin can list users across organizations | 999.1-04 | ✓ SATISFIED | `/admin/users/page.tsx` aggregates `authClient.organization.list() × GET /api/organizations/:orgId/users` |
| AUTH-04 | Admin can create users (platform) | 999.1-04 | ✓ SATISFIED | `CreatePlatformUserDialog` POSTs to `/api/organizations/:orgId/users`; OrgAdminGuard super-admin bypass allows cross-org create |
| TENANT-04 | Cross-tenant isolation (writes) | 999.1-01 | ✓ SATISFIED | OrgAdminGuard rejects org admin of org A accessing org B (test `rejects org admin from accessing a DIFFERENT org` green). UsersController now class-scoped under OrgAdminGuard. |
| TENANT-05 | Org Admin can CRUD their team | 999.1-04 (+01) | ✓ SATISFIED | `/app/team/page.tsx` + `AddTeamMemberDialog` + `TeamTable.Remove`; enum restricted to Member roles (no `User.role=admin` minting); self-removal hidden |

No orphaned requirements. REQUIREMENTS.md mappings for AUTH-03/04/TENANT-04/05 consumed across plans 01 + 04.

---

## 7. Anti-Pattern Scan

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `/admin/dashboard/page.tsx`, `/admin/audit-log/page.tsx` | "future release" placeholder copy | ℹ️ INFO | Intentional stub per user decision §3; not a defect |
| `/app/sites/page.tsx` | "ships in a follow-up phase" placeholder | ℹ️ INFO | Intentional — prevents 404 on TenantNav Sites link |
| No `TODO/FIXME/HACK` in new files | — | — | Clean |
| No empty handlers (`() => {}`) in new files | — | — | Clean |
| No `console.log`-only handlers | — | — | Clean |

---

## 8. Threat Mitigations (from Plan frontmatter)

| Threat | Mitigation | Verified |
|--------|------------|----------|
| T-999.1-01 (wrong portal) | /admin guards non-admin → /app/dashboard; /app guards admin → /admin | ✓ (code inspection + Plan 02 grep) |
| T-999.1-02 (unauthorised nav hint) | TenantNav uses `permitted.includes(href)` allowlist + feature intersection | ✓ (5/6 automated tenant-nav tests green, 1 stale) |
| T-999.1-03 (cross-tenant write) | OrgAdminGuard queries Member with `{userId, orgId, role:'admin'}` | ✓ (`org-admin-guard.test.ts` 5/5 green) |
| T-999.1-04 (info disclosure on /members/me) | userId from session, 404 on non-member | ✓ (`members-me.test.ts` 5/5 green) |
| T-999.1-10 (role escalation to User.role=admin) | CreateUserSchema enum = Member roles only | ✓ (`create-user.ts`) |
| T-999.1-11 (self-removal) | TeamTable hides Remove when `member.userId === currentUserId` | ✓ (code inspection) |

---

## 9. Status Determination

- All 4 observable truths: ✓ VERIFIED
- All declared requirements (AUTH-03/04, TENANT-04/05): ✓ SATISFIED
- Key links: ✓ WIRED
- Data flow (Level 4): ✓ FLOWING (2 intentional static stubs)
- Automated tests: API 17/17 green; Web 10/27 green (17 stale — see F-1)
- tsc delta: +2 files on a pre-existing repo-wide zod v4 incompatibility axis (F-2)
- Visual / end-to-end flows not covered by unit tests → human verification section non-empty

Per Step 9 decision tree: **`human_needed`** — all programmatic checks that address the phase goal pass, but the UI/E2E flows (sign-in → role-aware redirect → filtered sidebar) require human confirmation, and the stale Wave 0 tests should be rewritten before they can provide regression value.

---

## 10. Gaps Summary

**No blocking gaps.** The phase delivered exactly what the goal demanded: a two-portal SaaS shell with role-based sidebars, backend guard enforcement, and a caller-role lookup endpoint. Two flags remain:

- **F-1 (test staleness):** 17 Wave 0 web tests assert a server-redirect / older-copy UI; the implementation chose client-redirect + verbatim-UI-SPEC copy. The implementation is correct per UI-SPEC; tests need rewriting. Plan 04 summary already flagged this as a follow-up.
- **F-2 (+2 tsc errors):** The two new dialog files share a pre-existing repo-wide zod-v4 / @hookform/resolvers-3 overload mismatch. Runtime behaviour is unaffected; recommend a single cross-cutting fix for all 7 call sites.

Neither flag blocks the phase goal. Recommend the orchestrator open a `/gsd-quick` task pair:
1. Refresh Wave 0 web tests to match UI-SPEC verbatim strings and client-redirect pattern.
2. Unify @hookform/resolvers pinning across all 7 zodResolver call sites.

---

_Verified: 2026-04-15T18:25:00Z_
_Verifier: Claude (gsd-verifier)_
