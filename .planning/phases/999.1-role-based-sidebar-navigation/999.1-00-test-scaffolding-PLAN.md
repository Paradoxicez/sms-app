---
phase: 999.1
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/test-utils/mock-auth-client.ts
  - apps/web/src/test-utils/mock-use-features.ts
  - apps/web/src/__tests__/sign-in-redirect.test.tsx
  - apps/web/src/__tests__/admin-layout.test.tsx
  - apps/web/src/__tests__/app-layout.test.tsx
  - apps/web/src/__tests__/tenant-nav.test.tsx
  - apps/web/src/__tests__/platform-nav.test.tsx
  - apps/web/src/__tests__/recordings-feature-gate.test.tsx
  - apps/web/src/__tests__/root-route.test.tsx
  - apps/web/src/__tests__/team-page.test.tsx
  - apps/web/src/__tests__/platform-users-page.test.tsx
  - apps/api/tests/users/members-me.test.ts
  - apps/api/tests/users/org-admin-guard.test.ts
autonomous: true
requirements:
  - AUTH-03
  - AUTH-04
  - TENANT-04
  - TENANT-05
must_haves:
  truths:
    - "Failing test stubs exist for every Nyquist-required behavior (sign-in redirect, admin layout guard, app layout guard, tenant nav role/feature filter, platform nav, root redirector, recordings feature gate, members/me endpoint, OrgAdminGuard)"
    - "Mock utilities exist for authClient session and useFeatures hook so downstream test files can share stubs"
  artifacts:
    - path: "apps/web/src/test-utils/mock-auth-client.ts"
      provides: "Shared session/org mock for web tests"
    - path: "apps/web/src/test-utils/mock-use-features.ts"
      provides: "Shared feature-flag mock"
    - path: "apps/web/src/__tests__/sign-in-redirect.test.tsx"
      provides: "D-02/D-21 — role-aware post-login redirect"
    - path: "apps/web/src/__tests__/admin-layout.test.tsx"
      provides: "D-22 — /admin rejects non-admins"
    - path: "apps/web/src/__tests__/app-layout.test.tsx"
      provides: "D-22 — /app rejects admins + bootstraps active org"
    - path: "apps/web/src/__tests__/tenant-nav.test.tsx"
      provides: "D-11 + D-13 — role & feature filtering of tenant nav"
    - path: "apps/web/src/__tests__/platform-nav.test.tsx"
      provides: "D-04 — platform nav exactly 6 items"
    - path: "apps/web/src/__tests__/recordings-feature-gate.test.tsx"
      provides: "UAT-8 — feature-disabled page shows empty state"
    - path: "apps/web/src/__tests__/root-route.test.tsx"
      provides: "D-23 — / redirects by role"
    - path: "apps/web/src/__tests__/team-page.test.tsx"
      provides: "TENANT-05 — Org Admin create-member dialog copy + no User.role=admin option"
    - path: "apps/web/src/__tests__/platform-users-page.test.tsx"
      provides: "AUTH-03/AUTH-04 — Super Admin users list + role filter"
    - path: "apps/api/tests/users/members-me.test.ts"
      provides: "GET /orgs/:id/members/me returns caller Member.role"
    - path: "apps/api/tests/users/org-admin-guard.test.ts"
      provides: "OrgAdminGuard allows org admin, rejects non-members (T-999.1-03)"
  key_links:
    - from: "Wave 1 implementation tasks"
      to: "Wave 0 test files"
      via: "vitest runs against implementation"
      pattern: "pnpm --filter (web|api) test -- --run <file>"
---

<objective>
Create all failing test stubs and shared mocks required by VALIDATION.md so every
Wave 1+ task has a <verify><automated> command that maps to an existing test file.

Purpose: Nyquist compliance — no implementation task may ship without a failing test
to turn green. This plan also seeds shared mocks (authClient, useFeatures) so later
test files stay DRY.
Output: 13 files (2 utilities + 11 tests). All tests MUST fail for the right reason
(module not found / behavior not implemented) — NOT for syntax errors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/999.1-role-based-sidebar-navigation/999.1-CONTEXT.md
@.planning/phases/999.1-role-based-sidebar-navigation/999.1-RESEARCH.md
@.planning/phases/999.1-role-based-sidebar-navigation/999.1-UI-SPEC.md
@.planning/phases/999.1-role-based-sidebar-navigation/999.1-VALIDATION.md

<interfaces>
<!-- Existing assets Wave 0 stubs will mock/reference -->

From apps/api/src/users/dto/create-user.dto.ts:
```typescript
export const CreateUserSchema = z.object({
  email: z.string().email(),
  name: z.string().min(1).max(200),
  password: z.string().min(8).max(128),
  role: z.enum(['admin', 'operator', 'developer', 'viewer']).default('viewer'),
});
```

From apps/web/src/hooks/use-features.ts:
```typescript
export function useFeatures(orgId: string | null | undefined): {
  features: Record<string, boolean>;
  isEnabled: (key: string) => boolean;
  loading: boolean;
  error: string | null;
};
```

From apps/web/src/lib/auth-client.ts (Better Auth):
```typescript
authClient.getSession(): Promise<{ data: { user: { id, email, name, role: 'admin'|'user' }, session: { activeOrganizationId: string | null } } | null }>
authClient.organization.list(): Promise<{ data: Array<{ id: string; name: string }> }>
authClient.organization.setActive(args: { organizationId: string }): Promise<unknown>
authClient.signOut(): Promise<unknown>
```

From apps/api/src/prisma/schema.prisma:
- User.role: "admin" | "user"
- Member.role: "admin" | "operator" | "developer" | "viewer"
- Session.activeOrganizationId: string | null
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared mock utilities for web tests</name>
  <read_first>
    - apps/web/src/lib/auth-client.ts (to mirror Better Auth API shape)
    - apps/web/src/hooks/use-features.ts (to mirror useFeatures signature)
    - apps/web/vitest.config.ts (to confirm jsdom + path aliases)
    - .planning/phases/999.1-role-based-sidebar-navigation/999.1-VALIDATION.md (mock requirements)
  </read_first>
  <files>apps/web/src/test-utils/mock-auth-client.ts, apps/web/src/test-utils/mock-use-features.ts</files>
  <action>
    Create `apps/web/src/test-utils/mock-auth-client.ts` exporting:
    - `createMockSession(opts: { userRole?: 'admin'|'user'; activeOrgId?: string|null; memberships?: Array<{id:string;name:string}> })` that returns a shape matching `authClient.getSession()` return value.
    - `mockAuthClient` — a vi-mocked module fixture exposing `getSession`, `organization.list`, `organization.setActive`, `signOut` as vi.fn() with sensible defaults. Helper `resetAuthMocks()` to clear between tests.

    Create `apps/web/src/test-utils/mock-use-features.ts` exporting:
    - `createMockFeatures(overrides?: Record<string, boolean>)` returning a full feature map with defaults `{ recordings: true, webhooks: true, map: true, apiKeys: true, auditLog: true }`.
    - `mockUseFeatures(features: Record<string,boolean>)` — vi-mocked hook return `{ features, isEnabled: (k)=>!!features[k], loading: false, error: null }`.

    Both files must be pure TS (no JSX). Import `vi` from "vitest". Export named functions only. Do NOT call `vi.mock()` at module scope — that's the test file's job. Keep under 80 lines each.
  </action>
  <verify>
    <automated>pnpm --filter @sms-platform/web exec tsc --noEmit apps/web/src/test-utils/mock-auth-client.ts apps/web/src/test-utils/mock-use-features.ts 2>&1 | head -20 || true; test -f apps/web/src/test-utils/mock-auth-client.ts && test -f apps/web/src/test-utils/mock-use-features.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist and export named helpers as specified
    - `grep -l createMockSession apps/web/src/test-utils/mock-auth-client.ts` succeeds
    - `grep -l createMockFeatures apps/web/src/test-utils/mock-use-features.ts` succeeds
    - No `vi.mock(` calls at module top-level (reserved for test files)
    - TypeScript compiles (no `any` leaks — use `unknown` if needed)
  </acceptance_criteria>
  <done>Shared mock utilities compile and export the specified helpers; downstream test files can import them.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create failing web test stubs (9 files)</name>
  <read_first>
    - apps/web/src/test-utils/mock-auth-client.ts (created in Task 1)
    - apps/web/src/test-utils/mock-use-features.ts (created in Task 1)
    - apps/web/src/__tests__/ (sibling tests for RTL pattern) — list existing ones for style
    - .planning/phases/999.1-role-based-sidebar-navigation/999.1-VALIDATION.md Per-Task Verification Map
    - .planning/phases/999.1-role-based-sidebar-navigation/999.1-UI-SPEC.md (nav item lists for assertions)
  </read_first>
  <files>apps/web/src/__tests__/sign-in-redirect.test.tsx, apps/web/src/__tests__/admin-layout.test.tsx, apps/web/src/__tests__/app-layout.test.tsx, apps/web/src/__tests__/tenant-nav.test.tsx, apps/web/src/__tests__/platform-nav.test.tsx, apps/web/src/__tests__/recordings-feature-gate.test.tsx, apps/web/src/__tests__/root-route.test.tsx, apps/web/src/__tests__/team-page.test.tsx, apps/web/src/__tests__/platform-users-page.test.tsx</files>
  <action>
    Create 9 Vitest + React Testing Library test files. Each file MUST fail on first run because the module it imports does not yet exist. Use `vi.mock("@/lib/auth-client", ...)` + `vi.mock("@/hooks/use-features", ...)` + helpers from Task 1.

    Required test cases per file (use `describe`/`it` with verbatim names so later tasks can target them):

    1. `sign-in-redirect.test.tsx` — imports `@/app/(auth)/sign-in/page`:
       - it("redirects admin to /admin after login")  — asserts `router.push("/admin")` with User.role='admin'
       - it("redirects non-admin to /app/dashboard after login") — asserts `router.push("/app/dashboard")` with User.role='user'

    2. `admin-layout.test.tsx` — imports `@/app/admin/layout`:
       - it("renders children for User.role=admin")
       - it("redirects User.role=user to /app/dashboard")
       - it("redirects unauthenticated to /sign-in")

    3. `app-layout.test.tsx` — imports `@/app/app/layout`:
       - it("renders children for User.role=user with active org")
       - it("redirects User.role=admin to /admin")
       - it("calls organization.setActive with first org when activeOrganizationId is null")
       - it("redirects to /sign-in with toast when user has zero organizations")

    4. `tenant-nav.test.tsx` — imports `@/components/nav/tenant-nav`:
       - it("Org Admin sees all 12 nav items when all features enabled") — assert labels from UI-SPEC
       - it("Operator sees exactly {Dashboard, Cameras, Map, Recordings, Audit Log}") — D-11
       - it("Developer sees exactly {Dashboard, Cameras, Map, API Keys, Webhooks, Audit Log}") — D-11
       - it("Viewer sees exactly {Dashboard, Cameras, Map, Recordings, Audit Log}") — D-11
       - it("hides Recordings when features.recordings=false") — D-13
       - it("hides API Keys + Webhooks when respective features disabled") — D-14

    5. `platform-nav.test.tsx` — imports `@/components/nav/platform-nav`:
       - it("renders exactly 6 items: Organizations, Packages, Cluster Nodes, SRS Engine, Platform Audit, Users") — D-04
       - it("does NOT render tenant items (Cameras, Projects, Recordings, etc)") — D-06
       - it("header badge reads 'Platform'") — UI-SPEC

    6. `recordings-feature-gate.test.tsx` — imports `@/app/app/recordings/page`:
       - it("renders empty state 'Recordings are not included in your plan' when features.recordings=false") — UAT-8 / UI-SPEC verbatim copy
       - it("renders the recordings page content when features.recordings=true")

    7. `root-route.test.tsx` — imports `@/app/page`:
       - it("redirects User.role=admin to /admin")
       - it("redirects User.role=user to /app")
       - it("redirects unauthenticated to /sign-in") — D-23 replacement

    8. `team-page.test.tsx` — imports `@/app/app/team/page`:
       - it("renders 'Add Team Member' button for Org Admin") — assert button text verbatim per UI-SPEC
       - it("opens create dialog with title 'Add Team Member' and role options limited to admin|operator|developer|viewer") — assert NO `User.role=admin` platform option
       - it("submits POST /api/organizations/:orgId/users with Member role and shows success toast") — happy path + verbatim toast copy 'User created. They can sign in now.'
       - it("hides 'Add Team Member' button for operator/developer/viewer roles") — D-11 role gate

    9. `platform-users-page.test.tsx` — imports `@/app/admin/users/page`:
       - it("renders users table with columns per UI-SPEC: Email, Name, Role, Org, Created At") — assert verbatim column headers
       - it("renders role filter with options {admin, operator, developer, viewer}") — role filter exists
       - it("filters list when role filter changes") — asserts fetch param includes role filter
       - it("opens 'Add Platform User' dialog with User.role enum {admin, user}") — distinguishes from Team dialog

    Every test uses `@testing-library/react` `render` + `vi.mock("next/navigation", ...)` with `useRouter`. Use `waitFor` for assertions after effects. Import `createMockSession` / `createMockFeatures` from test-utils.

    Each file must be runnable standalone — full vitest run on each must error with "Cannot find module" or "not a function" (proving module absence), NOT a syntax error.
  </action>
  <verify>
    <automated>for f in sign-in-redirect admin-layout app-layout tenant-nav platform-nav recordings-feature-gate root-route team-page platform-users-page; do test -f "apps/web/src/__tests__/$f.test.tsx" || { echo "missing $f"; exit 1; }; done; echo ALL_PRESENT; pnpm --filter @sms-platform/web test -- --run src/__tests__/platform-nav.test.tsx 2>&1 | tail -10 | grep -E "(FAIL|Cannot find)" && echo EXPECTED_FAIL</automated>
  </verify>
  <acceptance_criteria>
    - All 9 files exist at the specified paths
    - Running `pnpm --filter web test -- --run src/__tests__/platform-nav.test.tsx` exits non-zero with "Cannot find module '@/components/nav/platform-nav'" or equivalent
    - Running `pnpm --filter web test -- --run src/__tests__/tenant-nav.test.tsx` fails for the same reason
    - `grep -c "it(" apps/web/src/__tests__/tenant-nav.test.tsx` returns >= 6
    - `grep "Recordings are not included in your plan" apps/web/src/__tests__/recordings-feature-gate.test.tsx` matches (verbatim copy per UI-SPEC)
    - Each test file imports from `@/test-utils/mock-auth-client` OR `@/test-utils/mock-use-features`
  </acceptance_criteria>
  <done>All 9 web test files exist and fail for the correct reason (missing implementation module), ready for Wave 1 to turn green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create failing API test stubs (members/me + OrgAdminGuard)</name>
  <read_first>
    - apps/api/tests/users/org-user-management.test.ts (style reference for NestJS integration tests)
    - apps/api/src/users/users.controller.ts (endpoint shape + SuperAdminGuard pattern)
    - apps/api/src/auth/guards/super-admin.guard.ts (guard pattern to mirror)
    - apps/api/src/prisma/schema.prisma (Member model fields: id, userId, organizationId, role)
    - .planning/phases/999.1-role-based-sidebar-navigation/999.1-VALIDATION.md rows TBD-08, TBD-09, TBD-12
  </read_first>
  <files>apps/api/tests/users/members-me.test.ts, apps/api/tests/users/org-admin-guard.test.ts</files>
  <action>
    Create 2 Vitest integration test files following the pattern in `apps/api/tests/users/org-user-management.test.ts` (bootstrap Nest app, use PrismaService, seed test org + users, issue HTTP requests via supertest).

    `apps/api/tests/users/members-me.test.ts` — targets `GET /api/organizations/:orgId/members/me`:
    - it("returns 401 when no session")
    - it("returns 404 when caller is not a member of :orgId")
    - it("returns { role: 'admin', userId, organizationId } when caller is Org Admin in :orgId")
    - it("returns { role: 'operator' } for an operator member")
    - it("returns { role: 'viewer' } for a viewer member")

    `apps/api/tests/users/org-admin-guard.test.ts` — targets OrgAdminGuard behaviour via `POST /api/organizations/:orgId/users`:
    - it("allows super admin (User.role=admin) to create user in any org") — existing behavior preserved
    - it("allows org admin (Member.role=admin in :orgId) to create user in own org") — D-19
    - it("rejects org admin from creating user in a DIFFERENT org (T-999.1-03)") — cross-tenant write blocked → 403
    - it("rejects operator/developer/viewer from creating user in own org") — 403
    - it("rejects unauthenticated with 401")

    Both files MUST fail on first run because the endpoint / guard is not yet implemented. Use the existing test seed helpers if present (check `apps/api/tests/helpers/`); otherwise inline the seed calls using PrismaService.

    Register these in users.module test context — if users.controller.ts currently rejects org admins with 401 via SuperAdminGuard, the "allows org admin" case will fail with 401, which is the expected initial red.
  </action>
  <verify>
    <automated>test -f apps/api/tests/users/members-me.test.ts && test -f apps/api/tests/users/org-admin-guard.test.ts && pnpm --filter @sms-platform/api test -- --run tests/users/members-me.test.ts 2>&1 | tail -20 | grep -E "(FAIL|404|Cannot)" && echo EXPECTED_FAIL</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist at specified paths
    - `grep -c "it(" apps/api/tests/users/org-admin-guard.test.ts` returns >= 5
    - `grep "members/me" apps/api/tests/users/members-me.test.ts` matches
    - `grep -i "cross-tenant\|different org\|T-999.1-03" apps/api/tests/users/org-admin-guard.test.ts` matches (documents threat)
    - Running the members-me test produces a failing result (endpoint 404) — NOT syntax error
  </acceptance_criteria>
  <done>Both API test files exist and fail for the correct reason (endpoint/guard not yet implemented), ready for Plan 01 to turn green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → production code | Tests exercise prod modules; no untrusted input — scaffolding only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-999.1-01 | E (Elevation of privilege) | test stubs | accept | Test files are dev-only, never shipped. Risk: none. |
| T-999.1-02 | I (Information disclosure) | mock-auth-client | accept | Mock returns fabricated UUIDs; no real secrets embedded. Verified via grep for known credential patterns. |
| T-999.1-03 | T (Tampering) | test fixtures | accept | Prisma test DB is ephemeral (docker-compose); isolated from prod. |
</threat_model>

<verification>
- All 13 files created
- 9 web tests fail with "module not found" for missing implementations
- 2 api tests fail with 401/404/403 for not-yet-implemented endpoint+guard
- Mock utilities compile cleanly under tsc
- No watch-mode flags in any verify command
</verification>

<success_criteria>
- Wave 0 complete: every Wave 1+ task references an existing test file
- VALIDATION.md `wave_0_complete` can flip to `true`
- Every Nyquist-mapped behavior (TBD-01 through TBD-12) has a failing red test
</success_criteria>

<output>
After completion, create `.planning/phases/999.1-role-based-sidebar-navigation/999.1-00-SUMMARY.md`
</output>
