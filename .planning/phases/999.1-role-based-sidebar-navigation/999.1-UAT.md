---
status: complete
phase: 999.1-role-based-sidebar-navigation
source: [999.1-VERIFICATION.md human_verification section]
started: 2026-04-15T19:10:00Z
updated: 2026-04-15T20:30:00Z
---

## Current Test

[testing complete — 8 passed, 1 partial (minor UX gaps logged)]

## Inline Fixes During UAT

All committed on main:
- /admin/users used authClient.organization.list (Member-scoped) instead of /api/admin/organizations — blocked Org Admin creation
- UsersService.createUser stored SHA-256 hex instead of Better Auth scrypt hash — login 500
- UsersService.createUser left User.role unset → schema default "viewer" (should be "user")
- useCurrentRole fetched memberRole only when userRole === "user" — missed "viewer" default, crashed TenantNav
- GET /api/organizations/:orgId/features was SuperAdminGuard-only — tenant feature gate stayed hidden
- /admin and /app layouts flipped loading=false in finally — caused React hook-order crash during role redirect
- Throttle ceilings 100/60/30 per min too low for UAT — raised to 2000/1000/500 in non-prod

---

## Previous Current Test (archived)

number: 2
name: Org Admin Sign-in → /app/dashboard
expected: |
  Sign in as an Org Admin user.
  Landing: /app/dashboard
  Sidebar: TenantNav with all 4 groups (Overview, Cameras, Organization, Developer) + Team link
  Badge: org name (truncated)
awaiting: user response

## Tests

### 1. Super Admin Sign-in → /admin portal
expected: Sign in as admin@superadmin.local. Land on /admin with PlatformNav (6 items) + "Platform" badge. No tenant links.
result: pass

### 2. Org Admin Sign-in → /app/dashboard
expected: Sign in as an Org Admin. Land on /app/dashboard with TenantNav (all 4 groups: Overview, Cameras, Organization, Developer) + Team link + badge shows org name (truncated).
result: pass

### 3. Operator Sign-in → TenantNav filtered
expected: Sign in as Operator. TenantNav shows only {Dashboard, Cameras, Map, Recordings, Audit Log}. Team and Developer links hidden.
result: pass

### 4. Direct /app/team as Operator — role-gated empty state
expected: Lock icon + "You do not have access to this page" + "Team management is restricted to organization admins."
result: pass

### 5. Direct /admin as non-admin → redirect to /app/dashboard
expected: Navigating to /admin while signed in as Org Admin (or any non-super-admin) redirects to /app/dashboard.
result: pass

### 6. Direct /app as super admin → redirect to /admin
expected: Super admin never sees tenant shell; navigating /app redirects to /admin.
result: pass

### 7. Org Admin creates team member via /app/team
expected: Add Team Member dialog submits → POST /api/organizations/:orgId/users succeeds → toast "User created. They can sign in now." → table refetches and shows new member.
result: pass

### 8. Super Admin creates platform user via /admin/users
expected: Create User dialog → POST succeeds → aggregated users list refreshes → new user appears in correct org(s).
result: issue
reported: "(A-minor) Dialog copy mismatch: title says 'Create a super admin or org admin' but Role dropdown is Member-only roles {admin, operator, developer, viewer}. (B-minor) Organization dropdown defaults to 'System' platform-internal org. (C-blocker, RESOLVED) Organization dropdown was filtered to orgs where super admin is a Member (System only), making tenant Org Admin creation impossible — fixed in this session by switching to /api/admin/organizations endpoint. After fix: submit succeeds, user appears in Test Org."
severity: minor

### 9. Visual UI-SPEC parity
expected: Green theme, active-nav border present, mobile 56px top bar with Sheet nav. Matches UI-SPEC screenshots.
result: pass

## Summary

total: 9
passed: 8
issues: 1
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Create Platform User dialog correctly describes its capabilities and defaults appropriately for Org Admin creation flow"
  status: failed
  reason: "Dialog title 'Create a super admin or org admin' mismatches Role dropdown (Member-only roles). Organization default = 'System' is platform-internal org, confusing for tenant Org Admin creation."
  severity: minor
  test: 8
  artifacts:
    - path: "apps/web/src/app/admin/users/components/create-platform-user-dialog.tsx"
      issue: "Copy mismatch + wrong default org selection"
  missing:
    - "Update dialog description to reflect actual capability (create org members with cross-org privilege) — remove 'super admin' claim"
    - "Default Organization to first non-System org, or require explicit selection (no preselected value)"
    - "Optionally hide System from the Organization dropdown unless explicitly needed"
