---
phase: 27-caddy-reverse-proxy-auto-tls
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/.env.production.example
  - deploy/docker-compose.yml
autonomous: true
requirements:
  - DEPLOY-06
  - DEPLOY-09
must_haves:
  truths:
    - "deploy/.env.production.example contains ACME_EMAIL in Section 1 (Required, no default)"
    - "deploy/.env.production.example contains ACME_CA in Section 3 (Defaults, override-only)"
    - "deploy/.env.production.example contains MINIO_PUBLIC_URL in Section 1 (Required for prod TLS — wires plan 27-03 fix)"
    - "deploy/docker-compose.yml api service env block exports MINIO_PUBLIC_URL (consumed by MinioService.buildPublicUrl from plan 27-03)"
    - "init-secrets.sh is NOT modified (D-20 — ACME_EMAIL is human input, not random secret; MINIO_PUBLIC_URL derives from DOMAIN)"
    - "`docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config --quiet` exits 0"
  artifacts:
    - path: "deploy/.env.production.example"
      provides: "Production env template with 3 new variables: ACME_EMAIL (required), ACME_CA (optional), MINIO_PUBLIC_URL (required for TLS-served pages)"
      contains: "ACME_EMAIL=, ACME_CA=, MINIO_PUBLIC_URL="
    - path: "deploy/docker-compose.yml"
      provides: "api service env block wires MINIO_PUBLIC_URL through to MinioService"
      contains: "MINIO_PUBLIC_URL"
  key_links:
    - from: "deploy/.env.production.example ACME_EMAIL"
      to: "deploy/Caddyfile global block `email {$ACME_EMAIL}`"
      via: "compose env-file → caddy service environment block (plan 27-02) → Caddyfile env-var substitution at load time"
      pattern: "ACME_EMAIL"
    - from: "deploy/.env.production.example ACME_CA"
      to: "deploy/Caddyfile global block `acme_ca {$ACME_CA:default}`"
      via: "compose env-file → caddy service environment block → Caddyfile (empty = prod CA, set = staging override)"
      pattern: "ACME_CA"
    - from: "deploy/.env.production.example MINIO_PUBLIC_URL"
      to: "apps/api/src/recordings/minio.service.ts buildPublicUrl helper (plan 27-03)"
      via: "compose env-file → api service environment block → ConfigService.get('MINIO_PUBLIC_URL')"
      pattern: "MINIO_PUBLIC_URL"
---

<objective>
Patch `deploy/.env.production.example` to add the 3 new operator-supplied environment variables that Phase 27 introduces (`ACME_EMAIL`, `ACME_CA`, `MINIO_PUBLIC_URL`), AND wire `MINIO_PUBLIC_URL` through the compose api service environment block (so plan 27-03's MinioService refactor can read it at runtime).

Purpose: Closes the operator-facing surface for Phase 27 — every variable the Caddyfile and the MinioService need is now documented in the env template + threaded through compose. Without this plan, `docker compose up -d` either fails to start (empty `${DOMAIN}` / `${ACME_EMAIL}` cause Caddyfile load errors) or silently produces broken `http://` MinIO URLs.

Output: ~16 added lines in `.env.production.example` + 1 added line in `deploy/docker-compose.yml` api service env block. Zero existing values modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-caddy-reverse-proxy-auto-tls/27-CONTEXT.md
@.planning/phases/27-caddy-reverse-proxy-auto-tls/27-RESEARCH.md
@.planning/phases/26-production-compose-migrate-init-networking-volumes/26-CONTEXT.md
@CLAUDE.md
@deploy/.env.production.example
@deploy/docker-compose.yml

<interfaces>
Section structure of the existing .env.production.example (Phase 26 product). New entries MUST land in the correct section per D-19 + D-25.

Section 1: Required (no default)
- DOMAIN=
- DB_PASSWORD=change-me-db-password
- NEXTAUTH_SECRET=change-me-nextauth-secret
- BETTER_AUTH_SECRET=change-me-better-auth-secret
- MINIO_ROOT_PASSWORD=change-me-minio-root-password
- JWT_PLAYBACK_SECRET=change-me-jwt-playback-secret
- ADMIN_EMAIL=
- ADMIN_PASSWORD=change-me-admin-password
- (NEW Phase 27) ACME_EMAIL=          ← human-set, no default
- (NEW Phase 27) MINIO_PUBLIC_URL=    ← human-set (typically https://${DOMAIN})

Section 2: Image refs
- GHCR_ORG=
- IMAGE_TAG=latest

Section 3: Defaults (override-only)
- POSTGRES_USER=sms
- POSTGRES_DB=sms_platform
- MINIO_ROOT_USER=minioadmin
- REDIS_PASSWORD=
- (NEW Phase 27) ACME_CA=             ← optional, empty = prod CA

Section 4: Computed (commented out)
- DATABASE_URL, REDIS_URL, MINIO_ENDPOINT, MINIO_PORT, MINIO_USE_SSL

Compose api service environment block (deploy/docker-compose.yml lines ~187-209):
- Currently has MINIO_PUBLIC_ENDPOINT, MINIO_PUBLIC_PORT (from Phase 26).
- Plan 27-04 adds: `MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-}` — operator sets in `.env`, defaults to empty (legacy fallback path in MinioService kicks in for dev).

init-secrets.sh (deploy/scripts/init-secrets.sh — Phase 26 product):
- Pattern-matches `change-me-*` prefixes; ACME_EMAIL is empty (no prefix), MINIO_PUBLIC_URL is empty.
- D-20: do NOT add either to SECRET_VARS — neither is a random secret.
- → init-secrets.sh stays unmodified.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ACME_EMAIL + ACME_CA + MINIO_PUBLIC_URL to deploy/.env.production.example (D-19, D-26 hand-off)</name>
  <files>deploy/.env.production.example</files>
  <read_first>
    - deploy/.env.production.example (FULL — must understand the 4-section structure introduced by Phase 26 D-25; new vars MUST land in the correct section)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-CONTEXT.md (D-19, D-20 — locked content for the 2 new ACME vars + reason init-secrets.sh stays unmodified)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-03-PLAN.md (the MinioService refactor that consumes MINIO_PUBLIC_URL — confirms the env var name + expected value `https://${DOMAIN}`)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-RESEARCH.md (Code Examples §Example 3 — verbatim block content for ACME_EMAIL + ACME_CA)
    - .planning/phases/26-production-compose-migrate-init-networking-volumes/26-CONTEXT.md (D-25 — 4-section structure)
  </read_first>
  <action>
Apply three surgical patches to `deploy/.env.production.example`. DO NOT touch any existing variable line — only add new lines in the correct sections.

**Patch 1 — Add ACME_EMAIL to Section 1 (Required, no default).** Insert immediately after the `ADMIN_PASSWORD=change-me-admin-password` line (currently line 36) and BEFORE the `# === Section 2: Image refs ===` separator (currently line 38). Append exactly these 5 lines (with leading blank line for readability):

```
# Phase 27 — Let's Encrypt contact for cert renewal warning emails.
# Caddy registers an anonymous ACME account if empty; operator misses
# expiry notifications. See deploy/DOMAIN-SETUP.md.
ACME_EMAIL=

```

**Patch 2 — Add MINIO_PUBLIC_URL to Section 1 (Required, no default).** Insert immediately after the ACME_EMAIL block from Patch 1 (still in Section 1). Append exactly these 6 lines:

```
# Phase 27 — Public-facing prefix for avatars + snapshots URLs emitted by
# the api. In production with Caddy + TLS, set this to: https://${DOMAIN}
# (e.g. https://example.com — no trailing slash, no port). Empty value
# falls back to legacy MINIO_PUBLIC_ENDPOINT+MINIO_PUBLIC_PORT composition
# (used in dev). See apps/api/src/recordings/minio.service.ts.
MINIO_PUBLIC_URL=

```

**Patch 3 — Add ACME_CA to Section 3 (Defaults, override-only).** Insert immediately after the `REDIS_PASSWORD=` line (currently line 59) and BEFORE the `# === Section 4: Computed (derived ...)` separator (currently line 61). Append exactly these 5 lines:

```
# Phase 27 — Optional: staging CA for ACME debugging without burning
# Let's Encrypt rate limit (5 failed validations / hostname / hour).
# Default empty: prod CA. Staging URL:
#   https://acme-staging-v02.api.letsencrypt.org/directory
# See deploy/DOMAIN-SETUP.md "Staging-CA toggle".
ACME_CA=
```

DO NOT modify any other line in the file. DO NOT change `init-secrets.sh` (D-20 — ACME_EMAIL is human input, MINIO_PUBLIC_URL derives from operator-set DOMAIN; neither is a random secret to generate).

After saving, validate via the Task 2 verify command. The new entries with empty values are valid env-file lines; compose just emits warnings about empty `${ACME_EMAIL}` etc., not errors.

Mitigates threats:
- T-27-ACME-DOS: ACME_CA documented as a one-line toggle for operator debug → no Let's Encrypt rate-limit lockout.
- T-27-MIXED: MINIO_PUBLIC_URL documented + present (plan 27-03 reads it; without this template entry operator wouldn't know to set it).
  </action>
  <verify>
    <automated>grep -c '^ACME_EMAIL=$' deploy/.env.production.example | grep -q '^1$' && grep -c '^ACME_CA=$' deploy/.env.production.example | grep -q '^1$' && grep -c '^MINIO_PUBLIC_URL=$' deploy/.env.production.example | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^ACME_EMAIL=$' deploy/.env.production.example` returns 1 (empty value, no `change-me-` prefix because not a secret)
    - `grep -c '^ACME_CA=$' deploy/.env.production.example` returns 1
    - `grep -c '^MINIO_PUBLIC_URL=$' deploy/.env.production.example` returns 1
    - ACME_EMAIL is in Section 1: `awk '/^# === Section 1/,/^# === Section 2/{print}' deploy/.env.production.example | grep -c '^ACME_EMAIL='` returns 1
    - MINIO_PUBLIC_URL is in Section 1: `awk '/^# === Section 1/,/^# === Section 2/{print}' deploy/.env.production.example | grep -c '^MINIO_PUBLIC_URL='` returns 1
    - ACME_CA is in Section 3: `awk '/^# === Section 3/,/^# === Section 4/{print}' deploy/.env.production.example | grep -c '^ACME_CA='` returns 1
    - Existing variables preserved: `grep -c '^DOMAIN=$' deploy/.env.production.example` returns 1; `grep -c '^DB_PASSWORD=change-me-db-password$' deploy/.env.production.example` returns 1; `grep -c '^GHCR_ORG=$' deploy/.env.production.example` returns 1
    - Comment text references DOMAIN-SETUP.md: `grep -c 'DOMAIN-SETUP.md' deploy/.env.production.example` returns at least 2 (ACME_EMAIL block + ACME_CA block)
    - Comment text references plan 27-03 file: `grep -c 'minio.service.ts' deploy/.env.production.example` returns 1
    - init-secrets.sh NOT modified: `git diff deploy/scripts/init-secrets.sh` returns empty
  </acceptance_criteria>
  <done>3 new env-var entries land in the correct sections of `deploy/.env.production.example`, all 10 grep acceptance criteria pass, and `init-secrets.sh` is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire MINIO_PUBLIC_URL through deploy/docker-compose.yml api service env block (plan 27-03 hand-off)</name>
  <files>deploy/docker-compose.yml</files>
  <read_first>
    - deploy/docker-compose.yml (lines ~182-225 — the `api:` service block, specifically the `environment:` sub-block at lines ~187-209 where MINIO_PUBLIC_ENDPOINT + MINIO_PUBLIC_PORT already live)
    - apps/api/src/recordings/minio.service.ts (post plan 27-03 — confirms the env var name `MINIO_PUBLIC_URL` and the expected prefix format `https://${DOMAIN}`)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-03-PLAN.md (`buildPublicUrl` helper reads `MINIO_PUBLIC_URL`; this task wires the value)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-CONTEXT.md (D-26 — fix scope locked to URL emission)
  </read_first>
  <action>
Apply ONE surgical patch to `deploy/docker-compose.yml`. DO NOT touch any other service or any other env line.

**Patch — Add MINIO_PUBLIC_URL to the api service environment block.** Insert immediately AFTER the existing line `MINIO_PUBLIC_PORT: "443"` (currently line ~200) and BEFORE the line `SRS_HTTP_API_URL: http://srs:1985` (currently line ~201). Use the exact 6-space indentation of surrounding lines. The new line must be exactly:

```
      MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-}
```

The `${MINIO_PUBLIC_URL:-}` (default-empty) syntax means: if the operator's `.env` has `MINIO_PUBLIC_URL=` empty or unset, compose passes an empty string to the api container; the `MinioService.buildPublicUrl` helper from plan 27-03 then falls back to the legacy MINIO_PUBLIC_ENDPOINT+MINIO_PUBLIC_PORT composition (dev compatibility preserved). When operator sets `MINIO_PUBLIC_URL=https://example.com`, the api emits `https://example.com/avatars/<id>.webp` — same-origin, no mixed content.

DO NOT remove the existing MINIO_PUBLIC_ENDPOINT or MINIO_PUBLIC_PORT lines — they remain for backward compatibility with the dev-loop fallback path. Plan 27-03 keeps that fallback functional.

DO NOT add MINIO_PUBLIC_URL to the `sms-migrate` service env block (lines ~160-167) — it doesn't emit avatar/snapshot URLs and doesn't need it.

After saving, validate with the env file from Task 1:
```
docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config --quiet
```

Then verify the env var resolves through to the api service:
```
docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config | awk '/^  api:$/,/^  [a-z]/{print}' | grep -E 'MINIO_PUBLIC_URL'
```

Mitigates threats:
- T-27-MIXED: completes the wire from operator env → compose env → api container → MinioService.buildPublicUrl → browser-bound URLs with `https://` prefix on TLS pages.
  </action>
  <verify>
    <automated>docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config --quiet</automated>
  </verify>
  <acceptance_criteria>
    - api service env block contains the new line: `awk '/^  api:$/,/^  [a-z][a-z]*:$/{print}' deploy/docker-compose.yml | grep -c 'MINIO_PUBLIC_URL: \${MINIO_PUBLIC_URL:-}'` returns 1
    - sms-migrate service env block does NOT contain MINIO_PUBLIC_URL: `awk '/^  sms-migrate:$/,/^  [a-z][a-z]*:$/{print}' deploy/docker-compose.yml | grep -c MINIO_PUBLIC_URL` returns 0
    - Existing api env lines preserved: `grep -c 'MINIO_PUBLIC_ENDPOINT: \${DOMAIN}' deploy/docker-compose.yml` returns 1; `grep -c 'MINIO_PUBLIC_PORT: "443"' deploy/docker-compose.yml` returns 1
    - Compose validates: `docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config --quiet` exits 0
    - Resolved compose shows MINIO_PUBLIC_URL in api: `docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config | awk '/^  api:$/,/^  [a-z]/{print}' | grep -c 'MINIO_PUBLIC_URL'` returns at least 1
    - Diff is additions-only on the api service: `git diff deploy/docker-compose.yml` shows ONLY the inserted MINIO_PUBLIC_URL line (no `-` lines)
  </acceptance_criteria>
  <done>The `MINIO_PUBLIC_URL` env var is wired into the api service env block, all 6 grep + compose-validate acceptance criteria pass, and no other compose content is modified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → deploy/.env (chmod 600) | Trusted; secrets at rest. init-secrets.sh hardens permissions. |
| deploy/.env → docker compose engine | Read via `--env-file`; values substituted into service env blocks at compose runtime (NOT baked into images per Pitfall 8 of Phase 24). |
| Compose engine → caddy + api containers | Service-scoped env vars; not visible cross-service unless explicitly listed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-ACME-DOS | Denial of Service (rate-limit lockout) | ACME issuance | mitigate | `ACME_CA` documented as a 1-line toggle in Section 3 of the env template; comment block points operator to staging URL + DOMAIN-SETUP.md procedure (plan 27-05 finalizes the doc). |
| T-27-MIXED | Information Disclosure / Availability | api → browser URLs | mitigate | `MINIO_PUBLIC_URL` documented as required-for-prod in Section 1 (with example `https://${DOMAIN}`); compose api service exports the value to the container so plan 27-03's helper picks it up. Without this wire, plan 27-03's fix would be inert. |
| T-27-EMPTY-DOMAIN-CRASH | Availability | Caddy first-boot | accept | Operator-side error: empty DOMAIN → Caddyfile parse fails. Documented in plan 27-05 DOMAIN-SETUP.md (Pitfall 7). Phase 29 `bootstrap.sh` will validate before `up`. Out of scope for this plan. |
| T-27-ENV-FILE-LEAK | Information Disclosure | deploy/.env on disk | accept | Phase 26 init-secrets.sh sets chmod 600; root .dockerignore (Phase 24) blocks `.env*` from image context. Phase 30 host-hardening covers further. |
</threat_model>

<verification>
1. Task 1 grep acceptance criteria all pass (10 conditions)
2. Task 2 grep + compose-validate acceptance criteria all pass (6 conditions)
3. `docker compose -f deploy/docker-compose.yml --env-file deploy/.env.production.example config --quiet` exits 0
4. `git diff deploy/.env.production.example` shows ONLY the 3 new var blocks added (no existing line modified)
5. `git diff deploy/docker-compose.yml` shows ONLY the `MINIO_PUBLIC_URL: ${MINIO_PUBLIC_URL:-}` line added in the api service env block (no other content modified)
6. `git diff deploy/scripts/init-secrets.sh` is empty (D-20 enforced)
</verification>

<success_criteria>
- `deploy/.env.production.example` has 3 new env-var entries (ACME_EMAIL + MINIO_PUBLIC_URL in Section 1, ACME_CA in Section 3) with correct comment blocks pointing operators to plan 27-05 doc and plan 27-03 fix.
- `deploy/docker-compose.yml` api service env block exports `MINIO_PUBLIC_URL` with the default-empty fallback so dev compose isn't broken.
- `init-secrets.sh` is byte-identical to its Phase 26 state.
- All grep acceptance criteria pass; `docker compose config --quiet` exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/27-caddy-reverse-proxy-auto-tls/27-04-SUMMARY.md` summarizing:
- Lines added (3 var blocks in env template + 1 line in compose)
- init-secrets.sh untouched (D-20 confirmed)
- Compose validation result
- Threats mitigated (T-27-ACME-DOS, T-27-MIXED — completing the wire from plan 27-03)
- Hand-off to plan 27-05: 3 vars are now in the env template; DOMAIN-SETUP.md must reference all 3 in its workflow + error tables
</output>
