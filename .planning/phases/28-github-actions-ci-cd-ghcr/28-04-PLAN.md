---
phase: 28-github-actions-ci-cd-ghcr
plan: 04
type: execute
wave: 3
depends_on:
  - 28-02
  - 28-03
files_modified:
  - deploy/.env.production.example
  - .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md
autonomous: false
requirements:
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
must_haves:
  truths:
    - "deploy/.env.production.example GHCR_ORG comment block explains source (matches `${{ github.repository_owner }}` in CI) with concrete example for a fresh operator (D-18)"
    - "9 verification checkpoints (CONTEXT D-22 #1-9) are documented in 28-04-VERIFICATION.md with exact commands + expected output"
    - "Operator manual step (GHCR public-visibility toggle, D-19) is documented with screenshot-friendly instructions"
    - "Live verification of `v1.3.0-test` tag push successfully published api+web images to GHCR with 4-tag-variant attestation"
    - "Stable `v1.3.0` tag push exercises the production code path (D-22 #9 — `latest` + `vX.Y` re-attached after prerelease)"
    - "Phase 23 test.yml continues to pass on the same commits build-images.yml runs (D-22 #8)"
  artifacts:
    - path: "deploy/.env.production.example"
      provides: "Operator-facing doc explaining how GHCR_ORG maps to GitHub repo owner"
      contains: "github.repository_owner"
    - path: ".planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md"
      provides: "Step-by-step runbook for the 9 verification checkpoints + checkpoint state log"
      contains: "v1.3.0-test"
  key_links:
    - from: "deploy/.env.production.example GHCR_ORG"
      to: "Plan 03 build-images.yml IMAGE_NAMESPACE"
      via: "operator sets GHCR_ORG to the same value as github.repository_owner"
      pattern: "GHCR_ORG=.*github\\.repository_owner"
    - from: "Phase 28 verification runbook"
      to: "Live GitHub Actions run + GHCR registry"
      via: "operator pushes test tag, observes workflow logs, runs gh CLI verification"
      pattern: "gh attestation verify oci://"
---

<objective>
Close Phase 28 with two artifacts:

1. **Patch `deploy/.env.production.example`** to expand the `GHCR_ORG` comment block (D-18) with a concrete example connecting the operator's value to GitHub's `${{ github.repository_owner }}` context — the link Plan 03 build-images.yml uses for namespace and Plan 02 release.yml uses for image refs.

2. **Author `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md`** — a runbook documenting the 9 verification checkpoints from CONTEXT D-22 (#1-9) with exact CLI commands, expected output, and a checkpoint state log the operator updates as each gate passes. Includes the one-time GHCR public-visibility toggle (D-19) which cannot be automated.

Then **execute the runbook live**:
- Push tag `v1.3.0-test` → verify checkpoints #1, #3, #4, #5, #6, #7, #8 pass
- After test tag green, push tag `v1.3.0` → verify checkpoint #9 passes (stable semver re-attaches `:latest` + `:v1.3`)
- Toggle GHCR images to public (D-19) → verify checkpoint #2 (anonymous `docker pull` from a clean machine)

Purpose: This is the BLOCKING gate before Phase 28 is marked complete. Without live execution, we cannot prove DEPLOY-03/04/05 actually work end-to-end on real GHCR + real GitHub Actions runners. The wave-3 placement ensures Plans 01-03 are committed and pushable before the test tag is cut.

Output: An updated env example, a verification runbook with state log, and live evidence of the 9 checkpoints either passing or producing an actionable failure trail.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/28-github-actions-ci-cd-ghcr/28-CONTEXT.md
@deploy/.env.production.example
@deploy/docker-compose.yml
@.github/workflows/release.yml
@.github/workflows/build-images.yml
@.github/scripts/smoke-api.sh
@.github/scripts/smoke-web.sh

<interfaces>
<!-- What this plan exercises against external systems. -->

GitHub APIs touched (via gh CLI):
- `gh api /users/<owner>/packages/container/sms-api/versions` — list pushed image versions/tags
- `gh attestation verify oci://ghcr.io/<owner>/sms-api:<tag> --owner <owner>` — sigstore attestation verification
- `gh release view <tag>` — confirm release.yml ran and body matches template

Docker registry operations:
- `docker pull ghcr.io/<owner>/sms-{api,web}:v1.3.0-test` (must work without `docker login`, post D-19 visibility toggle)
- `docker inspect ghcr.io/<owner>/sms-api:v1.3.0-test` — confirm RepoTags + Labels (OCI metadata)
- `docker history ghcr.io/<owner>/sms-api:v1.3.0-test` — Pitfall 8 leak check (no .env layer)

Manual operator step (cannot automate):
- GHCR UI → packages → sms-api → "Change package visibility" → Public (one-time per package)
- GHCR UI → packages → sms-web → same toggle
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand GHCR_ORG comment block in deploy/.env.production.example (D-18)</name>
  <read_first>
    - deploy/.env.production.example (current GHCR_ORG block at L51-57 — Phase 26 D-25 product; only modify the comment lines preceding `GHCR_ORG=`, do NOT touch other variables)
    - .planning/phases/28-github-actions-ci-cd-ghcr/28-CONTEXT.md §D-18 (verbatim comment block)
    - .planning/phases/26-production-compose-migrate-init-networking-volumes/26-CONTEXT.md §D-25 (verifies original 4-section structure to preserve)
  </read_first>
  <files>deploy/.env.production.example</files>
  <action>
Use the Edit tool. The current section 2 of `deploy/.env.production.example` (L50-57) reads:

```
# === Section 2: Image refs ===
# GitHub org or user name that owns the GHCR images.
# Image refs resolve to: ghcr.io/${GHCR_ORG}/sms-{api,web}:${IMAGE_TAG}
GHCR_ORG=

# Image tag to pull. Pin a semver (e.g. v1.3.0) in production.
# `latest` is acceptable for dev/staging; surprising on prod (Pitfall 4 anti-pattern).
IMAGE_TAG=latest
```

Replace it with the expanded version below. The substantive change is replacing the 2-line GHCR_ORG comment with the 4-line version from CONTEXT D-18 (adds the `${{ github.repository_owner }}` provenance + the acme-corp example). Other lines (Section header, IMAGE_TAG block, blank lines) preserve byte-for-byte.

NEW content (replaces the block above):

```
# === Section 2: Image refs ===
# GitHub owner ที่ build-images.yml workflow publish image ไป.
# ตรงกับ ${{ github.repository_owner }} ใน CI (เจ้าของ repo).
# ตัวอย่าง: ถ้า repo อยู่ที่ github.com/acme-corp/sms-platform → GHCR_ORG=acme-corp
# Image refs resolve to: ghcr.io/${GHCR_ORG}/sms-{api,web}:${IMAGE_TAG}
GHCR_ORG=

# Image tag to pull. Pin a semver (e.g. v1.3.0) in production.
# `latest` is acceptable for dev/staging; surprising on prod (Pitfall 4 anti-pattern).
IMAGE_TAG=latest
```

Why preserve the existing `Image refs resolve to` line: it documents the path operators care about (where their compose pulls from). Phase 26 added it — we keep it as the closing line of the GHCR_ORG comment block for continuity with Phase 26's hand-off.

Why not duplicate text in deploy/README.md: per D-18 (CONTEXT.md L276-277) — README is Phase 29 territory; the env-file comment is the canonical operator-facing doc for Phase 28. Phase 29 will reference this file rather than re-describe the variable.

The other 3 sections (Required, Defaults override-only, Computed) and all other variables (DOMAIN, DB_PASSWORD, NEXTAUTH_SECRET, etc.) MUST NOT be modified — they belong to Phase 26 ownership.
  </action>
  <verify>
    <automated>grep -B 4 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'github.repository_owner' && grep -B 4 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'acme-corp' && grep -q '^GHCR_ORG=$' deploy/.env.production.example && grep -q '^IMAGE_TAG=latest$' deploy/.env.production.example && grep -q '^DOMAIN=$' deploy/.env.production.example && grep -q '^DB_PASSWORD=change-me-db-password$' deploy/.env.production.example</automated>
  </verify>
  <acceptance_criteria>
    Comment expansion (D-18):
    - `grep -B 4 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'build-images.yml'` → exit 0
    - `grep -B 4 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'github.repository_owner'` → exit 0
    - `grep -B 4 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'acme-corp'` → exit 0
    - `grep -B 4 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'sms-platform → GHCR_ORG=acme-corp'` → exit 0
    - `grep -B 5 '^GHCR_ORG=' deploy/.env.production.example | grep -q 'Image refs resolve to: ghcr.io/\${GHCR_ORG}/sms-{api,web}:\${IMAGE_TAG}'` → exit 0

    Variables preserved (Phase 26 ownership untouched):
    - `grep -q '^GHCR_ORG=$' deploy/.env.production.example` → exit 0 (still empty default)
    - `grep -q '^IMAGE_TAG=latest$' deploy/.env.production.example` → exit 0
    - `grep -q '^DOMAIN=$' deploy/.env.production.example` → exit 0
    - `grep -q '^DB_PASSWORD=change-me-db-password$' deploy/.env.production.example` → exit 0
    - `grep -q '^MINIO_ROOT_PASSWORD=change-me-minio-root-password$' deploy/.env.production.example` → exit 0
    - `grep -q '^ACME_EMAIL=$' deploy/.env.production.example` → exit 0
    - `grep -q '^MINIO_PUBLIC_URL=$' deploy/.env.production.example` → exit 0

    Section headers preserved:
    - `grep -c '^# === Section [1-4]' deploy/.env.production.example` returns exactly 4

    No new variables added:
    - `grep -cE '^[A-Z][A-Z0-9_]*=' deploy/.env.production.example` returns the same count as before the edit (run `git diff deploy/.env.production.example` and confirm only added lines are comment lines starting with `#`)
  </acceptance_criteria>
  <done>The GHCR_ORG block now contains 4 comment lines (including the github.repository_owner reference and acme-corp example), all other variables are byte-identical to the pre-edit file, and the file still has exactly 4 section headers.</done>
</task>

<task type="auto">
  <name>Task 2: Author 28-04-VERIFICATION.md runbook with 9-checkpoint state log</name>
  <read_first>
    - .planning/phases/28-github-actions-ci-cd-ghcr/28-CONTEXT.md §D-19 (manual public-visibility step), §D-22 (full 9-checkpoint spec — verbatim)
    - .planning/phases/27-caddy-reverse-proxy-auto-tls/27-04-PLAN.md (or 27-05-PLAN.md) — pattern reference for verification-runbook structure used in Phase 27
    - .github/workflows/build-images.yml (Plan 03 product — confirm step names + matrix arm names match what `gh run view` will surface)
    - .github/workflows/release.yml (Plan 02 product — confirm release body template)
  </read_first>
  <files>.planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md</files>
  <action>
Create `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md`. Content:

```markdown
# Phase 28 Verification Runbook

**Purpose:** Live-execute the 9 verification checkpoints from CONTEXT D-22 against
the GitHub Actions workflows landed by Plans 01-03. Plan 04 (this runbook) is
the BLOCKING gate before Phase 28 is marked complete.

**Prerequisites:**
- Plans 28-01, 28-02, 28-03 committed to `main` (workflows exist on default branch)
- Operator has push permission to the repo (for tag push + GHCR_ORG resolution)
- Operator has `gh` CLI installed and authenticated (`gh auth status` exit 0)
- Operator has `docker` CLI installed
- Operator knows the GitHub repo owner (e.g. `acme-corp` or username) — referenced as `<OWNER>` below

## Manual operator action — D-19 GHCR public visibility toggle (one-time)

GHCR images default visibility on first publish:
- If the repo is **public** → images inherit public visibility automatically
- If the repo is **private** → images publish as private; operator must toggle to public per D-19

**Why public:** D-19 + Pitfall 11 — public images let operators `docker compose pull` without `docker login ghcr.io` (no PAT trap). This is the explicit Phase 28 stance for the OSS-friendly self-hostable v1.3 deploy model.

**Steps (one-time, after the FIRST publish completes):**
1. Navigate to `https://github.com/<OWNER>?tab=packages` (or `https://github.com/orgs/<OWNER>/packages` for org accounts)
2. Click `sms-api`
3. Right sidebar → "Package settings" → scroll to "Danger Zone" → "Change package visibility" → select "Public" → confirm by typing the package name
4. Repeat for `sms-web`

**Note:** This toggle persists. Future tag pushes inherit the visibility. If the toggle is set BEFORE first publish, it cannot be toggled (the package does not yet exist) — must run after Checkpoint #1 completes successfully.

***

## Checkpoint state log

Update each row's `Status` column with `pass`, `fail`, or `blocked` as the operator runs the checkpoint. Include a one-line note on failure or blocker.

| # | Checkpoint | Status | Note |
|---|------------|--------|------|
| 1 | Tag push triggers matrix build, both jobs green within 10 min | pending | |
| 2 | Anonymous `docker pull` succeeds for both images (D-19 public visibility) | pending | |
| 3 | `docker inspect` shows correct RepoTags + OCI labels for prerelease | pending | |
| 4 | `gh attestation verify` exits 0 for both images (DEPLOY-05) | pending | |
| 5 | GitHub Release `v1.3.0-test` exists with prerelease badge + image-ref body | pending | |
| 6 | PR build runs but does NOT push to GHCR | pending | |
| 7 | Push to main publishes `:main` + `:latest` + `:sha-<7>` only | pending | |
| 8 | Phase 23 test.yml passes on the same commits | pending | |
| 9 | Stable `v1.3.0` tag re-attaches `:latest` + `:v1.3` | pending | |

***

## Checkpoint commands + expected output

### Checkpoint 1 — Test-tag matrix build (D-22 #1)

```sh
# Push the test tag (must NOT collide with an existing release tag)
git tag v1.3.0-test
git push origin v1.3.0-test

# Watch the run (replace with the run ID gh prints)
gh run watch
```

**Expected:** `Build & Publish Images / build (api)` and `Build & Publish Images / build (web)` both complete successfully. Total wall-clock < 10 minutes (cold cache; warm cache <3 min).

**Pass criterion:** Both matrix jobs end with green checkmarks; `gh run list --workflow=build-images.yml --limit 1 --json conclusion --jq '.[0].conclusion'` returns `"success"`.

***

### Checkpoint 2 — Anonymous public pull (D-22 #2, D-19)

**Precondition:** Checkpoint 1 passed AND D-19 visibility toggle done.

Run from a machine that has NEVER run `docker login ghcr.io`:

```sh
# Use a docker context with no GHCR auth, OR delete ~/.docker/config.json registry entry first
docker logout ghcr.io 2>/dev/null || true

docker pull ghcr.io/<OWNER>/sms-api:v1.3.0-test
docker pull ghcr.io/<OWNER>/sms-web:v1.3.0-test
```

**Expected:** Both pulls succeed without `unauthorized` error.

**Pass criterion:** Both `docker pull` commands exit 0. If either returns `denied: requested access to the resource is denied` or `401 Unauthorized`, re-confirm the D-19 visibility toggle was applied to BOTH packages.

***

### Checkpoint 3 — RepoTags + OCI labels (D-22 #3, D-05, D-07)

```sh
docker inspect ghcr.io/<OWNER>/sms-api:v1.3.0-test \
  --format '{{json .RepoTags}}' | jq

docker inspect ghcr.io/<OWNER>/sms-api:v1.3.0-test \
  --format '{{json .Config.Labels}}' | jq
```

**Expected RepoTags (D-06 prerelease policy):**
- Includes: `ghcr.io/<OWNER>/sms-api:v1.3.0-test`, `ghcr.io/<OWNER>/sms-api:sha-<7-char>`
- Excludes: `ghcr.io/<OWNER>/sms-api:latest`, `ghcr.io/<OWNER>/sms-api:v1.3`

**Expected Labels (D-07 OCI metadata):**
- `org.opencontainers.image.source` set to `https://github.com/<OWNER>/<repo>`
- `org.opencontainers.image.revision` set to commit SHA
- `org.opencontainers.image.version` set to `v1.3.0-test`
- `org.opencontainers.image.created` set to ISO 8601 timestamp

**Pass criterion:** `RepoTags` array contains 2 entries (the version tag + sha tag); `Config.Labels` JSON contains all 4 OCI keys above.

***

### Checkpoint 4 — Provenance attestation (D-22 #4, DEPLOY-05)

```sh
gh attestation verify oci://ghcr.io/<OWNER>/sms-api:v1.3.0-test --owner <OWNER>
gh attestation verify oci://ghcr.io/<OWNER>/sms-web:v1.3.0-test --owner <OWNER>
```

**Expected output (per image):**
```
Loaded digest sha256:<...> for oci://ghcr.io/<OWNER>/sms-api:v1.3.0-test
Loaded 1 attestation from GitHub API
✓ Verification succeeded!

The following policy criteria were met:
- Subject:        sha256:<...>
- Issuer:         https://token.actions.githubusercontent.com
- Predicate type: https://slsa.dev/provenance/v1
```

**Pass criterion:** Both commands exit 0 and print "Verification succeeded".

***

### Checkpoint 5 — GitHub Release entry (D-22 #5, Plan 02)

```sh
gh release view v1.3.0-test --json tagName,isPrerelease,body --jq '{tag: .tagName, prerelease: .isPrerelease, body: .body}'
```

**Expected:**
- `prerelease: true` (D-17 regex matched `-test`)
- `body` contains all of:
  - `ghcr.io/<OWNER>/sms-api:v1.3.0-test`
  - `ghcr.io/<OWNER>/sms-web:v1.3.0-test`
  - `gh attestation verify oci://`
  - `docker compose pull`
  - Auto-generated commit-history section (entries from previous tag → this tag)

**Pass criterion:** `gh release view` returns a JSON object with `prerelease: true` and the body matches all 5 markers above.

***

### Checkpoint 6 — PR build-only mode (D-22 #6, D-02)

Open a draft PR that touches `apps/api/Dockerfile` (e.g. add a `# Phase 28 verification PR — discard` comment line):

```sh
git checkout -b phase-28-verify-pr-build-only
echo "# Phase 28 verification PR — discard" >> apps/api/Dockerfile
git add apps/api/Dockerfile
git commit -m "test(28-04): verify PR build does not push to GHCR"
git push origin phase-28-verify-pr-build-only
gh pr create --draft --title "Phase 28 verification (do not merge)" --body "Verification only — D-22 #6 — discard after Checkpoint 6 passes"
```

Watch the PR run. After it completes, verify:

```sh
# Should show NO new versions for v1.3.0-test... only Checkpoint 1's existing version
gh api /users/<OWNER>/packages/container/sms-api/versions --jq '.[].metadata.container.tags' | sort -u
```

**Expected:** PR run completed (build + smoke green for both matrix arms); GHCR version list does NOT contain a new entry tied to the PR ref. Only Checkpoint 1's tags remain.

**Pass criterion:** PR workflow succeeds AND GHCR shows no PR-pushed version. After confirmation, close the PR without merging and delete the branch:

```sh
gh pr close phase-28-verify-pr-build-only --delete-branch
```

***

### Checkpoint 7 — Main push tag set (D-22 #7, D-05)

After Checkpoint 6, merge a non-trivial commit to main (e.g. a docs-only change or this VERIFICATION.md file landing). Watch the workflow on main:

```sh
gh run watch
```

After completion:

```sh
gh api /users/<OWNER>/packages/container/sms-api/versions --jq '.[0].metadata.container.tags' | jq
```

**Expected tags on the latest version:** `main`, `latest`, `sha-<7-char>` (exactly 3 tags — no semver, no prerelease).

**Pass criterion:** Tag set on the latest GHCR version matches `["main", "latest", "sha-XXXXXXX"]` (any order; SHA matches `git rev-parse --short HEAD` truncated to 7 chars).

***

### Checkpoint 8 — Phase 23 test.yml co-existence (D-22 #8, D-20)

For the same commit SHA Checkpoint 7 ran against:

```sh
gh run list --workflow=test.yml --limit 1 --json conclusion,headSha --jq '.[0]'
```

**Expected:**
- `conclusion: "success"`
- `headSha` matches the commit SHA Checkpoint 7 ran on

**Pass criterion:** `test.yml` is green on the same commit `build-images.yml` ran. Confirms D-20 (no test breakage from Phase 28 introducing the build workflow).

***

### Checkpoint 9 — Stable semver tag (D-22 #9, D-05, D-06)

Push the real production tag:

```sh
git tag v1.3.0
git push origin v1.3.0
gh run watch
```

After completion:

```sh
gh api /users/<OWNER>/packages/container/sms-api/versions --jq '.[0].metadata.container.tags' | jq
```

**Expected tags on the latest version:** `v1.3.0`, `v1.3`, `latest`, `sha-<7-char>` (exactly 4 — D-05 stable semver re-attaches `:latest` + `:v1.3` because `v1.3.0` is NOT prerelease).

**Pass criterion:** Tag set is `["v1.3.0", "v1.3", "latest", "sha-XXXXXXX"]` (any order). Run `gh attestation verify oci://ghcr.io/<OWNER>/sms-api:v1.3.0 --owner <OWNER>` and confirm exit 0 — proves attestation works on stable releases too.

***

## Bonus: Pitfall 8 leak check (recommended, not in D-22)

```sh
docker history ghcr.io/<OWNER>/sms-api:v1.3.0 --no-trunc | grep -c "\.env"
```

**Expected:** Returns 0 (no `.env` layer in image history). If non-zero, IMMEDIATELY rotate all secrets and investigate the build context — Phase 24 root `.dockerignore` + Phase 25 per-app `.dockerignore` should prevent this.

***

## Failure handling

If any checkpoint fails:

1. Update its row in the state log with `fail` and a one-line note
2. Capture the failing command output to `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-FAILURE-<N>.md`
3. STOP execution — do not proceed to subsequent checkpoints
4. Triage:
   - Workflow YAML bug → escalate to Plan 03 (re-execute with fixes)
   - Smoke script bug → escalate to Plan 01
   - Release body bug → escalate to Plan 02
   - GHCR visibility bug → re-confirm D-19 toggle was applied
   - GitHub-side flake → retry once before declaring fail

After all 9 checkpoints pass, this runbook serves as the verification log for Phase 28 SUMMARY.md.
```

The runbook lives in the phase directory (parallel to PLAN files); the operator updates it during execution. Failure scenarios escalate to specific upstream plans for fix-and-retry, preserving the wave-3 ordering invariant (Plans 01-03 are the source of truth for fixes; Plan 04 only consumes them).
  </action>
  <verify>
    <automated>test -f .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md && grep -q '^# Phase 28 Verification Runbook$' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md && grep -q '## Manual operator action — D-19' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md && grep -q '## Checkpoint state log' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md && grep -c '### Checkpoint [1-9]' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md | grep -q '^9$' && grep -q 'gh attestation verify' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md && grep -q 'docker history.*\.env' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    File created:
    - `test -f .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '^# Phase 28 Verification Runbook$' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0

    Required sections:
    - `grep -q '## Manual operator action — D-19' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '## Checkpoint state log' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '## Checkpoint commands + expected output' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '## Failure handling' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0

    All 9 checkpoints documented:
    - `grep -c '### Checkpoint [1-9]' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` returns exactly 9
    - `grep -q '### Checkpoint 1 — Test-tag matrix build' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 2 — Anonymous public pull' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 3 — RepoTags + OCI labels' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 4 — Provenance attestation' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 5 — GitHub Release entry' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 6 — PR build-only mode' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 7 — Main push tag set' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 8 — Phase 23 test.yml co-existence' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q '### Checkpoint 9 — Stable semver tag' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0

    Required commands present:
    - `grep -q 'git tag v1.3.0-test' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q 'git tag v1.3.0' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q 'gh attestation verify oci://' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q 'docker pull ghcr.io/<OWNER>/sms-api' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q 'docker inspect ghcr.io/<OWNER>/sms-api' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -q 'gh release view v1.3.0-test' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0

    State log structure:
    - `grep -q '| # | Checkpoint | Status | Note |' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
    - `grep -c '| pending |' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` returns exactly 9

    Pitfall 8 bonus check:
    - `grep -q 'docker history ghcr.io/<OWNER>/sms-api:v1.3.0' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` → exit 0
  </acceptance_criteria>
  <done>The runbook file exists with all 4 required top-level sections, 9 numbered checkpoint subsections, the 9-row state log table with all `pending` entries, the D-19 manual toggle instructions, the failure-handling escalation path, and the bonus Pitfall 8 leak check. All ~30 acceptance-criteria greps pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Execute the 9 verification checkpoints live</name>
  <read_first>
    - .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md (Task 2 product — operator follows this runbook)
    - .planning/phases/28-github-actions-ci-cd-ghcr/28-CONTEXT.md §D-19 (manual visibility toggle context), §D-22 (checkpoint reference)
  </read_first>
  <files>.planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md (consumed; updated in-place by operator)</files>
  <action>
This task is a blocking human checkpoint — there is no executor-side automation. The executor pauses here and waits for the operator to (1) confirm Plans 01-03 + Tasks 1-2 of this plan are landed on main, (2) follow .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md top-to-bottom, executing all 9 checkpoint commands and updating the state log table from pending to pass/fail/blocked, (3) toggle GHCR package visibility per the manual D-19 instructions in the runbook (after Checkpoint 1 completes), (4) capture any failure output to .planning/phases/28-github-actions-ci-cd-ghcr/28-04-FAILURE-N.md and escalate to the upstream plan that owns the fix, (5) reply with the resume signal once all 9 rows show pass.
  </action>
  <what-built>
The full Phase 28 surface is now landed on `main`:
- `.github/scripts/smoke-api.sh` + `.github/scripts/smoke-web.sh` (Plan 01)
- `.github/workflows/release.yml` (Plan 02)
- `.github/workflows/build-images.yml` (Plan 03)
- Updated `deploy/.env.production.example` GHCR_ORG comment block (this plan, Task 1)
- Verification runbook `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` (this plan, Task 2)

This checkpoint is the BLOCKING gate where the operator runs the runbook live, exercises GitHub Actions on real runners, pushes test + stable tags, runs the GHCR public-visibility toggle (one-time, manual), and confirms 9 of 9 checkpoints pass.

Why blocking + manual: Checkpoint #2 requires `docker pull` from a machine with NO `docker login ghcr.io` credentials (the reverse-proof of D-19 public visibility). Checkpoint #5 requires visual inspection of the GitHub Release UI's prerelease badge. Both are inherently outside CI's reach.
  </what-built>
  <how-to-verify>
1. Plans 01-03 + Task 1-2 of this plan must be on `main` (so workflows resolve from default branch)
2. Open `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` and follow the runbook top-to-bottom:
   - Run "Manual operator action — D-19 GHCR public visibility toggle" AFTER Checkpoint 1 completes
   - For each of Checkpoints 1-9, run the documented commands, capture output, update the state log row from `pending` to `pass` or `fail`
3. If any checkpoint fails:
   - Update state log with `fail` + one-line note
   - Capture the failing command output to `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-FAILURE-<N>.md`
   - Triage per the Failure Handling section (escalate to upstream plan that owns the fix)
   - STOP — return to this checkpoint after the upstream plan re-runs
4. Run the bonus Pitfall 8 leak check (`docker history ... | grep .env`) — expect 0
5. After all 9 checkpoints pass, capture final state-log table in the resume signal
  </how-to-verify>
  <resume-signal>
Type one of:
- `verified — all 9 checkpoints pass` (then paste the state-log table with all 9 rows showing `pass`)
- `failed — checkpoint <N> blocking on <one-line reason>` (then escalate to the upstream plan per Failure Handling)
- `partial — N pass, M fail, K blocked` (then list per-checkpoint status)
  </resume-signal>
  <verify>
    <automated>test -f .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md && grep -c '| pass |' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md | awk '{exit ($1 < 9)}'</automated>
  </verify>
  <acceptance_criteria>
    - 28-04-VERIFICATION.md state log shows 9 rows with `Status: pass` (`grep -c '| pass |' .planning/phases/28-github-actions-ci-cd-ghcr/28-04-VERIFICATION.md` ≥ 9)
    - Operator's resume signal contains the literal string `verified — all 9 checkpoints pass`
    - GHCR registry contains both `ghcr.io/<owner>/sms-api:v1.3.0` and `ghcr.io/<owner>/sms-web:v1.3.0` with the 4 stable-tag aliases (verify via `gh api /users/<owner>/packages/container/sms-api/versions`)
    - At least one `gh attestation verify oci://...` invocation has been logged with exit 0 (Checkpoint 4)
    - Phase 23 `.github/workflows/test.yml` is green on the same SHA `build-images.yml` ran on (Checkpoint 8)
  </acceptance_criteria>
  <done>All 9 verification rows in the state log table are `pass`, both production and prerelease tag variants are live on GHCR with provenance attestation, and the operator has typed the `verified — all 9 checkpoints pass` resume signal with the full state-log table appended.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → GitHub Actions runner | Operator pushes git tags; CI runs with auto-provisioned `GITHUB_TOKEN` (cannot be exfiltrated to external services without code change) |
| Operator → GHCR registry UI | Operator manually toggles package visibility via authenticated browser session |
| Anonymous puller → public GHCR image | Once visibility is public, any internet host can `docker pull` (intentional — D-19) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-18 | Information Disclosure | Operator pastes verification output containing internal infra details into runbook | accept | The verification commands print: image refs (already public via D-19), workflow run URLs (already public on GitHub), commit SHAs (already public), attestation predicates (already public via Sigstore Rekor). No secret material is produced |
| T-28-19 | Tampering | Operator edits a workflow file to bypass smoke gate before pushing test tag | mitigate | Branch protection on `main` (Phase 23 + future Phase 30 ratification) gates the test-tag's BASE state. Plan 04 explicitly requires Plans 01-03 to be on `main` (not on a branch) before Checkpoint 1 — encoded in the runbook prerequisites |
| T-28-20 | Denial of Service | Failed `v1.3.0-test` tag exhausts GHCR free-tier storage | accept | GHCR free tier: 50GB storage, 1GB/month bandwidth on free private; UNLIMITED on public images. D-19 public visibility means storage cost is the only constraint. Phase 25 image size budget (≤450MB api + ≤220MB web ≈ 670MB total per tag set) gives ~75 tag sets before storage caps. Verification cycle uses 2 tag sets (test + stable) |
| T-28-21 | Spoofing | Verification runs on a forked commit, not the actual main HEAD | mitigate | `gh run list --workflow=build-images.yml --limit 1 --json headSha` exposes the SHA the latest workflow ran on. Checkpoint 7 explicitly cross-checks `git rev-parse --short HEAD` against the GHCR `sha-<7>` tag. Mismatch = fail |
| T-28-22 | Forgotten manual step | Operator skips D-19 visibility toggle, leaving images private | mitigate | Checkpoint 2 (`docker pull` from anon machine) is explicitly gated on D-19 toggle. If skipped, Checkpoint 2 fails with `unauthorized` and the runbook's failure-handling section directs the operator to "re-confirm the D-19 visibility toggle was applied to BOTH packages" |
</threat_model>

<verification>
1. Task 1: `deploy/.env.production.example` GHCR_ORG block matches D-18 expansion (run all Task 1 acceptance-criteria greps)
2. Task 2: `28-04-VERIFICATION.md` exists with all 4 sections, 9 checkpoint subsections, state-log table (run all Task 2 acceptance-criteria greps)
3. Task 3: All 9 state-log rows show `pass`; resume signal received; GHCR + sigstore attestation evidence captured

Phase complete when:
- All 3 tasks done
- ROADMAP success criteria #1-4 demonstrably met (mapped 1:1 from D-22 #1, #3, #4, #9 + #5)
- Phase 23 test.yml still green on same SHA (D-22 #8 → no test breakage)
</verification>

<success_criteria>
1. `deploy/.env.production.example` GHCR_ORG comment expanded with `${{ github.repository_owner }}` reference + acme-corp example
2. `28-04-VERIFICATION.md` runbook exists with all 9 checkpoints + state log + failure-handling
3. Operator has executed all 9 checkpoints live; state log shows 9× `pass`
4. `v1.3.0` stable tag landed; `:v1.3.0` + `:v1.3` + `:latest` + `:sha-<7>` aliases all attached on GHCR
5. `gh attestation verify oci://ghcr.io/<owner>/sms-{api,web}:v1.3.0 --owner <owner>` exits 0 for both images
6. `gh release view v1.3.0` returns a release with image refs in body, NOT marked prerelease
7. `docker history ghcr.io/<owner>/sms-api:v1.3.0` shows zero `.env` layers (Pitfall 8 bonus)
</success_criteria>

<output>
After completion, create `.planning/phases/28-github-actions-ci-cd-ghcr/28-04-SUMMARY.md` capturing:
- Final state-log table (all 9 `pass`)
- The exact tag values used (`v1.3.0-test` + `v1.3.0`) and the timestamps each was pushed
- Cross-references to the upstream plans whose artifacts were live-validated (Plans 01, 02, 03)
- Any operational notes for Phase 29 deploy/README.md (e.g. "GHCR_ORG must be set to <owner-value-used> in production .env")
- Confirmation that DEPLOY-03, DEPLOY-04, DEPLOY-05 are demonstrably met on real GHCR + real GitHub Actions
</output>
