---
phase: 24
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/README.md
  - deploy/scripts/.gitkeep
autonomous: true
requirements: []

must_haves:
  truths:
    - "A deploy/ directory exists at the repo root with placeholder skeleton (D-01, D-02, D-03)"
    - "git ls-files deploy/ returns exactly two paths: deploy/README.md and deploy/scripts/.gitkeep"
    - "deploy/README.md is a 1-2 paragraph stub explicitly stating Phase 29 will overwrite it (D-02)"
    - "deploy/scripts/ exists as an empty directory tracked via .gitkeep (D-03), ready for Phase 29 to land bootstrap.sh / update.sh / backup.sh / restore.sh / init-secrets.sh"
    - "The skeleton does NOT pollute pnpm workspaces (no package.json under deploy/) per D-19"
  artifacts:
    - path: "deploy/README.md"
      provides: "Stub README declaring deploy/ scope; placeholder until Phase 29 (DEPLOY-23) overwrites with 5-step quickstart"
      min_lines: 5
    - path: "deploy/scripts/.gitkeep"
      provides: "Placeholder so empty deploy/scripts/ is git-tracked"
      contains: ""
  key_links:
    - from: "deploy/README.md"
      to: ".planning/ROADMAP.md §Phase 24-30"
      via: "stub references the canonical roadmap for what lands when"
      pattern: "ROADMAP|REQUIREMENTS"
    - from: "deploy/scripts/.gitkeep"
      to: "Phase 29 operator scripts (DEPLOY-18..21)"
      via: "empty placeholder reserves the directory"
      pattern: ""
---

<objective>
Create the `deploy/` directory at the repo root as a placeholder skeleton for production-only artifacts. Phase 24 ships ONLY the skeleton (`deploy/README.md` stub + `deploy/scripts/.gitkeep`); subsequent Phases 25-30 fill in the real content (compose, Caddyfile, scripts, env example, prod docs).

Purpose: Pitfall 18 (BLOCKER) — adding a `deploy/` folder later, ad-hoc, risks contaminating dev workflow. Locking the skeleton + convention now (Phase 24) prevents future subagents from misplacing artifacts and prevents pnpm workspace globs from accidentally picking up `deploy/` as a workspace member.

Output: Two new files committed under `deploy/` with zero impact on dev workflow, no source-code edits, no compose changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- No code interfaces. This plan creates two static placeholder files. -->
<!-- Phase 29 (DEPLOY-23) will overwrite deploy/README.md with the real 5-step quickstart -->
<!-- and Phase 29 (DEPLOY-18..21) will populate deploy/scripts/ with bootstrap/update/backup/restore. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deploy/README.md stub</name>
  <files>deploy/README.md</files>
  <read_first>
    - .planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-CONTEXT.md (D-01, D-02 — exact stub scope: 1-2 paragraph, NO content that will be overwritten)
    - .planning/ROADMAP.md §Phase 24-30 (lines 90-169 — what each downstream phase delivers, so the stub's pointer to ROADMAP is accurate)
    - .planning/REQUIREMENTS.md §Coverage line 155 (Phase 24 owns no REQ-IDs; DEPLOY-* land in Phases 25-30)
  </read_first>
  <action>
Create the file `deploy/README.md` with the EXACT content below. This is a deliberately minimal stub — Phase 29 (DEPLOY-23) will overwrite it with the real 5-step quickstart, so do NOT add content that will be deleted later.

```markdown
# deploy/

Production deployment artifacts. **This directory is production-only — do not place dev tooling here.**

Each phase of the v1.3 milestone fills in specific files:

- Phase 25 — `apps/api/Dockerfile`, `apps/web/Dockerfile`, per-app `.dockerignore` (image pipeline; lives under `apps/`, not here)
- Phase 26 — `deploy/docker-compose.yml`, `deploy/.env.production.example`
- Phase 27 — `deploy/Caddyfile`, `deploy/DOMAIN-SETUP.md`
- Phase 28 — `.github/workflows/build-images.yml`, `.github/workflows/release.yml` (CI/CD; lives under `.github/`, not here)
- Phase 29 — `deploy/scripts/{bootstrap,update,backup,restore,init-secrets}.sh`, `deploy/BACKUP-RESTORE.md`, `deploy/TROUBLESHOOTING.md`, and a real 5-step quickstart that **overwrites this README**

See `.planning/ROADMAP.md` (Phases 24-30) and `.planning/REQUIREMENTS.md` (DEPLOY-01..26) for the canonical scope.

**Convention lock:** see `CLAUDE.md` §"Deploy Folder Convention" — `deploy/` = production-only artifacts; `apps/` = dev workflow source; `pnpm-workspace.yaml` MUST NOT include `deploy/`.
```

Hard rules for the executor:
- Use the exact text above. Do NOT paraphrase, expand, or add quickstart instructions — those land in Phase 29.
- Do NOT add a `package.json` to `deploy/` (would make it a pnpm workspace member; see D-19).
- Do NOT create any subdirectories beyond `deploy/scripts/` (created by Task 2).
- Do NOT touch `pnpm-workspace.yaml`, `docker-compose.yml`, or `package.json` (D-19, D-20).
  </action>
  <acceptance_criteria>
    - `test -f deploy/README.md` exits 0
    - `wc -l deploy/README.md` shows ≥ 10 lines (the stub block above is exactly 13 non-empty lines)
    - `grep -F '# deploy/' deploy/README.md` exits 0 (heading present)
    - `grep -F 'production-only' deploy/README.md` exits 0
    - `grep -F 'overwrites this README' deploy/README.md` exits 0 (Phase 29 hand-off marker)
    - `grep -F 'ROADMAP.md' deploy/README.md` exits 0
    - `grep -F 'REQUIREMENTS.md' deploy/README.md` exits 0
    - `grep -F 'Deploy Folder Convention' deploy/README.md` exits 0 (cross-link to PLAN 05's CLAUDE.md section)
    - `! test -f deploy/package.json` (no workspace pollution)
  </acceptance_criteria>
  <verify>
    <automated>test -f deploy/README.md && grep -qF '# deploy/' deploy/README.md && grep -qF 'production-only' deploy/README.md && grep -qF 'overwrites this README' deploy/README.md && ! test -f deploy/package.json</automated>
  </verify>
  <done>deploy/README.md exists with the stub content; no package.json in deploy/.</done>
</task>

<task type="auto">
  <name>Task 2: Create deploy/scripts/.gitkeep</name>
  <files>deploy/scripts/.gitkeep</files>
  <read_first>
    - .planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-CONTEXT.md (D-03 — empty file, no real script in Phase 24; Phase 29 fills bootstrap/update/backup/restore/init-secrets)
  </read_first>
  <action>
Create an empty placeholder file at `deploy/scripts/.gitkeep`:

```bash
mkdir -p deploy/scripts && touch deploy/scripts/.gitkeep
```

Hard rules:
- The file MUST be empty (zero bytes). `.gitkeep` convention: empty file whose only purpose is to make git track an otherwise-empty directory.
- Do NOT add any script content. Phase 29 (DEPLOY-18..21) creates `bootstrap.sh`, `update.sh`, `backup.sh`, `restore.sh`, `init-secrets.sh`.
- Do NOT create any other files under `deploy/scripts/`.
  </action>
  <acceptance_criteria>
    - `test -f deploy/scripts/.gitkeep` exits 0
    - `test ! -s deploy/scripts/.gitkeep` exits 0 (file exists and is empty — `-s` is "size > 0", negation means size == 0)
    - `ls deploy/scripts/ | wc -l` outputs `1` (only `.gitkeep`, no other files)
    - `git ls-files deploy/scripts/` outputs exactly `deploy/scripts/.gitkeep` after staging
  </acceptance_criteria>
  <verify>
    <automated>test -f deploy/scripts/.gitkeep && test ! -s deploy/scripts/.gitkeep && [ "$(ls deploy/scripts/ | wc -l | tr -d ' ')" = "1" ]</automated>
  </verify>
  <done>Empty .gitkeep exists in deploy/scripts/; no other files in that directory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo file layout → future docker build context | `deploy/` content will be referenced by future build/deploy steps; structural decisions made here echo into Phases 25-30 |
| pnpm workspace globs → directory traversal | `pnpm-workspace.yaml` could pick up `deploy/` if a `package.json` is dropped there |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information disclosure (LOW) | deploy/README.md stub content | accept | Stub is generic placeholder text with no secrets / no real config; risk is zero. Phase 29 owns the real 5-step quickstart. |
| T-24-02 | Tampering (MEDIUM) | future Phase 25-30 subagent drops dev tooling into deploy/ | mitigate | Plan 05 (Wave 2) adds CLAUDE.md "## Deploy Folder Convention" section with 5 explicit rules forbidding dev tooling in deploy/. README stub itself contains the convention-lock pointer (see acceptance criterion `grep -F 'Deploy Folder Convention' deploy/README.md`). |
| T-24-03 | Tampering (LOW) | accidental package.json drop into deploy/ pollutes pnpm workspace | mitigate | Acceptance criterion `! test -f deploy/package.json` enforces no workspace pollution at execution time; PLAN 05's checklist re-verifies post-merge. |
</threat_model>

<verification>
- `git ls-files deploy/` returns exactly two paths: `deploy/README.md` and `deploy/scripts/.gitkeep`
- `pnpm install` at repo root does NOT traverse into `deploy/` (no `package.json` to discover)
- `pnpm dev` continues to work byte-identically (this plan touches no dev-workflow files)
</verification>

<success_criteria>
- [ ] `test -f deploy/README.md` exits 0
- [ ] `test -f deploy/scripts/.gitkeep` exits 0 AND file is empty
- [ ] `git ls-files deploy/` outputs exactly two lines: `deploy/README.md` and `deploy/scripts/.gitkeep`
- [ ] No `package.json` exists under `deploy/` (workspace not polluted)
- [ ] No edits to `pnpm-workspace.yaml`, `docker-compose.yml`, `package.json` scripts, or any source code
</success_criteria>

<output>
After completion, create `.planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-01-SUMMARY.md` documenting: files created, git ls-files output, confirmation no workspace pollution.
</output>
