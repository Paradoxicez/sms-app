---
phase: 24
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .dockerignore
autonomous: true
requirements: []

must_haves:
  truths:
    - "A root-level `.dockerignore` exists with comprehensive secret/state/planning exclusions per D-07 (Pitfall 8 BLOCKER for GA)"
    - "`.env*` patterns are excluded; `.env.example` is whitelisted via negation `!.env.example` AND the negation appears AFTER the `.env*` exclusion (Docker BuildKit applies rules in order — negation must follow exclusion)"
    - "Dependency artifacts (`node_modules`), build artifacts (`dist`, `.next`, `out`, `*.tsbuildinfo`), test coverage (`coverage`), VCS (`.git`), planning state (`.planning/`), claude/agent state (`.claude/`), local data (`docker-data/`), IDE (`.vscode`, `.idea`, swap files), OS (`.DS_Store`, `Thumbs.db`), logs (`*.log`), and example bulk-import files with potential real creds (`bulk-import-*-EXAMPLE.csv`, `bulk-import-*-EXAMPLE.xlsx`) are all excluded per D-07"
    - "Comments group patterns by category for readability per D-08"
    - "`Dockerfile*`, `*.md` are NOT excluded per D-09 (not security issues, may be referenced by future build steps)"
  artifacts:
    - path: ".dockerignore"
      provides: "Root-level docker build context exclusion list — closes Pitfall 8 (BLOCKER for GA: .env in image layer)"
      min_lines: 35
      contains: ".env"
  key_links:
    - from: ".dockerignore"
      to: "Future Phase 25 multi-stage Dockerfiles"
      via: "Docker BuildKit applies closest .dockerignore to each build context — root .dockerignore is the baseline for all future builds"
      pattern: "\\.env\\*"
    - from: ".dockerignore line: !.env.example"
      to: ".dockerignore line: .env.*"
      via: "negation pattern must appear AFTER the exclusion it negates"
      pattern: "^!\\.env\\.example$"
---

<objective>
Create a comprehensive root-level `.dockerignore` so that any future `docker build` from the repo root does NOT copy `.env*`, `.git/`, `node_modules/`, `.planning/`, `docker-data/`, `.claude/`, build/coverage artifacts, IDE files, OS metadata, logs, or local bulk-import example files into the image build context.

Purpose: Pitfall 8 (BLOCKER for GA — `.env` in image layer is detectable via `docker history` and visible to anyone who pulls the image). Today the dev `apps/api/Dockerfile` does `COPY . .` with NO `.dockerignore` — every secret in `.env`, every byte of `.planning/`, every dev-only file ends up in the image. Phase 24 lands the protection ONCE at the repo root before Phase 25 builds the prod multi-stage Dockerfile.

Comprehensive scope (D-07): exceeds the roadmap success criterion #3 minimal list because Pitfall 8 is BLOCKER for GA and `.dockerignore` is "fix-once-or-rot" — missing patterns silently leak secrets later. Per-app `.dockerignore` (Phase 25) will inherit and extend this baseline (Docker BuildKit uses the closest `.dockerignore` per build context, but the root file is the project-wide default and a safety net for any root-level `docker build`).

Output: One new file `.dockerignore` at repo root. Zero edits to source code, dev compose, package.json, pnpm-workspace.yaml, or any other file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-CONTEXT.md
@.planning/research/PITFALLS.md
@.gitignore

<interfaces>
<!-- Existing .gitignore (template / reference for some patterns; .dockerignore is a SUPERSET because it must also block .git, coverage, docker-data, *.tsbuildinfo, .claude): -->

```
# Dependencies
node_modules/

# Environment
.env
.env.local
apps/api/.env.test

# Build output
dist/
.next/
out/
*.tsbuildinfo

# Prisma
apps/api/src/prisma/generated/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# Logs
*.log

# GSD internals (worktrees + quick scratch + Claude Code session state)
.claude/
apps/web/.planning/

# Local bulk-import examples (may contain real credentials/URLs — do not commit)
bulk-import-*-EXAMPLE.csv
bulk-import-*-EXAMPLE.xlsx
```

<!-- Pitfall 8 research-validated baseline (.planning/research/PITFALLS.md:243-255), expanded with .claude + bulk-import-* + monorepo glob patterns: -->

```
.env
.env.*
!.env.example
.git
node_modules
.next
dist
coverage
.planning
docker-data
```

<!-- Docker BuildKit ordering note: negation patterns (!) MUST appear AFTER the exclusion they negate. Order matters. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create root .dockerignore with comprehensive grouped patterns</name>
  <files>.dockerignore</files>
  <read_first>
    - .planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-CONTEXT.md (D-07 — exact pattern list with categories; D-08 — group by category with comments; D-09 — do NOT exclude Dockerfile* or *.md)
    - .planning/research/PITFALLS.md §Pitfall 8 (lines 230-266 — "BLOCKER for GA, easy to verify mechanically"; sample minimal `.dockerignore` at lines 243-255 is the baseline we expand)
    - .gitignore (template for some patterns; .dockerignore is a superset adding `.git`, `coverage`, `docker-data`, `*.tsbuildinfo`, `.claude`, monorepo glob duplicates)
  </read_first>
  <action>
Create `.dockerignore` at the repo ROOT (not under `apps/api/` or `deploy/` — those land in later phases) with EXACTLY the content below. Pattern ordering is significant: the negation `!.env.example` MUST appear AFTER `.env*` so Docker BuildKit re-includes the example file after the broad exclusion.

Write the file with this exact body (preserve blank lines and comment exactly):

```
# .dockerignore — root build context exclusions for the SMS Platform monorepo.
# Closes Pitfall 8 (BLOCKER for GA): .env / secrets / planning state must NEVER
# enter a docker build context, because COPY . . layers are visible via
# `docker history` and trivially extractable from any pulled image.
#
# Phase 24 lands this baseline. Phase 25 may add per-app `.dockerignore` files
# under apps/api/ and apps/web/ — Docker BuildKit applies the closest
# `.dockerignore` to each build context, so this file remains the project-wide
# default for any root-level `docker build`.
#
# Pattern ordering note: the negation pattern `!.env.example` MUST appear AFTER
# `.env*` because BuildKit applies rules in order. Reordering the Secrets group
# below would silently re-leak the real .env file.

# === Secrets (HIGHEST PRIORITY — BLOCKER for GA) ===
.env
.env.*
!.env.example

# === Version control ===
.git
.gitignore
.gitattributes

# === Dependencies ===
node_modules
**/node_modules

# === Build artifacts ===
dist
**/dist
.next
**/.next
out
**/out
*.tsbuildinfo
**/*.tsbuildinfo

# === Test / coverage ===
coverage
**/coverage

# === Planning / GSD state ===
.planning
apps/*/.planning

# === Local bind-mounted data (dev compose HLS bind mount, snapshots, etc.) ===
docker-data

# === IDE / editor ===
.vscode
.idea
*.swp
*.swo

# === OS metadata ===
.DS_Store
Thumbs.db

# === Claude / agent state ===
.claude

# === Logs ===
*.log
**/*.log

# === Local bulk-import examples (may contain real customer URLs / creds) ===
bulk-import-*-EXAMPLE.csv
bulk-import-*-EXAMPLE.xlsx
```

Hard rules:
- Use the EXACT pattern list above. Do NOT add or remove patterns.
- Keep the ordering as written: Secrets first (so the `!.env.example` negation correctly follows `.env.*`), then VCS, Dependencies, Build, Coverage, Planning, Data, IDE, OS, Agent state, Logs, Examples.
- Do NOT exclude `Dockerfile*`, `apps/api/Dockerfile.dev`, `*.md`, or `*.yml` per D-09 (not security issues; may be referenced by future build steps).
- Do NOT add the file to a sub-directory. Phase 25 owns `apps/api/.dockerignore` and `apps/web/.dockerignore`.
- Do NOT touch `.gitignore`, `docker-compose.yml`, `package.json`, `pnpm-workspace.yaml`, or any source code (D-19, D-20).

The negation-ordering test (acceptance criterion below) uses `awk` to confirm `!.env.example` appears on a higher-numbered line than `.env*`.
  </action>
  <acceptance_criteria>
    - `test -f .dockerignore` exits 0
    - `wc -l .dockerignore` shows ≥ 50 lines (the body above is exactly 60 lines including blank lines and comments)
    - `! test -f apps/api/.dockerignore` (Phase 25 owns per-app files; Phase 24 must not pre-create them)
    - `! test -f apps/web/.dockerignore` (same — Phase 25)
    - **Secrets group:**
      - `grep -nE '^\.env$' .dockerignore` exits 0
      - `grep -nE '^\.env\.\*$' .dockerignore` exits 0
      - `grep -nE '^!\.env\.example$' .dockerignore` exits 0
      - **Negation ordering check:** `awk '/^\.env\.\*$/{e=NR} /^!\.env\.example$/{n=NR} END{exit !(e>0 && n>e)}' .dockerignore` exits 0 (`.env.*` line number must be less than `!.env.example` line number)
    - **VCS group:** `grep -F '.git' .dockerignore` exits 0; `grep -F '.gitignore' .dockerignore` exits 0; `grep -F '.gitattributes' .dockerignore` exits 0
    - **Dependencies:** `grep -nE '^node_modules$' .dockerignore` exits 0; `grep -nE '^\*\*/node_modules$' .dockerignore` exits 0
    - **Build artifacts:** `grep -nE '^dist$' .dockerignore && grep -nE '^\*\*/dist$' .dockerignore && grep -nE '^\.next$' .dockerignore && grep -nE '^\*\*/\.next$' .dockerignore && grep -nE '^out$' .dockerignore && grep -nE '^\*\*/out$' .dockerignore && grep -nE '^\*\.tsbuildinfo$' .dockerignore && grep -nE '^\*\*/\*\.tsbuildinfo$' .dockerignore` all exit 0
    - **Coverage:** `grep -nE '^coverage$' .dockerignore && grep -nE '^\*\*/coverage$' .dockerignore` both exit 0
    - **Planning:** `grep -nE '^\.planning$' .dockerignore && grep -nE '^apps/\*/\.planning$' .dockerignore` both exit 0
    - **Local data:** `grep -nE '^docker-data$' .dockerignore` exits 0
    - **IDE:** `grep -nE '^\.vscode$' .dockerignore && grep -nE '^\.idea$' .dockerignore && grep -nE '^\*\.swp$' .dockerignore && grep -nE '^\*\.swo$' .dockerignore` all exit 0
    - **OS:** `grep -F '.DS_Store' .dockerignore && grep -F 'Thumbs.db' .dockerignore` both exit 0
    - **Claude state:** `grep -nE '^\.claude$' .dockerignore` exits 0
    - **Logs:** `grep -nE '^\*\.log$' .dockerignore && grep -nE '^\*\*/\*\.log$' .dockerignore` both exit 0
    - **Bulk-import examples:** `grep -F 'bulk-import-*-EXAMPLE.csv' .dockerignore && grep -F 'bulk-import-*-EXAMPLE.xlsx' .dockerignore` both exit 0
    - **NOT excluded (must remain copyable):** `! grep -nE '^Dockerfile' .dockerignore` AND `! grep -nE '^\*\.md$' .dockerignore` (D-09: do not exclude Dockerfile* or *.md)
    - **Comment groups present (D-08 readability):** `grep -F '=== Secrets' .dockerignore && grep -F '=== Version control' .dockerignore && grep -F '=== Dependencies' .dockerignore && grep -F '=== Build artifacts' .dockerignore && grep -F '=== Test / coverage' .dockerignore && grep -F '=== Planning' .dockerignore && grep -F '=== Local bind-mounted data' .dockerignore && grep -F '=== IDE' .dockerignore && grep -F '=== OS' .dockerignore && grep -F '=== Claude' .dockerignore && grep -F '=== Logs' .dockerignore && grep -F '=== Local bulk-import' .dockerignore` all exit 0 (12 group headers)
  </acceptance_criteria>
  <verify>
    <automated>test -f .dockerignore && grep -qE '^\.env$' .dockerignore && grep -qE '^\.env\.\*$' .dockerignore && grep -qE '^!\.env\.example$' .dockerignore && awk '/^\.env\.\*$/{e=NR} /^!\.env\.example$/{n=NR} END{exit !(e>0 && n>e)}' .dockerignore && grep -qE '^\.git$' .dockerignore && grep -qE '^node_modules$' .dockerignore && grep -qE '^\*\*/node_modules$' .dockerignore && grep -qE '^\.planning$' .dockerignore && grep -qE '^docker-data$' .dockerignore && grep -qE '^\.claude$' .dockerignore && grep -qE '^\*\.tsbuildinfo$' .dockerignore && grep -qF 'bulk-import-*-EXAMPLE.csv' .dockerignore && ! grep -qE '^Dockerfile' .dockerignore && ! grep -qE '^\*\.md$' .dockerignore</automated>
  </verify>
  <done>.dockerignore exists at repo root with comprehensive grouped patterns; negation `!.env.example` correctly ordered after `.env.*`; Dockerfile* and *.md NOT excluded; per-app .dockerignore files NOT pre-created (Phase 25 owns those).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo working dir → docker build context | `COPY . .` in any Dockerfile copies everything not excluded by `.dockerignore` into image layers; layers are extractable by anyone who pulls the image |
| `.env` file (chmod 600 on dev/prod machine) → image registry | Without `.dockerignore`, `.env` enters image and reaches GHCR — secrets are now permanently in registry layer history |
| Symlinked `apps/web/.env` → root `.env` | pnpm workspace creates `apps/web/.env` as a symlink to root `.env`; docker COPY dereferences symlinks, so even excluding `.env` at the wrong scope leaks secrets via the symlink target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information disclosure (HIGH — BLOCKER for GA) | `.env*` files in image layers via `COPY . .` | mitigate | This plan: `.env` + `.env.*` exclusion at line 1-2 of Secrets group; `!.env.example` negation correctly ordered AFTER (acceptance criterion enforces line-order via awk). Future Phase 25 multi-stage Dockerfile MUST never `COPY .env` — secrets come at runtime via compose env_file. Verified by Phase 30 smoke test (`docker history` should not show .env layer). |
| T-24-07 | Information disclosure (MEDIUM) | `.planning/` directory leaked into image layer | mitigate | This plan: `.planning` and `apps/*/.planning` excluded. Planning docs may include unredacted credentials, internal architecture, customer names — not safe to ship in registry. |
| T-24-08 | Information disclosure (MEDIUM) | `.git/` directory leaked into image (entire history including any past `.env` commits) | mitigate | This plan: `.git` excluded. Even if `.env` is gitignored today, any past accidental commit is in `.git/objects/` — only `.git` exclusion prevents that history reaching the registry. |
| T-24-09 | Information disclosure (LOW-MEDIUM) | `bulk-import-*-EXAMPLE.csv/xlsx` may contain real customer camera URLs / credentials | mitigate | This plan: explicit pattern excludes both `.csv` and `.xlsx` example files. Already in `.gitignore`; `.dockerignore` mirrors. |
| T-24-10 | Information disclosure (LOW) | `.claude/` agent state (worktrees, scratch) leaked | mitigate | This plan: `.claude` excluded. Already in `.gitignore`; `.dockerignore` mirrors. |
| T-24-11 | Tampering (LOW) | Future operator inverts negation order (puts `!.env.example` BEFORE `.env.*`) | mitigate | Acceptance criterion uses awk to compare line numbers; CI/manual checklist surfaces inversion. PLAN 05 D-22 manual checklist re-runs the awk check. |
</threat_model>

<verification>
- `cat .dockerignore | grep -c '^!'` returns `1` (exactly one negation line — `!.env.example`)
- `awk '/^\.env\.\*$/{e=NR} /^!\.env\.example$/{n=NR} END{exit !(e>0 && n>e)}' .dockerignore` exits 0 (negation ordered AFTER exclusion)
- The file is the ONLY `.dockerignore` in the repo (no `apps/api/.dockerignore` or `apps/web/.dockerignore` yet — Phase 25 owns those): `find . -name '.dockerignore' -not -path './node_modules/*' -not -path './.git/*' | wc -l` outputs `1`
- `pnpm dev` continues to work (this plan touches no dev-workflow files; verified in PLAN 04 smoke script and PLAN 05 D-22 checklist)
</verification>

<success_criteria>
- [ ] `.dockerignore` exists at repo root
- [ ] All 11 categories from D-07 are present, each with explicit comment header (D-08)
- [ ] `.env` and `.env.*` are excluded; `!.env.example` is the only negation and appears after the exclusion
- [ ] `.git`, `.gitignore`, `.gitattributes`, `node_modules`, `**/node_modules`, `dist`, `**/dist`, `.next`, `**/.next`, `out`, `**/out`, `*.tsbuildinfo`, `**/*.tsbuildinfo`, `coverage`, `**/coverage`, `.planning`, `apps/*/.planning`, `docker-data`, `.vscode`, `.idea`, `*.swp`, `*.swo`, `.DS_Store`, `Thumbs.db`, `.claude`, `*.log`, `**/*.log`, `bulk-import-*-EXAMPLE.csv`, `bulk-import-*-EXAMPLE.xlsx` are all excluded
- [ ] `Dockerfile*` and `*.md` are NOT excluded (D-09)
- [ ] No per-app `.dockerignore` is created (Phase 25 owns those)
- [ ] No edits to `.gitignore`, `docker-compose.yml`, `package.json`, `pnpm-workspace.yaml`, or any source code
</success_criteria>

<output>
After completion, create `.planning/phases/24-deploy-folder-structure-dev-workflow-guardrails/24-03-SUMMARY.md` documenting: file path, line count, output of grep tests for each pattern category, awk negation-ordering check result, confirmation that no per-app `.dockerignore` was pre-created.
</output>
