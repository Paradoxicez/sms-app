---
phase: 22-camera-metadata-utilization-surface-tags-description-across-
plan: 09
type: execute
wave: 1
depends_on: [22-01]
files_modified:
  - apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx
  - apps/web/src/app/admin/cameras/components/__tests__/view-stream-sheet.test.tsx
autonomous: true
decisions_addressed: [D-16]
threat_model_summary: "Pure frontend display. No new auth surface. React auto-escapes the description string. whitespace-pre-line preserves user newlines without HTML interpretation."
requirements: [D-16]
must_haves:
  truths:
    - "view-stream-sheet renders a Notes section ABOVE the existing Tabs and BELOW the SheetHeader"
    - "Notes section appears ONLY when camera.description is non-empty (length > 0 after trim)"
    - "Notes heading reads exactly 'Notes' with text-xs font-medium uppercase tracking-wide text-muted-foreground"
    - "Notes body uses text-sm whitespace-pre-line and preserves user-entered line breaks"
    - "Notes section has no edit affordance (read-only — D-16)"
    - "Section gap mb-6 (24px) separates Notes from Tabs per UI-SPEC §Spacing"
  artifacts:
    - path: "apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx"
      provides: "Notes block conditional render between SheetHeader and Tabs"
      contains: "Notes"
  key_links:
    - from: "view-stream-sheet.tsx"
      to: "camera.description"
      via: "conditional render guard"
      pattern: "description.*&&\\|description\\?\\.length"
---

<objective>
Add the "Notes" read-only section to `view-stream-sheet.tsx` per D-16. Conditional render based on `camera.description?.length > 0`. No edit affordance — edit lives in the form.

Purpose: Description is currently never surfaced in the view-stream-sheet — only persisted via the form. This plan closes that gap for the surface where users naturally look for camera context.

Output:
- Notes block inserted between SheetHeader and Tabs in `view-stream-sheet.tsx`.
- Existing test file extended with Notes rendering cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-CONTEXT.md
@.planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-UI-SPEC.md
@apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx
@apps/web/src/app/admin/cameras/components/__tests__/view-stream-sheet.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Notes section + extend tests</name>
  <files>apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx, apps/web/src/app/admin/cameras/components/__tests__/view-stream-sheet.test.tsx</files>
  <read_first>
    - apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx (entire file — find SheetHeader end and Tabs start; the Notes section slots between them per UI-SPEC line 365)
    - apps/web/src/app/admin/cameras/components/__tests__/view-stream-sheet.test.tsx (existing test pattern — DO NOT regress)
    - .planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-UI-SPEC.md (§"view-stream-sheet — Notes section" line 198-203 + §"Surface-by-Surface Contract Summary" line 365 + §"Spacing" line 60)
  </read_first>
  <behavior>
    Test 1 — Renders when description present: render with camera.description='First line\nSecond line'. Expect heading 'Notes' visible AND body containing 'First line' AND 'Second line' (whitespace-pre-line preserves the newline).
    Test 2 — Hidden when description empty: render with camera.description=''. Expect 'Notes' heading NOT in DOM.
    Test 3 — Hidden when description null: render with camera.description=null. Expect 'Notes' heading NOT in DOM.
    Test 4 — Notes appears BEFORE Tabs: in a render with description present, query the DOM for both 'Notes' heading and the first Tab trigger; assert Notes appears earlier in document order (e.g., compareDocumentPosition).
    Test 5 — No edit button in Notes block: render with description present; query within the Notes block for any `<button>` matching 'edit' (case-insensitive). Expect 0 matches.
  </behavior>
  <action>
    1. **Read `view-stream-sheet.tsx` fully.** Locate:
       - The SheetHeader closing tag (existing).
       - The Tabs opening tag (existing — `<Tabs>` or similar).
       - The existing camera info area structure (so the new section is inserted between them, not breaking layout).

    2. **Insert the Notes block** between SheetHeader and Tabs:
       ```tsx
       {camera.description && camera.description.trim().length > 0 && (
         <section className="mb-6" aria-labelledby="camera-notes-heading">
           <h3
             id="camera-notes-heading"
             className="text-xs font-medium uppercase tracking-wide text-muted-foreground mb-2"
           >
             Notes
           </h3>
           <p className="text-sm whitespace-pre-line">{camera.description}</p>
         </section>
       )}
       ```

       Position constraints (per UI-SPEC line 365):
       - INSIDE the Sheet body
       - AFTER the existing SheetHeader / camera info area
       - BEFORE the Tabs start

    3. **Extend `__tests__/view-stream-sheet.test.tsx`** — Add a new `describe('Phase 22: Notes section', ...)` block with the 5 cases above. Use the existing test setup (mock router, mock camera prop) — do NOT replace any existing tests.

       For Test 4 (DOM order), pattern:
       ```tsx
       const heading = screen.getByText('Notes');
       const tabsList = screen.getByRole('tablist');
       expect(heading.compareDocumentPosition(tabsList) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy();
       ```
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && pnpm --filter @sms-platform/web test -- view-stream-sheet -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "camera-notes-heading\\|Notes" apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` returns 2+ matches (heading id + heading text)
    - `grep -n "whitespace-pre-line" apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` returns 1 match (Notes body)
    - `grep -n "uppercase tracking-wide" apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` returns 1 match (Notes heading per UI-SPEC)
    - `grep -nE "description.*&&|description\\?\\.length" apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` returns 1+ match (conditional render guard)
    - `grep -n "mb-6" apps/web/src/app/admin/cameras/components/view-stream-sheet.tsx` returns 1+ match (section spacing per UI-SPEC §Spacing)
    - Test file `view-stream-sheet.test.tsx` contains a `describe('Phase 22: Notes section'` or equivalent block
    - Test command above exits 0 with all 5 Phase 22 cases passing AND no regression in existing tests
  </acceptance_criteria>
  <done>
    Notes section live in view-stream-sheet. Conditional render correct. Position correct. Read-only confirmed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-saved description → DOM render | description string from camera record passed through React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | Tampering (XSS) | description rendering | mitigate | React auto-escapes children. whitespace-pre-line preserves newlines via CSS, NOT via dangerouslySetInnerHTML. |
</threat_model>

<verification>
- view-stream-sheet tests pass (existing + 5 Phase 22 cases)
- Web build clean
- Manual: open camera View Stream → Notes block visible above Tabs when description set; absent when empty
</verification>

<success_criteria>
- Notes section conditionally rendered between SheetHeader and Tabs
- Heading + body styling matches UI-SPEC
- 5 Phase 22 tests pass
- Plan 22-09 SUMMARY committed
</success_criteria>

<output>
After completion, create `.planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-09-SUMMARY.md`
</output>
