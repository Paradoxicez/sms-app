---
phase: 22-camera-metadata-utilization-surface-tags-description-across-
plan: 12
type: execute
wave: 6
depends_on: [22-02, 22-03]
files_modified:
  - apps/web/src/app/app/developer/docs/api-workflow/page.tsx
  - apps/web/src/app/app/developer/docs/webhooks/page.tsx
autonomous: true
decisions_addressed: [D-23, D-27]
threat_model_summary: "Static documentation pages. No new auth or data surface. Per project memory feedback_api_docs_static_templates, MUST use static placeholders only — never inject real account data."
requirements: [D-23, D-27]
must_haves:
  truths:
    - "API workflow docs page documents the ?tags[]= query parameter on GET /cameras"
    - "Tags filter docs use static placeholder example values (e.g. tags[]=lobby&tags[]=entrance)"
    - "Webhook docs page documents the tags: string[] field added to camera.online and camera.offline payloads"
    - "Webhook payload example shows tags as a sample array (e.g. tags: ['Outdoor', 'Perimeter'])"
    - "Docs do NOT mention description field for webhooks (D-22 explicit)"
    - "Docs do NOT auto-inject real user account data (per feedback_api_docs_static_templates memory)"
    - "Docs use English-only copy (per feedback_language_english_default memory)"
  artifacts:
    - path: "apps/web/src/app/app/developer/docs/api-workflow/page.tsx"
      provides: "?tags[]= filter param documented"
      contains: "tags"
    - path: "apps/web/src/app/app/developer/docs/webhooks/page.tsx"
      provides: "tags field in camera.online/offline payload documented"
      contains: "tags"
  key_links:
    - from: "api-workflow/page.tsx"
      to: "GET /cameras filter section"
      via: "documentation prose + code sample"
      pattern: "tags\\[\\]"
---

<objective>
Document the new query parameter (D-23, D-27) and webhook payload extension. Static placeholders only — per `feedback_api_docs_static_templates` memory and `quick-260426-2vj` (Developer Portal Overview Step 2 was rewritten to use static templates).

Purpose: External developers consuming the API need discoverable documentation for the new filter and webhook payload field. Without this, the new capability is invisible.

Output:
- API workflow page documents `?tags[]=` filter on GET /cameras with example.
- Webhooks page documents the `tags` field added to `camera.online` / `camera.offline` payloads.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-CONTEXT.md
@apps/web/src/app/app/developer/docs/api-workflow/page.tsx
@apps/web/src/app/app/developer/docs/webhooks/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document tags[] query param + webhook tags field</name>
  <files>apps/web/src/app/app/developer/docs/api-workflow/page.tsx, apps/web/src/app/app/developer/docs/webhooks/page.tsx</files>
  <read_first>
    - apps/web/src/app/app/developer/docs/api-workflow/page.tsx (full file — find the GET /cameras section; identify existing filter examples like ?siteId=)
    - apps/web/src/app/app/developer/docs/webhooks/page.tsx (full file — find the camera.online and camera.offline event sections)
    - apps/web/src/app/app/developer/docs/streaming-basics/page.tsx OR similar (reference for project's static-template prose style after `quick-260426-2vj`)
    - .planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-CONTEXT.md (D-22, D-23, D-27)
    - ~/.claude/projects/-Users-suraboonsung-Documents-Programming-DMASS-gsd-sms-app/memory/feedback_api_docs_static_templates.md (referenced via path; if unable to load, follow rule: use CAMERA_ID and YOUR_API_KEY placeholders, never inject real values)
  </read_first>
  <action>
    1. **Read `api-workflow/page.tsx` fully** — Identify:
       - The GET /cameras section (probably has filter examples like `?siteId=...`).
       - The existing prose tone (English, professional, code blocks via `<pre>` or a `<CodeBlock>` component).
       - The existing import of code-formatting components.

    2. **Add a `tags[]` filter section to `api-workflow/page.tsx`** — Insert a new subsection or extend the existing "Filtering" section. Use the project's existing markup style (likely JSX with Tailwind, code blocks, inline `<code>`):
       ```tsx
       {/* New section / extension to existing GET /cameras docs */}
       <h3>Filter cameras by tags</h3>
       <p>
         Use the <code>tags[]</code> query parameter to filter cameras by tag.
         Multiple values combine with OR semantics — a camera matches if it has
         at least one of the requested tags. Matching is case-insensitive.
       </p>
       <pre>
       {`GET /api/cameras?tags[]=lobby&tags[]=entrance
       Authorization: X-API-Key: YOUR_API_KEY

       Response (200 OK):
       [
         {
           "id": "CAMERA_ID",
           "name": "Camera 1",
           "tags": ["Lobby"],
           ...
         }
       ]`}
       </pre>
       <p>
         Tag values in the response preserve the original casing entered by
         the user. For example, a camera tagged <code>"Lobby"</code> will
         match <code>?tags[]=lobby</code> and the response will return
         <code>"Lobby"</code> verbatim.
       </p>
       ```

       Match the project's existing markup style by adapting tags / code-block components. **CRITICAL:** Use only static placeholders — `CAMERA_ID`, `YOUR_API_KEY` per `feedback_api_docs_static_templates`. Do NOT call any function that returns the user's actual key or camera ID.

    3. **Add a `tags` payload field to `webhooks/page.tsx`** — Find the `camera.online` and `camera.offline` event documentation. Update the payload schema example:
       ```tsx
       <h3>camera.online / camera.offline</h3>
       <p>Fired when a camera transitions between online and offline states.</p>
       <pre>
       {`{
         "cameraId": "CAMERA_ID",
         "status": "online",
         "previousStatus": "offline",
         "timestamp": "2026-04-26T12:00:00.000Z",
         "tags": ["Outdoor", "Perimeter"]
       }`}
       </pre>
       <p>
         The <code>tags</code> array reflects the camera's tags at the moment
         of dispatch (preserved casing). Subscribers can use this to filter
         events client-side, e.g. trigger an alert only when a camera tagged
         <code>"perimeter"</code> goes offline.
       </p>
       ```

       Ensure the docs do NOT mention `description` or `cameraName` in webhook payloads (D-22 explicit exclusion).

    4. **English copy only** per `feedback_language_english_default` memory.

    5. **Static placeholders only** — verify with grep that no auto-injection function is called inside this file (no `useUser()`, no `apiKey.value`, no `process.env` for user data).
  </action>
  <verify>
    <automated>cd /Users/suraboonsung/Documents/Programming/DMASS/gsd/sms-app && grep -lE 'tags\[\]|"tags":' apps/web/src/app/app/developer/docs/api-workflow/page.tsx apps/web/src/app/app/developer/docs/webhooks/page.tsx; pnpm --filter @sms-platform/web build 2>&1 | grep -E "error|Error" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "tags\\[\\]" apps/web/src/app/app/developer/docs/api-workflow/page.tsx` returns 1+ match
    - `grep -nE "\"tags\":" apps/web/src/app/app/developer/docs/webhooks/page.tsx` returns 1+ match (in payload example)
    - `grep -n "OR semantics\\|case-insensitive" apps/web/src/app/app/developer/docs/api-workflow/page.tsx` returns 1+ match (D-06 documented)
    - `grep -n "CAMERA_ID\\|YOUR_API_KEY" apps/web/src/app/app/developer/docs/api-workflow/page.tsx` returns 1+ matches (static placeholders per memory)
    - `grep -nE "description.*camera\\.|cameraName" apps/web/src/app/app/developer/docs/webhooks/page.tsx` returns 0 matches (D-22 exclusion — these MUST NOT appear in webhook docs)
    - **Negative check:** `grep -nE "useUser|useSession|currentApiKey" apps/web/src/app/app/developer/docs/api-workflow/page.tsx` returns 0 matches (no real-user injection)
    - Web build clean
    - Manual: navigate to `/app/developer/docs/api-workflow` → Tags filter section visible with example. Navigate to `/app/developer/docs/webhooks` → camera.online payload shows tags field.
  </acceptance_criteria>
  <done>
    Both docs pages updated with tags[] filter + tags payload field. Static placeholders only. English-only copy.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static page content → public-facing developer docs | No user data injection per project memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-16 | Information Disclosure | docs page rendering | mitigate | Static placeholders only (CAMERA_ID, YOUR_API_KEY); negative check in acceptance criteria asserts no useUser/useSession imports leaking real data into the static template. |
</threat_model>

<verification>
- Both docs pages updated
- Static placeholders only (per memory)
- Web build clean
- Manual smoke through both pages
</verification>

<success_criteria>
- Tags filter documented in api-workflow page
- tags webhook field documented in webhooks page
- No description or cameraName in webhook docs (D-22)
- No real-user data injection (per memory)
- Plan 22-12 SUMMARY committed
</success_criteria>

<output>
After completion, create `.planning/phases/22-camera-metadata-utilization-surface-tags-description-across-/22-12-SUMMARY.md`
</output>
