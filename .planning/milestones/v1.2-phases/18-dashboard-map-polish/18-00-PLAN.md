---
phase: 18
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/web/src/test-utils/camera-fixtures.ts
  - apps/web/src/__tests__/tenant-dashboard-page.test.tsx
  - apps/web/src/__tests__/platform-dashboard-page.test.tsx
  - apps/web/src/components/dashboard/issues-panel.test.tsx
  - apps/web/src/components/dashboard/platform-issues-panel.test.tsx
  - apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx
  - apps/web/src/components/dashboard/storage-forecast-card.test.tsx
  - apps/web/src/components/dashboard/recent-audit-highlights.test.tsx
  - apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx
  - apps/web/src/components/map/camera-marker.test.tsx
  - apps/web/src/components/map/camera-map-inner.test.tsx
  - apps/web/src/components/map/camera-popup.test.tsx
  - apps/api/tests/admin/admin-dashboard.test.ts
  - apps/api/tests/dashboard/dashboard.test.ts
autonomous: true
requirements: [UI-05, UI-06]

must_haves:
  truths:
    - "Every UI-05/UI-06 verifiable behavior has an it.todo or it stub before implementation begins"
    - "Shared camera fixture file exists and exports online/offline/degraded/reconnecting/recording/maintenance permutations"
    - "PreviewVideo memoization regression guard stub exists in camera-popup.test.tsx"
    - "Marker XSS guard test stub exists in camera-marker.test.tsx"
  artifacts:
    - path: "apps/web/src/test-utils/camera-fixtures.ts"
      provides: "DashboardCamera + MapCamera fixtures with all Phase 15 flags populated"
      exports: ["onlineCamera", "offlineCamera", "degradedCamera", "reconnectingCamera", "recordingCamera", "maintenanceCamera", "makeDashboardCamera", "makeMapCamera"]
    - path: "apps/web/src/components/map/camera-marker.test.tsx"
      provides: "Teardrop SVG render + recording/maintenance badges + XSS escape stubs"
      contains: "escapeHtml"
    - path: "apps/web/src/components/map/camera-popup.test.tsx"
      provides: "Preview dimensions, status overlay, badges, memo regression stubs"
      contains: "PreviewVideo does not remount"
    - path: "apps/api/tests/admin/admin-dashboard.test.ts"
      provides: "Stubs for 6 new admin methods"
      contains: "getActiveStreamsCount"
  key_links:
    - from: "all new component test files"
      to: "apps/web/src/test-utils/camera-fixtures.ts"
      via: "import { onlineCamera, makeDashboardCamera } from '@/test-utils/camera-fixtures'"
      pattern: "camera-fixtures"
---

<objective>
Create Wave 0 test scaffolds for every Phase 18 verifiable behavior using `it.todo` stubs. This is the Nyquist gate — every downstream implementation task must flip an `it.todo` to `it` with assertions. Also creates the shared camera fixtures file used across all component tests.

Purpose: Enforce sampling rate per VALIDATION.md and prevent shallow implementations. Every subsequent plan has a green automated verification path.
Output: 14 test files + 1 fixtures file with `it.todo` placeholders mapped to UI-05/UI-06 behaviors, all passing (because todo is a pass).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dashboard-map-polish/18-CONTEXT.md
@.planning/phases/18-dashboard-map-polish/18-RESEARCH.md
@.planning/phases/18-dashboard-map-polish/18-UI-SPEC.md
@.planning/phases/18-dashboard-map-polish/18-VALIDATION.md

<interfaces>
<!-- Test infrastructure already in repo -->

Existing vitest config (apps/web):
- `apps/web/vitest.config.ts` — jsdom env, setup at `src/test-utils/setup.ts`, uses `@testing-library/react`
- Existing test pattern exemplars: `apps/web/src/__tests__/*.test.tsx`, `apps/web/src/components/**/*.test.tsx`

Existing vitest config (apps/api):
- `apps/api/vitest.config.ts` — Node env, 30s timeout, global-setup runs migrations on sms_platform_test
- Existing test pattern exemplar: `apps/api/tests/dashboard/dashboard.test.ts` (already has `it.todo` stubs for DashboardService enrichment)

DashboardCamera type from `apps/web/src/hooks/use-dashboard-stats.ts:32-39`:
```typescript
export interface DashboardCamera {
  id: string;
  name: string;
  status: 'online' | 'offline' | 'degraded' | 'connecting' | 'reconnecting';
  lastOnlineAt: string | null;
  viewerCount: number;
  bandwidth: number;
}
```

MapCamera type from `apps/web/src/components/map/camera-map.tsx:14-21`:
```typescript
export interface MapCamera {
  id: string;
  name: string;
  status: string;
  latitude: number | null;
  longitude: number | null;
  viewerCount?: number;
}
```

Phase 18 will EXTEND DashboardCamera with `isRecording, maintenanceMode, maintenanceEnteredBy, maintenanceEnteredAt` and MapCamera with the same plus `lastOnlineAt, retentionDays`. Fixtures must include these extended fields.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared camera fixtures + frontend test stub files</name>
  <files>apps/web/src/test-utils/camera-fixtures.ts, apps/web/src/__tests__/tenant-dashboard-page.test.tsx, apps/web/src/__tests__/platform-dashboard-page.test.tsx, apps/web/src/components/dashboard/issues-panel.test.tsx, apps/web/src/components/dashboard/platform-issues-panel.test.tsx, apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx, apps/web/src/components/dashboard/storage-forecast-card.test.tsx, apps/web/src/components/dashboard/recent-audit-highlights.test.tsx, apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx, apps/web/src/components/map/camera-marker.test.tsx, apps/web/src/components/map/camera-map-inner.test.tsx, apps/web/src/components/map/camera-popup.test.tsx</files>
  <read_first>
    - apps/web/vitest.config.ts (test runner config)
    - apps/web/src/test-utils/setup.ts (existing setup)
    - apps/web/src/hooks/use-dashboard-stats.ts (DashboardCamera type)
    - apps/web/src/components/map/camera-map.tsx (MapCamera type — to be extended)
    - apps/web/src/components/map/camera-popup.tsx (PreviewVideo memo at lines 26-32 — regression guard context)
    - apps/web/src/components/map/camera-marker.tsx (existing divIcon at lines 44-58 — will be replaced)
    - .planning/phases/18-dashboard-map-polish/18-RESEARCH.md §Validation Architecture (Phase Requirements → Test Map table lines 849-907)
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md (copy, dimensions, selectors)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md (D-01..D-24)
  </read_first>
  <behavior>
    Fixtures file `camera-fixtures.ts`:
    - Exports `onlineCamera`, `offlineCamera`, `degradedCamera`, `reconnectingCamera`, `recordingCamera`, `maintenanceCamera` — each a ready-to-use object.
    - Exports factory helpers `makeDashboardCamera(overrides)` and `makeMapCamera(overrides)`.
    - Fields include Phase 18 extensions: `isRecording: boolean`, `maintenanceMode: boolean`, `maintenanceEnteredBy: string | null`, `maintenanceEnteredAt: string | null` (schema spelling — per RESEARCH Pitfall 5), `lastOnlineAt: string | null`, `retentionDays: number | null`.
    - `maintenanceCamera.maintenanceEnteredBy === 'Jane Doe'`, `maintenanceCamera.maintenanceEnteredAt === '2026-04-21T00:00:00.000Z'`, `maintenanceCamera.maintenanceMode === true`.
    - `recordingCamera.isRecording === true`, `recordingCamera.retentionDays === 7`.

    Test stub files — each uses `describe` + `it.todo` per behavior from RESEARCH §Validation Architecture table. Exact stubs:

    `tenant-dashboard-page.test.tsx`:
      - `it.todo('UI-05: removes SystemMetrics component (D-01) — no <SystemMetrics /> rendered')`
      - `it.todo('UI-05: renders 6 stat cards with labels Cameras Online, Cameras Offline, Recording, In Maintenance, Total Viewers, Stream Bandwidth (D-02)')`
      - `it.todo('UI-05: grid uses classes grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6 (UI-SPEC)')`
      - `it.todo('UI-05: keeps BandwidthChart and ApiUsageChart (D-03)')`
      - `it.todo('UI-05: replaces CameraStatusTable with IssuesPanel (D-04)')`
      - `it.todo('UI-05: removes isSuperAdmin / userRole state (no longer needed after SystemMetrics removal)')`

    `platform-dashboard-page.test.tsx`:
      - `it.todo('UI-05: renders 7 stat cards including Active Streams and Recordings Active (D-05)')`
      - `it.todo('UI-05: grid uses classes grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-7 (UI-SPEC)')`
      - `it.todo('UI-05: renders PlatformIssuesPanel, ClusterNodesPanel, StorageForecastCard, OrgHealthDataTable, RecentAuditHighlights in vertical stack order (D-07)')`
      - `it.todo('UI-05: keeps 4 SystemMetrics cards (D-06)')`
      - `it.todo('UI-05: replaces Organization Summary Table with OrgHealthDataTable (D-12)')`

    `issues-panel.test.tsx`:
      - `it.todo('UI-05: empty state renders CheckCircle2 + "All cameras healthy" (D-04 reward signal)')`
      - `it.todo('UI-05: sorts issues severity offline → degraded → reconnecting → recording-failed → maintenance (D-04)')`
      - `it.todo('UI-05: offline row action Investigate navigates to /app/cameras/{id}')`
      - `it.todo('UI-05: maintenance row shows "Maintenance · by {user} · {time}"')`
      - `it.todo('UI-05: empty-state body shows "{N} cameras online, 0 issues."')`

    `platform-issues-panel.test.tsx`:
      - `it.todo('UI-05: empty state renders "Platform healthy" (D-09)')`
      - `it.todo('UI-05: renders SRS down issue row with Investigate action')`
      - `it.todo('UI-05: renders edge-disconnected row with View cluster action')`

    `cluster-nodes-panel.test.tsx`:
      - `it.todo('UI-05: consumes useClusterNodes hook (D-08)')`
      - `it.todo('UI-05: renders 5 columns: Node, Role, Status, Uptime, Connections')`
      - `it.todo('UI-05: empty state renders "No cluster nodes registered."')`

    `storage-forecast-card.test.tsx`:
      - `it.todo('UI-05: toggle group switches between 7 days and 30 days, default 7 days (D-10)')`
      - `it.todo('UI-05: caption shows "Estimated {N} days until full"')`
      - `it.todo('UI-05: warning styling when daysUntilFull <= 14')`

    `recent-audit-highlights.test.tsx`:
      - `it.todo('UI-05: renders up to 7 entries (D-11)')`
      - `it.todo('UI-05: footer link "View full audit log" navigates to /admin/audit')`
      - `it.todo('UI-05: entry format "{actor} {verb} {target} · {time}"')`
      - `it.todo('UI-05: empty state renders "No recent platform activity."')`

    `org-health-data-table.test.tsx`:
      - `it.todo('UI-05: default sort by usage percent desc (D-12)')`
      - `it.todo('UI-05: row click navigates to /admin/organizations/{id}')`
      - `it.todo('UI-05: cameras cell shows "{used} / {limit}" with Progress bar')`
      - `it.todo('UI-05: View action navigates to /admin/organizations/{id}, Manage to /settings')`
      - `it.todo('UI-05: status cell renders destructive badge when issues > 0, Healthy outline badge when 0')`

    `camera-marker.test.tsx`:
      - `it.todo('UI-06: renders teardrop SVG with iconSize [28, 36] and iconAnchor [14, 36] (D-13)')`
      - `it.todo('UI-06: pin fill = green #22c55e when status=online (UI-SPEC)')`
      - `it.todo('UI-06: pin fill = red #ef4444 when status=offline')`
      - `it.todo('UI-06: pin fill = amber #f59e0b when status=degraded or reconnecting')`
      - `it.todo('UI-06: renders recording red dot 8x8 upper-right when isRecording=true (D-14)')`
      - `it.todo('UI-06: renders wrench badge 10x10 gray lower-right when maintenanceMode=true (D-14)')`
      - `it.todo('UI-06: recording dot has animate-pulse class')`
      - `it.todo('T-18-XSS-MARKER: escapes HTML in camera name inside aria-label — name with <script> renders as &lt;script&gt;')`

    `camera-map-inner.test.tsx`:
      - `it.todo('UI-06: iconCreateFunction returns red bubble when any child has status=offline (D-16)')`
      - `it.todo('UI-06: iconCreateFunction returns amber bubble when worst child is degraded/reconnecting')`
      - `it.todo('UI-06: iconCreateFunction returns green bubble when all children are online/connecting')`
      - `it.todo('UI-06: cluster refresh triggered on camera status change')`

    `camera-popup.test.tsx`:
      - `it.todo('UI-06: preview container is 240x135 (D-17)')`
      - `it.todo('UI-06: popup renders REC overlay top-left when isRecording and status=online (D-18)')`
      - `it.todo('UI-06: popup renders Maintenance overlay when maintenanceMode=true (D-18)')`
      - `it.todo('UI-06: renders Recording badge with "{N} days retention" when retentionDays present (D-19)')`
      - `it.todo('UI-06: renders Maintenance badge with by-user + relative time (D-19)')`
      - `it.todo('UI-06: renders "Offline {time} ago" only when status=offline (D-19)')`
      - `it.todo('UI-06: two primary action buttons: View Stream + View Recordings (D-21)')`
      - `it.todo('UI-06: ⋮ dropdown has Set Location, Toggle Maintenance, Open Camera Detail (D-21)')`
      - `it.todo('UI-06: Toggle Maintenance opens confirmation dialog (Phase 15-04 reuse)')`
      - `it.todo('UI-06: Toggle Maintenance confirm calls POST /api/cameras/:id/maintenance')`
      - `it.todo('UI-06: View Recordings navigates to /app/recordings?camera={id}')`
      - `it.todo('UI-06 REGRESSION GUARD: PreviewVideo does not remount when viewerCount prop changes on parent (Phase 13 runaway viewer count bug)')`
      - `it.todo('UI-06: popup Leaflet maxWidth=320 minWidth=280 (D-22)')`
  </behavior>
  <action>
1. Create `apps/web/src/test-utils/camera-fixtures.ts` exporting:
   - `makeDashboardCamera(overrides?: Partial&lt;DashboardCameraExt&gt;): DashboardCameraExt`
   - `makeMapCamera(overrides?: Partial&lt;MapCameraExt&gt;): MapCameraExt`
   - Named fixtures `onlineCamera`, `offlineCamera`, `degradedCamera`, `reconnectingCamera`, `recordingCamera`, `maintenanceCamera`.
   - The `DashboardCameraExt` type extends `DashboardCamera` with `isRecording`, `maintenanceMode`, `maintenanceEnteredBy`, `maintenanceEnteredAt`.
   - `MapCameraExt` extends `MapCamera` with the same plus `lastOnlineAt, retentionDays`.
   - Field spelling MUST be `maintenanceEnteredBy` / `maintenanceEnteredAt` (not Enabled) — per RESEARCH Pitfall 5 and prisma/schema.prisma:223-224.
   - `recordingCamera`: `isRecording: true, retentionDays: 7, status: 'online'`
   - `maintenanceCamera`: `maintenanceMode: true, maintenanceEnteredBy: 'Jane Doe', maintenanceEnteredAt: '2026-04-21T00:00:00.000Z', status: 'online'`

2. Create each test file listed in `<files>`. Each file:
   - Uses `describe('{Component name}', () => { ... })`
   - Contains `it.todo('{exact text from behavior section}')` for every listed stub.
   - No real assertions yet — all `it.todo` so the file is green and runnable.
   - Imports fixtures from `@/test-utils/camera-fixtures` where appropriate (even if not yet used, to verify path resolution).

3. Run tests to confirm green:
   ```
   cd apps/web && pnpm test -- --run src/__tests__/tenant-dashboard-page.test.tsx
   cd apps/web && pnpm test -- --run src/components/dashboard/issues-panel.test.tsx
   cd apps/web && pnpm test -- --run src/components/map/camera-marker.test.tsx
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/__tests__/tenant-dashboard-page.test.tsx src/__tests__/platform-dashboard-page.test.tsx src/components/dashboard/issues-panel.test.tsx src/components/dashboard/platform-issues-panel.test.tsx src/components/dashboard/cluster-nodes-panel.test.tsx src/components/dashboard/storage-forecast-card.test.tsx src/components/dashboard/recent-audit-highlights.test.tsx src/app/admin/dashboard/components/org-health-data-table.test.tsx src/components/map/camera-marker.test.tsx src/components/map/camera-map-inner.test.tsx src/components/map/camera-popup.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/test-utils/camera-fixtures.ts` succeeds
    - `test -f apps/web/src/__tests__/tenant-dashboard-page.test.tsx` succeeds
    - `test -f apps/web/src/__tests__/platform-dashboard-page.test.tsx` succeeds
    - `test -f apps/web/src/components/dashboard/issues-panel.test.tsx` succeeds
    - `test -f apps/web/src/components/dashboard/platform-issues-panel.test.tsx` succeeds
    - `test -f apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx` succeeds
    - `test -f apps/web/src/components/dashboard/storage-forecast-card.test.tsx` succeeds
    - `test -f apps/web/src/components/dashboard/recent-audit-highlights.test.tsx` succeeds
    - `test -f apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx` succeeds
    - `test -f apps/web/src/components/map/camera-marker.test.tsx` succeeds
    - `test -f apps/web/src/components/map/camera-map-inner.test.tsx` succeeds
    - `test -f apps/web/src/components/map/camera-popup.test.tsx` succeeds
    - `grep -c "maintenanceEnteredBy" apps/web/src/test-utils/camera-fixtures.ts` >= 1 (schema spelling)
    - `grep -c "maintenanceEnabledBy" apps/web/src/test-utils/camera-fixtures.ts` == 0 (no old spelling)
    - `grep -c "PreviewVideo does not remount" apps/web/src/components/map/camera-popup.test.tsx` >= 1 (regression guard)
    - `grep -c "T-18-XSS-MARKER" apps/web/src/components/map/camera-marker.test.tsx` >= 1 (XSS guard)
    - `grep -c "it.todo" apps/web/src/components/map/camera-marker.test.tsx` >= 8 (all stubs present)
    - `grep -c "it.todo" apps/web/src/components/map/camera-popup.test.tsx` >= 13 (all stubs present)
    - `cd apps/web && pnpm test -- --run src/components/map/camera-marker.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    14 test files + fixtures file created. All `it.todo` stubs match behaviors from RESEARCH.md §Validation Architecture. Vitest runs green (todos count as pass). Schema field spelling `maintenanceEnteredBy` / `maintenanceEnteredAt` verified.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create backend admin-dashboard + dashboard test stubs</name>
  <files>apps/api/tests/admin/admin-dashboard.test.ts, apps/api/tests/dashboard/dashboard.test.ts</files>
  <read_first>
    - apps/api/tests/dashboard/dashboard.test.ts (existing file — may have it.todo stubs per RESEARCH line 871; plan flips it.todo to it in Plan 01)
    - apps/api/src/admin/admin-dashboard.service.ts (existing methods getPlatformStats, getSystemMetrics, getOrgSummary)
    - apps/api/src/dashboard/dashboard.service.ts (existing getStats, getCameraStatusList at line 217)
    - apps/api/src/prisma/schema.prisma (Camera, Organization, Package, ApiKeyUsage, AuditLog, SrsNode, RecordingSegment models)
    - .planning/phases/18-dashboard-map-polish/18-RESEARCH.md §Validation Architecture (lines 865-871) for test-to-method mapping
    - apps/api/vitest.config.ts (test runner config — uses sms_platform_test DB, 30s timeout)
  </read_first>
  <behavior>
    `apps/api/tests/admin/admin-dashboard.test.ts` — create if missing; add `describe('AdminDashboardService Phase 18 additions')` with stubs:
      - `it.todo('getActiveStreamsCount returns SRS publisher count')`
      - `it.todo('getActiveStreamsCount returns 0 when SRS unreachable')`
      - `it.todo('getRecordingsActive counts cameras with isRecording=true across all orgs')`
      - `it.todo('getPlatformIssues returns srs-down when SRS versions endpoint throws')`
      - `it.todo('getPlatformIssues returns edge-down rows for SrsNode role=EDGE status in (OFFLINE, DEGRADED)')`
      - `it.todo('getPlatformIssues returns org-offline-rate rows for orgs with >50% cameras offline')`
      - `it.todo('getPlatformIssues excludes system org from org-offline-rate calculation')`
      - `it.todo('getStorageForecast returns daily bytes sums grouped by DATE(createdAt) over range')`
      - `it.todo('getStorageForecast computes estimatedDaysUntilFull via linear regression')`
      - `it.todo('getStorageForecast validates range query against enum [7d, 30d]')`
      - `it.todo('getRecentAuditHighlights filters by event types org.created, org.package_changed, user.suspended, cluster.node_added, cluster.node_removed, limit 7')`
      - `it.todo('getRecentAuditHighlights joins actor name + org name')`
      - `it.todo('getOrgHealthOverview returns org rows with cameraUsagePct + storageUsagePct sorted desc')`
      - `it.todo('getOrgHealthOverview excludes system org')`
      - `it.todo('getOrgHealthOverview computes bandwidth today from ApiKeyUsage sum where date >= startOfDay')`
      - `it.todo('getClusterNodes returns SrsNode rows mapped to display shape')`
      - `it.todo('T-18-AUTHZ-ADMIN: all new endpoints are guarded by SuperAdminGuard on controller')`

    `apps/api/tests/dashboard/dashboard.test.ts` — if file exists, ADD these stubs; if missing, create the file with `describe('DashboardService Phase 18 enrichments')`:
      - `it.todo('getCameraStatusList includes isRecording, maintenanceMode, maintenanceEnteredBy, maintenanceEnteredAt, retentionDays')`
      - `it.todo('getCameraStatusList scopes to org (TENANCY_CLIENT no cross-tenant leak) — T-18-TENANCY-ISSUES')`
      - `it.todo('getStats adds camerasRecording (count where isRecording=true) and camerasInMaintenance (count where maintenanceMode=true)')`
  </behavior>
  <action>
1. Check if `apps/api/tests/admin/admin-dashboard.test.ts` exists. If not, create it with the test framework imports matching `apps/api/tests/dashboard/dashboard.test.ts` pattern (likely `import { describe, it, expect, beforeEach } from 'vitest'` + `Test.createTestingModule` + test DB reset helpers from `apps/api/tests/global-setup.ts`).

2. Create/extend the file with `describe('AdminDashboardService Phase 18 additions', () => { ... })` containing all 17 `it.todo` stubs from the behavior section.

3. Check if `apps/api/tests/dashboard/dashboard.test.ts` exists (RESEARCH line 871 says it does with an it.todo for getCameraStatusList). Open it and append a new `describe('DashboardService Phase 18 enrichments', () => { ... })` block with the 3 stubs from behavior section — OR flip the existing getCameraStatusList it.todo if present.

4. Both test files must compile (TypeScript OK) and run green. `it.todo` is a vitest pass.

5. Run to verify:
   ```
   cd apps/api && pnpm test -- --run tests/admin/admin-dashboard.test.ts
   cd apps/api && pnpm test -- --run tests/dashboard/dashboard.test.ts
   ```
  </action>
  <verify>
    <automated>cd apps/api && pnpm test -- --run tests/admin/admin-dashboard.test.ts tests/dashboard/dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/api/tests/admin/admin-dashboard.test.ts` succeeds
    - `test -f apps/api/tests/dashboard/dashboard.test.ts` succeeds
    - `grep -c "it.todo" apps/api/tests/admin/admin-dashboard.test.ts` >= 17
    - `grep -c "getActiveStreamsCount" apps/api/tests/admin/admin-dashboard.test.ts` >= 2
    - `grep -c "getStorageForecast" apps/api/tests/admin/admin-dashboard.test.ts` >= 3
    - `grep -c "getOrgHealthOverview" apps/api/tests/admin/admin-dashboard.test.ts` >= 3
    - `grep -c "SuperAdminGuard" apps/api/tests/admin/admin-dashboard.test.ts` >= 1 (T-18-AUTHZ-ADMIN)
    - `grep -c "isRecording" apps/api/tests/dashboard/dashboard.test.ts` >= 1
    - `grep -c "TENANCY_CLIENT\|T-18-TENANCY" apps/api/tests/dashboard/dashboard.test.ts` >= 1
    - `cd apps/api && pnpm test -- --run tests/admin/admin-dashboard.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Backend test stubs created for all Phase 18 service methods + security threat markers. Tests run green. Plan 01 executors will flip `it.todo` to `it` with real assertions.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None (this plan) | Wave 0 adds only test stubs. No runtime code, no endpoints, no user input paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-00-01 | Tampering | Test fixtures | mitigate | Fixtures are test-only; never imported by production code. Enforced via test file path `src/test-utils/` convention. Downstream plans verify via grep that production imports do not reach test-utils. |
| T-18-00-02 | Information Disclosure | Test stubs | accept | `it.todo` stubs contain behavior descriptions but no real data. No PII, no secrets. |

All runtime STRIDE threats (T-18-XSS-MARKER, T-18-XSS-POPUP, T-18-AUTHZ-ADMIN, T-18-TENANCY-ISSUES, T-18-INFO-LEAK-STORAGE) are covered in Plans 01-05 where actual code lands. This plan only creates the stubs that will enforce those threats during implementation.
</threat_model>

<verification>
Full suite: `cd apps/web && pnpm test && cd ../api && pnpm test` — both green.
Todos count: across all 14 test files ≥ 55 `it.todo` stubs total.
Fixtures file export count: exactly 6 named fixtures + 2 factory helpers.
</verification>

<success_criteria>
- 14 test files created (12 web + 2 api) with all `it.todo` stubs per RESEARCH §Validation Architecture
- Shared camera fixtures file exists with Phase 15 schema field names (`maintenanceEnteredBy` / `maintenanceEnteredAt`)
- All test files run green (todos pass)
- Regression guard stub exists for PreviewVideo memoization (Phase 13 runaway viewer count bug)
- XSS guard stub exists for marker name escaping (T-18-XSS-MARKER)
- Security threat stubs exist for admin authz (T-18-AUTHZ-ADMIN) and tenancy (T-18-TENANCY-ISSUES)
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-map-polish/18-00-SUMMARY.md` summarizing files created, stub counts, and link between stubs and D-XX / T-18-XX references.
</output>
