---
phase: 18
plan: 06
type: execute
wave: 3
depends_on: [18-00, 18-01, 18-05]
files_modified:
  - apps/web/src/hooks/use-platform-dashboard.ts
  - apps/web/src/app/admin/dashboard/components/org-health-columns.tsx
  - apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx
  - apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx
  - apps/web/src/components/pages/platform-dashboard-page.tsx
  - apps/web/src/__tests__/platform-dashboard-page.test.tsx
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Super admin dashboard renders exactly 7 stat cards: Organizations, Total Cameras, Cameras Online, Cameras Offline, Stream Bandwidth, Active Streams, Recordings Active (per D-05)"
    - "Stat card grid uses classes grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-7 (UI-SPEC)"
    - "SystemMetrics 4-card section kept unchanged (per D-06)"
    - "Vertical stack order: stat cards → SystemMetrics → PlatformIssuesPanel → ClusterNodesPanel → StorageForecastCard → OrgHealthDataTable → RecentAuditHighlights (per D-07)"
    - "Organization Summary Table replaced by OrgHealthDataTable (DataTable migration per D-12)"
    - "OrgHealthDataTable default sort is max(cameraUsagePct, storageUsagePct) descending"
    - "OrgHealthDataTable row click navigates to /admin/organizations/{id}"
  artifacts:
    - path: "apps/web/src/app/admin/dashboard/components/org-health-columns.tsx"
      provides: "TanStack column factory for Org Health"
      contains: "cameraUsagePct"
    - path: "apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx"
      provides: "DataTable wrapper for Org Health Overview"
      contains: "usage"
    - path: "apps/web/src/components/pages/platform-dashboard-page.tsx"
      provides: "Refactored page composing 7 stat cards + 4 new widgets + DataTable, in priority stack order"
      contains: "OrgHealthDataTable"
    - path: "apps/web/src/hooks/use-platform-dashboard.ts"
      provides: "Adds useOrgHealthOverview and useActiveStreamsCount + useRecordingsActive (extends Plan 05 file)"
      exports: ["useOrgHealthOverview"]
  key_links:
    - from: "apps/web/src/components/pages/platform-dashboard-page.tsx"
      to: "apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx"
      via: "<OrgHealthDataTable />"
      pattern: "OrgHealthDataTable"
    - from: "apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx"
      to: "apps/web/src/components/ui/data-table/data-table.tsx"
      via: "DataTable wrapper + columns factory (Phase 14 pattern)"
      pattern: "DataTable"
    - from: "apps/web/src/components/pages/platform-dashboard-page.tsx"
      to: "7 stat cards + 4 new widgets from Plan 05"
      via: "imports + vertical stack JSX"
      pattern: "PlatformIssuesPanel\\|ClusterNodesPanel\\|StorageForecastCard\\|RecentAuditHighlights\\|OrgHealthDataTable"
---

<objective>
Compose Plan 05 widgets + new Org Health DataTable into the refactored super admin dashboard page per D-05, D-07, D-12:
1. **D-05:** Add 2 stat cards (Active Streams, Recordings Active) → 7 total.
2. **D-07:** Rearrange page into vertical priority stack: stat cards → SystemMetrics → PlatformIssues → Cluster → StorageForecast → OrgHealth → RecentAudit.
3. **D-12:** Migrate Organization Summary table to DataTable (Phase 14 pattern) with columns Org, Plan, Cameras, Storage, Bandwidth (today), Status, Actions. Default sort by max(cameraUsagePct, storageUsagePct) descending.

Purpose: Close UI-05 super admin surface. This is the composition plan that depends on Plan 01 (backend endpoints) + Plan 05 (widget components).
Output: Refactored `platform-dashboard-page.tsx` + `OrgHealthDataTable` + extended `use-platform-dashboard` hook + 6 flipped page test stubs + 5 flipped DataTable test stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dashboard-map-polish/18-CONTEXT.md
@.planning/phases/18-dashboard-map-polish/18-RESEARCH.md
@.planning/phases/18-dashboard-map-polish/18-UI-SPEC.md

<interfaces>
<!-- After Plan 01 and Plan 05 ship, these data shapes exist. -->

`GET /api/admin/dashboard/active-streams` → `{ count: number }`
`GET /api/admin/dashboard/recordings-active` → `{ count: number }`

`GET /api/admin/dashboard/org-health` → `OrgHealth[]`:
```ts
type OrgHealth = {
  orgId: string;
  orgName: string;
  orgSlug: string;
  packageName: string | null;
  camerasUsed: number;
  camerasLimit: number | null;
  cameraUsagePct: number;
  storageUsedBytes: string;
  storageLimitGb: number | null;
  storageUsagePct: number;
  bandwidthTodayBytes: string;
  issuesCount: number;
};
```

Existing `DataTable` wrapper at `apps/web/src/components/ui/data-table/data-table.tsx`:
- Props: `columns`, `data`, `emptyState?`, `onRowClick?`, `facetedFilters?`, `pagination?`, etc.
- Supports client-side sorting via TanStack; accepts `ColumnDef[]`.

Existing column factory pattern: `apps/web/src/app/admin/organizations/components/organizations-columns.tsx`.

Widgets from Plan 05:
- `<PlatformIssuesPanel />` from `@/components/dashboard/platform-issues-panel`
- `<ClusterNodesPanel />` from `@/components/dashboard/cluster-nodes-panel`
- `<StorageForecastCard />` from `@/components/dashboard/storage-forecast-card`
- `<RecentAuditHighlights />` from `@/components/dashboard/recent-audit-highlights`

Hooks from Plan 05 (will be extended here): `usePlatformIssues`, `useStorageForecast`, `useRecentAudit`. This plan adds `useActiveStreamsCount`, `useRecordingsActive`, `useOrgHealthOverview`.

Current page structure (`platform-dashboard-page.tsx`):
- 5 stat cards (Organizations, Total Cameras, Cameras Online, Cameras Offline, Stream Bandwidth)
- System Metrics (4 cards: CPU, Memory, Load, SRS Uptime)
- Organization Summary Table (raw `<Table>` at lines 214-264)

Current stat grid: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-5` (line 138). Must change to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-7`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend use-platform-dashboard with remaining sub-hooks + create OrgHealthDataTable</name>
  <files>apps/web/src/hooks/use-platform-dashboard.ts, apps/web/src/app/admin/dashboard/components/org-health-columns.tsx, apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx, apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/use-platform-dashboard.ts (Plan 05 output — add 3 sub-hooks)
    - apps/web/src/components/ui/data-table/data-table.tsx (existing wrapper)
    - apps/web/src/app/admin/organizations/components/organizations-columns.tsx (Phase 14 column-factory exemplar — follow same structure)
    - apps/web/src/components/ui/data-table/data-table-column-header.tsx (column header primitive with sort icons)
    - apps/web/src/components/ui/progress.tsx (shadcn Progress primitive — used in Cameras/Storage cells); if missing, run `shadcn add progress`
    - apps/web/src/components/ui/badge.tsx (outline, destructive variants)
    - apps/web/src/components/ui/dropdown-menu.tsx (for row Actions ⋮)
    - apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx (Plan 00 — 5 stubs)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md §D-12 (columns, actions, default sort)
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Super Admin Copy (lines 204-211) — exact column labels
    - .planning/phases/18-dashboard-map-polish/18-RESEARCH.md §Pattern 4 (DataTable migration), §Pattern 5 (OrgHealth query), §Pitfall 8 (sort by computed %)
  </read_first>
  <behavior>
    1) Extend `apps/web/src/hooks/use-platform-dashboard.ts` with 3 new sub-hooks (append to the file):
       - `useActiveStreamsCount()` → GET `/api/admin/dashboard/active-streams` → `{ count, loading, error }`. 30s polling.
       - `useRecordingsActive()` → GET `/api/admin/dashboard/recordings-active` → `{ count, loading, error }`. 30s polling.
       - `useOrgHealthOverview()` → GET `/api/admin/dashboard/org-health` → `{ orgs: OrgHealth[], loading, error, refetch }`. 30s polling.
       - Mirror existing pattern (useState, useCallback, useEffect + setInterval).

    2) Verify shadcn `progress` primitive exists: `ls apps/web/src/components/ui/progress.tsx`. If missing, run `cd apps/web && npx shadcn@latest add progress`.

    3) Create `apps/web/src/app/admin/dashboard/components/org-health-columns.tsx`:
       - Exports `orgHealthColumns: ColumnDef<OrgHealth>[]` (8 columns counting Actions).
       - Exports `OrgHealth` type (or re-export from hook file).
       - Column 1 `orgName`: `DataTableColumnHeader title="Organization"`; cell `<span className="font-medium">{row.original.orgName}</span>`.
       - Column 2 `packageName`: `header: 'Plan'`; cell `<Badge variant="outline">{row.original.packageName ?? 'No plan'}</Badge>`; `filterFn` for faceted filter.
       - Column 3 `cameras` (id, not accessor):
         ```tsx
         header: 'Cameras',
         cell: ({ row }) => (
           <div className="flex items-center gap-2">
             <span>{row.original.camerasUsed} / {row.original.camerasLimit ?? '∞'}</span>
             <Progress value={row.original.cameraUsagePct} className="h-1 w-12" />
           </div>
         ),
         sortingFn: (a, b) => a.original.cameraUsagePct - b.original.cameraUsagePct,
         ```
       - Column 4 `storage`: similar, display `{bytes→GB} / {limit GB}` + Progress bar; sortingFn by storageUsagePct.
       - Column 5 `bandwidthTodayBytes`: formatted bytes; no Progress bar.
       - Column 6 `issuesCount`:
         ```tsx
         cell: ({ row }) => row.original.issuesCount > 0
           ? <Badge variant="destructive">{row.original.issuesCount} issues</Badge>
           : <Badge variant="outline">Healthy</Badge>,
         ```
       - Column 7 `actions` (id):
         ```tsx
         cell: ({ row }) => (
           <DropdownMenu>
             <DropdownMenuTrigger asChild>
               <Button variant="ghost" size="icon"><MoreVertical /></Button>
             </DropdownMenuTrigger>
             <DropdownMenuContent align="end">
               <DropdownMenuItem onClick={() => router.push(`/admin/organizations/${row.original.orgId}`)}>
                 <Eye className="mr-2 h-4 w-4" />View
               </DropdownMenuItem>
               <DropdownMenuItem onClick={() => router.push(`/admin/organizations/${row.original.orgId}/settings`)}>
                 <Settings className="mr-2 h-4 w-4" />Manage
               </DropdownMenuItem>
             </DropdownMenuContent>
           </DropdownMenu>
         ),
         ```
         Since column cells can't call useRouter directly, factor a `createActionsColumn(router)` function or pass via meta. Simplest: factor the columns into a function `makeOrgHealthColumns(router: ReturnType<typeof useRouter>)` that returns `ColumnDef<OrgHealth>[]`. Consumer calls it inside a component.

    4) Create `apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx`:
       - `'use client'`.
       - Imports: `useOrgHealthOverview` from `@/hooks/use-platform-dashboard`, `DataTable` from `@/components/ui/data-table/data-table`, `makeOrgHealthColumns`, `useRouter` from `next/navigation`, Card primitives, Skeleton.
       - Card wrapper with `<CardTitle>Organization Health</CardTitle>`.
       - Inside card body: DataTable.
       - Default sort: sorted on `cameras` column desc (OR compute a derived sort: use `data.sort((a, b) => Math.max(b.cameraUsagePct, b.storageUsagePct) - Math.max(a.cameraUsagePct, a.storageUsagePct))` before passing to DataTable as initialData since TanStack sort is column-based).
       - `onRowClick={(row) => router.push(`/admin/organizations/${row.orgId}`)}`.
       - Empty state: "No organizations yet."
       - Loading: DataTable's built-in skeleton (Phase 14 pattern provides).

    5) Flip 5 it.todo stubs in `org-health-data-table.test.tsx`:
       - Default sort by usage percent desc (seed 3 orgs with varied %, assert DOM row order).
       - Row click navigates to /admin/organizations/{id} (mock router, userEvent.click on row).
       - Cameras cell shows "{used} / {limit}" with Progress bar (inspect innerText + role="progressbar").
       - View action navigates to /admin/organizations/{id}, Manage to /settings.
       - Status cell: destructive badge when issues > 0, outline "Healthy" when 0.
  </behavior>
  <action>
1. Extend `apps/web/src/hooks/use-platform-dashboard.ts` with 3 new exports. Follow the existing template:
   ```ts
   export function useActiveStreamsCount() { /* GET /api/admin/dashboard/active-streams, 30s polling */ }
   export function useRecordingsActive() { /* GET /api/admin/dashboard/recordings-active, 30s polling */ }
   export function useOrgHealthOverview() { /* GET /api/admin/dashboard/org-health, 30s polling */ }
   ```

2. Verify/install `progress` shadcn primitive:
   ```bash
   cd apps/web && ls src/components/ui/progress.tsx || npx shadcn@latest add progress
   ```

3. Create `apps/web/src/app/admin/dashboard/components/org-health-columns.tsx` per behavior spec. Export `OrgHealth` type (or re-export from hook). Export `makeOrgHealthColumns(router)` function returning `ColumnDef<OrgHealth>[]`.

4. Create `apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx` per behavior spec. Sort data client-side by max usage % before passing to DataTable.

5. Flip 5 it.todo stubs in `org-health-data-table.test.tsx`. Mock `useOrgHealthOverview` + `next/navigation`. Seed test fixture:
   ```ts
   const orgs: OrgHealth[] = [
     { orgId: 'a', orgName: 'Alpha', packageName: 'Pro', camerasUsed: 18, camerasLimit: 20, cameraUsagePct: 90, storageUsedBytes: '0', storageLimitGb: 100, storageUsagePct: 5, bandwidthTodayBytes: '0', issuesCount: 2 },
     { orgId: 'b', orgName: 'Beta', packageName: 'Free', camerasUsed: 1, camerasLimit: 5, cameraUsagePct: 20, storageUsedBytes: '0', storageLimitGb: 10, storageUsagePct: 10, bandwidthTodayBytes: '0', issuesCount: 0 },
   ];
   ```
   Assert DOM row order shows Alpha first (max(90, 5) = 90 > max(20, 10) = 20).

6. Run:
   ```
   cd apps/web && pnpm test -- --run src/app/admin/dashboard/components/org-health-data-table.test.tsx
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/app/admin/dashboard/components/org-health-data-table.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export function useActiveStreamsCount" apps/web/src/hooks/use-platform-dashboard.ts` >= 1
    - `grep -c "export function useRecordingsActive" apps/web/src/hooks/use-platform-dashboard.ts` >= 1
    - `grep -c "export function useOrgHealthOverview" apps/web/src/hooks/use-platform-dashboard.ts` >= 1
    - `test -f apps/web/src/components/ui/progress.tsx` succeeds
    - `test -f apps/web/src/app/admin/dashboard/components/org-health-columns.tsx` succeeds
    - `grep -c "makeOrgHealthColumns\|orgHealthColumns" apps/web/src/app/admin/dashboard/components/org-health-columns.tsx` >= 1
    - `grep -c "cameraUsagePct\|storageUsagePct" apps/web/src/app/admin/dashboard/components/org-health-columns.tsx` >= 2
    - `test -f apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx` succeeds
    - `grep -c "Organization Health" apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx` >= 1
    - `grep -c "/admin/organizations/" apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx` >= 1
    - `grep -c "it.todo" apps/web/src/app/admin/dashboard/components/org-health-data-table.test.tsx` == 0
    - `cd apps/web && pnpm test -- --run src/app/admin/dashboard/components/org-health-data-table.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    3 new sub-hooks added to usePlatformDashboard. OrgHealthDataTable built with Phase 14 DataTable pattern, default sorted by max usage %. Row click + actions navigate correctly. 5 test stubs flipped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor platform-dashboard-page.tsx into vertical priority stack with 7 stat cards + 4 widgets + OrgHealthDataTable (D-05, D-07, D-12)</name>
  <files>apps/web/src/components/pages/platform-dashboard-page.tsx, apps/web/src/__tests__/platform-dashboard-page.test.tsx</files>
  <read_first>
    - apps/web/src/components/pages/platform-dashboard-page.tsx (current composition — lines 65-267; understand current state-management patterns)
    - apps/web/src/__tests__/platform-dashboard-page.test.tsx (Plan 00 — 5 stubs)
    - apps/web/src/components/dashboard/stat-card.tsx (StatCard API)
    - apps/web/src/components/dashboard/platform-issues-panel.tsx (Plan 05 output)
    - apps/web/src/components/dashboard/cluster-nodes-panel.tsx (Plan 05 output)
    - apps/web/src/components/dashboard/storage-forecast-card.tsx (Plan 05 output)
    - apps/web/src/components/dashboard/recent-audit-highlights.tsx (Plan 05 output)
    - apps/web/src/app/admin/dashboard/components/org-health-data-table.tsx (Task 1 output)
    - apps/web/src/hooks/use-platform-dashboard.ts (useActiveStreamsCount, useRecordingsActive from Task 1)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md §D-05, §D-06, §D-07, §D-12
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Super Admin Dashboard Layout (lines 287-340) — exact vertical stack + grid classes
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Copy lines 174-184 (stat card labels)
  </read_first>
  <behavior>
    Refactored `platform-dashboard-page.tsx`:
    - Imports added: StatCard (kept); `Activity, Circle` from lucide (for Active Streams + Recordings Active icons per UI-SPEC); `useActiveStreamsCount`, `useRecordingsActive` from `@/hooks/use-platform-dashboard`; `PlatformIssuesPanel`, `ClusterNodesPanel`, `StorageForecastCard`, `RecentAuditHighlights`; `OrgHealthDataTable`.
    - Imports removed: `Table`, `TableBody`, `TableCell`, `TableHead`, `TableHeader`, `TableRow` (no longer used — OrgHealthDataTable replaces).
    - Remove state `orgs`, `orgsLoading`, `OrgSummary` type (replaced by OrgHealthDataTable's own hook).
    - Keep state `stats`, `metrics`, `statsLoading`, `metricsLoading`, `error`.
    - Add hook calls `useActiveStreamsCount()` and `useRecordingsActive()` near top of component.
    - Remove `loadOrgs()` function and its useEffect/setInterval lines.
    - Stat card grid classes change from `lg:grid-cols-5` to `lg:grid-cols-4 xl:grid-cols-7`.
    - Skeleton block during stats loading: 7× skeletons (matching 7-col grid).
    - Stat cards (exact order per UI-SPEC Copy):
      1. `Organizations` — value `stats.totalOrgs`, icon `<Building2 />` (unchanged)
      2. `Total Cameras` — value `stats.totalCameras`, icon `<Camera />` (unchanged)
      3. `Cameras Online` — value `stats.camerasOnline`, icon `<Camera />`, badge 'Live' if > 0 (unchanged)
      4. `Cameras Offline` — value `stats.camerasOffline`, icon `<MonitorOff />`, badge `{N} down` destructive if > 0 (unchanged)
      5. `Stream Bandwidth` — value `formatBandwidth(stats.streamBandwidth * 125)`, icon `<Wifi />` (unchanged)
      6. `Active Streams` — value `activeStreams?.count ?? 0`, icon `<Activity className="h-4 w-4" />`
      7. `Recordings Active` — value `recordingsActive?.count ?? 0`, icon `<Circle className="h-4 w-4 fill-[hsl(0_84%_60%)] text-[hsl(0_84%_60%)]" />` (red filled dot)
    - SystemMetrics 4-card section unchanged (D-06 — keep as-is lines 180-212).
    - Replace `<Card>` Organization Summary block (lines 214-264) with vertical stack of new widgets in D-07 order:
      ```tsx
      <PlatformIssuesPanel />
      <ClusterNodesPanel />
      <StorageForecastCard />
      <OrgHealthDataTable />
      <RecentAuditHighlights />
      ```
    - Outer container `<div className="space-y-6">` retains vertical rhythm.

    Tests (`platform-dashboard-page.test.tsx`) — flip all 5 it.todo stubs:
    - 7 stat cards with correct labels.
    - Grid classes `lg:grid-cols-4 xl:grid-cols-7`.
    - 5 new widgets + DataTable rendered in order (use `within()` on container, check DOM order).
    - SystemMetrics 4 cards still present.
    - Organization Summary Table absent (no raw `<Table>` with "Online"/"Offline" headers).

    Mock all hooks (useDashboardStats, useActiveStreamsCount, useRecordingsActive, and the downstream widgets' hooks via `vi.mock`).
  </behavior>
  <action>
1. Edit `apps/web/src/components/pages/platform-dashboard-page.tsx`:
   - Update imports: add lucide `Activity, Circle`; add hook imports `useActiveStreamsCount, useRecordingsActive` from `@/hooks/use-platform-dashboard`; add widget imports; remove Table primitives import.
   - Remove `orgs`, `orgsLoading`, `OrgSummary` state + related `loadOrgs` function.
   - Add inside component: `const { count: activeStreamsData, loading: activeStreamsLoading } = useActiveStreamsCount();` (extract `count` via destructure OR read object and index). Match useDashboardStats-style return shape.
   - Update stat grid class from `grid-cols-1 sm:grid-cols-2 lg:grid-cols-5` to `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 xl:grid-cols-7` (both loading-skeleton div AND loaded-stats div).
   - Change skeleton count from 5 to 7.
   - Append 2 new StatCards per behavior spec (Active Streams + Recordings Active).
   - Replace the entire `<Card><CardHeader>...Organization Summary...</Table>...</Card>` block (lines 214-264) with:
     ```tsx
     <PlatformIssuesPanel />
     <ClusterNodesPanel />
     <StorageForecastCard />
     <OrgHealthDataTable />
     <RecentAuditHighlights />
     ```
   - Verify SystemMetrics 4-card block (lines 180-212) unchanged.

2. Flip 5 it.todo stubs in `apps/web/src/__tests__/platform-dashboard-page.test.tsx`:
   - Mock `useDashboardStats` (extend to return 5 fields) + `useActiveStreamsCount` + `useRecordingsActive` + the widget hooks.
   - Assertions:
     - All 7 labels present.
     - Grid class contains `xl:grid-cols-7`.
     - `screen.getByRole('heading', { name: /Platform Issues/i })`, similar for Cluster, Storage Forecast, Organization Health, Recent Activity all present.
     - DOM order: use `screen.getAllByRole('heading')` + index assertion or `within(container)` traversal.
     - `screen.queryByText('Organization Summary')` should be null (old table removed).
     - `screen.getAllByText(/CPU/i).length >= 1` (SystemMetrics kept).

3. Run:
   ```
   cd apps/web && pnpm test -- --run src/__tests__/platform-dashboard-page.test.tsx
   cd apps/web && pnpm test   # full web suite
   cd apps/web && pnpm build  # no type errors
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/__tests__/platform-dashboard-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Active Streams" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "Recordings Active" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "useActiveStreamsCount" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "useRecordingsActive" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "xl:grid-cols-7" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 2 (stat grid + skeleton grid)
    - `grep -c "PlatformIssuesPanel" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "ClusterNodesPanel" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "StorageForecastCard" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "OrgHealthDataTable" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "RecentAuditHighlights" apps/web/src/components/pages/platform-dashboard-page.tsx` >= 1
    - `grep -c "Organization Summary" apps/web/src/components/pages/platform-dashboard-page.tsx` == 0
    - `grep -c "from '@/components/ui/table'" apps/web/src/components/pages/platform-dashboard-page.tsx` == 0
    - `grep -c "it.todo" apps/web/src/__tests__/platform-dashboard-page.test.tsx` == 0
    - `cd apps/web && pnpm test -- --run src/__tests__/platform-dashboard-page.test.tsx` exits 0
    - `cd apps/web && pnpm build` exits 0
    - `cd apps/web && pnpm test` exits 0 (full web suite)
  </acceptance_criteria>
  <done>
    Super admin dashboard shows 7 stat cards + SystemMetrics (kept) + 4 new widgets + OrgHealthDataTable + Recent Activity, all in D-07 priority stack order. Organization Summary Table removed. 5 page test stubs flipped. Full web test suite green. Build green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Super admin portal → /api/admin/dashboard/* | Inherits SuperAdminGuard from Plan 01 |
| DataTable row click → navigation | Read-only navigation to /admin/organizations; no state mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-AUTHZ-ADMIN | Elevation of Privilege | All new data fetching | mitigate | Endpoints guarded in Plan 01. Frontend page itself is under `/admin/` which is gated by existing super-admin layout route. |
| T-18-INFO-LEAK-STORAGE | Information Disclosure | Super admin sees all-org storage/bandwidth | accept | Super admin scope by design. |
| T-18-XSS-ORG-NAME | Tampering | Org name rendered in DataTable cells | mitigate | React auto-escapes `{row.original.orgName}`. Grep verifies no dangerouslySetInnerHTML. |
| T-18-DOS-DASH | Denial of Service | Multiple polling hooks at 30s concurrently | accept | 30s is the existing baseline (not tightened). Independent hooks parallel-fetch; total concurrent request count bounded by the number of widgets (~7) which is trivial. |
| T-18-SORT-OVERFLOW | Availability | Client-side sort on possibly hundreds of orgs | accept | Current scale is tens of orgs (per research). Revisit if orgs > 1000 — defer to future pagination. |
</threat_model>

<verification>
- `cd apps/web && pnpm test` green (full web suite)
- `cd apps/web && pnpm build` green (all types resolve)
- `cd apps/api && pnpm test` green (Plan 01 backend suite still green)
- Visual sanity (manual — VALIDATION.md): open /admin/dashboard with super-admin session, verify 7 stat cards, SystemMetrics, Platform Issues panel, Cluster Nodes panel, Storage Forecast chart, Organization Health DataTable (sortable + row click), Recent Activity feed.
</verification>

<success_criteria>
- D-05: 7 stat cards (Organizations, Total Cameras, Cameras Online, Cameras Offline, Stream Bandwidth, Active Streams, Recordings Active)
- D-06: SystemMetrics 4-card section unchanged
- D-07: Vertical stack order — stat cards → SystemMetrics → PlatformIssues → Cluster → StorageForecast → OrgHealth → RecentAudit
- D-12: OrgHealthDataTable replaces raw Organization Summary Table, default sorted by max usage % desc
- 10 it.todo stubs flipped (5 in DataTable + 5 in page)
- No `dangerouslySetInnerHTML`
- Full web test suite green, build green, api suite still green
- No schema changes
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-map-polish/18-06-SUMMARY.md` documenting page refactor, DataTable migration, and decision coverage matrix (D-05, D-06, D-07, D-12 fully delivered).
</output>
