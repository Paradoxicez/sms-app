---
phase: 18
plan: 02
type: execute
wave: 2
depends_on: [18-00, 18-01]
files_modified:
  - apps/web/src/hooks/use-dashboard-stats.ts
  - apps/web/src/hooks/use-dashboard-issues.ts
  - apps/web/src/components/dashboard/issues-panel.tsx
  - apps/web/src/components/pages/tenant-dashboard-page.tsx
  - apps/web/src/components/dashboard/issues-panel.test.tsx
  - apps/web/src/__tests__/tenant-dashboard-page.test.tsx
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "Tenant dashboard renders exactly 6 stat cards: Cameras Online, Cameras Offline, Recording, In Maintenance, Total Viewers, Stream Bandwidth (per D-02)"
    - "Tenant dashboard no longer imports or renders SystemMetrics (per D-01)"
    - "Tenant dashboard no longer imports or renders CameraStatusTable — replaced by IssuesPanel (per D-04)"
    - "IssuesPanel renders empty state 'All cameras healthy' with reward messaging when no issues"
    - "IssuesPanel sorts issues severity desc: offline → degraded → reconnecting → recording-failed → maintenance"
    - "BandwidthChart + ApiUsageChart remain present (per D-03)"
  artifacts:
    - path: "apps/web/src/components/dashboard/issues-panel.tsx"
      provides: "IssuesPanel component + useDashboardIssues composition"
      contains: "All cameras healthy"
    - path: "apps/web/src/hooks/use-dashboard-issues.ts"
      provides: "useDashboardIssues hook deriving severity-sorted issue list"
      exports: ["useDashboardIssues"]
    - path: "apps/web/src/components/pages/tenant-dashboard-page.tsx"
      provides: "Refactored tenant dashboard — 6 stat cards + IssuesPanel, no SystemMetrics, no CameraStatusTable"
      contains: "IssuesPanel"
    - path: "apps/web/src/hooks/use-dashboard-stats.ts"
      provides: "Extended DashboardCamera + DashboardStats types with Phase 15 fields"
      contains: "camerasRecording"
  key_links:
    - from: "apps/web/src/components/pages/tenant-dashboard-page.tsx"
      to: "apps/web/src/components/dashboard/issues-panel.tsx"
      via: "import { IssuesPanel } + <IssuesPanel />"
      pattern: "<IssuesPanel"
    - from: "apps/web/src/components/dashboard/issues-panel.tsx"
      to: "apps/web/src/hooks/use-dashboard-issues.ts"
      via: "useDashboardIssues hook"
      pattern: "useDashboardIssues"
    - from: "apps/web/src/hooks/use-dashboard-issues.ts"
      to: "useCameraStatusList + useCameraStatus (Socket.IO) from Plan 01"
      via: "composes data from existing hooks"
      pattern: "useCameraStatusList"
---

<objective>
Refactor tenant dashboard (`/app/dashboard`) per decisions D-01 through D-04:
1. **D-01:** Remove `<SystemMetrics />` entirely (not just the `isSuperAdmin` guard).
2. **D-02:** Expand stat cards from 4 to 6: add Recording + In Maintenance using enriched data from Plan 01.
3. **D-03:** Keep both BandwidthChart + ApiUsageChart.
4. **D-04:** Replace `<CameraStatusTable />` with new `<IssuesPanel />` that (a) shows only cameras needing attention, (b) sorts by severity desc, (c) has reward empty state, (d) updates via Socket.IO + 30s polling.

Purpose: Close UI-05 tenant surface. Dashboard becomes "ops attention" view, with SystemMetrics moved to super admin only (enforces portal separation, Phase 16 D-02 precedent).
Output: Refactored `tenant-dashboard-page.tsx` + new `IssuesPanel` component + `useDashboardIssues` derivation hook; `camera-status-table.tsx` no longer imported (file can remain unused — may be deleted in cleanup or left for safety).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dashboard-map-polish/18-CONTEXT.md
@.planning/phases/18-dashboard-map-polish/18-RESEARCH.md
@.planning/phases/18-dashboard-map-polish/18-UI-SPEC.md

<interfaces>
<!-- After Plan 01 completion, these extended shapes exist on the wire. -->

Extended `DashboardStats` shape returned by `/api/dashboard/stats`:
```ts
interface DashboardStats {
  camerasOnline: number;
  camerasOffline: number;
  totalCameras: number;
  totalViewers: number;
  bandwidth: number;
  streamBandwidth: number;
  // NEW from Plan 01:
  camerasRecording: number;
  camerasInMaintenance: number;
}
```

Extended `DashboardCamera` shape returned by `/api/dashboard/cameras`:
```ts
interface DashboardCamera {
  id: string;
  name: string;
  status: 'online' | 'offline' | 'degraded' | 'connecting' | 'reconnecting';
  lastOnlineAt: string | null;
  viewerCount: number;
  bandwidth: number;
  // NEW from Plan 01:
  isRecording: boolean;
  maintenanceMode: boolean;
  maintenanceEnteredBy: string | null;
  maintenanceEnteredAt: string | null;
  retentionDays: number | null;
}
```

Existing components reused (zero modifications):
- `StatCard` from `apps/web/src/components/dashboard/stat-card.tsx`
- `BandwidthChart` from `apps/web/src/components/dashboard/bandwidth-chart.tsx`
- `ApiUsageChart` from `apps/web/src/components/dashboard/api-usage-chart.tsx`
- `Card, CardContent, CardHeader, CardTitle` from `apps/web/src/components/ui/card.tsx`
- `Button` from `apps/web/src/components/ui/button.tsx`
- `Badge` from `apps/web/src/components/ui/badge.tsx`
- `Skeleton` from `apps/web/src/components/ui/skeleton.tsx`
- `useCameraStatus` from `apps/web/src/hooks/use-camera-status.ts` (Socket.IO)
- `useDashboardStats, useCameraStatusList` from `apps/web/src/hooks/use-dashboard-stats.ts`

Icons (lucide-react, already installed):
- `Camera`, `Eye`, `Wifi`, `MonitorOff` — existing
- `Video` — for Recording stat card (new)
- `Wrench` — for In Maintenance stat card + maintenance rows (new)
- `CheckCircle2` — empty state reward icon
- `ArrowRight` — Investigate button suffix
- `RotateCw` — Restart button prefix

Navigation pattern: `router.push('/app/cameras/{id}')` via `next/navigation`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DashboardCamera+DashboardStats types, create useDashboardIssues hook + IssuesPanel component (D-02, D-04)</name>
  <files>apps/web/src/hooks/use-dashboard-stats.ts, apps/web/src/hooks/use-dashboard-issues.ts, apps/web/src/components/dashboard/issues-panel.tsx, apps/web/src/components/dashboard/issues-panel.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/use-dashboard-stats.ts (existing DashboardCamera + DashboardStats shapes — lines 6-13, 32-39)
    - apps/web/src/hooks/use-camera-status.ts (Socket.IO event shape for status + viewerCount)
    - apps/web/src/components/dashboard/issues-panel.test.tsx (Plan 00 stubs — flip to real)
    - apps/web/src/test-utils/camera-fixtures.ts (fixtures created in Plan 00)
    - apps/web/src/components/dashboard/camera-status-table.tsx (existing pattern — mine for severity-sort reference + row rendering patterns)
    - apps/web/src/components/ui/card.tsx, button.tsx, badge.tsx (primitives)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md §D-04, §D-23, §D-24
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Tenant Dashboard Copywriting (lines 153-171) — exact row copy per severity
    - .planning/phases/18-dashboard-map-polish/18-RESEARCH.md §Pattern 7 §Pitfall 6 (severity rank composition)
  </read_first>
  <behavior>
    Type extensions in `use-dashboard-stats.ts`:
    - `DashboardStats` adds `camerasRecording: number`, `camerasInMaintenance: number`.
    - `DashboardCamera` adds `isRecording: boolean`, `maintenanceMode: boolean`, `maintenanceEnteredBy: string | null`, `maintenanceEnteredAt: string | null`, `retentionDays: number | null`.

    `useDashboardIssues()` hook (`use-dashboard-issues.ts`):
    - Calls `useCameraStatusList()` (already polls 30s).
    - Derives issues: filter cameras where status ∈ ['offline','degraded','reconnecting'] OR `maintenanceMode === true`.
    - Severity rank function (lower = worse):
      - offline = 0, degraded = 1, reconnecting = 2, maintenance = 4 (lowest severity because intentional)
      - recording-failed (status=online && recordingExpected && !isRecording) — out of scope for this plan per RESEARCH Open Question #1; skip recording-failed detection until backend exposes a signal. Add TODO comment referencing OQ #1.
    - Returns `{ issues, loading, error, onlineCount }` where `onlineCount` is total online cameras (for empty-state body).
    - Sort: severity asc, then by lastOnlineAt asc (oldest failure first).
    - Does NOT emit navigation callbacks — consumer wires those via Next.js router (decoupled).

    `IssuesPanel` component (`issues-panel.tsx`):
    - Renders shadcn `<Card>` with `<CardHeader><CardTitle>Issues</CardTitle></CardHeader>`.
    - Loading state: 3× `<Skeleton className="h-14 w-full rounded-md" />` rows.
    - Error state: destructive banner "Couldn't load issues" + Retry button.
    - Empty state (issues.length === 0 && !loading && !error):
      - Centered `<CheckCircle2 className="mx-auto h-8 w-8 text-primary" />`
      - `<p className="mt-3 text-sm font-semibold">All cameras healthy</p>`
      - `<p className="text-xs text-muted-foreground">{onlineCount} cameras online, 0 issues.</p>`
    - Issues list:
      - Subtitle under title: `{N} camera{s} need{s} attention`.
      - Row per issue: `{name}` bold + meta line + action button right.
      - offline: meta "Offline · last seen {relative time}" + button "Investigate" with `ArrowRight` suffix → router.push(`/app/cameras/${id}`).
      - degraded/reconnecting: meta "{Status capitalized} · {duration} since status change" + button "View" → `/app/cameras/${id}`.
      - maintenance: meta "Maintenance · by {user} · {relative time}" + button "View" → `/app/cameras/${id}`.
      - Row is `button` or `role="button" tabIndex={0}` with `aria-label`.
    - Uses `formatDistanceToNowStrict` from `date-fns`.

    Tests (issues-panel.test.tsx) — flip all 5 it.todo stubs:
    - Empty state render + copy.
    - Severity sort order (seed mix: 1 offline, 1 degraded, 1 reconnecting, 1 maintenance → assert DOM order matches).
    - Investigate action navigation (mock `next/navigation`'s `useRouter().push`).
    - Maintenance row copy includes by-user + relative time.
    - Empty state body shows "{N} cameras online, 0 issues."
  </behavior>
  <action>
1. Edit `apps/web/src/hooks/use-dashboard-stats.ts`:
   - Extend `DashboardStats` interface (line 6-13) with `camerasRecording: number;` and `camerasInMaintenance: number;`.
   - Extend `DashboardCamera` interface (line 32-39) with `isRecording: boolean; maintenanceMode: boolean; maintenanceEnteredBy: string | null; maintenanceEnteredAt: string | null; retentionDays: number | null;`.
   - No other logic changes in this file.

2. Create `apps/web/src/hooks/use-dashboard-issues.ts`:
   - Named export `useDashboardIssues()`.
   - Import `useCameraStatusList` from `./use-dashboard-stats`.
   - Compute `issues = cameras.filter(c => c.status === 'offline' || c.status === 'degraded' || c.status === 'reconnecting' || c.maintenanceMode === true)`.
   - Severity rank function (commented OQ-01 TODO):
     ```ts
     function severityRank(c: DashboardCamera): number {
       if (c.status === 'offline') return 0;
       if (c.status === 'degraded') return 1;
       if (c.status === 'reconnecting') return 2;
       // TODO (OQ-01): recording-failed = 3 once backend exposes the signal.
       if (c.maintenanceMode) return 4;
       return 99;
     }
     ```
   - Sort issues by `severityRank` asc, then by `lastOnlineAt` asc (null last).
   - Return `{ issues, loading, error, onlineCount: cameras.filter(c => c.status === 'online' && !c.maintenanceMode).length }`.

3. Create `apps/web/src/components/dashboard/issues-panel.tsx`:
   - `'use client'` directive.
   - Imports: `Card`, `CardContent`, `CardHeader`, `CardTitle` from `@/components/ui/card`; `Button` from `@/components/ui/button`; `Skeleton`; lucide icons `CheckCircle2`, `ArrowRight`, `Wrench`; `formatDistanceToNowStrict` from `date-fns`; `useRouter` from `next/navigation`; `useDashboardIssues` from `@/hooks/use-dashboard-issues`.
   - Component exported `IssuesPanel`.
   - Render layout matching UI-SPEC §Tenant Dashboard Layout lines 270-282.
   - Exact empty-state copy: heading "All cameras healthy", body "{onlineCount} cameras online, 0 issues." (no trailing space).
   - Exact offline row meta: ``Offline · last seen ${formatDistanceToNowStrict(new Date(lastOnlineAt), { addSuffix: true })}``.
   - Exact degraded/reconnecting meta: ``${status.charAt(0).toUpperCase() + status.slice(1)} · {duration} since status change``. For "duration since status change", use `formatDistanceToNowStrict(new Date(lastOnlineAt))` as approximation since no dedicated "statusChangedAt" field exists (add TODO if needed).
   - Exact maintenance meta: ``Maintenance · by ${maintenanceEnteredBy ?? 'unknown'} · ${formatDistanceToNowStrict(new Date(maintenanceEnteredAt), { addSuffix: true })}``.
   - Row button `aria-label` exactly: ``{severity capitalized} camera {name} — click to {action}``.

4. Flip all 5 it.todo stubs in `apps/web/src/components/dashboard/issues-panel.test.tsx` to real tests using the fixtures from `@/test-utils/camera-fixtures`. Mock `next/navigation` via `vi.mock('next/navigation', () => ({ useRouter: () => ({ push: vi.fn() }) }))`. Assert DOM content + ordering via `within(row)` queries.

5. Run:
   ```
   cd apps/web && pnpm test -- --run src/components/dashboard/issues-panel.test.tsx
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/components/dashboard/issues-panel.test.tsx src/hooks/use-dashboard-stats.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "camerasRecording: number" apps/web/src/hooks/use-dashboard-stats.ts` >= 1
    - `grep -c "camerasInMaintenance: number" apps/web/src/hooks/use-dashboard-stats.ts` >= 1
    - `grep -c "isRecording: boolean" apps/web/src/hooks/use-dashboard-stats.ts` >= 1
    - `grep -c "maintenanceEnteredBy: string | null" apps/web/src/hooks/use-dashboard-stats.ts` >= 1
    - `grep -c "maintenanceEnabledBy\|maintenanceEnabledAt" apps/web/src/hooks/use-dashboard-stats.ts` == 0
    - `test -f apps/web/src/hooks/use-dashboard-issues.ts` succeeds
    - `grep -c "export function useDashboardIssues" apps/web/src/hooks/use-dashboard-issues.ts` >= 1
    - `grep -c "severityRank" apps/web/src/hooks/use-dashboard-issues.ts` >= 1
    - `grep -c "TODO.*OQ-01" apps/web/src/hooks/use-dashboard-issues.ts` >= 1
    - `test -f apps/web/src/components/dashboard/issues-panel.tsx` succeeds
    - `grep -c "All cameras healthy" apps/web/src/components/dashboard/issues-panel.tsx` >= 1
    - `grep -c "CheckCircle2" apps/web/src/components/dashboard/issues-panel.tsx` >= 1
    - `grep -c "formatDistanceToNowStrict" apps/web/src/components/dashboard/issues-panel.tsx` >= 1
    - `grep -c "it.todo" apps/web/src/components/dashboard/issues-panel.test.tsx` == 0 (all flipped)
    - `cd apps/web && pnpm test -- --run src/components/dashboard/issues-panel.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    DashboardCamera + DashboardStats types extended. `useDashboardIssues` hook composes severity-sorted issues. `IssuesPanel` renders empty/loading/error/list states with exact UI-SPEC copy. All 5 panel tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refactor tenant-dashboard-page.tsx — remove SystemMetrics + CameraStatusTable, add 6 stat cards + IssuesPanel (D-01, D-02, D-03)</name>
  <files>apps/web/src/components/pages/tenant-dashboard-page.tsx, apps/web/src/__tests__/tenant-dashboard-page.test.tsx</files>
  <read_first>
    - apps/web/src/components/pages/tenant-dashboard-page.tsx (current composition — lines 26-135; remove isSuperAdmin state block lines 29-45, 74)
    - apps/web/src/__tests__/tenant-dashboard-page.test.tsx (Plan 00 stubs — 6 to flip)
    - apps/web/src/components/dashboard/stat-card.tsx (StatCard props — label, value, icon, badge)
    - apps/web/src/components/dashboard/issues-panel.tsx (Task 1 output)
    - apps/web/src/components/dashboard/bandwidth-chart.tsx, api-usage-chart.tsx (kept per D-03)
    - apps/web/src/components/dashboard/system-metrics.tsx (DO NOT DELETE — still used in /admin; just remove import from tenant page)
    - apps/web/src/components/dashboard/camera-status-table.tsx (DO NOT DELETE — just remove import; cleanup deferred)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md §D-01 (remove entirely, not just guard), §D-02 (6 cards), §D-03 (keep charts)
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Tenant Dashboard Layout (line 262-285) — exact grid classes
  </read_first>
  <behavior>
    Refactored `tenant-dashboard-page.tsx`:
    - Imports removed: `SystemMetrics` from `../dashboard/system-metrics`, `CameraStatusTable` from `../dashboard/camera-status-table`, `authClient` (only used for isSuperAdmin), `useState.userRole`, related `useEffect` loading session for role.
    - Imports added: `Video, Wrench` from `lucide-react`; `IssuesPanel` from `../dashboard/issues-panel`.
    - State removed: `userRole`, `isSuperAdmin` variable.
    - State kept: `orgId` (still needed for Socket.IO in useCameraStatus).
    - Stat cards grid: `className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6"` (6 cards).
    - 6 cards in exact order per UI-SPEC Copy table (lines 156-161):
      1. `Cameras Online` — value `stats.camerasOnline`, icon `<Camera />`, badge `{ text: 'Live', variant: 'default' }` if > 0.
      2. `Cameras Offline` — value `stats.camerasOffline`, icon `<MonitorOff />`, badge `{ text: '{N} down', variant: 'destructive' }` if > 0.
      3. `Recording` — value `stats.camerasRecording`, icon `<Video />`, badge `{ text: '{N} active', variant: 'default' }` if > 0.
      4. `In Maintenance` — value `stats.camerasInMaintenance`, icon `<Wrench />`, no badge.
      5. `Total Viewers` — value `stats.totalViewers`, icon `<Eye />` (unchanged).
      6. `Stream Bandwidth` — value `formatBandwidth(stats.streamBandwidth * 125)`, icon `<Wifi />` (unchanged).
    - Skeleton block during loading: 6× skeleton matching the 6-card grid classes.
    - `{isSuperAdmin && <SystemMetrics />}` block DELETED entirely.
    - `<CameraStatusTable cameras={cameras} loading={camerasLoading} />` REPLACED with `<IssuesPanel />` (no props needed — IssuesPanel pulls data internally via useDashboardIssues).
    - BandwidthChart + ApiUsageChart section unchanged.
    - `useCameraStatus` hook call unchanged (still needed for any pages consuming it).

    Tests (`tenant-dashboard-page.test.tsx`) — flip all 6 it.todo stubs:
    - No SystemMetrics rendered (queryByText/queryByTestId).
    - 6 stat cards with exact labels.
    - Grid classes `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6`.
    - BandwidthChart + ApiUsageChart present.
    - IssuesPanel present, CameraStatusTable absent.
    - isSuperAdmin state removed (no authClient.getSession call for role purpose).

    Mock `fetch`/`apiFetch` to return enriched DashboardStats via fixture.
  </behavior>
  <action>
1. Edit `apps/web/src/components/pages/tenant-dashboard-page.tsx`:
   - Line 5: remove `import { authClient } from '@/lib/auth-client';`.
   - Line 9: remove `import { SystemMetrics } from '@/components/dashboard/system-metrics';`.
   - Line 12: remove `import { CameraStatusTable } from '@/components/dashboard/camera-status-table';`.
   - Update line 4 lucide imports: ADD `Video, Wrench` → `import { Camera, Eye, Wifi, MonitorOff, Video, Wrench } from 'lucide-react';`.
   - After existing dashboard imports, add: `import { IssuesPanel } from '@/components/dashboard/issues-panel';`.
   - Remove `const [userRole, setUserRole] = useState<string | null>(null);` (line 29).
   - Remove the `useEffect` block at lines 32-45 that loads session for role (keep only if needed for `orgId`; refactor: inline a minimal `orgId`-only session load, or keep full block but DROP the `setUserRole` line).
   - Remove `const isSuperAdmin = userRole === 'admin';` (line 74).
   - Remove `{isSuperAdmin && <SystemMetrics />}` (line 123).
   - Skeleton grid classes change: replace `sm:grid-cols-2 lg:grid-cols-4` with `sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-6`. Render 6 skeletons: `{[1,2,3,4,5,6].map(...)}`.
   - Stat cards grid div (line 88): change `lg:grid-cols-4` to `lg:grid-cols-3 xl:grid-cols-6`.
   - Add 2 new StatCard entries after existing Cameras Offline card and before Total Viewers, per behavior order:
     ```tsx
     <StatCard label="Recording" value={stats.camerasRecording} icon={<Video className="h-4 w-4" />} badge={stats.camerasRecording > 0 ? { text: `${stats.camerasRecording} active`, variant: 'default' } : undefined} />
     <StatCard label="In Maintenance" value={stats.camerasInMaintenance} icon={<Wrench className="h-4 w-4" />} />
     ```
   - Replace `<CameraStatusTable cameras={cameras} loading={camerasLoading} />` (line 132) with `<IssuesPanel />`.
   - Since IssuesPanel consumes its own hook, the local `cameras, setCameras, camerasLoading` variables are now unused for direct display but STILL required for useCameraStatus subscription (setCameras) — keep them.

2. Flip all 6 it.todo stubs in `apps/web/src/__tests__/tenant-dashboard-page.test.tsx`:
   - Use `render(<TenantDashboardPage />)` with mocked fetch returning enriched stats.
   - Assertions:
     - `expect(screen.queryByText('CPU Usage')).toBeNull()` (SystemMetrics removed — CPU Usage is a label only shown inside SystemMetrics)
     - `expect(screen.getByText('Cameras Online')).toBeInTheDocument()` etc. for all 6 labels
     - `const grid = screen.getByTestId('stat-cards-grid')` — add `data-testid="stat-cards-grid"` to the grid div in page, OR assert via container.querySelector
     - `expect(grid.className).toContain('xl:grid-cols-6')`
     - `expect(screen.getByText(/View full/i) || screen.getByRole('region', {name: /Bandwidth/i}))` — chart markers
     - `expect(screen.getByRole('heading', {name: /Issues/i}))` — IssuesPanel
     - `expect(screen.queryByText(/Status/i))` to verify no CameraStatusTable column header
   - Mock `useCameraStatus` + `useDashboardIssues` to avoid Socket.IO + API calls in tests.

3. Run:
   ```
   cd apps/web && pnpm test -- --run src/__tests__/tenant-dashboard-page.test.tsx
   cd apps/web && pnpm test   # full web suite
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/__tests__/tenant-dashboard-page.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SystemMetrics" apps/web/src/components/pages/tenant-dashboard-page.tsx` == 0 (import + usage gone)
    - `grep -c "CameraStatusTable" apps/web/src/components/pages/tenant-dashboard-page.tsx` == 0
    - `grep -c "isSuperAdmin\|userRole" apps/web/src/components/pages/tenant-dashboard-page.tsx` == 0
    - `grep -c "IssuesPanel" apps/web/src/components/pages/tenant-dashboard-page.tsx` >= 1
    - `grep -c "camerasRecording" apps/web/src/components/pages/tenant-dashboard-page.tsx` >= 1
    - `grep -c "camerasInMaintenance" apps/web/src/components/pages/tenant-dashboard-page.tsx` >= 1
    - `grep -c "xl:grid-cols-6" apps/web/src/components/pages/tenant-dashboard-page.tsx` >= 2 (stat grid + skeleton grid)
    - `grep -c "BandwidthChart" apps/web/src/components/pages/tenant-dashboard-page.tsx` >= 1 (kept per D-03)
    - `grep -c "ApiUsageChart" apps/web/src/components/pages/tenant-dashboard-page.tsx` >= 1 (kept per D-03)
    - `grep -c "it.todo" apps/web/src/__tests__/tenant-dashboard-page.test.tsx` == 0 (all flipped)
    - `cd apps/web && pnpm test -- --run src/__tests__/tenant-dashboard-page.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    Tenant dashboard page shows 6 stat cards, no SystemMetrics, no CameraStatusTable, keeps both charts, renders IssuesPanel. All 6 page tests flipped to green. UI-05 tenant surface complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/dashboard/* | Tenant browser requests org-scoped data; session + TENANCY_CLIENT enforce. Inherited from Plan 01. |
| DOM → rendered strings | React auto-escapes. No `dangerouslySetInnerHTML` used in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-TENANCY-ISSUES | Information Disclosure | useCameraStatusList → IssuesPanel render | mitigate | Backend (Plan 01) scopes via TENANCY_CLIENT. Frontend merely renders what backend returns. No cross-org leak possible at UI layer. |
| T-18-XSS-DASH-NAME | Tampering | Camera `name`, `maintenanceEnteredBy` rendered in IssuesPanel rows | mitigate | React auto-escapes interpolated strings (`{camera.name}`, `{camera.maintenanceEnteredBy}`). No `dangerouslySetInnerHTML`. Verify via grep. |
| T-18-CSRF-NAV | Tampering | Investigate/View row action navigates to /app/cameras/{id} | accept | Navigation is GET-only via router.push; no state mutation. Route itself has session guard. |
</threat_model>

<verification>
- `cd apps/web && pnpm test` green
- `cd apps/web && pnpm build` green (no TypeScript errors after type extensions)
- Visual sanity (manual): open /app/dashboard, verify 6 cards render, no SystemMetrics panel, Issues panel shows empty state or rows.
</verification>

<success_criteria>
- D-01: `<SystemMetrics />` import + usage removed from tenant-dashboard-page.tsx
- D-02: Exactly 6 StatCards rendered in grid `lg:grid-cols-3 xl:grid-cols-6`
- D-03: BandwidthChart + ApiUsageChart still present
- D-04: `<CameraStatusTable />` replaced with `<IssuesPanel />`
- IssuesPanel empty state renders reward copy
- Severity sort tested and passing
- 11 it.todo stubs flipped (5 in issues-panel.test.tsx + 6 in tenant-dashboard-page.test.tsx)
- No `dangerouslySetInnerHTML` usage
- No schema changes
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-map-polish/18-02-SUMMARY.md` documenting component refactor, decisions implemented (D-01..D-04), and test counts.
</output>
