---
phase: 18
plan: 05
type: execute
wave: 2
depends_on: [18-00, 18-01]
files_modified:
  - apps/web/src/hooks/use-platform-dashboard.ts
  - apps/web/src/components/dashboard/platform-issues-panel.tsx
  - apps/web/src/components/dashboard/cluster-nodes-panel.tsx
  - apps/web/src/components/dashboard/storage-forecast-card.tsx
  - apps/web/src/components/dashboard/recent-audit-highlights.tsx
  - apps/web/src/components/dashboard/platform-issues-panel.test.tsx
  - apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx
  - apps/web/src/components/dashboard/storage-forecast-card.test.tsx
  - apps/web/src/components/dashboard/recent-audit-highlights.test.tsx
  - apps/web/src/components/ui/toggle-group.tsx
autonomous: true
requirements: [UI-05]

must_haves:
  truths:
    - "PlatformIssuesPanel renders cross-org issues with empty state 'Platform healthy' (per D-09)"
    - "ClusterNodesPanel consumes useClusterNodes and renders 5 columns: Node, Role, Status, Uptime, Connections (per D-08)"
    - "StorageForecastCard toggles 7d/30d (default 7d), renders Recharts LineChart, shows daysUntilFull caption (per D-10)"
    - "RecentAuditHighlights renders up to 7 entries + footer link to /admin/audit (per D-11)"
    - "shadcn toggle-group primitive installed under apps/web/src/components/ui/toggle-group.tsx"
    - "usePlatformDashboard hook exposes 4 data-fetching sub-hooks (polling 30s each)"
  artifacts:
    - path: "apps/web/src/components/ui/toggle-group.tsx"
      provides: "shadcn ToggleGroup primitive (installed if missing)"
      contains: "ToggleGroup"
    - path: "apps/web/src/hooks/use-platform-dashboard.ts"
      provides: "usePlatformIssues, useClusterNodesAggregate (or wraps useClusterNodes), useStorageForecast(range), useRecentAudit"
      exports: ["usePlatformIssues", "useStorageForecast", "useRecentAudit"]
    - path: "apps/web/src/components/dashboard/platform-issues-panel.tsx"
      provides: "Super admin platform-wide Issues panel"
      contains: "Platform healthy"
    - path: "apps/web/src/components/dashboard/cluster-nodes-panel.tsx"
      provides: "Cluster & Edge Nodes status panel consuming useClusterNodes"
      contains: "Cluster & Edge Nodes"
    - path: "apps/web/src/components/dashboard/storage-forecast-card.tsx"
      provides: "Storage trend LineChart + days-until-full caption + 7d/30d toggle"
      contains: "Estimated"
    - path: "apps/web/src/components/dashboard/recent-audit-highlights.tsx"
      provides: "7-entry audit feed with View full audit log link"
      contains: "View full audit log"
  key_links:
    - from: "apps/web/src/components/dashboard/storage-forecast-card.tsx"
      to: "apps/web/src/components/ui/toggle-group.tsx"
      via: "import ToggleGroup for 7d/30d switcher"
      pattern: "ToggleGroup"
    - from: "apps/web/src/hooks/use-platform-dashboard.ts"
      to: "/api/admin/dashboard/platform-issues, /cluster-nodes, /storage-forecast, /recent-audit"
      via: "apiFetch per sub-hook"
      pattern: "/api/admin/dashboard"
    - from: "apps/web/src/components/dashboard/cluster-nodes-panel.tsx"
      to: "apps/web/src/hooks/use-cluster-nodes.ts"
      via: "reuse existing hook (has Socket.IO real-time)"
      pattern: "useClusterNodes"
---

<objective>
Build the 4 new super admin dashboard widgets + shared hook + missing shadcn primitive:
1. **D-08:** `ClusterNodesPanel` consuming existing `useClusterNodes` hook (Socket.IO real-time).
2. **D-09:** `PlatformIssuesPanel` with reward empty state.
3. **D-10:** `StorageForecastCard` with 7d/30d toggle (requires shadcn `toggle-group` primitive) + Recharts LineChart.
4. **D-11:** `RecentAuditHighlights` with 7-entry feed + link to full audit log.

Also creates `usePlatformDashboard` hook (3 new sub-hooks) and installs `shadcn@latest add toggle-group` if missing (verified not present in repo at planning time).

Purpose: Provide the building-block widgets for Plan 06 (super admin page refactor + OrgHealthDataTable). Decoupled from Plan 06 so widgets can be developed/tested in parallel.
Output: 4 new widget components + hook file + toggle-group primitive + 14 test stubs flipped to green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-dashboard-map-polish/18-CONTEXT.md
@.planning/phases/18-dashboard-map-polish/18-RESEARCH.md
@.planning/phases/18-dashboard-map-polish/18-UI-SPEC.md

<interfaces>
<!-- API endpoint shapes from Plan 01. -->

`GET /api/admin/dashboard/platform-issues` returns `PlatformIssue[]`:
```ts
type PlatformIssue = {
  type: 'srs-down' | 'edge-down' | 'org-offline-rate' | 'minio-down' | 'ffmpeg-saturated';
  severity: 'critical' | 'warning';
  label: string;
  meta?: Record<string, any>;
};
```

`GET /api/admin/dashboard/cluster-nodes` returns `SrsNode[]` — reuse `SrsNode` type from `@/hooks/use-cluster-nodes.ts`:
```ts
interface SrsNode { id, name, role: 'ORIGIN'|'EDGE', status: 'ONLINE'|'OFFLINE'|'DEGRADED'|'CONNECTING', uptime, viewers, ... }
```

`GET /api/admin/dashboard/storage-forecast?range=7d|30d` returns:
```ts
type StorageForecast = {
  points: Array<{ date: string; bytes: string }>;  // bigint as string
  estimatedDaysUntilFull: number | null;
};
```

`GET /api/admin/dashboard/recent-audit?limit=7` returns:
```ts
type AuditHighlight = {
  id: string;
  action: string;
  resource: string;
  actorName: string | null;
  orgName: string | null;
  createdAt: string;
};
```

Existing primitives:
- `Card, CardHeader, CardTitle, CardContent` from `@/components/ui/card`
- `Button`, `Badge`, `Skeleton`, `Table*` all present
- `useClusterNodes` hook at `@/hooks/use-cluster-nodes.ts` — already polls + Socket.IO
- `apiFetch` from `@/lib/api` — auth-aware fetch wrapper
- `formatDistanceToNowStrict` from `date-fns`
- Recharts `LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer` — installed

Icons (lucide-react): `CheckCircle2`, `ArrowRight`, `Wrench`, `Circle`, `Activity`, `AlertTriangle`.

Navigation: `useRouter` from `next/navigation`.

`shadcn@latest add toggle-group` — run once if `apps/web/src/components/ui/toggle-group.tsx` missing (verified not present via `ls` at planning).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install shadcn toggle-group, create usePlatformDashboard hook, build PlatformIssuesPanel + ClusterNodesPanel</name>
  <files>apps/web/src/components/ui/toggle-group.tsx, apps/web/src/hooks/use-platform-dashboard.ts, apps/web/src/components/dashboard/platform-issues-panel.tsx, apps/web/src/components/dashboard/cluster-nodes-panel.tsx, apps/web/src/components/dashboard/platform-issues-panel.test.tsx, apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx</files>
  <read_first>
    - apps/web/src/hooks/use-cluster-nodes.ts (existing hook — has Socket.IO + REST; reuse verbatim for ClusterNodesPanel)
    - apps/web/src/hooks/use-dashboard-stats.ts (existing polling hook pattern — 30s interval template)
    - apps/web/src/lib/api.ts (apiFetch wrapper)
    - apps/web/src/components/dashboard/platform-issues-panel.test.tsx (Plan 00 — 3 stubs to flip)
    - apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx (Plan 00 — 3 stubs to flip)
    - apps/web/src/components/pages/platform-dashboard-page.tsx (current page — see line 180-213 for SystemMetrics 4-card pattern to mirror in new cards)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md §D-08 (Cluster Nodes), §D-09 (Platform Issues)
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Super Admin Copy (lines 185-211) — exact row copy for each issue type, cluster columns, audit entry format
    - .planning/phases/18-dashboard-map-polish/18-RESEARCH.md §Pattern 5 (admin aggregation), §Validation Architecture
  </read_first>
  <behavior>
    1) Install shadcn `toggle-group` primitive:
       ```
       cd apps/web && npx shadcn@latest add toggle-group
       ```
       (Safety gate not required per UI-SPEC — shadcn official.)
       Expected file: `apps/web/src/components/ui/toggle-group.tsx` exporting `ToggleGroup`, `ToggleGroupItem`.

    2) `apps/web/src/hooks/use-platform-dashboard.ts` — polling hooks (30s each via `setInterval`):
       - `usePlatformIssues()` → GET `/api/admin/dashboard/platform-issues` → returns `{ issues: PlatformIssue[], loading, error, refetch }`.
       - `useStorageForecast(range: '7d' | '30d')` → GET `/api/admin/dashboard/storage-forecast?range={range}` → returns `{ forecast: StorageForecast | null, loading, error }`. Re-fetches on range change.
       - `useRecentAudit(limit = 7)` → GET `/api/admin/dashboard/recent-audit?limit={limit}` → returns `{ entries: AuditHighlight[], loading, error }`.
       - All sub-hooks follow the existing `useDashboardStats` pattern (useState, useCallback, useEffect + setInterval, clear interval on unmount).
       - No new hook for cluster nodes; reuse `useClusterNodes` from `@/hooks/use-cluster-nodes`.

    3) `PlatformIssuesPanel` component (`platform-issues-panel.tsx`):
       - `'use client'`.
       - Renders `<Card>` with `<CardHeader><CardTitle className="text-sm">Platform Issues</CardTitle></CardHeader>`.
       - Loading: 3× Skeleton rows.
       - Error: destructive banner.
       - Empty state: centered `<CheckCircle2 className="mx-auto h-8 w-8 text-primary" />` + "Platform healthy" + "All subsystems operational."
       - Rows per issue type (UI-SPEC copy lines 188-192):
         - `srs-down`: `SRS origin unreachable` + Investigate button.
         - `edge-down`: `Edge node {name} disconnected` + meta `{relative time}` + `View cluster` button → router.push('/admin/cluster').
         - `minio-down`: `MinIO storage unreachable` + Investigate.
         - `ffmpeg-saturated`: `FFmpeg worker pool saturated ({N}/{max})` + View processes.
         - `org-offline-rate`: `{Org Name}: {N}% cameras offline` + View org → router.push(`/admin/organizations/${orgId}`).
       - Actions are shadcn Button with `variant="ghost"` + `ArrowRight` suffix.
       - `aria-label` descriptive per row.

    4) `ClusterNodesPanel` component (`cluster-nodes-panel.tsx`):
       - `'use client'`.
       - Renders `<Card>` with `<CardTitle className="text-sm">Cluster & Edge Nodes</CardTitle>`.
       - Uses `useClusterNodes()` — already real-time via Socket.IO.
       - Renders shadcn `<Table>` with headers `Node`, `Role`, `Status`, `Uptime`, `Connections`.
       - Status cell: colored dot + status text (ONLINE green, OFFLINE red, DEGRADED amber, CONNECTING blue).
       - Uptime cell: format seconds → "12d 4h" using existing `formatUptime` helper (copy from `platform-dashboard-page.tsx:54` OR extract to `@/lib/format.ts`). For this plan: inline small helper or copy+paste.
       - Connections: `{node.viewers}`.
       - Empty state: "No cluster nodes registered."
       - Loading: 3× Skeleton rows.

    Tests to flip (6 total):

    `platform-issues-panel.test.tsx`:
    - Empty state "Platform healthy" render.
    - SRS down row with Investigate action.
    - Edge disconnected row with View cluster action (mock router.push).

    `cluster-nodes-panel.test.tsx`:
    - Consumes useClusterNodes (mock hook to return fixture nodes).
    - Renders 5 column headers.
    - Empty state "No cluster nodes registered."
  </behavior>
  <action>
1. Run `cd apps/web && npx shadcn@latest add toggle-group` to install primitive. Verify file exists after. If shadcn CLI fails (offline, etc.), manually create `apps/web/src/components/ui/toggle-group.tsx` using the current shadcn toggle-group template (https://ui.shadcn.com/docs/components/toggle-group — requires `@radix-ui/react-toggle-group` which may or may not be in package.json; add dep if missing via `pnpm --filter web add @radix-ui/react-toggle-group`).

2. Create `apps/web/src/hooks/use-platform-dashboard.ts`:
   - Export `usePlatformIssues`, `useStorageForecast(range)`, `useRecentAudit(limit)`.
   - Mirror `useDashboardStats` pattern exactly: useState for data/loading/error, useCallback for fetcher, useEffect with setInterval(30000), cleanup on unmount.
   - For `useStorageForecast`: add `range` to useEffect deps so switching re-fetches immediately.

3. Create `apps/web/src/components/dashboard/platform-issues-panel.tsx`:
   - Imports per interfaces. Use `usePlatformIssues()`.
   - Render per behavior spec. Map issue type → row label + action handler via switch.
   - Navigation handlers via `useRouter`.

4. Create `apps/web/src/components/dashboard/cluster-nodes-panel.tsx`:
   - Imports per interfaces. Use `useClusterNodes()` (existing hook).
   - Render per behavior spec. Include small `formatUptime(seconds)` helper inline (or factor to `@/lib/format.ts` if refactor preferred — but scope-minimal this plan).

5. Flip 6 it.todo stubs across the 2 test files:
   - Mock hooks (`vi.mock('@/hooks/use-platform-dashboard', ...)` and `vi.mock('@/hooks/use-cluster-nodes', ...)`).
   - Mock `next/navigation` router.
   - Use fixtures/factories for issue objects and node objects.

6. Run:
   ```
   cd apps/web && pnpm test -- --run src/components/dashboard/platform-issues-panel.test.tsx src/components/dashboard/cluster-nodes-panel.test.tsx
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/components/dashboard/platform-issues-panel.test.tsx src/components/dashboard/cluster-nodes-panel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/components/ui/toggle-group.tsx` succeeds
    - `grep -c "ToggleGroup" apps/web/src/components/ui/toggle-group.tsx` >= 2 (ToggleGroup + ToggleGroupItem)
    - `test -f apps/web/src/hooks/use-platform-dashboard.ts` succeeds
    - `grep -c "export function usePlatformIssues" apps/web/src/hooks/use-platform-dashboard.ts` >= 1
    - `grep -c "export function useStorageForecast" apps/web/src/hooks/use-platform-dashboard.ts` >= 1
    - `grep -c "export function useRecentAudit" apps/web/src/hooks/use-platform-dashboard.ts` >= 1
    - `grep -c "30000" apps/web/src/hooks/use-platform-dashboard.ts` >= 1 (polling interval)
    - `test -f apps/web/src/components/dashboard/platform-issues-panel.tsx` succeeds
    - `grep -c "Platform healthy" apps/web/src/components/dashboard/platform-issues-panel.tsx` >= 1
    - `grep -c "All subsystems operational" apps/web/src/components/dashboard/platform-issues-panel.tsx` >= 1
    - `test -f apps/web/src/components/dashboard/cluster-nodes-panel.tsx` succeeds
    - `grep -c "Cluster & Edge Nodes" apps/web/src/components/dashboard/cluster-nodes-panel.tsx` >= 1
    - `grep -c "useClusterNodes" apps/web/src/components/dashboard/cluster-nodes-panel.tsx` >= 1
    - `grep -c "it.todo" apps/web/src/components/dashboard/platform-issues-panel.test.tsx` == 0
    - `grep -c "it.todo" apps/web/src/components/dashboard/cluster-nodes-panel.test.tsx` == 0
    - `cd apps/web && pnpm test -- --run src/components/dashboard/platform-issues-panel.test.tsx src/components/dashboard/cluster-nodes-panel.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    toggle-group primitive installed. usePlatformDashboard hook exports 3 sub-hooks (polling 30s). PlatformIssuesPanel + ClusterNodesPanel components render with loading/empty/error states + real rows. 6 test stubs flipped.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build StorageForecastCard + RecentAuditHighlights</name>
  <files>apps/web/src/components/dashboard/storage-forecast-card.tsx, apps/web/src/components/dashboard/recent-audit-highlights.tsx, apps/web/src/components/dashboard/storage-forecast-card.test.tsx, apps/web/src/components/dashboard/recent-audit-highlights.test.tsx</files>
  <read_first>
    - apps/web/src/components/dashboard/bandwidth-chart.tsx (existing Recharts LineChart pattern — exact imports, stroke, dimensions)
    - apps/web/src/components/dashboard/api-usage-chart.tsx (second Recharts exemplar)
    - apps/web/src/components/ui/toggle-group.tsx (created in Task 1)
    - apps/web/src/hooks/use-platform-dashboard.ts (Task 1 output — useStorageForecast, useRecentAudit)
    - apps/web/src/components/dashboard/storage-forecast-card.test.tsx (Plan 00 — 3 stubs)
    - apps/web/src/components/dashboard/recent-audit-highlights.test.tsx (Plan 00 — 4 stubs)
    - .planning/phases/18-dashboard-map-polish/18-CONTEXT.md §D-10 (Storage forecast), §D-11 (Recent audit)
    - .planning/phases/18-dashboard-map-polish/18-UI-SPEC.md §Super Admin Copy (lines 196-202 storage, 200-203 audit) + §Loading States
    - .planning/phases/18-dashboard-map-polish/18-RESEARCH.md §Pattern 6 (storage forecast query + regression math)
  </read_first>
  <behavior>
    `StorageForecastCard` component (`storage-forecast-card.tsx`):
    - `'use client'`.
    - Local state `const [range, setRange] = useState<'7d' | '30d'>('7d');`
    - `useStorageForecast(range)` for data.
    - Card with `<CardTitle>Storage Forecast</CardTitle>` + `<ToggleGroup type="single" value={range} onValueChange={(v) => v && setRange(v as '7d' | '30d')} defaultValue="7d">` with 2 items `ToggleGroupItem value="7d">7 days` and `ToggleGroupItem value="30d">30 days`.
    - Recharts `<ResponsiveContainer width="100%" height={256}><LineChart data={points}><XAxis dataKey="date" /><YAxis /><Tooltip /><Line type="monotone" dataKey="bytesGB" stroke="var(--chart-1)" strokeWidth={2} dot={false} /></LineChart></ResponsiveContainer>`.
    - Convert `points[].bytes` (string bigint) to GB number: `bytesGB: Number(BigInt(p.bytes) / BigInt(1024*1024*1024))`.
    - Caption below chart:
      - If `estimatedDaysUntilFull !== null`: `Estimated {estimatedDaysUntilFull} days until full at current growth rate.`
      - If `daysUntilFull <= 14`: add `text-destructive` class to caption.
      - If `estimatedDaysUntilFull === null`: "Not enough data yet."
    - Loading: `<Skeleton className="h-64 w-full rounded-md" />` + `<Skeleton className="h-4 w-48" />`.

    `RecentAuditHighlights` component (`recent-audit-highlights.tsx`):
    - `'use client'`.
    - `useRecentAudit(7)` for data.
    - Card with `<CardTitle>Recent Activity</CardTitle>` + 5-10 entries (default 7) + footer link.
    - Entry rendering:
      ```tsx
      <div className="flex items-center justify-between py-2 text-sm">
        <span>
          {entry.actorName ?? 'System'} {verbForAction(entry.action)} {entry.resource} {entry.orgName ? entry.orgName : ''}
        </span>
        <span className="text-xs text-muted-foreground">{formatDistanceToNowStrict(new Date(entry.createdAt), { addSuffix: true })}</span>
      </div>
      ```
    - `verbForAction(action: string)` helper: maps `create → 'created'`, `update → 'updated'`, `delete → 'deleted'`, falls back to action verbatim.
    - Footer: `<Link href="/admin/audit" className="inline-flex items-center gap-1 text-sm text-primary">View full audit log <ArrowRight className="h-3 w-3" /></Link>` (use `next/link`).
    - Empty state: "No recent platform activity."
    - Loading: 5× Skeleton rows.

    Tests to flip (7 total):

    `storage-forecast-card.test.tsx`:
    - Toggle group switches between 7 days and 30 days, default 7 days.
    - Caption shows "Estimated {N} days until full".
    - Warning styling (destructive class) when daysUntilFull ≤ 14.

    `recent-audit-highlights.test.tsx`:
    - Renders up to 7 entries.
    - Footer link "View full audit log" points to /admin/audit.
    - Entry format "{actor} {verb} {target} · {time}".
    - Empty state "No recent platform activity.".
  </behavior>
  <action>
1. Create `apps/web/src/components/dashboard/storage-forecast-card.tsx`:
   - Imports: Card primitives, ToggleGroup + ToggleGroupItem, Skeleton, Recharts (LineChart, Line, XAxis, YAxis, Tooltip, ResponsiveContainer), useStorageForecast.
   - Implement per behavior. Convert bigint string → number GB for chart.
   - Apply `text-destructive` class conditionally.

2. Create `apps/web/src/components/dashboard/recent-audit-highlights.tsx`:
   - Imports: Card primitives, Skeleton, Link from `next/link`, ArrowRight from lucide, formatDistanceToNowStrict, useRecentAudit.
   - Implement per behavior. Include `verbForAction` helper.

3. Flip 7 it.todo stubs across the 2 test files:
   - Mock `useStorageForecast` + `useRecentAudit` via `vi.mock('@/hooks/use-platform-dashboard', ...)`.
   - For toggle test, use `userEvent.click(screen.getByRole('radio', { name: /30 days/i }))` and assert the hook's range arg changed (via mock.calls).
   - For caption warning: render with `estimatedDaysUntilFull: 7` fixture → `expect(screen.getByText(/Estimated 7 days/i).closest('p')?.className).toContain('text-destructive')`.
   - For audit format: fixture entry `{ actorName: 'Jane Doe', action: 'create', resource: 'organization', orgName: 'Acme', createdAt: new Date(Date.now() - 5*60*1000).toISOString() }` → assert text `Jane Doe created organization Acme` + `5 minutes ago` (or equivalent date-fns output).

4. Run:
   ```
   cd apps/web && pnpm test -- --run src/components/dashboard/storage-forecast-card.test.tsx src/components/dashboard/recent-audit-highlights.test.tsx
   cd apps/web && pnpm test   # full web suite green
   ```
  </action>
  <verify>
    <automated>cd apps/web && pnpm test -- --run src/components/dashboard/storage-forecast-card.test.tsx src/components/dashboard/recent-audit-highlights.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `test -f apps/web/src/components/dashboard/storage-forecast-card.tsx` succeeds
    - `grep -c "ToggleGroup" apps/web/src/components/dashboard/storage-forecast-card.tsx` >= 1
    - `grep -c "useStorageForecast" apps/web/src/components/dashboard/storage-forecast-card.tsx` >= 1
    - `grep -c "Estimated" apps/web/src/components/dashboard/storage-forecast-card.tsx` >= 1
    - `grep -c "text-destructive" apps/web/src/components/dashboard/storage-forecast-card.tsx` >= 1 (14-day warning)
    - `grep -c "Not enough data yet" apps/web/src/components/dashboard/storage-forecast-card.tsx` >= 1
    - `grep -c "LineChart\|ResponsiveContainer" apps/web/src/components/dashboard/storage-forecast-card.tsx` >= 2
    - `test -f apps/web/src/components/dashboard/recent-audit-highlights.tsx` succeeds
    - `grep -c "Recent Activity" apps/web/src/components/dashboard/recent-audit-highlights.tsx` >= 1
    - `grep -c "View full audit log" apps/web/src/components/dashboard/recent-audit-highlights.tsx` >= 1
    - `grep -c "/admin/audit" apps/web/src/components/dashboard/recent-audit-highlights.tsx` >= 1
    - `grep -c "No recent platform activity" apps/web/src/components/dashboard/recent-audit-highlights.tsx` >= 1
    - `grep -c "verbForAction\|created\|updated\|deleted" apps/web/src/components/dashboard/recent-audit-highlights.tsx` >= 1
    - `grep -c "it.todo" apps/web/src/components/dashboard/storage-forecast-card.test.tsx` == 0
    - `grep -c "it.todo" apps/web/src/components/dashboard/recent-audit-highlights.test.tsx` == 0
    - `cd apps/web && pnpm test -- --run src/components/dashboard/storage-forecast-card.test.tsx src/components/dashboard/recent-audit-highlights.test.tsx` exits 0
  </acceptance_criteria>
  <done>
    StorageForecastCard + RecentAuditHighlights implemented with exact UI-SPEC copy. 7 test stubs flipped. Both components consume Plan 01 endpoints via Task 1 hooks. Widgets ready for Plan 06 page composition.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/admin/dashboard/* | All endpoints guarded by SuperAdminGuard in Plan 01 |
| DOM → rendered content | React auto-escapes; no dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-AUTHZ-ADMIN | Elevation of Privilege | New widgets call super-admin endpoints | mitigate | Backend (Plan 01) enforces via class-level `@UseGuards(SuperAdminGuard)`. Frontend simply calls endpoints; tenant users receive 403 from guard. |
| T-18-INFO-LEAK-STORAGE | Information Disclosure | StorageForecastCard renders platform-wide storage | accept | Super admin scope by design. Endpoint guarded. Tenant portal has no link to this component. |
| T-18-AUDIT-PII | Information Disclosure | RecentAuditHighlights renders actor names + org names | accept | Already exposed in existing `/admin/audit` page. No new PII surface. |
| T-18-XSS-AUDIT | Tampering | Audit entry text interpolates actorName + orgName | mitigate | React auto-escapes. No `dangerouslySetInnerHTML` (grep verifies). |
| T-18-DOS-FORECAST | Denial of Service | Forecast chart fetches on range change | accept | 30s polling + user-driven range changes. Inline throttling via hook useEffect deps. |
</threat_model>

<verification>
- `cd apps/web && pnpm test -- --run src/components/dashboard` green
- `cd apps/web && pnpm build` green (toggle-group typed correctly)
- Visual sanity (manual): open /admin/dashboard AFTER Plan 06 ships to verify widgets render. Until Plan 06 lands, widget tests alone prove behavior.
</verification>

<success_criteria>
- shadcn `toggle-group` primitive installed
- `use-platform-dashboard.ts` exports `usePlatformIssues`, `useStorageForecast(range)`, `useRecentAudit(limit)` with 30s polling
- `PlatformIssuesPanel` renders empty state + 5 issue type rows per UI-SPEC copy
- `ClusterNodesPanel` renders 5-column table using existing `useClusterNodes` Socket.IO hook
- `StorageForecastCard` renders Recharts LineChart + 7d/30d ToggleGroup + daysUntilFull caption with destructive styling at ≤14 days
- `RecentAuditHighlights` renders 7 entries + footer link to /admin/audit
- All 13 widget test stubs flipped (3+3+3+4 = 13)
- No `dangerouslySetInnerHTML`
- No schema changes
</success_criteria>

<output>
After completion, create `.planning/phases/18-dashboard-map-polish/18-05-SUMMARY.md` documenting widget components, shadcn install, hook signatures, test counts. Note that Plan 06 composes these widgets into the super admin dashboard page.
</output>
