---
phase: 21.1-active-job-collision-fix-for-hot-reload-restart-gap-closure
verified: 2026-04-25T14:10:04Z
status: passed
score: 12/12 must-haves verified
re_verification:
  is_re_verification: false
gaps: []
deferred: []
human_verification: []
---

# Phase 21.1: Active-job collision fix for hot-reload restart — Verification Report

**Phase Goal:** Make profile changes actually restart cameras whose BullMQ `start` job is currently in active+locked state. Phase 21 left audit-log-correct but runtime-broken behavior in this case. Post-fix, profile edits must produce an FFmpeg PID change within 30s for the common case (running camera with live FFmpeg from boot recovery).

**Verified:** 2026-04-25T14:10:04Z
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | Publisher branch publishes to `camera:{id}:restart` on `existingJob.isActive() === true` and skips `queue.add` | ✓ VERIFIED | `streams.service.ts:271-299` — `existingJob.isActive()` guard, `redis.publish(channel, payload)` at line 290, `continue` at line 298. Mock-compat guard (`typeof isActive === 'function'`) at line 272 lets existing tests fall through unchanged. Confirmed by 7 unit tests in `profile-restart-active-job-pubsub.test.ts` (all PASS). |
| 2  | Audit ordering preserved (D-07): `auditService.log` fires BEFORE the active/non-active branch | ✓ VERIFIED | `streams.service.ts:194-213` — `auditService.log` runs first, then `getJob` at line 266, branch at line 276. Unit test "audit-ordering preserved" asserts `mock.invocationCallOrder[0]` (audit) < `mock.invocationCallOrder[0]` (publish) — PASS. |
| 3  | Subscriber wiring: `process()` opens `redis.duplicate()`, subscribes to `camera:{id}:restart`, calls `gracefulRestart` on signal | ✓ VERIFIED | `stream.processor.ts:106` — `subscriber = this.redis.duplicate()`. Channel literal at line 107: `` `camera:${cameraId}:restart` ``. `gracefulRestart(cameraId, 5_000)` called at line 140. Confirmed by integration test "single PATCH" (308ms — PASS) and unit test "single signal triggers gracefulRestart". |
| 4  | M1 — Subscriber lifecycle: `unsubscribe()` + `quit()` inside `finally` block | ✓ VERIFIED | `stream.processor.ts:199-206` — `finally { ... await subscriber.unsubscribe().catch(() => {}); await subscriber.quit().catch(() => {}); }`. Both calls grep-confirmed in same finally block. |
| 5  | M2 — Fingerprint safety net: subscribe-ready callback reads camera+profile from Prisma, fingerprints, calls `gracefulRestart` if mismatch | ✓ VERIFIED | `stream.processor.ts:223-263` — `runFingerprintSafetyNet`. Reads `systemPrisma.camera.findUnique({ include: { streamProfile: true }})`, computes `fingerprintProfile()` for both, compares `dbFingerprint !== jobFingerprint`, calls `gracefulRestart`. Unit test "DB profile differs → gracefulRestart fires" — PASS. |
| 6  | M3 — `restartingCameras: Set<string>` dedup in signal handler AND safety net | ✓ VERIFIED | `stream.processor.ts:44` — field declaration. `:112` (handler has-check), `:118` (handler add), `:142` (handler delete via finally), `:254` (safety-net has-check), `:255` (safety-net add), `:259` (safety-net delete via finally). Unit test "two rapid signals → only ONE call" — PASS. |
| 7  | M4 — BKR06 11-PATCH integration test against real Redis with `for (let i = 0; i < 11; i++)` loop and `expect(invocationCount).toBe(11)` | ✓ VERIFIED | `tests/integration/profile-restart-active-job.integration.test.ts:191` (loop), `:210` (assertion). Real `ioredis` connection at `:59-67`, sync TCP probe at `:39-51` for skip-on-no-Redis. Test PASSES against running `sms-app-redis-1` Redis (1601ms — invocationCount === 11). |
| 8  | D-13 strict scope: `startStream`, `stopStream`, `status.service` notifyQueue, B-1 collision guard all byte-identical | ✓ VERIFIED | `grep -c "Remove any existing job for this camera"` = 1 (startStream untouched). `grep -c "Kill FFmpeg FIRST"` = 1 (stopStream untouched). `status.service.ts:86-91` notifyQueue still uses original remove-then-add. `camera-health.service.ts:144-160` B-1 guard preserved (sha `8d257e6`). No `signalActiveOrEnqueue()` helper exists. |
| 9  | Reuses (not redefined): `gracefulRestart`, `fingerprintProfile`, `ioredis.duplicate()`, REDIS_CLIENT pattern | ✓ VERIFIED | `stream.processor.ts:5-10` — imports `FfmpegService`, `fingerprintProfile`, `Redis from 'ioredis'`. Calls `this.ffmpegService.gracefulRestart(cameraId, 5_000)` not redefined. `fingerprintProfile()` imported from `../profile-fingerprint.util`. `ffmpeg.service.ts:127-144` `gracefulRestart` unchanged. REDIS_CLIENT factory matches `dashboard.module.ts` pattern. |
| 10 | Goal-backward test result: PATCH on running camera produces FFmpeg PID change within ~5s — empirically `expect(invocationCount).toBe(11)` is the precise inversion of the BKR06 failure mode | ✓ VERIFIED | `pnpm --filter @sms-platform/api test -- profile-restart-active-job.integration` exits 0 with "2 passed". The 11-PATCH BKR06 reproducer asserts 11 distinct gracefulRestart invocations against real Redis with a hung `startStream` simulating a live FFmpeg holding the BullMQ lock. Pre-fix this would assert 0 (silently dropped publishes); post-fix asserts 11. |
| 11 | REDIS_CLIENT provider in StreamsModule matches dashboard.module.ts pattern | ✓ VERIFIED | `streams.module.ts:46-54` — `provide: REDIS_CLIENT, useFactory: () => new Redis({ host: process.env.REDIS_HOST \|\| 'localhost', port: parseInt(process.env.REDIS_PORT \|\| '6379', 10) })`. Byte-equivalent to `dashboard.module.ts` factory. Not exported (module-local), provider only. |
| 12 | Build clean: `pnpm --filter @sms-platform/api build` exits 0 | ✓ VERIFIED | `nest build` → `Successfully compiled: 161 files with swc (197.87ms)`. Exit 0. |

**Score:** 12/12 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/api/src/streams/streams.service.ts` | Active-vs-not branching in enqueueProfileRestart with pub/sub publish on active branch | ✓ VERIFIED | 364 lines. Imports `Redis from 'ioredis'` (line 4) + `REDIS_CLIENT` (line 10). Constructor 7th param `@Optional() @Inject(REDIS_CLIENT) private readonly redis?: Redis` at line 40. Branch at lines 271-299. Audit-write at lines 196-213 (BEFORE branch). |
| `apps/api/src/streams/streams.module.ts` | REDIS_CLIENT provider | ✓ VERIFIED | 65 lines. `import Redis from 'ioredis'` (line 3), `import { REDIS_CLIENT } from '../api-keys/api-keys.service'` (line 14), provider block at lines 46-54. NOT exported (module-local). |
| `apps/api/src/streams/processors/stream.processor.ts` | Subscriber + 3 mitigations | ✓ VERIFIED | 264 lines. Imports `Redis`, `REDIS_CLIENT`, `SystemPrismaService`, `fingerprintProfile`. Field `private restartingCameras = new Set<string>()` at line 44. Constructor params at lines 55-56. Subscriber at lines 104-173. `runFingerprintSafetyNet` private method at lines 223-263. Try/finally cleanup at lines 175-207. |
| `apps/api/tests/streams/profile-restart-active-job-pubsub.test.ts` | Plan 01 publisher unit coverage (≥60 lines) | ✓ VERIFIED | 237 lines. 7 tests (active-branch publishes, remove not called, payload contents, queued-state fall-through, no-job fall-through, audit ordering, audit row written even on active branch). All PASS. |
| `apps/api/tests/streams/stream-processor-restart-signal.test.ts` | Plan 02 subscriber + M3 unit coverage (≥70 lines) | ✓ VERIFIED | 225 lines. 5 tests including "two rapid signals while gracefulRestart is mid-flight → only ONE call" (M3 dedup) and lifecycle/cleanup assertions. All PASS. |
| `apps/api/tests/streams/stream-processor-fingerprint-safety-net.test.ts` | Plan 02 M2 unit coverage (≥50 lines) | ✓ VERIFIED | 149 lines. 5 tests covering DB-differs, DB-matches, camera-not-found, systemPrisma-undefined, null-DB-profile-vs-sha256:none. All PASS. |
| `apps/api/tests/integration/profile-restart-active-job.integration.test.ts` | M4 real-Redis integration (≥80 lines) | ✓ VERIFIED | 217 lines. 2 tests including BKR06 11-PATCH reproducer with `for (let i = 0; i < 11; i++)` and `expect(invocationCount).toBe(11)`. PASSES against running Redis (1601ms). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `streams.service.ts:enqueueProfileRestart` (active branch) | Redis pub/sub channel `camera:{id}:restart` | `this.redis.publish(channel, JSON.stringify({ profile, inputUrl, needsTranscode, fingerprint }))` | ✓ WIRED | Line 290 — `await this.redis.publish(channel, payload)`. Confirmed by 7 unit tests + 1 integration test all asserting `redis.publish` is invoked with the correct channel + payload shape. |
| `streams.module.ts` | ioredis singleton via `REDIS_CLIENT` | `useFactory: () => new Redis({ host, port })` | ✓ WIRED | Line 46-54. Verified imported by `StreamsService` constructor (line 40 `@Inject(REDIS_CLIENT)`) and `StreamProcessor` constructor (`stream.processor.ts:55`). |
| `stream.processor.ts:process()` | Redis subscriber via `ioredis.duplicate()` | `this.redis.duplicate(); subscriber.subscribe(channel, ...)` | ✓ WIRED | Line 106 (`duplicate`), line 153 (`subscribe`). Both calls confirmed by integration test (subscriber count returned by publisher = 1, asserted at integration test line 164). |
| Subscriber message handler | `FfmpegService.gracefulRestart` | `subscriber.on('message', async () => { await this.ffmpegService.gracefulRestart(cameraId, 5_000); })` | ✓ WIRED | Line 109 (handler), line 140 (call). Integration test asserts `gracefulRestart` called with `(cameraId, 5000)` after publish (`:173`). |
| Subscribe-ready callback | `fingerprintProfile` + Prisma SELECT | `systemPrisma.camera.findUnique → fingerprintProfile compare → gracefulRestart on mismatch` | ✓ WIRED | Lines 223-263. Imports `fingerprintProfile` from `../profile-fingerprint.util` (line 10). Two call sites: `dbProfileInput` (line 247) and `jobProfile` (line 248). Mismatch branch at line 250 calls `gracefulRestart`. Unit test "DB differs → gracefulRestart fires" PASSES. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `streams.service.ts:enqueueProfileRestart` (active branch) | `profile`, `inputUrl`, `fingerprint` payload | `prisma.streamProfile.findUnique` (line 228) + `cam.streamUrl/streamKey` (line 244) + `args.newFingerprint` arg | ✓ Yes — DB query reads live profile row; integration test confirms publisher payload contains real values | ✓ FLOWING |
| `stream.processor.ts:process()` | Subscriber message → `profile`, `inputUrl` reassignment | `JSON.parse(message)` of publisher's `redis.publish` payload | ✓ Yes — integration test verifies the `'message'` event fires with non-empty body and gracefulRestart is invoked | ✓ FLOWING |
| `runFingerprintSafetyNet` | `dbFingerprint` vs `jobFingerprint` comparison | `systemPrisma.camera.findUnique({ include: { streamProfile: true }})` | ✓ Yes — unit test stubs Prisma to return drifted profile; safety net correctly fires gracefulRestart on mismatch | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build cleanly compiles all sources | `pnpm --filter @sms-platform/api build` | "Successfully compiled: 161 files with swc (197.87ms)" exit 0 | ✓ PASS |
| Three Phase 21.1 unit test files all green | `pnpm --filter @sms-platform/api test -- profile-restart-active-job-pubsub stream-processor-restart-signal stream-processor-fingerprint-safety-net` | "Test Files 3 passed (3) / Tests 17 passed (17) / 2.15s" exit 0 | ✓ PASS |
| BKR06 11-PATCH integration reproducer asserts invocationCount === 11 | `pnpm --filter @sms-platform/api test -- profile-restart-active-job.integration` | "Test Files 1 passed (1) / Tests 2 passed (2) / 2.96s" — BKR06 reproducer 1601ms with `expect(invocationCount).toBe(11)` ✓ | ✓ PASS |
| Full backend suite has zero regressions | `pnpm --filter @sms-platform/api test` | "Test Files 94 passed | 11 skipped (105) / Tests 703 passed | 117 todo (820) / 34.22s" exit 0 — Phase 21 baseline 684 + 19 new = 703 | ✓ PASS |
| Redis reachable for integration test | `bash -c 'exec 3<>/dev/tcp/localhost/6379'` | exit 0 | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| `GAP-21-DEFECT-ACTIVE-JOB-COLLISION` | 21.1-01-PLAN.md, 21.1-02-PLAN.md, 21.1-03-PLAN.md | Synthetic gap-closure ID — closes the runtime defect documented in 21-VERIFICATION.md `gaps_found` verdict (truth "Running FFmpeg processes are automatically killed and respawned with the new settings within 30 seconds" → status: partial). NOT in REQUIREMENTS.md (no new product requirement; closes a Phase 21 verification gap). | ✓ SATISFIED | Publisher path (Plan 01), subscriber + 3 mitigations (Plan 02), 4 test files including BKR06 reproducer (Plan 03) all present and verified. The integration test's `expect(invocationCount).toBe(11)` is the precise inversion of the Phase 21 BKR06 UAT failure mode (11 PATCHes → 0 FFmpeg PID changes). Phase 21's gap is now closed. |

**Note on synthetic ID:** `GAP-21-DEFECT-ACTIVE-JOB-COLLISION` is correctly absent from REQUIREMENTS.md per the phase prompt — it is a verification-gap-closure marker, not a new product requirement. The Phase 21 truth that previously failed (FFmpeg restart within 30s) is now empirically met by the integration test reproducer.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| (none) | — | No TODO / FIXME / XXX / HACK / placeholder strings introduced. No empty `return null` / `return {}` / `return []` patterns added. No `console.log`-only handlers. No hardcoded empty data flowing to user-visible output. | — | — |

Spot-checks performed:
- `grep -nE "TODO|FIXME|XXX|HACK|PLACEHOLDER" apps/api/src/streams/streams.service.ts apps/api/src/streams/processors/stream.processor.ts apps/api/src/streams/streams.module.ts` → no new entries (existing comments unchanged).
- `grep -nE "console\.log" apps/api/src/streams/processors/stream.processor.ts apps/api/src/streams/streams.service.ts` → no matches; logging uses `this.logger.{log,debug,warn,error}` consistently.
- `try { JSON.parse(message) } catch (err) { this.logger.warn(...) }` (stream.processor.ts:125-136) — defensive parse with logger.warn on bad payload (not silent swallow). Acceptable.
- `await subscriber.unsubscribe().catch(() => {})` and `await subscriber.quit().catch(() => {})` (lines 204-205) — silent error swallow on shutdown. Acceptable per threat-model T-21.1-05 (subscriber being torn down anyway; ioredis transient errors during quit are not actionable).

### Human Verification Required

(none — all observable truths verified programmatically; the BKR06 11-PATCH reproducer that previously required manual UAT is now a runnable assertion in `profile-restart-active-job.integration.test.ts`)

### D-12..D-15 Decision Honors

| Decision | Source | Honored? | Evidence |
|----------|--------|----------|----------|
| D-12 Pub/sub signal mechanism | CONTEXT.md decisions | ✓ Yes | `streams.service.ts:283` (channel), `:290` (publish); `stream.processor.ts:107` (channel), `:153` (subscribe), `:140` (gracefulRestart on signal). |
| D-12 M1 lifecycle (try/finally + sub.quit) | CONTEXT.md | ✓ Yes | `stream.processor.ts:199-206`. |
| D-12 M2 fingerprint safety net | CONTEXT.md | ✓ Yes | `stream.processor.ts:223-263` `runFingerprintSafetyNet`. |
| D-12 M3 `restartingCameras: Set<string>` | CONTEXT.md | ✓ Yes | `stream.processor.ts:44`, used in handler (`:112-142`) and safety net (`:254-260`). |
| D-12 M4 hybrid test layer | CONTEXT.md | ✓ Yes | 3 unit + 1 integration test files exist; integration uses real Redis with `describe.skipIf(!isRedisAvailable)` skip-on-no-Redis. |
| D-13 strict scope (only enqueueProfileRestart) | CONTEXT.md | ✓ Yes | `startStream`, `stopStream`, `notifyQueue`, B-1 guard all byte-identical. No `signalActiveOrEnqueue()` helper extracted. |
| D-14 hybrid test strategy | CONTEXT.md | ✓ Yes | `ioredis-mock` for unit tests + real `ioredis` for integration test. Skip-on-no-Redis via sync `bash /dev/tcp` probe. |
| D-15 standard restart rollout | CONTEXT.md | ✓ Yes | No migration code, no admin endpoint, no maintenance-window requirement added. M2 safety net covers the deploy-window-PATCH case per CONTEXT.md `<decisions>` D-15. |

### Gaps Summary

No gaps. Every must-have from the phase prompt is verified:

1. ✓ Publisher branch (`existingJob.isActive()` → `redis.publish('camera:{id}:restart', ...)`)
2. ✓ Audit ordering preserved (D-07)
3. ✓ Subscriber wiring (`redis.duplicate()` → subscribe → `gracefulRestart`)
4. ✓ M1 subscriber lifecycle (`unsubscribe + quit` in `finally`)
5. ✓ M2 fingerprint safety net (DB-vs-job fingerprint compare)
6. ✓ M3 `restartingCameras` Set dedup
7. ✓ M4 integration test with `for (let i = 0; i < 11; i++)` and `expect(invocationCount).toBe(11)`
8. ✓ D-13 strict scope (4 byte-identity checks all pass)
9. ✓ Reuses (gracefulRestart, fingerprintProfile, duplicate, OnModuleDestroy precedent)
10. ✓ Goal-backward test result (BKR06 reproducer integration test PASSES against real Redis)
11. ✓ REDIS_CLIENT provider in StreamsModule matches dashboard.module.ts
12. ✓ Build exit 0

The active-job collision defect from Phase 21 is closed. The PATCH → FFmpeg PID change cycle is now empirically proven by the BKR06 11-PATCH integration test (1.6s wall time for 11 distinct restart invocations against real Redis), well within the phase goal's 30-second budget.

Phase 21 verdict (`gaps_found`, 5/6 must-haves) → Phase 21.1 verdict (`passed`, 12/12 must-haves). Phase 21's truth "Running FFmpeg processes are automatically killed and respawned with the new settings within 30 seconds" is now satisfied for the active+locked case that was the reported BKR06 failure mode.

---

*Verified: 2026-04-25T14:10:04Z*
*Verifier: Claude (gsd-verifier)*
