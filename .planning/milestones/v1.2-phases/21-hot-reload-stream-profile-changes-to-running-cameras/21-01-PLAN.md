---
phase: 21
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/tests/streams/profile-fingerprint.test.ts
  - apps/api/tests/streams/stream-profile-restart.test.ts
  - apps/api/tests/streams/profile-restart-audit.test.ts
  - apps/api/tests/streams/profile-restart-dedup.test.ts
  - apps/api/tests/streams/profile-restart-failure-fallthrough.test.ts
  - apps/api/tests/streams/ffmpeg-graceful-restart.test.ts
  - apps/api/tests/streams/stream-profile-delete-protection.test.ts
  - apps/api/tests/cameras/camera-profile-reassign.test.ts
  - apps/api/tests/resilience/camera-health-restart-collision.test.ts
  - apps/web/src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx
  - .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md
autonomous: true
requirements: []
# No REQUIREMENTS.md IDs map to Phase 21 — phase closes a code-audit gap discovered 2026-04-25
# (see RESEARCH.md §Phase Requirements). Phase requirement is encoded in CONTEXT.md D-01..D-11.
must_haves:
  truths:
    - "All 9 backend test files exist as .test.ts with it.todo stubs covering every D-01..D-11 + maintenance-gate + status-filter behavior + B-1 camera-health collision"
    - "1 frontend test file exists as .test.tsx with it.todo stubs for D-06 toast"
    - "21-VALIDATION.md per-task map is filled with task IDs from plans 02-06"
    - "Wave 0 sign-off — running `vitest --run --reporter=verbose tests/streams/profile-fingerprint.test.ts` reports the file with 0 passing 0 failing N pending (it.todo)"
  artifacts:
    - path: "apps/api/tests/streams/profile-fingerprint.test.ts"
      provides: "D-01 fingerprint hash-stability test scaffold"
    - path: "apps/api/tests/streams/stream-profile-restart.test.ts"
      provides: "D-01/D-04/maintenance-gate/status-filter test scaffold"
    - path: "apps/api/tests/streams/profile-restart-audit.test.ts"
      provides: "D-07 audit row shape + enqueue-time + system-trigger scaffold"
    - path: "apps/api/tests/streams/profile-restart-dedup.test.ts"
      provides: "D-03 remove-then-add (Q5 latest-wins) scaffold"
    - path: "apps/api/tests/streams/profile-restart-failure-fallthrough.test.ts"
      provides: "D-09 retry exhaustion → degraded scaffold"
    - path: "apps/api/tests/streams/ffmpeg-graceful-restart.test.ts"
      provides: "D-05 SIGTERM→wait→SIGKILL helper scaffold"
    - path: "apps/api/tests/streams/stream-profile-delete-protection.test.ts"
      provides: "D-10 409 + happy delete scaffold"
    - path: "apps/api/tests/cameras/camera-profile-reassign.test.ts"
      provides: "D-02 reassign trigger + null-cases scaffold"
    - path: "apps/api/tests/resilience/camera-health-restart-collision.test.ts"
      provides: "B-1 CameraHealthService.enqueueStart collision-guard scaffold (preserves in-flight 'restart' jobs)"
    - path: "apps/web/src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx"
      provides: "D-06 toast frontend scaffold"
    - path: ".planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md"
      provides: "Per-task verification map filled with task IDs"
  key_links:
    - from: "21-VALIDATION.md per-task map"
      to: "task IDs in 21-02..21-06 plans"
      via: "manual map fill after plans 02-06 are written"
      pattern: "Task ID column references 21-NN-T<n> from each plan"
---

<objective>
Wave 0: Create the 9 backend + 1 frontend test scaffold files enumerated in 21-VALIDATION.md "Wave 0 Requirements" so every code-producing task in plans 02-06 has an `<automated>` test target satisfying the Nyquist rule. All scaffolds use `it.todo` (vitest) for unstarted assertions; later plans flip them to real `it()` blocks.

Purpose: Plans 02-06 each modify production code. Without these test files in place first, those tasks would have no automated verify command (Nyquist failure). This plan also fills the 21-VALIDATION.md per-task map after task IDs are assigned, completing the Wave 0 sign-off contract.

The 9th backend file (`tests/resilience/camera-health-restart-collision.test.ts`) was added in revision iter 1 to close blocker B-1: the camera-health 60s tick shares the canonical `camera:{id}:ffmpeg` jobId with Phase 21 'restart' enqueues, and without a guard, BullMQ's same-jobId remove-then-add would silently demote a 'restart' job to a 'start' job, dropping the SIGTERM+respawn-with-new-profile branch.

Output: 9 backend `.test.ts` files in `apps/api/tests/{streams,cameras,resilience}/`, 1 frontend `.test.tsx` file under `apps/web/src/app/admin/stream-profiles/components/__tests__/`, and an updated `21-VALIDATION.md` with per-task map filled and `wave_0_complete: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-CONTEXT.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-RESEARCH.md
@.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md
@apps/api/tests/streams/stream-processor.test.ts
@apps/api/tests/cameras/maintenance.test.ts
@CLAUDE.md

<interfaces>
<!-- Existing test fixture shape used across apps/api/tests — see stream-processor.test.ts:1-3 -->
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
// vitest provides it.todo for un-implemented tests:
//   it.todo('description without implementation');
// it.todo entries are reported as "todo" not failing — perfect for Wave 0 stubs.
```

<!-- Web test file pattern — apps/web uses vitest with React Testing Library; see existing tests under apps/web/**/__tests__/*.test.tsx -->
```typescript
import { describe, it } from 'vitest';
// it.todo() works identically in @sms-platform/web's vitest setup.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 21-01-T1: Create 9 backend test scaffold files with it.todo stubs</name>
  <files>
    apps/api/tests/streams/profile-fingerprint.test.ts,
    apps/api/tests/streams/stream-profile-restart.test.ts,
    apps/api/tests/streams/profile-restart-audit.test.ts,
    apps/api/tests/streams/profile-restart-dedup.test.ts,
    apps/api/tests/streams/profile-restart-failure-fallthrough.test.ts,
    apps/api/tests/streams/ffmpeg-graceful-restart.test.ts,
    apps/api/tests/streams/stream-profile-delete-protection.test.ts,
    apps/api/tests/cameras/camera-profile-reassign.test.ts,
    apps/api/tests/resilience/camera-health-restart-collision.test.ts
  </files>
  <read_first>
    apps/api/tests/streams/stream-processor.test.ts (reference scaffold/pattern — note `import { describe, it, expect, vi, beforeEach } from 'vitest';` and how existing scaffolds use vi.fn() mocks),
    apps/api/tests/cameras/maintenance.test.ts (reference for camera-side service tests with prisma mock injection),
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-RESEARCH.md §8 (Validation Architecture — exact decision-to-test mapping is the source of truth for what each scaffold must contain)
  </read_first>
  <action>
Create exactly 9 backend test files. Each file MUST start with `import { describe, it, expect } from 'vitest';` (add `vi`, `beforeEach` only where needed). Each file MUST have ONE top-level `describe('Phase 21 — D-XX <name>', () => { ... })` block containing the listed `it.todo(...)` entries verbatim. NO real test bodies — leave every assertion as `it.todo`.

If `apps/api/tests/resilience/` does not yet exist, create it (`mkdir -p` semantics in your file write).

File 1 — `apps/api/tests/streams/profile-fingerprint.test.ts`:
- describe: "Phase 21 — D-01 profile fingerprint"
- it.todo("fingerprintProfile(null) returns 'sha256:none' sentinel")
- it.todo("fingerprintProfile of two structurally identical profiles returns identical hash")
- it.todo("flipping codec changes the fingerprint")
- it.todo("flipping preset changes the fingerprint")
- it.todo("flipping resolution changes the fingerprint")
- it.todo("flipping fps changes the fingerprint")
- it.todo("flipping videoBitrate changes the fingerprint")
- it.todo("flipping audioCodec changes the fingerprint")
- it.todo("flipping audioBitrate changes the fingerprint")
- it.todo("name and description fields are NOT part of the fingerprint")
- it.todo("output starts with the literal prefix 'sha256:' and is 71 chars total ('sha256:' + 64 hex)")
- it.todo("Passthrough profile (codec='copy', audioCodec='copy', other fields null) yields a stable hash distinct from libx264 cases")

File 2 — `apps/api/tests/streams/stream-profile-restart.test.ts`:
- describe: "Phase 21 — D-01 StreamProfileService.update restart trigger + D-04 jitter + maintenance-gate + status-filter"
- it.todo("StreamProfileService.update with no FFmpeg-affecting field changes does NOT enqueue any restart job")
- it.todo("StreamProfileService.update with codec change enqueues a restart job per affected camera")
- it.todo("StreamProfileService.update with name-only change enqueues NO restart job")
- it.todo("StreamProfileService.update with description-only change enqueues NO restart job")
- it.todo("Only cameras with status in {online, connecting, reconnecting, degraded} get enqueued — offline cameras are skipped")
- it.todo("Cameras with maintenanceMode=true are skipped at enqueue time even if status matches")
- it.todo("Each enqueued job carries a delay in [0, 30000) ms (D-04 jitter)")
- it.todo("100 enqueues over a synthetic camera set produce delay distribution within [0, 30000) for ALL of them")
- it.todo("D-08: cameras with isRecording=true are NOT special-cased — they enqueue normally with no extra branch")

File 3 — `apps/api/tests/streams/profile-restart-audit.test.ts`:
- describe: "Phase 21 — D-07 audit row at enqueue time"
- it.todo("Each affected camera gets exactly one audit row with action='camera.profile_hot_reload'")
- it.todo("Audit row resource='camera' and resourceId equals cameraId (not profileId)")
- it.todo("Audit row details contains profileId, oldFingerprint (sha256:...), newFingerprint (sha256:...), and triggeredBy")
- it.todo("triggeredBy is { userId, userEmail } when req.user is present")
- it.todo("triggeredBy is { system: true } when no user context (defensive — script callpath)")
- it.todo("Audit row is written at ENQUEUE time, before queue.add — so even if the job is later removed/superseded the audit persists")
- it.todo("method='PATCH' and path matches the originating /api/stream-profiles/:id or /api/cameras/:id request URL")

File 4 — `apps/api/tests/streams/profile-restart-dedup.test.ts`:
- describe: "Phase 21 — D-03 + Q5 remove-then-add (latest save wins, NOT pure BullMQ dedup)"
- it.todo("enqueue calls queue.getJob('camera:{id}:ffmpeg') to look for existing job before adding")
- it.todo("when existingJob is present, enqueue calls existingJob.remove() before queue.add")
- it.todo("two rapid-fire profile saves for the same camera produce: first remove (no-op, no job) → first add → second remove of first → second add — net 1 job in queue with second-save's data")
- it.todo("jobId is exactly the literal 'camera:' + cameraId + ':ffmpeg' (matches Phase 15 D-11 + streams.service.ts:101 + boot-recovery.service.ts + camera-health.service.ts)")
- it.todo("queue.add is called with options { jobId, attempts: 20, backoff: { type: 'exponential', delay: 1000 }, removeOnComplete: true, removeOnFail: false } — matches startStream's options for downstream consistency")

File 5 — `apps/api/tests/streams/profile-restart-failure-fallthrough.test.ts`:
- describe: "Phase 21 — D-09 fallthrough to Phase 15 resilience on repeated failure"
- it.todo("StreamProcessor consuming a profile-restart job uses the same exponential backoff as a regular start (no Phase 21 retry override)")
- it.todo("after BullMQ exhausts attempts (default 20), the job is failed and StatusService.transition fires for 'degraded' via the existing pipeline")
- it.todo("the existing 30s notification debounce (status.service.ts:86-106) coalesces the 'degraded' transition with any preceding 'reconnecting' transition")

File 6 — `apps/api/tests/streams/ffmpeg-graceful-restart.test.ts`:
- describe: "Phase 21 — D-05 FfmpegService.gracefulRestart helper"
- it.todo("gracefulRestart returns immediately when isRunning(cameraId) is false (no-op)")
- it.todo("gracefulRestart calls stopStream(cameraId) which sets intentionalStops and SIGTERM-kills the process")
- it.todo("gracefulRestart polls isRunning every 100ms; resolves when isRunning returns false BEFORE graceMs elapses")
- it.todo("gracefulRestart calls forceKill(cameraId) when isRunning still returns true after graceMs (default 5000)")
- it.todo("default graceMs is 5000 (5 seconds) — restart-flow value, not the 10s shutdown grace")
- it.todo("forceKill is NOT called when SIGTERM succeeds within graceMs")

File 7 — `apps/api/tests/streams/stream-profile-delete-protection.test.ts`:
- describe: "Phase 21 — D-10 service-layer 409 protection (Option B, no schema change)"
- it.todo("DELETE /api/stream-profiles/:id with 0 cameras using it returns 200 and removes the row")
- it.todo("DELETE /api/stream-profiles/:id with 1 camera using it throws ConflictException with status 409")
- it.todo("409 response body shape is { message: string, usedBy: [{ cameraId, name }] }")
- it.todo("409 response with 2+ cameras returns all of them in usedBy[]")
- it.todo("usedBy query is scoped to the requester's org (Camera.findMany via TENANCY_CLIENT) — cross-org camera names never appear (T-21-02)")
- it.todo("Prisma row is NOT deleted when ConflictException is thrown")

File 8 — `apps/api/tests/cameras/camera-profile-reassign.test.ts`:
- describe: "Phase 21 — D-02 CamerasService.updateCamera profile reassignment trigger"
- it.todo("updateCamera with no streamProfileId in dto enqueues NO restart")
- it.todo("updateCamera with streamProfileId same as current value enqueues NO restart (no-op assignment)")
- it.todo("updateCamera with new streamProfileId pointing to a profile of identical fingerprint enqueues NO restart (D-02 fingerprint-equality skip)")
- it.todo("updateCamera with new streamProfileId pointing to a profile of DIFFERENT fingerprint enqueues exactly ONE restart for this single camera")
- it.todo("updateCamera changing streamProfileId from null → non-default profile enqueues a restart (camera previously had no profile)")
- it.todo("updateCamera changing streamProfileId from non-null → null enqueues a restart (now uses default {codec:'auto', audioCodec:'aac'} which has its own fingerprint)")
- it.todo("updateCamera respects status filter: offline camera reassignment does NOT enqueue (camera not running anyway)")
- it.todo("updateCamera respects maintenance gate: maintenanceMode=true camera reassignment does NOT enqueue")
- it.todo("Audit row 'camera.profile_hot_reload' is written for the affected camera with details.profileId pointing to the NEW profile")

File 9 (NEW — B-1 fix) — `apps/api/tests/resilience/camera-health-restart-collision.test.ts`:
- describe: "Phase 21 — B-1 CameraHealthService.enqueueStart collision guard"
- it.todo("skips enqueue when in-flight job has name='restart' (preserves Phase 21 SIGTERM branch)")
- it.todo("proceeds with enqueue when in-flight job has name='start' (normal recovery path)")
- it.todo("proceeds with enqueue when no in-flight job exists")
- it.todo("B-1 contract: an in-flight 'restart' job is NEVER replaced by a 'start' job from a health tick")

After writing all 9 files, do NOT run vitest yet — Wave 0 is purely scaffold. Verification command in &lt;verify&gt; only confirms the files parse.
  </action>
  <verify>
    <automated>cd apps/api && pnpm exec vitest --run --reporter=verbose tests/streams/profile-fingerprint.test.ts tests/streams/stream-profile-restart.test.ts tests/streams/profile-restart-audit.test.ts tests/streams/profile-restart-dedup.test.ts tests/streams/profile-restart-failure-fallthrough.test.ts tests/streams/ffmpeg-graceful-restart.test.ts tests/streams/stream-profile-delete-protection.test.ts tests/cameras/camera-profile-reassign.test.ts tests/resilience/camera-health-restart-collision.test.ts 2>&amp;1 | grep -E "Test Files|Tests:|todo" | tail -20</automated>
  </verify>
  <done>
All 9 files exist. `vitest --run` reports them with at least 1 todo test each (zero passing, zero failing). Each file's first line is `import { describe, it, expect } from 'vitest';` (or with `vi`, `beforeEach` appended). Every it.todo string from the action above appears verbatim. The combined run reports `Test Files  9 passed` and total todo count >= 54.
  </done>
</task>

<task type="auto">
  <name>Task 21-01-T2: Create frontend D-06 toast test scaffold</name>
  <files>
    apps/web/src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx
  </files>
  <read_first>
    apps/web/src/app/admin/stream-profiles/components/profile-form-dialog.tsx (current dialog — note `toast.success('Profile updated')` at line 152, the target the new toast logic replaces),
    apps/web/package.json (verify `"test": "vitest"` exists — confirms vitest is the framework for web)
  </read_first>
  <action>
Create exactly one file: `apps/web/src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx`.

File contents (verbatim it.todo strings):
- `import { describe, it } from 'vitest';`
- describe: "Phase 21 — D-06 ProfileFormDialog toast surfaces affectedCameras count"
- it.todo("on PATCH success with response.affectedCameras=0, toast text is 'Profile updated' (current behavior preserved)")
- it.todo("on PATCH success with response.affectedCameras=3, toast text is 'Profile updated · 3 camera(s) restarting with new settings'")
- it.todo("on PATCH success with response.affectedCameras=1, toast uses singular 'camera' (or accept 'camera(s)' literal — match implementation)")
- it.todo("toast severity is info-level when affectedCameras > 0 (per D-06 'info-level not warning')")
- it.todo("toast severity stays success-level when affectedCameras=0 (preserves existing 'Profile updated' tone)")
- it.todo("on PATCH failure (non-2xx), toast is error-level 'Failed to update profile' — no restart-count surfacing")
- it.todo("dialog only renders the toast variant for EDIT mode (isEdit=true); CREATE mode keeps 'Profile created' unchanged")

Do NOT add real test bodies. Do NOT import the dialog component yet — Wave 0 is scaffold-only.
  </action>
  <verify>
    <automated>cd apps/web &amp;&amp; pnpm exec vitest --run --reporter=verbose src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx 2>&amp;1 | grep -E "Test Files|Tests:|todo" | tail -10</automated>
  </verify>
  <done>
File exists. Vitest reports it with 7 todo tests, 0 passing, 0 failing.
  </done>
</task>

<task type="auto">
  <name>Task 21-01-T3: Fill 21-VALIDATION.md per-task map and set wave_0_complete</name>
  <files>
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md
  </files>
  <read_first>
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md (current state — replace per-task map placeholder rows),
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-02-PLAN.md,
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-03-PLAN.md,
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-04-PLAN.md,
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-05-PLAN.md,
    .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-06-PLAN.md
  </read_first>
  <action>
Edit `21-VALIDATION.md` in two places:

(1) Replace the entire "Per-Task Verification Map" placeholder table with the filled rows below (keep the column headers and status legend exactly as-is):

| Task ID | Plan | Wave | Decision | Test Type | Automated Command | File Exists | Status |
|---------|------|------|----------|-----------|-------------------|-------------|--------|
| 21-01-T1 | 21-01 | 0 | scaffold | unit | (scaffold only — no behavior) | ✓ created in this task | ✅ green |
| 21-01-T2 | 21-01 | 0 | D-06 scaffold | component | (scaffold only) | ✓ created in this task | ✅ green |
| 21-02-T1 | 21-02 | 1 | D-01 fingerprint util | unit | `pnpm --filter @sms-platform/api test -- tests/streams/profile-fingerprint.test.ts` | ✓ from 21-01-T1 | ⬜ pending |
| 21-02-T2 | 21-02 | 1 | D-01 + D-07 update trigger | unit | `pnpm --filter @sms-platform/api test -- tests/streams/stream-profile-restart.test.ts tests/streams/profile-restart-audit.test.ts` | ✓ from 21-01-T1 | ⬜ pending |
| 21-03-T1 | 21-03 | 1 | D-02 + D-07 reassign trigger | unit | `pnpm --filter @sms-platform/api test -- tests/cameras/camera-profile-reassign.test.ts` | ✓ from 21-01-T1 | ⬜ pending |
| 21-04-T1 | 21-04 | 2 | D-05 graceful kill | unit | `pnpm --filter @sms-platform/api test -- tests/streams/ffmpeg-graceful-restart.test.ts` | ✓ from 21-01-T1 | ⬜ pending |
| 21-04-T2 | 21-04 | 2 | D-03/D-04/D-05/D-08/D-09 enqueue+execution + B-1 collision guard | unit | `pnpm --filter @sms-platform/api test -- tests/streams/profile-restart-dedup.test.ts tests/streams/profile-restart-failure-fallthrough.test.ts tests/streams/stream-processor.test.ts tests/resilience/camera-health-restart-collision.test.ts` | ✓ from 21-01-T1 | ⬜ pending |
| 21-05-T1 | 21-05 | 3 | D-10 DELETE 409 | integration | `pnpm --filter @sms-platform/api test -- tests/streams/stream-profile-delete-protection.test.ts` | ✓ from 21-01-T1 | ⬜ pending |
| 21-05-T2 | 21-05 | 3 | D-06 toasts (admin + tenant + camera dialog) | component | `pnpm --filter @sms-platform/web test -- src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx` | ✓ from 21-01-T2 | ⬜ pending |
| 21-06-T1 | 21-06 | 4 | full suite green | integration | `pnpm --filter @sms-platform/api test &amp;&amp; pnpm --filter @sms-platform/web test &amp;&amp; pnpm --filter @sms-platform/web build` | n/a | ⬜ pending |
| 21-06-T2 | 21-06 | 4 | manual UAT | manual | (see Manual-Only Verifications) | n/a | ⬜ pending |

(2) Update the YAML frontmatter at top of file:
- Change `status: draft` → `status: ready`
- Change `nyquist_compliant: false` → `nyquist_compliant: true`
- Change `wave_0_complete: false` → `wave_0_complete: true`
- Add `last_updated: 2026-04-25`

(3) Update the "Wave 0 Requirements" checklist to add the new B-1 scaffold (line ~62 area):

Append after the existing 8 backend bullets, INSIDE the same checklist (before the "Frontend (single Wave 0 item)" header):
- [ ] `apps/api/tests/resilience/camera-health-restart-collision.test.ts` — B-1 CameraHealthService.enqueueStart collision guard (preserves in-flight 'restart' jobs)

(4) Update the "Validation Sign-Off" checklist at the bottom — check ALL boxes:
- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all 9 backend + 1 frontend test scaffolds listed above
- [x] No watch-mode flags in any task command
- [x] Feedback latency < 30s on unit subset
- [x] `nyquist_compliant: true` set in frontmatter once per-task map is filled and verified

Change `**Approval:** pending` → `**Approval:** approved (Wave 0 sign-off — 2026-04-25)`.
  </action>
  <verify>
    <automated>grep -c "21-02-T1\|21-03-T1\|21-04-T1\|21-04-T2\|21-05-T1\|21-05-T2\|21-06-T1\|21-06-T2" .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md &amp;&amp; grep "wave_0_complete: true" .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md &amp;&amp; grep "nyquist_compliant: true" .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md &amp;&amp; grep -c "camera-health-restart-collision" .planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - grep returns count >= 8 task IDs in 21-VALIDATION.md
    - frontmatter `wave_0_complete: true` present (literal)
    - frontmatter `nyquist_compliant: true` present (literal)
    - `**Approval:** approved` line replaces `**Approval:** pending`
    - Per-task map shows ⬜ pending for tasks 21-02-T1 onward (those tasks haven't run yet)
    - `camera-health-restart-collision` appears at least twice in the file (Wave 0 list + per-task map row 21-04-T2 command)
    - Wave 0 Requirements section lists "9 backend + 1 frontend" (or equivalent — explicit count of camera-health-restart-collision included)
  </acceptance_criteria>
  <done>21-VALIDATION.md is the canonical per-task verification map and has wave_0_complete=true, nyquist_compliant=true, status=ready. The B-1 collision-guard scaffold appears in both the Wave 0 list and the per-task map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced by Wave 0) | Wave 0 creates only test scaffolds and edits a planning markdown file. No new endpoints, no new business logic, no new authentication paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01..T-21-07 | (full register) | n/a | deferred to plans 02-06 | Wave 0 introduces no production code path, so no new attack surface. The full STRIDE register lives in the threat_model blocks of plans 02-05 where the actual triggers/endpoints/audit rows are added. Plan 06 verifies the threats are mitigated. (Renumbered through T-21-07 per W-2; T-21-06=DoS processor loop, T-21-07=tampering/race incl. B-1 camera-health collision.) |
</threat_model>

<verification>
- 9 backend test files exist in `apps/api/tests/{streams,cameras,resilience}/` and parse via vitest --run.
- 1 frontend test file exists under `apps/web/src/app/admin/stream-profiles/components/__tests__/` and parses via vitest --run.
- `21-VALIDATION.md` per-task map references task IDs from plans 02-06 (including the camera-health-restart-collision command for 21-04-T2).
- `wave_0_complete: true` and `nyquist_compliant: true` are set in the validation frontmatter.
</verification>

<success_criteria>
1. `pnpm --filter @sms-platform/api exec vitest --run tests/streams/ tests/cameras/camera-profile-reassign.test.ts tests/resilience/camera-health-restart-collision.test.ts 2>&1 | grep "Tests:"` reports >= 54 todo and 0 failing.
2. `pnpm --filter @sms-platform/web exec vitest --run src/app/admin/stream-profiles/components/__tests__/profile-form-dialog-toast.test.tsx 2>&1 | grep "Tests:"` reports 7 todo and 0 failing.
3. `21-VALIDATION.md` contains all task IDs from plans 02-06 and has the three frontmatter flags set.
</success_criteria>

<output>
After completion, create `.planning/phases/21-hot-reload-stream-profile-changes-to-running-cameras/21-01-SUMMARY.md` documenting:
- Wave 0 scaffold filenames + line counts (9 backend + 1 frontend)
- Total it.todo count per file
- 21-VALIDATION.md per-task map snapshot
- Confirmation that subsequent plans (02-06) inherit a fully-mapped Nyquist contract
- Note: B-1 collision-guard scaffold added in revision iter 1
</output>
</content>
