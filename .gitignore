# Dependencies
node_modules/

# Environment
.env
.env.local
apps/api/.env.test

# Build output
dist/
.next/
out/
*.tsbuildinfo

# Prisma
apps/api/src/prisma/generated/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Test coverage
coverage/

# Logs
*.log

# GSD internals (worktrees + quick scratch + Claude Code session state)
.claude/
apps/web/.planning/

# Local bulk-import examples (may contain real credentials/URLs — do not commit)
bulk-import-*-EXAMPLE.csv
bulk-import-*-EXAMPLE.xlsx

# Phase 30 (smoke test) — operator's local-only evidence (screenshots, raw logs)
# Stays out of git per T-30-05 mitigation. Each smoke run creates its own
# UTC-stamped subdirectory. The SMOKE-TEST-LOG.md does ship — only the binary
# evidence does not.
deploy/smoke-evidence/
